# 【数据资产】删除表数据时同步删除表生命周期 v7.0.2
> 来源：zentao-cases/dtstack-platform/数据资产/archive-cases/v7.0.2/【数据资产】删除表数据时同步删除表生命周期.csv
> 用例数：24

---

## 数据资产-元数据

##### 验证底层删除 底层删除-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 底层删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证底层删除 底层删除-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 底层删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证底层删除 底层删除-验证元数据同步离线表后底层删除表生命周期正常删除 「P1」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 底层删除表test_lifecycle_001 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证离线删除 离线删除-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 离线删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证离线删除 离线删除-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据模型-建表-新建一张明细表 | 表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 2 | 离线删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证离线删除 离线删除-验证元数据同步离线表后删除表生命周期正常删除 「P1」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 离线删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除源表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除元数据表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除源表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除元数据表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据模型-建表-新建一张明细表 | 表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除源表-验证元数据同步离线表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据模型删表 删除元数据表-验证元数据同步离线表后删除表生命周期正常删除 「P1」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据模型删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除源表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除元数据表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除源表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除元数据表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据模型-建表-新建一张明细表 | 表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除源表-验证元数据同步离线表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证元数据管理删表 删除元数据表-验证元数据同步离线表后删除表生命周期正常删除 「P1」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 元数据管理删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除源表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除元数据表-验证离线建表自动同步表至资产后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入离线创建一张表，等待metadata将表同步至资产 | 表同步成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除源表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除元数据表-验证模型建明细表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据模型-建表-新建一张明细表 | 表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询上述表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除源表-验证元数据同步离线表后删除表生命周期正常删除 「P2」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

##### 验证数据地图删表 删除元数据表-验证元数据同步离线表后删除表生命周期正常删除 「P1」

> 前置条件
```
已引入数据源，且包含库表，表内包含数据，离线相关表已经创建完成
建表语句：
CREATE TABLE if NOT EXISTS test_lifecycle_001(id INT,name VARCHAR(255));
表插入语句：
INSERT INTO test_lifecycle_001 VALUES (1,'mengfei'),(2,'kako');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元数据-元数据同步-新增周期同步任务将离线表同步至资产 | 元数据同步任务运行成功，表同步成 |
| 2 | 数据地图删除上述表 | 表删除成功，系统给出删除成功提示，该记录从列表中消失 |
| 3 | 进入pub业务库查询test_lifecycle_001表生命周期记录 | 没有对应记录，记录已被删除 |

