所属模块,用例标题,前置条件,步骤,预期结果,优先级
离线开发-数据开发-数据同步,验证历史向导模式正常运行,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
ALTER TABLE test_002
ADD COLUMN bir INT DEFAULT 1;
写入后语句：
DELETE FROM test_002
WHERE id = 1;","1. 进入数据开发模块，找到一个历史向导模式数据同步任务（doris-doris），编辑目标数据界面写入前后准备语句，点击保存
2. 点击临时运行
3. 查看doris中test_002表数据","1. 系统提示任务保存成功，任务已保存至任务列表，可继续编辑或提交
2. 任务状态更新为【运行成功】，运行日志无错误信息，输出结果符合预期
3. 成功从doris中表数据同步过来，字段包含id、name、bir，同时数据不包含id=1的数据",3
离线开发-数据开发-数据同步,验证使用错误的写入后准备语句，语句不执行,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
ALTER TABLE test_002
ADD COLUMN bir INT DEFAULT 1;
写入后语句：
dorp table test_22222222;","1. 进入数据开发模块，新建向导模式的数据同步任务
2. 点击数据来源--数据源下拉列表
3. 选择来源数据源，选择选择doris数据源，选择test_001，点击下一步
4. 选择目标数据源doris，选择schema及表test_002，输入写入前、写入后准备语句，点击下一步
5. 配置正确的映射，点击下一步
6. 点击下一步，点击保存，进行临时运行
7. 查看doris中test_002表数据","1. 系统提示新建成功，列表顶部出现新建的记录，记录内容与填写一致
2. 来源数据源支持doris数据源，下拉选择数据表，可选范围为数据库连接用户下的所有数据库
3. 进入选择目标页面
4. 进入配置映射界面
5. 进入通道控制页面
6. 配置以及环境无误的话，任务成功，系统给出成功反馈，相关页面/数据状态更新为最新
7. 成功从doris中表数据同步过来，字段包含id、name、bir，数据结果包含两条数据",2
离线开发-数据开发-数据同步,验证使用错误的写入前准备语句，语句不执行,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
dorp table test_22222222;
写入后语句：
DELETE FROM test_002
WHERE id = 1;","1. 进入数据开发模块，新建向导模式的数据同步任务
2. 点击数据来源--数据源下拉列表
3. 选择来源数据源，选择选择doris数据源，选择test_001，点击下一步
4. 选择目标数据源doris，选择schema及表test_002，输入写入前、写入后准备语句，点击下一步
5. 配置正确的映射，点击下一步
6. 点击下一步，点击保存，进行临时运行
7. 查看doris中test_002表数据","1. 系统提示新建成功，列表顶部出现新建的记录，记录内容与填写一致
2. 来源数据源支持doris数据源，下拉选择数据表，可选范围为数据库连接用户下的所有数据库
3. 进入选择目标页面
4. 进入配置映射界面
5. 进入通道控制页面
6. 配置以及环境无误的话，任务成功，系统给出成功反馈，相关页面/数据状态更新为最新
7. 成功从doris中表数据同步过来，字段包含id、name，同时数据不包含id=1的数据",2
离线开发-数据开发-数据同步,验证转脚本模式正常运行，相关sql存在,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
ALTER TABLE test_002
ADD COLUMN bir INT DEFAULT 1;
写入后语句：
DELETE FROM test_002
WHERE id = 1;","1. 进入数据开发模块，新建向导模式的数据同步任务
2. 点击数据来源--数据源下拉列表
3. 选择来源数据源，选择选择doris数据源，选择test_001，点击下一步
4. 选择目标数据源doris，选择schema及表test_002，输入写入前、写入后准备语句，点击下一步
5. 配置正确的映射，点击下一步
6. 点击下一步，点击保存，点击转换为脚本
7. 进行临时运行
8. 查看doris中test_002表数据","1. 系统提示新建成功，列表顶部出现新建的记录，记录内容与填写一致
2. 来源数据源支持doris数据源，下拉选择数据表，可选范围为数据库连接用户下的所有数据库
3. 进入选择目标页面
4. 进入配置映射界面
5. 进入通道控制页面
6. 脚本转换成功，搜索对应写入前后准备语句可正常搜索
7. 配置以及环境无误的话，任务成功，系统给出成功反馈，相关页面/数据状态更新为最新
8. 成功从doris中表数据同步过来，字段包含id、name、bir，同时数据不包含id=1的数据",2
离线开发-数据开发-数据同步,验证向导模式正常运行,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
ALTER TABLE test_002
ADD COLUMN bir INT DEFAULT 1;
写入后语句：
DELETE FROM test_002
WHERE id = 1;","1. 进入数据开发模块，新建向导模式的数据同步任务
2. 点击数据来源--数据源下拉列表
3. 选择来源数据源，选择选择doris数据源，选择test_001，点击下一步
4. 选择目标数据源doris，选择schema及表test_002，输入写入前、写入后准备语句，点击下一步
5. 配置正确的映射，点击下一步
6. 点击下一步，点击保存，进行临时运行
7. 查看doris中test_002表数据","1. 系统提示新建成功，列表顶部出现新建的记录，记录内容与填写一致
2. 来源数据源支持doris数据源，下拉选择数据表，可选范围为数据库连接用户下的所有数据库
3. 进入选择目标页面
4. 进入配置映射界面
5. 进入通道控制页面
6. 配置以及环境无误的话，任务成功，系统给出成功反馈，相关页面/数据状态更新为最新
7. 成功从doris中表数据同步过来，字段包含id、name、bir，同时数据不包含id=1的数据",1
离线开发-数据开发-数据同步,验证前端交互框校验,"建表语句
CREATE TABLE test_001 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
CREATE TABLE test_002 (
id INT,
name VARCHAR(255)
)
ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (
“replication_num“ = “1“
);
INSERT INTO test_001 VALUES (1,'mengfei'),(2,'kako');

写入前语句：
ALTER TABLE test_002
ADD COLUMN bir INT DEFAULT 1;
写入后语句：
DELETE FROM test_002
WHERE id = 1;","1. 进入数据开发模块，新建向导模式的数据同步任务
2. 选择来源数据源，选择选择doris数据源，选择test_001，点击下一步
3. 选择目标数据源，选择doris数据源","1. 系统提示新建成功，列表顶部出现新建的记录，记录内容与填写一致
2. 进入选择目标页面
3. 新增导入前准备语句和导入后准备语句交互框，非必填",1
