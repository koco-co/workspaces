# 202510-统一查询v1.0.3（XMind）
> 来源：zentao-cases/XMind/统一查询/202510-统一查询v1.0.3.xmind

---

## 数据预览增加分区过滤(#9371)
### 验证 RANGE 分区表数据预览默认仅查询最新分区
#### 进入【业务查询】-【SQL查询】-【数据库导航】
- 进入成功

#### 展开 Doris 数据源，选择 catalog → database → 表 doris_test_range
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 dt='2024-12-05' 分区数据

#### 手动执行 SQL : SELECT * FROM doris_test_range PARTITION (p202412)
- 返回与「数据预览」一致的数据行

#### 展开 Inceptor 数据源，选择 database → 表 inceptor_test_range
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 dt='2024-12-05' 分区数据

#### 手动执行 SQL : SELECT * FROM inceptor_test_range WHERE dt='2024-12-05'
- 返回与「数据预览」一致的数据行


### 验证 LIST 分区表数据预览仅显示最新分区
#### 进入【业务查询】-【SQL查询】-【数据库导航】
- 进入成功

#### 展开 Doris 数据源，选择 catalog → database → 表 doris_test_list
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 c_id=3 分区数据

#### 手动执行 SQL : SELECT * FROM doris_test_list PARTITION (p_guangzhou)
- 返回与「数据预览」一致的数据行

#### 展开 Inceptor 数据源，选择 database → 表 inceptor_test_list
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 city='guangzhou' 分区数据

#### 手动执行 SELECT * FROM inceptor_test_list WHERE city='guangzhou'
- 返回与「数据预览」一致的数据行


### 验证非分区表数据预览全量展示但不触发阻断
#### 进入【业务查询】-【SQL查询】-【数据库导航】
- 进入成功

#### 展开 Doris 数据源，选择 catalog → database → 表 doris_test_nopart
- 展示所有数据，不触发阻断

#### 展开 Inceptor 数据源，选择 database → 表 inceptor_test_nopart
- 展示所有数据，不触发阻断


### 验证多字段分区表最新分区组合识别正确
#### 进入【业务查询】-【SQL查询】-【数据库导航】
- 进入成功

#### 展开 Doris 数据源，选择 catalog → database → 表 doris_test_multi
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 (year=2024, month=11) 数据

#### 手动 SQL 验证 SELECT * FROM doris_test_multi PARTITION (p202411)
- 返回与「数据预览」一致的数据行

#### 展开 Inceptor 数据源，选择 database → 表 inceptor_test_multi
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 仅显示 (year=2024, month=11) 数据

#### 手动 SQL 验证 SELECT * FROM inceptor_test_multi WHERE year=2024 AND month=11
- 返回与「数据预览」一致的数据行


### 验证分区数据为空时，预览显示空结果且不报错
#### 进入【业务查询】-【SQL查询】-【数据库导航】
- 进入成功

#### 展开 Doris 数据源，选择 catalog → database → 表 doris_test_empty
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 显示空结果集，无报错

#### 手动 SQL 验证 SELECT * FROM doris_test_empty PARTITION (p202410)
- 返回空结果

#### 展开 Inceptor 数据源，选择 database → 表 inceptor_test_empty
- 表结构信息展示正确

#### 点击「数据预览」按钮
- 显示空结果集，无报错

#### 手动 SQL 验证 SELECT * FROM inceptor_test_empty WHERE dt='2024-10-01'
- 返回空结果



## 筛选条件新增常用操作符(#9372)
### 验证规则配置新增规则
#### 进入【业务查询】-【自助取数】页面
- 进入成功

#### 编辑取数记录A
- 进入编辑页面

#### 进入设置过滤器页面
- 进入成功

#### 展开规则下拉框
- 1) 新增六个规则: 大于等于、小于等于、大于、小于、匹配、不匹配
2) 规则位置排序依次为（从上到下）：大于等于、大于、小于等于、小于、等于、不等于、包含、不包含、有值、无值、匹配、不匹配


### 验证「大于等于」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 大于等于, 62000
- 配置成功

#### 点击「更新」
- 更新成功, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary >= 62000;


### 验证「大于」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 大于, 62000
- 配置成功

#### 点击「更新」
- 更新成功, 数据预览展示一条记录, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary > 62000;


### 验证「小于等于」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 大于, 62000
- 配置成功

#### 点击「更新」
- 更新成功, 数据预览展示四条记录, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary <= 62000;


### 验证「小于」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 大于, 62000
- 配置成功

#### 点击「更新」
- 更新成功, 数据预览展示三条记录, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary < 62000;


### 验证「匹配」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 匹配, 5000
- 配置成功

#### 点击「更新」
- 更新成功, 数据预览展示一条记录, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary like '%5000%';


### 验证「不匹配」过滤规则功能正常
#### 进入【业务查询-自助取数-新建自助取数】页面
- 进入成功

#### 配置如下:
数据模型: A
图表-字段: name, salary
图表-过滤器: salary
过滤规则: 不匹配, 5000
- 配置成功

#### 点击「更新」
- 更新成功, 数据预览展示四条记录, 结果验证: SELECT t1.name, t1.salary FROM left_test t1 INNER JOIN right_test t2 ON t1.id = t2.id WHERE t1.salary not like '%5000%';



## 查询性能优化(#9373)
### 验证表列表加载功能正常
#### 进入【业务查询】-【SQL查询】-【数据库导航】页面
- 进入成功

#### 点击Doris-${schemaA}名称左侧的三角图标展开表列表
- 表列表展开响应迅速

#### 观察初始加载的表数量
- 仅展示前 100 张表

#### 打开浏览器开发者工具 Network 面板
- 后端接口请求仅返回首批 100 条数据

#### 向下滚动至列表底部
- 页面自动触发加载请求

#### 查看 Network 请求
- 新请求返回第 101–200 张表数据

#### 持续滚动加载至 500 张表
- 滚动流畅，无卡顿或加载失败


### 验证分页加载稳定性（10000 张表）
#### 进入【业务查询】-【SQL查询】-【数据库导航】页面
- 进入成功

#### 点击Doris-${schemaA}名称左侧的三角图标展开表列表
- 表列表展开响应迅速

#### 持续滚动加载直至所有表加载完毕
- 滚动流畅

#### 尝试点击不同表名称查看字段
- 响应正常，无明显延迟

#### 点击其他数据库再展开
- 切换操作无卡顿


### 验证模糊搜索可匹配未渲染表名
#### 进入【业务查询】-【SQL查询】-【数据库导航】页面
- 进入成功

#### 展开表列表（默认加载前 100 张）
- 页面正常显示首批数据

#### 在搜索框中输入表名${tableA}
- 搜索请求触发查询

#### 检查返回结果
- 匹配结果包含未加载到页面的表名

#### 进入表详情
- 能正确加载表结构信息

#### 清空搜索框
- 表列表恢复至分页状态，显示前 100 张表



## 支持维度和度量的手动调整(#9374)
### 验证字段右侧图标变更且可展开操作菜单
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 编辑模型记录A
- 进入编辑页面

#### 检查模型预览
- 字段名称右侧图标调整 。点击图标可展开操作菜单

#### 点击设置图标
- 支持以下功能：
a.重命名：点击后字段名进入可编辑状态，交互效果与当前重命名逻辑保持一致。
b.类型切换：根据字段当前归属展示不同操作：
—若为维度字段，则显示“转换为度量”；
—若为度量字段，则显示“转换为维度”。


### 验证字段重命名功能正常
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 编辑模型记录A-模型预览窗口
- 进入成功

#### 选择维度字段name, 重命名为「姓名」
- 编辑成功

#### 选择度量字段age, 重命名为「年龄」
- 编辑成功

#### 保存配置后, 再次进入编辑页面-模型预览
- 字段名正常回显


### 验证字段自由切换为维度/度量功能正常
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 编辑模型记录A-模型预览窗口
- 进入成功

#### 选择维度字段name, 「转换为度量」
- 1) 编辑成功, Toast提示: 转换成功!
2) 字段位置从维度模块变更到度量模块

#### 选择度量字段age, 「转换为维度」
- 1) 编辑成功, Toast提示: 转换成功!
2) 字段位置从度量模块变更到维度模块

#### 保存配置后, 再次进入编辑页面-模型预览
- 字段位置正常回显



## 数据模型支持筛选条件(#9375)
### 验证“添加关联表”弹窗中新增筛选条件配置项显示正常
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 进入新建数据模型-添加关联表
- 进入「添加关联表」弹窗

#### 查看关联键区域下方
- 新增「筛选条件」配置块, 非必填

#### 检查筛选条件区域默认显示
- 默认展示 1 行筛选条件输入行，包括字段名、规则、字段值三列输入框

#### 检查各列默认值
- 字段名=“请选择字段”，关联规则=“请选择规则”，字段值=“请输入值”

#### 点击筛选条件中的“请选择字段”下拉框
- 弹出字段选择列表

#### 检查下拉框内容
- 显示关联表中所有字段名

#### 选择任意字段
- 字段名显示为选中字段名

#### 在其他筛选行中再次选择同一字段
- 系统允许重复选择

#### 点击“请选择规则”下拉框
- 弹出枚举选项

#### 检查选项内容
- 依次为：大于等于、大于、小于等于、小于、等于、不等于、包含、不包含、有值、无值、匹配、不匹配

#### 选择任意规则（如“等于”）
- 显示值更新为“等于”

#### 切换为其他规则
- 规则切换正常，输入框状态保持不变

#### 点击“请输入值”输入框
- 光标聚焦，支持输入

#### 输入任意字符（长度 ≤ 50）
- 输入成功

#### 尝试输入 51 个字符
- 提示 “最多输入 50 个字符”

#### 清空输入框
- 字段值可为空，不报错


### 验证筛选条件区域支持添加多条规则（≤20 条）
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 进入新建数据模型-添加关联表
- 进入「添加关联表」弹窗

#### 点击筛选条件右侧“+”图标
- 1) 新增一行筛选规则输入框
2) 两条规则之间用「且」逻辑连接

#### 重复点击 20 次
- 最多可添加 20 行筛选规则

#### 添加第 21 行
- 提示 “最多添加 20 条规则”

#### 点击任意行右侧“-”图标
- 删除该行规则成功

#### 当只剩 1 条规则时点击“-”
- Toast 提示 “必须有一条数据”，不允许删除最后一条


### 验证不同规则之间自由切换“且 / 或”逻辑
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 进入新建数据模型-添加关联表
- 进入「添加关联表」弹窗

#### 点击筛选条件右侧“+”图标, 在两条规则间查看逻辑运算符
- 默认显示 “且”

#### 点击逻辑符号
- 切换为 “或”

#### 再次点击
- 切换回 “且”

#### 保存该配置后, 重新进入该模型记录的添加关联表的编辑页面
- 页面内容回显正常


### 验证「添加关联表」筛选条件功能正常
#### 进入【平台管理】-【数据模型】页面
- 进入成功

#### 点击「新建数据模型」, 配置如下:
模型名称: test
模型编码: test
数据源: ${datasourceA}
Catalog: ${catalogA}
Schema: ${schemaA}
表: left_test
- 配置成功

#### 点击「添加关联表」, 配置如下:
关联类型: Inner join
Catalog: ${catalogA}
Schema: ${schemaA}
右表: right_test
关联键: id = id
- 配置成功

#### 点击更新数据
- 「模型预览」中显示 5 行数据（两表内联匹配 ID=1~5）

#### 编辑关联表规则配置, 添加筛选条件:
No.01: department = IT
No.02: rank_level > 1
No.03: region != US
逻辑关系: No.01 且 (No.02 或 No.03)
- 配置成功

#### 点击更新数据
- 「模型预览」显示1行数据, SQL验证: SELECT * FROM left_test AS l INNER JOIN right_test AS r ON l.id = r.id WHERE r.department = 'IT' AND (r.rank_level > 1 OR r.region != 'US');

#### 保存该配置后, 重新进入该模型记录的添加关联表的编辑页面
- 页面内容回显正常



