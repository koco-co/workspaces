---
suite_name: "数据模型建表支持配置是否带入数仓层级命名"
description: "数据模型建表支持配置是否带入数仓层级命名用例归档"
tags:
  - "废弃"
  - "勾选"
  - "建表"
  - "选项"
  - "不勾选"
  - "建表后"
  - "验证勾选"
  - "临时保存"
  - "编辑功能"
  - "取消勾选"
  - "建表功能"
  - "不展示层级"
  - "取消勾选功能"
  - "数据模型建表"
  - "没有临时保存功能"
prd_version: "v6.3.5"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 8
case_id: 8880
---

## 【废弃】

##### 【P1】验证勾选“不展示层级”临时保存，编辑功能(没有临时保存功能)

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，勾选“不展示层级”，临时保存 | 保存成功 |
| 2 | 查看数据 | 回填正确，不展示层级选择框勾选结果正确 |
| 3 | 编辑不展示层级选择框，点击建表 | 结果正确 |

##### 【P1】验证不勾选“不展示层级”建表后，编辑功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，不勾选“不展示层级”，进行建表 | 建表成功 |
| 2 | 点击编辑按钮 | 回填正确，不展示层级选择框置灰 |

##### 【P1】验证勾选“不展示层级”建表后，编辑功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，勾选“不展示层级”，进行建表 | 建表成功 |
| 2 | 点击编辑按钮 | 回填正确，不展示层级选择框置灰 |

##### 【P1】验证勾选“不展示层级”建表，层级筛选是否正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，取消勾选“不展示层级”，进行建表 | 建表成功 |
| 2 | 进入数据模型-新建表，选择明细原始数仓层级，取消勾选“不展示层级”，进行建表 | 建表成功 |
| 3 | 在数据模型页面，点击原始数仓层级查看表 | 展示步骤一中的表 |
| 4 | 在数据模型页面，点击明细数仓层级查看表 | 展示步骤二中的表 |

##### 【P0】验证取消勾选“不展示层级”建表功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，取消勾选“不展示层级”，进行建表 | 建表成功 |
| 2 | 查看原始数仓层级目录下是否存在步骤一中的表 | 表存在 |

##### 【P1】验证勾选“不展示层级”建表功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择原始数仓层级，勾选“不展示层级”，进行建表 | 建表成功 |
| 2 | 查看原始数仓层级目录下是否存在步骤一中的表 | 表存在 |

##### 【P1】验证不展示层级选项勾选/取消勾选功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择数仓层级 | 表名后展示“不展示层级”，默认勾选 |
| 2 | 查看表名 | 表名前展示层级前缀 |
| 3 | 取消勾选不展示层级 | 表名前不展示层级前缀，模型元素的名称依旧展示 |
| 4 | 切换勾选/取消勾选 | 层级前缀展示正确 |

##### 【P1】验证数据模型建表，选择数仓层级后展示“不展示层级”选项

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择数仓层级 | 表名后展示“不展示层级”，默认勾选 |
| 2 | 切换各种类型的数据源 | “不展示层级”都会展示 |
