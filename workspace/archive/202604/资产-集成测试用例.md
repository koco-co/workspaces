---
suite_name: "资产-集成测试用例"
description: "资产-集成测试用例用例归档"
tags:
  - "建表"
  - "正确"
  - "正常"
  - "新建"
  - "多表"
  - "联动"
  - "数据表"
  - "表结构"
  - "能正常"
  - "字段级"
  - "单字段"
  - "自定义"
  - "规则集"
  - "表行数"
  - "强规则"
prd_version: ""
dev_version:
  - "袋鼠云"
create_at: "2026-04-07"
status: "草稿"
origin: "xmind"
case_count: 37
---

## 通用前置条件

> 适用于本测试套件所有用例的全局环境约定

```
【环境信息】
- 数据源类型：Doris（Meta数据源）
- 离线项目名称：env_rebuild_test
- 质量项目：与离线项目 env_rebuild_test 绑定的数据质量项目

【数据库准备】
执行以下 SQL 文件，在 Doris 数据源对应数据库下完成基础表初始化：

1. sql/base-tables.sql
   - test_table（id INT, name VARCHAR(255), info VARCHAR(255)）— 含 3 条数据
   - doris_test（id INT, name STRING, age INT）— 含 1 条数据
   - doris_demo_data_types_source（17 字段多类型宽表）— 含 3 条数据
   - doris_demo1_data_types_source（17 字段 VARCHAR 宽表）— 含 2 条数据

2. sql/lineage-tables.sql
   - wwz_001、wwz_002、wwz_003（id INT, name VARCHAR(50)）— 用于血缘验证

3. sql/active-users.sql
   - active_users（user_id BIGINT, name VARCHAR(50), email VARCHAR(200), address VARCHAR(500),
     age TINYINT, sex TINYINT, last_active DATETIME, property0~3 TINYINT）— 含 2 条数据

4. sql/quality-tables.sql（已包含在 base-tables.sql 中）
   - doris_demo_data_types_source / doris_demo1_data_types_source（同上）

【元数据同步】
上述所有表均需已完成元数据同步至资产平台，方可执行数据地图/元数据管理相关用例。
```

## 资产盘点（#10373）

### 未分类

#### 未分类

##### 【P0】验证已接入数据源统计数据正确

> 自动化验证点
```
1. 页面成功加载，URL不包含login
2. “已接入数据源”文本可见
3. 页面包含统计卡片(.ant-card)或统计区域
4. 页面文本包含”数据源”/”表数”/”库数”/”存储”等关键指标
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入资产-【资产盘点】页面 | 进入成功，URL不包含login |
| 2 | 查看”已接入数据源” | 显示截止前一天为止接入的数据源类型的统计卡片 |
| 3 | 查看统计数据：昨日新增表数、源数、库数、表数以及存储量 | 统计数据为对应数据源类型下前一天的统计数据，页面包含数据源/表数/库数/存储等文本 |

## 元数据（#10374）

### 数据地图

#### 首页【资产类型】功能验证

##### 【P0】验证【数据表】表数量统计正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 离线项目：env_rebuild_test
- Doris 数据源下已存在表 test_table（见 sql/base-tables.sql）
  字段：id INT, name VARCHAR(255), info VARCHAR(255)
- 记录同步前【数据表】当前统计数量 N（用于后续断言 N+1 / N-1）
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1. 新增元数据同步-周期同步任务<br>2. 选择 Doris 数据源，选择对应 database<br>3. 选择数据表 test_table<br>4. 勾选【全部内容】<br>5. 点击【临时同步】按钮 | 元数据同步任务创建成功，任务状态显示"运行中"，实例状态显示"运行中" |
| 2 | 等待任务运行完成，实例状态变为"运行成功" | 数据地图首页【数据表】统计数量显示 N+1，页面数字与同步前相差恰好为 1 |
| 3 | 在数据地图中找到 test_table，点击【删除元表】并确认 | 【数据表】统计数量恢复为 N，页面数字与删除前相差恰好为 1 |

##### 【P0】验证【离线任务】任务数量统计正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 离线项目：env_rebuild_test
- Doris 数据源下已存在表 test_table（见 sql/base-tables.sql）
  字段：id INT, name VARCHAR(255), info VARCHAR(255)
- 记录操作前【离线任务】当前统计数量 M
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1. 在离线平台（项目 env_rebuild_test）新增 SQL 建表任务，目标表为 test_table<br>2. 点击【临时运行】或【提交】 | 【离线任务】任务统计数量显示 M+1，页面数字与操作前相差恰好为 1 |

#### 【数据地图】-二级页面【筛选过滤】功能验证

##### 【P0】验证筛选条件组合查询功能正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 选择【查询结果类型】为【数据表】 | 选择成功 |
| 2 | 选择【数据源类型】为${DATASOURCE_TYPE} | 1. 筛选框展示资产平台引入的所有数据源类型<br>2. 数据源类型数量统计正确<br>3. 选择成功 |
| 3 | 选择【数据源】为${DATASOURCE_NAME} | 1. 展示资产平台已引入${DATASOURCE_TYPE}类型的所有数据源名称<br>2. 选择成功 |
| 4 | 选择【数据库】为${DATABASE} | 1. 如果当前数据源-数据库下没有同步任何表到资产平台，则展示为空<br>2. 如果当前数据源-数据库下已经同步${TABLE_NAME}到资产平台，则【数据库】选择${DATABASE}成功 |
| 5 | 选择【负责人】为${TABLE_OWNER} | 1. 如果存在表指定负责人，则【负责人】选择${TABLE_OWNER}成功<br>2. 如果没有表指定负责人，则【负责人】展示为空 |
| 6 | 选择【表标签】为${TABLE_TAG} | 1. 如果存在表设置表标签，则【表标签】选择${TABLE_TAG}成功<br>2. 如果没有表设置表标签，则【表标签】展示为空 |
| 7 | 设置完所有筛选条件后，筛选 | 1. 过滤出所有符合组合条件的数据<br>2. 没有匹配条件的数据则展示“暂无数据”缺省页 |

#### 【数据地图】-【数据表】详情页面功能验证

##### 【P1】验证【表结构】-【建表语句】模块功能正常

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 离线项目：env_rebuild_test
- 表名：test_table（见 sql/base-tables.sql）
- 表字段结构：
    id    INT           COMMENT '主键'
    name  VARCHAR(255)  COMMENT '姓名'
    info  VARCHAR(255)  COMMENT '信息'
- 表数据：3 条（id=1/2/3）
- test_table 已完成元数据同步至资产平台，可在【数据地图】中查到该表详情
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入 test_table 表详情页，点击【建表语句】按钮 | 建表语句弹窗打开，内容包含"test_table"表名、字段 id/name/info 及其类型和 COMMENT，语法与 sql/base-tables.sql 保持一致 |
| 2 | 点击【复制】按钮，将建表语句粘贴至离线平台 SQL 编辑器执行 | SQL 执行成功，无报错；在 Doris 数据源中可查到结构一致的新表 |

##### 【P0】验证【血缘关系】功能正常

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 离线项目：env_rebuild_test
- 已在 Doris 数据源中创建以下三张表（见 sql/lineage-tables.sql）：
    wwz_001（id INT COMMENT '用户ID', name VARCHAR(50) COMMENT '用户姓名'）
    wwz_002（id INT COMMENT '用户ID', name VARCHAR(50) COMMENT '用户姓名'）
    wwz_003（id INT COMMENT '用户ID', name VARCHAR(50) COMMENT '用户姓名'）
- 在离线项目 env_rebuild_test 中，已存在以下血缘 SQL 任务（任务已运行成功并产生血缘）：
    样例一：INSERT INTO wwz_001 SELECT id, name FROM wwz_002;
    样例三：INSERT INTO wwz_001 SELECT * FROM wwz_002 UNION ALL SELECT * FROM wwz_003;
- 上述三张表已完成元数据同步至资产平台，在数据地图中可查到对应表详情
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 在数据地图中打开 wwz_001 表详情，点击【血缘关系】按钮 | 页面顶部/侧边展示【表级血缘】【字段级血缘】切换按钮；工具栏可见【展示文字信息】【居中】【放大】【放小】【下载】【展示/隐藏导航器】按钮；画布中央显示 wwz_001 表节点 |
| 2 | 点击【表级血缘】按钮 | 画布中显示 wwz_001 的上游节点（wwz_002、wwz_003）和下游节点；上下游节点数量与离线血缘 SQL 一致 |
| 3 | 查看血缘图提示文案 | 页面存在文字”每个节点都可右击查看该节点的全链路血缘，存在逆向可展开节点时建议右击查看~” |
| 4 | 点击画布中央的 wwz_001 【表按钮】 | 弹出表详情弹窗，弹窗可见 |
| 5 | 查看表详情弹窗内容 | 弹窗中显示”wwz_001”表名及复制按钮；技术属性模块、业务属性模块可见；字段列表展示 id、name 两个非分区字段；页面包含”字段名称”和”字段中文名”搜索框 |
| 6 | 在搜索框输入字段名”id”，点击搜索 | 字段列表过滤后仅显示 id 字段一行，字段类型为 INT，中文名为”用户ID” |
| 7 | 在搜索框输入不存在的字段名”xyz_not_exist”，点击搜索 | 字段列表区域显示”暂无数据” |
| 8 | 调整分页 pagesize（如改为 5/10/20），切换页码 | pagesize 切换后列表行数与所选值一致；页码跳转后数据对应正确，无重复/丢失 |
| 9 | 点击【字段级血缘】 | 画布切换为字段级视图，展示 wwz_001 的所有字段（id、name）及各字段对应的上游字段连线 |
| 10 | 点击字段”id”节点 | 因 wwz_002.id → wwz_001.id 存在血缘，弹出血缘字段面板并显示来源字段信息；点击无血缘字段时提示”字段血缘关系为空” |
| 11 | 点击【展示文字信息】按钮 | 画布中各节点显示完整表全名（如 database.wwz_001） |
| 12 | 点击【居中】按钮 | 血缘图在视口内居中展示，画布偏移量归零 |
| 13 | 点击【放大】按钮 | 血缘图比例放大，节点变大 |
| 14 | 点击【放小】按钮 | 血缘图比例缩小，节点变小 |
| 15 | 点击【下载】按钮 | 下载成功，生成 PNG 文件；PNG 内容包含 wwz_001、wwz_002、wwz_003 节点及连线 |
| 16 | 点击【展示/隐藏导航器】按钮 | 导航器小地图在显示/隐藏状态间切换，当前状态与按钮图标一致 |

### 未分类

#### 未分类

##### 【P2】元数据同步

> 自动化验证点
```
1. 元数据同步页面加载成功，表格/tab/按钮可见
2. "新增周期同步任务"按钮可见
3. 点击后进入同步任务配置流程
4. Doris数据源类型选择成功
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【元数据】-【元数据同步】页面 | 页面加载成功，"新增周期同步任务"按钮可见 |
| 2 | 点击【新增周期同步任务】 | 进入同步任务配置流程 |
| 3 | 选择数据源类型为Doris | 数据源类型选择成功 |
| 4 | 选择数据库、数据表 | 选择成功 |
| 5 | 依次点击【添加】→【下一步】→【新增】 | 同步任务创建成功 |
| 6 | 点击【临时同步】 | 同步任务开始运行 |

### 元模型管理

#### 未分类

##### 【P1】验证通用业务属性-新增功能-逻辑正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新增枚举属性：<br>1）属性类型为“枚举”<br>2）输入表单内容<br>3）点击【确定】 | 列表中显示新建的业务属性，且数据正确 |
| 2 | 新增string类型文本框属性：<br>1）属性类型为“文本框”，字段类型为“string”<br>2）输入表单内容<br>3）点击【确定】 | 列表中显示新建的业务属性，且数据正确 |
| 3 | 新增bigint类型文本框属性：<br>1）属性类型为“文本框”，字段类型为“bigint”<br>2）输入表单内容<br>3）点击【确定】 | 列表中显示新建的业务属性，且数据正确 |
| 4 | 新增树形目录属性：<br>1）属性类型为“树形目录”<br>2）输入表单内容<br>3）点击【确定】 | 列表中显示新建的业务属性，且数据正确 |
| 5 | 进入${DATASOURCE_TYPE}数据源类型的数据表详情页，查看「业务属性」 | 显示此新增的业务属性名 |

##### 【P1】验证通用业务属性-删除功能-逻辑正常

> 自动化验证点
```
1. 元模型管理页面加载成功
2. 列表中存在可删除的业务属性行
3. 点击删除后二次确认弹窗（popconfirm）
4. 确认删除后页面正常
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入元模型管理页面 | 页面加载成功，列表可见 |
| 2 | 点击业务属性行的"删除"按钮 | 弹出二次确认弹窗 |
| 3 | 确认删除 | 列表不显示该业务属性 |
| 4 | 进入${DATASOURCE_TYPE}数据源类型的数据表详情页，查看「业务属性」 | 该业务属性不显示 |

### 元数据管理

#### 未分类

##### 【P0】验证数据表列表-数据展示正确

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 位置：元数据-元数据管理-${DATASOURCE _TYPE}数据源-某数据库<br>查看列表 | 列表展示当前数据库下数据表+视图信息正确（数据与表详情页一直即可）：<br>		表名、表中文名、创建时间、存储大小、更新时间 |

##### 【P1】验证数据库列表-生命周期应用功能正确

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | tableB（未同步至资产）所属数据源和所属数据库为：datasourceB、schemaB<br>只配置datasourceB的生命周期为3天；schemaB未设置生命周期；<br>将tableB同步至资产；<br>查看tableB的生命周期 | tableB的生命周期为3天 |
| 2 | tableC（未同步至资产）所属数据源和所属数据库为：datasourceC、schemaC<br>datasourceC未设置生命周期；schemaC设置生命周期为10天；<br>将tableC同步至资产；<br>查看tableC的生命周期 | tableC的生命周期为10天 |
| 3 | tableA（未同步至资产）所属数据源和所属数据库为：datasourceA、schemaA<br>先配置datasourceA的生命周期为3天；后配置schemaA的生命周期为10天；<br>将tableA同步至资产；<br>查看tableA的生命周期 | tableA的生命周期为10天 |

## 数据标准（#10412）

### 标准管理

#### 标准定义

##### 【P0】验证数据标准-新建标准

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1）点击新建标准<br>2）输入合格的所有业务属性值和所有技术属性值，点击【保存】\|【上线】 | 1）页面跳转至数据标准列表页<br>2）列表刷新展示新建的数据标准，且状态为“待上线”\|"已上线" |
| 2 | 1）点击新建标准<br>2）输入合格的所有业务属性值和所有技术属性值，点击【取消】 | 页面跳转至数据标准列表页 |

##### 【P0】验证数据标准-查看详情

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击标准的标准名称 | 1）右侧标准详情抽屉的弹出<br>2）头部显示标准中文名、发布状态、创建时间、最近发布时间、最近发布用户<br>3）tab页显示“标准信息”和“版本变更” |

#### 标准映射

##### 【P1】验证标准映射-创建标准映射功能正常

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：active_users（见 sql/active-users.sql）
  字段：
    user_id   BIGINT NOT NULL   '用户ID'
    name      VARCHAR(50) NOT NULL  '用户姓名'
    email     VARCHAR(200) NULL  '邮箱地址'
    address   VARCHAR(500) NULL  '住址'
    age       TINYINT NULL      '用户年龄'
    sex       TINYINT NULL      '用户性别'
    last_active DATETIME        '最近活跃时间'
    property0~3 TINYINT NOT NULL
  数据：2 条（user_id: 1/2）
- active_users 已完成元数据同步至资产平台
- 已在【数据标准-标准管理-标准定义】中创建并上线数据标准：
    标准名：email，中文名：邮箱地址，对应字段类型为字符串
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据标准-标准映射，点击【标准映射】按钮，查看创建标准映射弹窗 | 弹窗打开，显示”数据源类型””数据源””数据库”级联选择控件 |
| 2 | “数据源类型”选择 Doris | “数据源”下拉项展示平台内所有 Doris 类型数据源名称 |
| 3 | “数据源”选择目标 Doris 数据源 | “数据库”下拉项展示该数据源下所有已同步的数据库（包含 active_users 所在库） |
| 4 | 选择 active_users 所在数据库，点击【添加】，再点击【确定】 | 全局提示”创建标准映射成功”；标准映射列表新增对应数据库的映射记录 |

##### 【P1】验证标准映射-标准映射字段绑功能正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 在标准映射列表找到”邮箱地址”标准行，点击【字段映射】按钮，查看字段绑定弹窗 | 字段绑定弹窗打开，显示”数据源类型””数据源””数据库””数据表”四级选择控件 |
| 2 | “数据源类型”选择 Doris | “数据源”下拉项展示平台内所有 Doris 数据源名称 |
| 3 | “数据源”选择目标 Doris 数据源 | “数据库”下拉项展示该数据源下所有已同步的数据库 |
| 4 | “数据库”选择 active_users 所在库 | “数据表”下拉项展示该库下所有已同步的表（包含 active_users） |
| 5 | “数据表”选择 active_users，字段选择 email，点击【确定】 | 全局提示”字段绑定成功”；弹窗关闭 |
| 6 | 在数据标准”邮箱地址”详情中查看【映射记录】tab | 映射记录列表新增一条记录，显示数据源名称、库名、表名 active_users、字段名 email |

### 标准基础

#### 词根管理

##### 【P1】验证词根管理-新建

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击新建词根 | 弹出新建词根弹窗 |
| 2 | 填写内容，点击确定 | 1）新增词根成功！<br>2）词根列表新增该词根 |

##### 【P1】验证词根管理-删除

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击删除并二次确认 | 1）提示删除成功；<br>2）词根列表删除该词根 |

#### 码表管理

##### 【P1】验证码表管理-新建

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击新建代码 | 弹出新建代码弹窗 |
| 2 | 填写内容，点击确定 | 1）提示新增代码成功！<br>2）代码列表新增该代码 |

#### 数据库拾取

##### 【P1】验证数据库拾取-拾取流程

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击数据库拾取页面的新建拾取icon | 弹出新建拾取弹窗 |
| 2 | 点击拾取来源下拉框 | 新增数据源类型下拉项 |
| 3 | 1）拾取类型选择词根管理<br>2）拾取来源选择${DATASOURCE_TYPE}数据源类型下的数据源<br>3）填写拾取条件<br>4）点击确定 | 全局提示：新建拾取成功；拾取列表新增该拾取 |
| 4 | 1）拾取类型选择数据标准<br>2）拾取来源选择${DATASOURCE_TYPE}数据源类型下的数据源<br>3）填写拾取条件<br>4）点击确定 | 全局提示：新建拾取成功；拾取列表新增该拾取 |
| 5 | 1）等待拾取完成<br>2）查看拾取 | 1）拾取成功<br>2）拾取列表数据正确 |

##### 【P1】验证数据库拾取-批量引用功能正常

> 前置条件
```
准备词根拾取以及数据标准拾取
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 位置：数据标准-标准基础-数据库拾取；<br>选择‘词根管理’类型，点击【查看拾取】；<br>点击【批量引用】，二次确认 | 当前页面所选词根变为“已引用”状态；<br>勾选的词根均可在‘词根管理’菜单中查询到； |
| 2 | 位置：数据标准-标准基础-数据库拾取；<br>选择‘数据标准’类型，点击【查看拾取】；<br>点击【批量引用】，二次确认 | 当前页面所选标准变为“已引用”状态；<br>勾选的标准均可在‘数据标准’菜单中查询到； |

## 数据模型（#10413）

### 建表

#### 未分类

##### 【P1】验证创建不同类型sparkThrift表功能正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建Hive PARQUET非事务内部表：<br>	- “表类型”选择“内部表”<br>	- “hdfs存储路径”为空 | 生成的建表语句正确；<br>PARQUET表创建成功； |
| 2 | 创建Hive ORC非事务内部表：<br>	- “表类型”选择“内部表”<br>	- “hdfs存储路径”为空 | 生成的建表语句正确；<br>ORC表创建成功； |
| 3 | 创建Hive TEXTFILE非事务内部表：<br>	- “表类型”选择“内部表”<br>	- “hdfs存储路径”为指定路径（非默认路径） | 生成的建表语句正确；<br>TEXTFILE表创建成功； |
| 4 | 创建Hive外部表：<br>	- “表类型”选择“外部表”<br>	- “hdfs存储路径”为指定路径（非默认路径） | 生成的建表语句正确；<br>事务表创建成功；<br>该表存储路径正确 |
| 5 | 创建Hive分区表 | 生成的建表语句正确；<br>分区表创建成功；<br>该表分区字段正确 |

##### 【P1】验证建表语句解析功能正确

> 前置条件
```
建表语句如下
CREATE TABLE IF NOT EXISTS `batch_test_v53x_new_asset`.`ods_bu_par4_df` (
`busine_line_id` STRING COMMENT '业务线ID',
`busine_line` INT COMMENT '业务线',
`bu_id` STRING COMMENT '事业 部ID',
`bu_name` STRING COMMENT '事业部',
`busi_team` STRING COMMENT '隶属商务团队',
`busi_team_date` STRING COMMENT '隶属时间',
`load_dt` TIMESTAMP COMMENT '隶属时间'
)
COMMENT 'dim维度表_新核心业务线'
PARTITIONED BY (
`data_dt` STRING COMMENT '数据时间',
`is_save` INT COMMENT '数据是否保留：0-否，1-是'
)
STORED AS PARQUET
TBLPROPERTIES ('transactional'='true');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 主键表建表语句解析 | 字段名、字段中文名、字段类型、字符长度、数值长度、字段精度、主键、分区字段、分区类型等解析成功 |
| 2 | 范围分区表语句解析 | 1）字段名、字段中文名、字段类型、字符长度、数值长度、字段精度、主键、分区字段、分区类型等解析成功<br>2）范围分区信息解析成功 |
| 3 | 各数据类型字段解析 | 所有支持的字段类型均解析 |

##### 【P1】验证「规范设计」-新增数仓层级功能正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「资产」-「数据模型」-「规范建表」页面 | 进入成功 |
| 2 | 点击「规范设计」按钮 | 进入「规范设计」页面成功 |
| 3 | 点击「新增数据层级」按钮 | 弹「新增数据层级」弹窗 |
| 4 | 「中文名称」输入「新增层级」<br>「英文名称」输入「add_catalog」<br>「描述」输入「test _ desc」 | 配置完成 |
| 5 | 点击「确定」按钮 | 新增数仓层级成功 |
| 6 | 选择「新增层级」，点击「规范设计」按钮 | 弹「规范设计」弹窗 |
| 7 | 「模型元素」配置「业务系统」「主题域」，点击「确定」按钮 | 配置成功 |
| 8 | 进入「建表」页面，「数仓层级」选择「新增层级」 | 展示「新增层级」下的「规范设计内容」 |
| 9 | 新增表流程结束 | 新增表成功 |
| 10 | 查看表所属数仓层级 | 该表存储在「新增层级」下 |

##### 【P0】验证数据模型-审批功能

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 数据开发角色用户，进入数据模型-新建表，选择doris数据源、数据库、数仓层级、模型元素<br>表名：自定义内容：「T_doris_A」<br>中文名：doris表A<br>数据模型类型：【主键表】<br>2）点击下一步 | 1）进入数据模型新建页面<br>2）进入表结构页面 |
| 2 | 新增多个字段，其中包含字段ID<br>字段id勾选主键，分区字段，动态分区，分桶字段(分桶数1)，编辑动态分区配置：同前置条件 | 新增成功 |
| 3 | 点击【生成建表语句】按钮，建表 | 1）提示：提交审批成功<br>2）我的模型-已审批页面记录新增一条记录为审批中 |
| 4 | 管理员用户进入「审批中心」，审批拒绝 | 1）表未新建，数据模型新增一条记录，为未建表状态<br>2）数据开发用户，我的模型-已审批页面记录状态变为已拒绝 |
| 5 | 管理员用户进入「审批中心」，审批通过 | 1）数据模型新增一条记录，为已建表状态<br>2）表详情正确<br>3）数据开发用户，我的模型-已审批页面记录状态变为已通过 |

## 数据质量（#10414）

### 规则任务配置

#### 未分类

##### 【P1】验证单表校验-【完整性校验-字段级-单字段】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_test（见 sql/quality-tables.sql）
  字段：id INT '主键ID', name STRING '姓名', age INT '年龄'
  数据：1 条（id=1, name='qq', age=11）— name/age 均非 NULL、非空串
- doris_test 已完成元数据同步至资产平台
- 数据质量项目已创建，数据源已授权给该质量项目
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建单表规则，选择 doris_test 表；规则类型选择"完整性校验-字段级"，选择字段 name，分别创建空值数、空值率、空串数、空串率子规则，配置阈值使校验通过（如空值数固定值 ≤ 0） | 规则创建成功，规则列表显示新增规则行；子规则数量为 4 |
| 2 | 点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态为"运行成功"；4 条子规则结果均显示"校验通过"；实际值：空值数=0、空值率=0%、空串数=0、空串率=0% |
| 3 | 编辑规则，将阈值修改为使所有子规则校验不通过（如空值数固定值 ≥ 1） | 编辑保存成功，规则配置显示已更新阈值 |
| 4 | 再次点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态为"运行成功"；4 条子规则结果均显示"校验不通过"；告警/异常标记可见 |

##### 【P0】验证单表校验-【准确性校验】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  关键字段：
    user_id BIGINT（1001/1002/1003）
    age TINYINT（25/30/22）— 均为正整数，无零值、无负值
    price DECIMAL(10,2)（99.99/199.50/49.99）
    order_count INT（5/8/3）
  数据行数：3
- doris_demo_data_types_source 已完成元数据同步
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建单表规则，选择 doris_demo_data_types_source 表；规则类型选择"准确性校验"，选择字段 age，分别创建求和、求平均、负值比、零值比、正值比子规则，配置阈值使校验通过（如求和固定值 ≤ 100，正值比 ≥ 100%） | 规则创建成功，子规则数量为 5 |
| 2 | 点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；各子规则结果显示"校验通过"；实际值：age 求和=77、求平均=25.67、负值比=0%、零值比=0%、正值比=100% |
| 3 | 编辑规则，将阈值修改为使所有子规则校验不通过（如正值比固定值 ≥ 200%） | 编辑保存成功，规则配置显示已更新阈值 |
| 4 | 再次点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；5 条子规则结果均显示"校验不通过" |

##### 【P0】验证单表校验-【规范性校验】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  关键字段：
    user_id BIGINT（1001/1002/1003，唯一无重复）
    gender VARCHAR(10)（'男'/'女'/'其他'，枚举值可控）
    department VARCHAR(20)（'技术部'/'市场部'/'销售部'）
    age TINYINT（25/30/22，范围 [0,150]）
  数据行数：3，无 NULL 值
- doris_demo_data_types_source 已完成元数据同步
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建单表规则，选择 doris_demo_data_types_source；规则类型选择"规范性校验"，针对相应字段分别创建取值范围、枚举范围、枚举个数、身份证号、手机号、邮箱、最大长度、最小长度、字符串长度、数据精度、空值数、重复数、枚举值子规则，配置阈值使所有校验通过 | 规则创建成功，子规则数量与配置项数量一致（13 条） |
| 2 | 点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；13 条子规则结果均显示"校验通过" |
| 3 | 编辑规则，修改阈值使所有子规则校验不通过（如取值范围调整为不可能的区间） | 编辑保存成功，规则配置页面显示已修改阈值 |
| 4 | 再次点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；13 条子规则结果均显示"校验不通过" |

##### 【P0】验证单表校验-【唯一性校验】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  关键字段：user_id BIGINT（1001/1002/1003，3 条记录全部唯一，无重复）
  数据行数：3
- doris_demo_data_types_source 已完成元数据同步
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建单表规则，选择 doris_demo_data_types_source；规则类型选择"唯一性校验"，选择字段 user_id，分别创建重复数、重复率、非重复个数、非重复占比子规则，配置阈值使校验通过（如重复数固定值 ≤ 0，非重复个数固定值 = 3） | 规则创建成功，子规则数量为 4 |
| 2 | 点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；4 条子规则结果均显示"校验通过"；实际值：重复数=0、重复率=0%、非重复个数=3、非重复占比=100% |
| 3 | 编辑规则，将阈值修改为使所有子规则校验不通过（如非重复个数固定值 ≥ 100） | 编辑保存成功，规则配置页面显示已更新阈值 |
| 4 | 再次点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；4 条子规则结果均显示"校验不通过" |

##### 【P0】验证单表校验-【自定义sql-单表逻辑】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_test（见 sql/quality-tables.sql）
  字段：id INT '主键ID', name STRING '姓名', age INT '年龄'
  数据：1 条（id=1, name='qq', age=11）
- doris_test 已完成元数据同步至资产平台

验证用样例 SQL（在规则中分别使用）：
  样例一：select * from doris_test
  样例二：select id,name,age from doris_test
  样例三：select id,name from doris_test
  样例四：select id,name,1 from doris_test
  样例五：select SUM(id) from doris_test
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建单表规则，选择 doris_test 表；规则类型选择"自定义SQL"，分别输入样例一至样例五的 SQL，阈值配置固定值 < 10000 | 规则创建成功；SQL 语法校验通过，规则列表显示新增记录 |
| 2 | 点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；所有样例 SQL 子规则结果均显示"校验通过"；样例五（SUM(id)=1）实际值为 1，< 10000 |
| 3 | 编辑规则，将阈值修改为固定值 < 0（使 SUM(id)=1 不满足条件） | 编辑保存成功，规则阈值已更新 |
| 4 | 再次点击【立即运行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；相关子规则结果显示"校验不通过"；实际值 1 不满足 < 0 的条件 |

##### 【P1】验证【规则集】功能正常

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 依赖表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  已含 3 条数据（user_id: 1001/1002/1003）
- doris_demo_data_types_source 已完成元数据同步至资产平台
- 规则集 SQL 模板（下载后按以下内容填写）：
    1）select * from doris_demo_data_types_source
    2）select user_id, name from doris_demo_data_types_source
    3）select * from doris_demo_data_types_source where user_id > 1005
    4）select 1 from doris_demo_data_types_source

-- 原始建表 SQL 参考 --
drop table if exists doris_demo_data_types_source;
CREATE TABLE IF NOT EXISTS doris_demo_data_types_source (
    user_id BIGINT COMMENT "用户ID",
    created_date DATE COMMENT "创建日期",
    name VARCHAR(50) COMMENT "姓名",
    age TINYINT COMMENT "年龄",
    status SMALLINT COMMENT "状态码",
    price DECIMAL(10, 2) COMMENT "价格",
    weight FLOAT COMMENT "重量",
    rating DOUBLE COMMENT "评分",
    description STRING COMMENT "描述信息",
    gender VARCHAR(10) COMMENT "性别",
    department VARCHAR(20) COMMENT "部门",
    created_time DATETIME COMMENT "创建时间",
    birth_date DATE COMMENT "出生日期",
    is_active BOOLEAN COMMENT "是否激活",
    tags VARCHAR(100) COMMENT "标签",
    total_amount BIGINT COMMENT "总金额",
    order_count INT COMMENT "订单数量"
)
ENGINE=olap
DUPLICATE KEY(user_id, created_date, name)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);

-- 插入示例数据
INSERT INTO doris_demo_data_types_source VALUES
(
    1001, '2024-01-15', '张三', 25, 1,
    99.99, 65.5, 4.5,
    '技术部员工', '男', '技术部',
    '2024-01-15 10:30:00', '1998-05-20',
    true, '科技,财经', 1500, 5
),
(
    1002, '2024-01-16', '李四', 30, 2,
    199.50, 55.2, 4.8,
    '市场部经理', '女', '市场部',
    '2024-01-16 14:20:00', '1993-12-10',
    true, '娱乐', 2500, 8
),
(
    1003, '2024-01-17', '王五', 22, 0,
    49.99, 70.1, 3.9,
    '销售专员', '其他', '销售部',
    '2024-01-17 16:45:00', '2001-08-25',
    false, '科技,体育', 800, 3
);

规则集sql：

1）select * from doris_demo_data_types_source；
2）select user_id，name from doris_demo_data_types_source；
3）select * from doris_demo_data_types_source where user_id >1005;
4)select 1 from doris_demo_data_types_source;

```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建规则集，选择 doris_demo_data_types_source 表，点击下一步 | 进入规则配置步骤，页面显示"规则集配置" |
| 2 | 点击【下载规则模板】，按前置条件中的 4 条 SQL 填写模板，上传文件 | 上传成功；规则列表显示 4 条规则，SQL 内容与填写内容一致 |
| 3 | 配置周期调度（如按天），点击【创建】 | 规则集创建成功，规则集列表显示新增一条记录，状态正常 |
| 4 | 点击【立即执行】，等待实例完成，查看运行结果 | 质量实例状态"运行成功"；SQL3（user_id>1005 无数据）返回 0 行；SQL1/2 返回 3 行；SQL4 返回 3 行；所有结果与表数据（3 条）一致 |

##### 【P1】验证【多表-同源比对】功能正常

> 前置条件
```
- 数据源类型：Doris（同源，左右表均为同一 Doris 数据源）
- 左表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  - 数据行数：3（user_id: 1001/1002/1003）
  - 关键字段类型：user_id BIGINT, created_date DATE, age TINYINT, price DECIMAL(10,2) 等
- 右表：doris_demo1_data_types_source（见 sql/quality-tables.sql）
  - 数据行数：2（user_id: 1001/1002）
  - 关键字段类型：user_id BIGINT, created_date VARCHAR(20), age INT, price VARCHAR(20) 等
  - 注意：与左表字段同名但部分类型不同（如 age: TINYINT vs INT，price: DECIMAL vs VARCHAR）
- 两张表均已完成元数据同步，数据质量项目已授权访问该数据源

-- 原始建表 SQL 参考 --
drop table if exists doris_demo_data_types_source;
CREATE TABLE IF NOT EXISTS doris_demo_data_types_source (
    user_id BIGINT COMMENT "用户ID",
    created_date DATE COMMENT "创建日期",
    name VARCHAR(50) COMMENT "姓名",
    age TINYINT COMMENT "年龄",
    status SMALLINT COMMENT "状态码",
    price DECIMAL(10, 2) COMMENT "价格",
    weight FLOAT COMMENT "重量",
    rating DOUBLE COMMENT "评分",
    description STRING COMMENT "描述信息",
    gender VARCHAR(10) COMMENT "性别",
    department VARCHAR(20) COMMENT "部门",
    created_time DATETIME COMMENT "创建时间",
    birth_date DATE COMMENT "出生日期",
    is_active BOOLEAN COMMENT "是否激活",
    tags VARCHAR(100) COMMENT "标签",
    total_amount BIGINT COMMENT "总金额",
    order_count INT COMMENT "订单数量"
)
ENGINE=olap
DUPLICATE KEY(user_id, created_date, name)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);

-- 插入示例数据
INSERT INTO doris_demo_data_types_source VALUES
(
    1001, '2024-01-15', '张三', 25, 1,
    99.99, 65.5, 4.5,
    '技术部员工', '男', '技术部',
    '2024-01-15 10:30:00', '1998-05-20',
    true, '科技,财经', 1500, 5
),
(
    1002, '2024-01-16', '李四', 30, 2,
    199.50, 55.2, 4.8,
    '市场部经理', '女', '市场部',
    '2024-01-16 14:20:00', '1993-12-10',
    true, '娱乐', 2500, 8
),
(
    1003, '2024-01-17', '王五', 22, 0,
    49.99, 70.1, 3.9,
    '销售专员', '其他', '销售部',
    '2024-01-17 16:45:00', '2001-08-25',
    false, '科技,体育', 800, 3
);





drop table if exists doris_demo1_data_types_source;
CREATE TABLE IF NOT EXISTS doris_demo1_data_types_source (
    user_id BIGINT COMMENT "用户ID",
    created_date VARCHAR(20) COMMENT "创建日期",
    name VARCHAR(50) COMMENT "姓名",
    age INT COMMENT "年龄",
    status INT COMMENT "状态码",
    price VARCHAR(20) COMMENT "价格",
    weight VARCHAR(20) COMMENT "重量",
    rating VARCHAR(20) COMMENT "评分",
    description VARCHAR(500) COMMENT "描述信息",
    gender VARCHAR(10) COMMENT "性别",
    department VARCHAR(20) COMMENT "部门",
    created_time VARCHAR(30) COMMENT "创建时间",
    birth_date VARCHAR(20) COMMENT "出生日期",
    is_active VARCHAR(10) COMMENT "是否激活",
    tags VARCHAR(100) COMMENT "标签",
    total_amount INT COMMENT "总金额",
    order_count INT COMMENT "订单数量"
)
ENGINE=olap
DUPLICATE KEY(user_id, created_date, name)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);

-- 插入示例数据
INSERT INTO doris_demo_data_types_source VALUES
(
    1001, '2024-01-15', '张三', 25, 1,
    99, 65, 4,
    '技术部员工', '男', '技术部',
    '2024-01-15 10:30:00', '1998-05-20',
    'true', '科技,财经', 1500, 5
),
(
    1002, '2024-01-16', '李四', 30, 2,
    199, 55, 4,
    '市场部经理', '女', '市场部',
    '2024-01-16 14:20:00', '1993-12-10',
    'true', '娱乐', 2500, 8
)
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建多表比对，左表选择 doris_demo_data_types_source | 右表数据源输入框自动回填左表所在数据源，且置灰不可修改 |
| 2 | 右表选择 doris_demo1_data_types_source，点击【下一步】 | 进入字段选择/映射步骤，页面显示两张表的字段列表 |
| 3 | 点击【同名映射】 | 字段类型一致的字段（如 user_id BIGINT→BIGINT、name VARCHAR→VARCHAR）自动建立映射；类型不一致的字段（如 age TINYINT vs INT、price DECIMAL vs VARCHAR）不映射，并在页面显示提示"主表与对照表字段类型不一致，无法比对" |
| 4 | 勾选 user_id 为主键 | 主键勾选成功，user_id 行显示主键标记 |
| 5 | 勾选【记录数百分比差异】，设置阈值为 0% | 勾选成功；阈值输入框显示 0% |
| 6 | 点击【创建规则】 | 规则创建成功，规则列表新增一条多表比对记录 |
| 7 | 点击【立即运行】，等待实例完成，查看结果 | 质量实例状态"运行成功"，校验结果显示"校验不通过"（左表 3 条、右表 2 条，记录数差异 33%，超出 0% 阈值） |
| 8 | 编辑规则，将【记录数百分比差异】阈值修改为 100% | 编辑保存成功，规则页面显示阈值已更新为 100% |
| 9 | 再次点击【立即运行】，等待实例完成，查看结果 | 质量实例状态"运行成功"，校验结果显示"校验通过"（记录数差异 33% ≤ 100%） |

##### 【P1】验证【多表-doris-mysql跨源比对】功能正常

> 前置条件
```
- 左表数据源：Doris；右表数据源：MySQL（跨源）
- 左表：doris_demo_data_types_source（见 sql/quality-tables.sql）
  - 数据行数：3（user_id: 1001/1002/1003）
  - 关键字段：user_id BIGINT, name VARCHAR(50), age TINYINT
- 右表：mysql_demo_data_types_source（MySQL 数据源）
  - 数据行数：2（user_id: '1001'/'1002'，VARCHAR 类型）
  - 主键：user_id VARCHAR(20)
  - 注意：字段名相同但类型全部为 VARCHAR
- 两张表均已完成元数据同步，数据质量项目已授权访问两个数据源

-- 原始建表 SQL 参考 --
drop table if exists doris_demo_data_types_source;
CREATE TABLE IF NOT EXISTS doris_demo_data_types_source (
    user_id BIGINT COMMENT "用户ID",
    created_date DATE COMMENT "创建日期",
    name VARCHAR(50) COMMENT "姓名",
    age TINYINT COMMENT "年龄",
    status SMALLINT COMMENT "状态码",
    price DECIMAL(10, 2) COMMENT "价格",
    weight FLOAT COMMENT "重量",
    rating DOUBLE COMMENT "评分",
    description STRING COMMENT "描述信息",
    gender VARCHAR(10) COMMENT "性别",
    department VARCHAR(20) COMMENT "部门",
    created_time DATETIME COMMENT "创建时间",
    birth_date DATE COMMENT "出生日期",
    is_active BOOLEAN COMMENT "是否激活",
    tags VARCHAR(100) COMMENT "标签",
    total_amount BIGINT COMMENT "总金额",
    order_count INT COMMENT "订单数量"
)
ENGINE=olap
DUPLICATE KEY(user_id, created_date, name)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);

-- 插入示例数据
INSERT INTO doris_demo_data_types_source VALUES
(
    1001, '2024-01-15', '张三', 25, 1,
    99.99, 65.5, 4.5,
    '技术部员工', '男', '技术部',
    '2024-01-15 10:30:00', '1998-05-20',
    true, '科技,财经', 1500, 5
),
(
    1002, '2024-01-16', '李四', 30, 2,
    199.50, 55.2, 4.8,
    '市场部经理', '女', '市场部',
    '2024-01-16 14:20:00', '1993-12-10',
    true, '娱乐', 2500, 8
),
(
    1003, '2024-01-17', '王五', 22, 0,
    49.99, 70.1, 3.9,
    '销售专员', '其他', '销售部',
    '2024-01-17 16:45:00', '2001-08-25',
    false, '科技,体育', 800, 3
);


2、mysql数据源已存在表：
-- 删除已存在的表（如果存在）
DROP TABLE IF EXISTS mysql_demo_data_types_source;
CREATE TABLE IF NOT EXISTS mysql_demo_data_types_source (
    user_id VARCHAR(20) COMMENT '用户ID',
    created_date VARCHAR(20) COMMENT '创建日期',
    name VARCHAR(50) COMMENT '姓名',
    age VARCHAR(10) COMMENT '年龄',
    status VARCHAR(10) COMMENT '状态码',
    price VARCHAR(20) COMMENT '价格',
    weight VARCHAR(20) COMMENT '重量',
    rating VARCHAR(20) COMMENT '评分',
    description VARCHAR(500) COMMENT '描述信息',
    gender VARCHAR(10) COMMENT '性别',
    department VARCHAR(20) COMMENT '部门',
    created_time VARCHAR(30) COMMENT '创建时间',
    birth_date VARCHAR(20) COMMENT '出生日期',
    is_active VARCHAR(10) COMMENT '是否激活(是/否)',
    tags VARCHAR(100) COMMENT '标签',
    total_amount VARCHAR(20) COMMENT '总金额',
    order_count VARCHAR(10) COMMENT '订单数量',
    PRIMARY KEY (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='MySQL数据源表';

INSERT INTO mysql_demo_data_types_source VALUES
('1001', '2024-01-15', '张三', '25', '1', '99.99', '65.5', '4.5', '技术部员工', '男', '技术部', '2024-01-15 10:30:00', '1998-05-20', '是', '科技,财经', '1500', '5'),
('1002', '2024-01-16', '李四', '30', '2', '199.50', '55.2', '4.8', '市场部经理', '女', '市场部', '2024-01-16 14:20:00', '1993-12-10', '是', '娱乐', '2500', '8')

```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 新建多表比对，左表选择 doris_demo_data_types_source，过滤条件输入 `part_date='${utc0yyyy_MM_dd_1d}'` | 右表数据源下拉框不自动填充左表数据源，允许选择质量项目下所有已授权数据源（包括 MySQL） |
| 2 | 右表数据源选择 MySQL 数据源，右表选择 mysql_demo_data_types_source，点击【下一步】 | 成功进入字段选择/映射步骤 |
| 3 | 点击【同名映射】 | 左右表字段名称相同但类型均不一致（Doris BIGINT vs MySQL VARCHAR 等）；不生成任何映射；页面提示"主表与对照表字段类型不一致，无法比对"（至少显示一处提示） |
| 4 | 手动勾选 user_id 为主键 | 主键勾选成功，user_id 行显示主键标记 |
| 5 | 勾选【记录数百分比差异】，设置阈值为 0% | 勾选成功；阈值输入框显示 0% |
| 6 | 点击【创建规则】 | 规则创建成功，规则列表新增一条跨源多表比对记录 |
| 7 | 点击【立即运行】，等待实例完成，查看结果 | 质量实例状态"运行成功"，校验结果显示"校验不通过"（左表 3 条、右表 2 条，差异 33% > 0%） |
| 8 | 编辑规则，将【记录数百分比差异】阈值修改为 50% | 编辑保存成功，规则页面显示阈值已更新为 50% |
| 9 | 再次点击【立即运行】，等待实例完成，查看结果 | 质量实例状态"运行成功"，校验结果显示"校验通过"（记录数差异 33% ≤ 50%） |

##### 【P0】【联动】验证质量和离线绑定，单规则包-【表行数-强规则-校验不通过】结果正确

> 前置条件
```
- 数据源类型：Doris（Meta数据源）
- 离线项目：env_rebuild_test
- doris1：源表，已存在 3 条数据（user_id: 1001/1002/1003），结构同 doris_demo_data_types_source
- doris2：目标空表，结构与 doris1 相同，初始行数为 0
- 离线项目 env_rebuild_test 中已存在数据同步任务：doris1 → doris2（覆盖模式）
- 质量规则：表行数校验，当前阈值配置为固定值 > 3（强规则，即校验不通过时下游离线任务失败）
- 质量任务已与上述离线同步任务绑定

-- 原始建表 SQL 参考 --
drop table if exists doris1;
CREATE TABLE IF NOT EXISTS doris_demo_data_types_source (
    user_id BIGINT COMMENT "用户ID",
    created_date DATE COMMENT "创建日期",
    name VARCHAR(50) COMMENT "姓名",
    age TINYINT COMMENT "年龄",
    status SMALLINT COMMENT "状态码",
    price DECIMAL(10, 2) COMMENT "价格",
    weight FLOAT COMMENT "重量",
    rating DOUBLE COMMENT "评分",
    description STRING COMMENT "描述信息",
    gender VARCHAR(10) COMMENT "性别",
    department VARCHAR(20) COMMENT "部门",
    created_time DATETIME COMMENT "创建时间",
    birth_date DATE COMMENT "出生日期",
    is_active BOOLEAN COMMENT "是否激活",
    tags VARCHAR(100) COMMENT "标签",
    total_amount BIGINT COMMENT "总金额",
    order_count INT COMMENT "订单数量"
)
ENGINE=olap
DUPLICATE KEY(user_id, created_date, name)
DISTRIBUTED BY HASH(user_id) BUCKETS 10
PROPERTIES (
    "replication_num" = "1"
);

-- 插入示例数据
INSERT INTO doris1 VALUES
(
    1001, '2024-01-15', '张三', 25, 1,
    99.99, 65.5, 4.5,
    '技术部员工', '男', '技术部',
    '2024-01-15 10:30:00', '1998-05-20',
    true, '科技,财经', 1500, 5
),
(
    1002, '2024-01-16', '李四', 30, 2,
    199.50, 55.2, 4.8,
    '市场部经理', '女', '市场部',
    '2024-01-16 14:20:00', '1993-12-10',
    true, '娱乐', 2500, 8
),
(
    1003, '2024-01-17', '王五', 22, 0,
    49.99, 70.1, 3.9,
    '销售专员', '其他', '销售部',
    '2024-01-17 16:45:00', '2001-08-25',
    false, '科技,体育', 800, 3
);

2、离线已存在doris2doris数据同步任务：doris1-->doris2 覆盖

3、doris2为空表，doris1中有3条表数据，覆盖模式


```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 编辑质量规则，表行数阈值设置为"固定值 > 3"，规则级别设为"强规则"，保存 | 规则保存成功；规则列表显示该规则类型为"表行数"、强规则标记可见 |
| 2 | 触发离线同步任务（doris1→doris2）运行，同时观察质量任务状态 | 质量任务列表新增一条实例，状态显示"运行中" |
| 3 | 等待质量实例和离线实例均运行结束，查看各自结果 | 1）质量实例状态"运行成功"，校验结果"校验不通过"；点击【查看明细】展示 doris1 中的 3 条数据（实际行数=3，不满足 > 3 条件）<br>2）离线实例状态"运行失败"，失败原因包含"上游质量任务失败"文字 |
| 4 | 进入【离线开发-运维中心-周期任务】，找到 doris2doris 任务，点击【补数据】，选择当前日期及下游，提交并等待完成 | 1）补数据工作流中包含 1 个规则包，离线任务运行失败，失败日志/原因包含"质量任务校验不通过"<br>2）质量任务新生成一条实例，校验结果显示"校验不通过" |

### 未分类

#### 未分类

##### 【P2】项目管理

> 自动化验证点
```
1. 项目列表页面加载成功
2. 页面包含项目列表(.ant-table)或项目卡片(.ant-card)
3. 新建/编辑/删除按钮可见
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据质量-项目管理页面 | 页面加载成功，项目列表/卡片可见 |
| 2 | 验证项目增删改查功能入口 | 新建/编辑/删除按钮可见 |

## 数据安全（#10415）

### 未分类

#### 未分类

##### 【P2】数据权限管理

> 自动化验证点
```
1. 权限管理页面加载成功
2. 权限分配tab/表格/卡片可见
3. “新增权限策略”或”新增”按钮可见
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入数据安全-权限管理页面 | 页面加载成功，权限分配tab/表格可见 |
| 2 | 查看”新增权限策略”按钮 | 按钮可见 |
| 3 | 验证数据权限-新增功能正常 | 「权限范围选择」：<br>添加第一条记录：数据库（schema1），数据表（s1_table1、s1_table2、s1_table3）<br>添加第二条记录：数据库（schema2），数据表（s2_table1）<br><br>「数据权限配置」：<br>1）选择”表级权限”：DDL、DML、DQL；<br>2）选择”有效期配置”：2023-10-20；<br>3）开启”是否开启行列权限配置”<br><br>「行列权限配置」：<br>配置s1_table1以及s2_table1的行级权限和列级权限<br><br>「权限生效用户」：<br>1）选择”用户组”；<br>2）选择”用户”<br><br>点击【新增权限策略】 |

##### 【P2】数据分级分类

> 自动化验证点
```
1. 级别管理页面加载成功，"添加级别"按钮可见
2. 自动分级页面加载成功，表格/卡片可见
3. 分级数据页面加载成功，表格/卡片可见
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入级别管理页面 | 页面加载成功，"添加级别"按钮可见 |
| 2 | 进入自动分级页面 | 页面加载成功，表格/卡片可见 |
| 3 | 进入分级数据页面 | 页面加载成功，表格/卡片可见 |

### 数据脱敏管理doris/sparkThrift

#### 未分类

##### 【P0】验证表脱敏功能正常

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1）准备“全部脱敏”类型规则<br>2）“全部脱敏”脱敏应用XX表的username字段；<br>3）查看该表数据预览 | 该表username字段数据全脱敏 |
| 2 | 1）准备“部分脱敏”类型规则<br>2）“部分脱敏”脱敏应用XX表的username字段；<br>3）查看该表数据预览 | 该表username字段数据部分脱敏 |
