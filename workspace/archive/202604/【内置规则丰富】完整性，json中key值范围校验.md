---
suite_name: "【内置规则丰富】完整性，json中key值范围校验"
description: "完整性校验支持对json类型字段做key值范围的校验"
prd_id: 15693
prd_version: "v6.3"
product: ""
tags:
  - "【内置规则丰富】完整性，json中key值范围校验"
create_at: "2026-04-05"
status: "草稿"
case_count: 29
origin: "xmind"
---

## 【内置规则丰富】完整性，json中key值范围校验

### 【内置规则丰富】完整性，json中key值范围校验

#### 规则配置-key范围校验基础功能

##### 【P0】验证key范围校验完整主流程：配置规则+执行校验+查看质量报告

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护如下key数据（含多层级）：第一层级：key1（姓名）、key2（年龄）；第二层级：key11（省份）、key22（城市）
已在Doris数据源中执行如下建表及灌数SQL：CREATE TABLE test_json_key_range (id INT, info JSON) DISTRIBUTED BY HASH(id) BUCKETS 3 PROPERTIES("replication_num" = "1"); INSERT INTO test_json_key_range VALUES (1, '{"key1":"张三","key2":25,"key11":"广东","key22":"深圳"}'), (2, '{"key1":"李四"}'), (3, '{"key2":30,"key11":"北京","key22":"朝阳"}');
已创建规则任务"task_json_key_range_test"并关联上述Doris表，任务状态为启用
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到规则任务"task_json_key_range_test"，点击【编辑规则】按钮，进入规则配置页面 |  |
| 3 | 点击【新增规则】，在统计函数下拉列表中选择【key范围校验】；在字段选择框中选择"info"（json类型字段）；校验方法选择【包含】；在校验内容下拉框中勾选"key1（姓名）"和"key2（年龄）"，点击【确认】；强弱规则保持【强规则】；点击【保存】 | 保存成功，规则配置参数展示区显示：规则类型=字段级、字段=info、统计函数=key范围校验、校验方法=包含、校验内容=key1-key2、强弱规则=强规则 |
| 4 | 返回规则任务列表，找到"task_json_key_range_test"，点击【立即执行】，等待任务执行状态变为"执行完成" | 任务执行完成，无报错 |
| 5 | 进入【数据质量 → 质量报告】，找到该任务最新一次执行记录，查看报告详情 | 质量报告中该规则行显示：规则类型列=完整性校验、规则名称列=key范围校验、字段类型列=json；id=1的记录（含key1和key2）质检结果=校验通过，详情说明=符合规则key范围包含"key1-key2"，操作列无查看详情；id=2、3的记录（缺少key1或key2其中一个）质检结果=校验不通过，未通过原因=key范围校验未通过，详情说明=不符合规则key范围包含"key1-key2"，操作列显示【查看详情】 |

##### 【P0】验证统计函数选择key范围校验后字段选择变为单选

> 前置条件
```
已创建规则任务"task_json_key_range_test"，关联的数据表含多个json/string类型字段：info（json类型）、extra_info（string类型）
进入该任务的规则配置编辑页面
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】进入规则配置页 |  |
| 3 | 点击【新增规则】，先选择一个普通统计函数（如：非空值数），在字段选择框中同时勾选"info"和"extra_info"两个字段，确认此时字段支持多选 | 字段选择框支持多选，可同时勾选多个字段 |
| 4 | 将统计函数切换为【key范围校验】，观察字段选择框的变化及悬浮提示 | 切换为key范围校验后，统计函数标签旁出现悬浮提示图标，鼠标悬浮显示文案"当选择key范围校验时，字段仅支持单选"；字段选择框变为单选模式，原多选值自动清空 |
| 5 | 在字段选择框中尝试同时勾选"info"和"extra_info"两个字段 | 字段选择框只允许选择一个字段，选择"extra_info"后"info"自动取消，最终只有一个字段被选中 |

##### 【P1】验证校验方法切换（包含↔不包含）规则保存和执行结果差异

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护key数据：key1（姓名）、key2（年龄）
Doris表test_json_key_range中已有数据：id=1记录包含key1和key2，id=2记录仅包含key1
已创建规则任务"task_json_method_switch"并关联上述Doris表
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_method_switch"，点击【编辑规则】，新增规则：统计函数=key范围校验、字段=info（json类型）、校验方法=【包含】、校验内容勾选"key1（姓名）"和"key2（年龄）"，保存后立即执行任务，等待执行完成 |  |
| 3 | 查看质量报告，记录id=1和id=2的质检结果 |  |
| 4 | 返回规则配置，将校验方法改为【不包含】，保存后再次立即执行任务，等待执行完成 |  |
| 5 | 再次查看质量报告，查看最新一次执行结果中id=1和id=2的质检结果 | 步骤3（包含逻辑）：id=1（含key1和key2）质检结果=校验通过；id=2（仅含key1，缺key2）质检结果=校验不通过，未通过原因=key范围校验未通过<br>步骤5（不包含逻辑）：id=1（含key1和key2，违反不包含规则）质检结果=校验不通过，详情说明=不符合规则key范围不包含"key1-key2"；id=2（仅含key1，key2不存在，部分符合）质检结果根据系统规则判定，详情说明中显示不包含的key信息 |

##### 【P1】验证校验内容支持多选和全选操作

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护以下key数据：key1（姓名）、key2（年龄）、key3（性别）、key11（省份）、key22（城市）、key33（区县）
已进入规则任务"task_json_key_range_test"的规则配置编辑页
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】，字段选择"info"，点击校验内容下拉框 |  |
| 3 | 在校验内容下拉框中依次勾选"key1（姓名）"、"key2（年龄）"、"key3（性别）"三个选项 | 勾选3个key后，下拉框内显示已选中3项，各key名前复选框呈选中状态 |
| 4 | 点击【全部】选项，观察所有key是否被全部勾选 | 点击"全部"后，所有6个key（key1、key2、key3、key11、key22、key33）全部被勾选，"全部"选项呈全选状态 |
| 5 | 再次点击【全部】选项，观察是否全部取消勾选 | 再次点击"全部"，所有key全部取消勾选，"全部"选项恢复未选状态 |
| 6 | 重新勾选"key1（姓名）"和"key11（省份）"，确认后查看校验内容回显 | 确认后，规则配置中校验内容回显格式为"key1-key11"（分层级用"-"拼接、不同配置组用";"分隔） |

##### 【P1】验证校验内容下拉框支持输入关键词搜索查询

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护以下key数据：key1（姓名）、key2（年龄）、key3（性别）、key11（省份）、key22（城市）、key33（区县）
已进入规则任务"task_json_key_range_test"规则配置编辑页，统计函数已选择key范围校验
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】，字段选择"info"，点击校验内容下拉框 |  |
| 3 | 在校验内容下拉框内的输入框中输入"key1" | 下拉列表过滤显示包含"key1"的结果：key1（姓名）、key11（省份），其余key不显示 |
| 4 | 清空搜索框内容，输入"省份" | 下拉列表过滤显示包含"省份"的结果：key11（省份），其余key不显示 |
| 5 | 输入"xyz_not_exist"（不存在的关键词） | 下拉列表显示"暂无数据"或空列表，不显示任何key选项 |

##### 【P1】验证key数据量超过200条时默认加载前200条及搜索功能

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护超过200条key数据，前200条为key_001至key_200，第201条起为key_201、key_202等
已进入规则任务"task_json_key_range_test"规则配置编辑页，统计函数已选择key范围校验
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】，字段选择"info"，点击校验内容下拉框，等待列表加载完成 |  |
| 3 | 在下拉框中统计可见的key条目总数，并滚动到列表底部查看是否有更多数据提示 | 下拉框中最多显示200条key数据（key_001至key_200），滚动到底部不再追加加载或有提示"已加载前200条" |
| 4 | 在搜索框中输入"key_201"，等待搜索结果加载 | 输入"key_201"后，搜索结果正确返回"key_201"（超出默认200条的数据可通过搜索找到），不受前200条默认加载限制 |

##### 【P1】验证校验内容回显格式为key1-key2;key11-key22格式

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护key数据：第一层级key1（姓名）、key2（年龄）；第二层级key11（省份）、key22（城市）
已进入规则任务"task_json_key_range_test"规则配置编辑页
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】，字段选择"info"，打开校验内容下拉框，在第一层级勾选"key1（姓名）"和"key2（年龄）"，在第二层级勾选"key11（省份）"和"key22（城市）"，点击确认 |  |
| 3 | 查看规则配置表单中"校验内容"字段的回显内容 | 校验内容回显格式为"key1-key2;key11-key22"，同层级key用"-"连接，不同层级配置组之间用";"分隔 |
| 4 | 保存规则后，再次打开规则配置编辑页，查看已保存的校验内容展示 | 已保存的规则中校验内容回显仍为"key1-key2;key11-key22"，格式不变 |

##### 【P1】验证校验内容悬浮展示：默认显示前两个key，悬浮展示全部

> 前置条件
```
已在规则任务"task_json_key_range_test"中配置key范围校验规则，校验内容共选择了4个key：key1（姓名）、key2（年龄）、key11（省份）、key22（城市）
规则已保存，进入规则配置查看页
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】进入规则配置页，查看已配置的key范围校验规则行中的"校验内容"列 |  |
| 3 | 观察"校验内容"列在非悬浮状态下的显示内容 | 校验内容列默认仅展示前两个key信息（如"key1-key2…"），超出部分以省略号或截断方式显示 |
| 4 | 将鼠标悬浮在"校验内容"列的文本上，等待tooltip出现 | 鼠标悬浮后，tooltip中完整展示全部4个key信息："key1-key2;key11-key22" |

##### 【P1】验证校验内容悬浮提示文案正确

> 前置条件
```
已进入规则任务的规则配置编辑页，统计函数已选择key范围校验
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到任意规则任务，点击【编辑规则】，新增规则，统计函数选择【key范围校验】 |  |
| 3 | 将鼠标悬浮在"校验内容"标签旁的提示图标上 | tooltip显示文案为"校验内容key信息需要在通用配置模块维护。"，文案完全匹配，无多余空格或乱码 |

#### 规则配置-字段类型限制

##### 【P0】验证json类型字段可成功配置key范围校验规则

> 前置条件
```
已创建规则任务"task_field_type_test"，关联的Doris表含以下字段：info（JSON类型）、name（VARCHAR类型）、age（INT类型）
已在通用配置中维护key1（姓名）、key2（年龄）
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_field_type_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】，展开字段选择列表 | 字段下拉列表中，JSON类型字段"info"可正常选择，INT类型字段"age"置灰不可选 |
| 3 | 观察字段下拉列表中各字段的可选状态，选择"info"（JSON类型字段），选择校验方法【包含】，校验内容勾选"key1（姓名）"，点击保存 | 选择"info"字段后保存成功，规则配置项正常展示 |

##### 【P1】验证string类型字段可成功配置key范围校验规则

> 前置条件
```
已创建规则任务"task_field_type_test"，关联的Doris表含string（VARCHAR/TEXT）类型字段：extra_info（VARCHAR类型，存储JSON字符串）
已在通用配置中维护key1（姓名）、key2（年龄）
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_field_type_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】 | 字段下拉列表中string类型字段"extra_info"可正常选择（不置灰） |
| 3 | 在字段下拉框中选择"extra_info"（string类型），校验方法选择【包含】，校验内容勾选"key1（姓名）"，点击保存 | 保存成功，规则配置参数中字段=extra_info，无报错提示 |

##### 【P1】验证选择非json/string类型字段时key范围校验不可用或提示错误

> 前置条件
```
已创建规则任务"task_field_type_test"，关联的Doris表含INT类型字段"age"、DATE类型字段"create_date"、BIGINT类型字段"user_id"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_field_type_test"，点击【编辑规则】，新增规则，统计函数选择【key范围校验】 | 字段下拉框中，INT类型字段"age"、DATE类型字段"create_date"、BIGINT类型字段"user_id"均置灰不可选，或不出现在可选列表中 |
| 3 | 观察字段下拉框，尝试选择"age"（INT类型）字段 | 无法选择"age"字段，系统未发生JS报错 |

#### 规则配置-表单校验（逆向）

##### 【P1】验证未选择字段时保存key范围校验规则提示必填

> 前置条件
```
已进入规则任务"task_json_key_range_test"的规则配置编辑页
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，点击【新增规则】，统计函数选择【key范围校验】 |  |
| 3 | 不选择任何字段，校验方法选择【包含】，校验内容勾选"key1（姓名）"，直接点击【保存】 | 保存失败，字段选择框下方显示红色错误提示"请选择字段"（或等效必填提示），页面不跳转，规则未被保存 |

##### 【P1】验证未选择校验内容时保存key范围校验规则提示必填

> 前置条件
```
已进入规则任务"task_json_key_range_test"的规则配置编辑页
已在通用配置中维护key1（姓名）、key2（年龄）
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，点击【新增规则】，统计函数选择【key范围校验】，字段选择"info"，校验方法选择【包含】 |  |
| 3 | 不选择任何校验内容（校验内容选择框保持空），直接点击【保存】 | 保存失败，校验内容选择框下方显示红色错误提示"请选择校验内容"（或等效必填提示），页面不跳转，规则未被保存 |

##### 【P1】验证未选择校验方法时保存key范围校验规则提示必填

> 前置条件
```
已进入规则任务"task_json_key_range_test"的规则配置编辑页
已在通用配置中维护key1（姓名）
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】，点击【新增规则】，统计函数选择【key范围校验】，字段选择"info"，校验内容勾选"key1（姓名）" |  |
| 3 | 清空/不选择校验方法（若默认有值则手动清除），直接点击【保存】 | 保存失败，校验方法下拉框下方显示红色错误提示"请选择校验方法"（或等效必填提示），页面不跳转，规则未被保存 |

#### 规则库-内置规则展示

##### 【P1】验证规则库中新增key范围校验内置规则展示信息正确

> 前置条件
```
当前环境为v6.3岚图定制化分支，数据质量模块规则库功能可用
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 导航至【数据质量 → 规则库】（或规则配置中的内置规则列表），在规则分类中选择【完整性校验】，查找"key范围校验"规则 | 规则库完整性校验分类下可找到"key范围校验"规则 |
| 3 | 点击"key范围校验"规则行或查看规则详情 | 规则详情显示：规则名称=key范围校验、规则解释=对数据中包含的key范围校验、规则分类=完整性校验、关联范围=字段、规则描述=校验json类型的字段中key名是否完整，对key的范围进行校验 |
| 4 | 将鼠标悬浮在规则列表中"key范围校验"的统计函数名称或提示图标上 | 悬浮提示内容为"对数据中包含的key范围校验"（与规则库中"规则解释"字段内容一致） |

#### 结果查询-校验明细与日志

##### 【P1】验证校验不通过时查看明细：标题、字段标红及全字段展示

> 前置条件
```
已在Doris表test_json_key_range中插入数据：INSERT INTO test_json_key_range VALUES (10, '{"key1":"王五"}');（仅含key1，缺key2）
规则任务"task_json_key_range_test"已配置key范围校验规则（包含key1和key2），并已执行完成，id=10记录质检结果为"校验不通过"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"最新执行记录，进入结果查询页面，找到质检结果为"校验不通过"的规则行 |  |
| 3 | 点击该规则行的【查看明细】或【查看详情】按钮，等待明细弹窗/页面加载完成 | 成功打开明细页面，页面加载不报错 |
| 4 | 观察明细页面的标题文案、数据列表字段列数、以及id=10记录的"info"字段展示样式 | 明细标题显示"查看"完整性校验-key范围校验"明细"（文案完全匹配）；数据列表展示原表全部字段（id、info等）；id=10记录的"info"字段内容以红色字体或红色背景高亮标红展示；不符合要求的数据（id=10）出现在列表中 |

##### 【P1】验证下载明细数据中校验字段标红展示

> 前置条件
```
规则任务"task_json_key_range_test"已执行，存在校验不通过记录，明细页面已打开
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 进入"task_json_key_range_test"结果查询页，点击校验不通过规则的【查看明细】，等待明细页加载完成 |  |
| 3 | 点击明细页面的【下载】或【导出】按钮，等待文件下载完成，打开下载的Excel/CSV文件 | 文件成功下载，文件格式为Excel（.xlsx）或CSV（.csv） |
| 4 | 查看下载文件中校验字段（info列）的单元格样式 | Excel文件中，不符合规则记录的"info"字段对应单元格呈红色背景或红色字体标注，与页面展示一致 |

##### 【P1】验证校验通过时结果查询页不显示明细入口

> 前置条件
```
已在Doris表test_json_key_range中插入数据：INSERT INTO test_json_key_range VALUES (20, '{"key1":"赵六","key2":35}');（包含key1和key2）
规则任务"task_json_key_range_test"已配置包含key1和key2的规则，并已执行完成，id=20记录质检结果为"校验通过"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"最新执行记录，进入结果查询页面 |  |
| 3 | 找到质检结果为"校验通过"的规则行，查看操作列 | 质检结果为"校验通过"的规则行，操作列不显示【查看明细】或【查看详情】按钮（显示"--"或操作列为空），无法进入明细页 |

##### 【P2】验证校验失败时支持查看日志

> 前置条件
```
已创建规则任务"task_json_fail_test"，规则配置中校验内容引用了已被删除的key（模拟校验失败场景），任务已执行，执行状态为"执行失败"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_fail_test"，进入结果查询页面，找到执行状态为"执行失败"的记录行 |  |
| 3 | 点击该记录行的【查看日志】按钮，等待日志内容加载 | 日志弹窗或页面正常打开，显示任务执行失败的错误日志内容，日志内容包含报错时间、错误类型或错误描述信息，不显示空白页 |

#### 质量报告-展示验证

##### 【P0】验证质量报告中校验通过行的各列展示内容正确

> 前置条件
```
规则任务"task_json_key_range_test"已执行完成，存在质检结果为"校验通过"的记录
该规则配置：字段=info（json类型）、统计函数=key范围校验、校验方法=包含、校验内容=key1-key2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，进入【质量报告】，查看最新一次执行的报告详情 |  |
| 3 | 找到质检结果为"校验通过"的规则行，逐列查看各字段值 | 该规则行各列展示正确：规则类型列=完整性校验；规则名称列=key范围校验；字段类型列=json；质检结果=校验通过；未通过原因列=--（无内容）；详情说明列=符合规则key范围包含"key1-key2"；操作列=--（无查看详情入口） |

##### 【P0】验证质量报告中校验不通过行的各列展示内容正确

> 前置条件
```
规则任务"task_json_key_range_test"已执行完成，存在质检结果为"校验不通过"的记录
该规则配置：字段=info（json类型）、统计函数=key范围校验、校验方法=包含、校验内容=key1-key2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，进入【质量报告】，查看最新一次执行的报告详情 |  |
| 3 | 找到质检结果为"校验不通过"的规则行，逐列查看各字段值，并点击操作列的【查看详情】 | 该规则行各列展示正确：规则类型列=完整性校验；规则名称列=key范围校验；字段类型列=json；质检结果=校验不通过；未通过原因列=key范围校验未通过；详情说明列=不符合规则key范围包含"key1-key2"；操作列显示【查看详情】按钮，点击后跳转至明细页 |

##### 【P1】验证质量报告中不包含校验方法的详情说明格式正确

> 前置条件
```
规则任务"task_json_not_include_test"已执行，配置：字段=info（json类型）、统计函数=key范围校验、校验方法=不包含、校验内容=key1-key2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_not_include_test"，进入【质量报告】，查看最新一次执行的报告详情 |  |
| 3 | 分别查看校验通过行和校验不通过行的"详情说明"列内容 | 校验通过行：详情说明=符合规则key范围不包含"key1-key2"；校验不通过行：详情说明=不符合规则key范围不包含"key1-key2"；两者格式均正确体现"不包含"校验方法 |

#### 规则配置参数展示

##### 【P1】验证规则配置参数卡片完整展示所有字段

> 前置条件
```
规则任务"task_json_key_range_test"中已配置并保存以下规则：字段=info（json类型）、统计函数=key范围校验、过滤条件=id > 0、校验方法=包含、校验内容=key1-key2;key11-key22、强弱规则=强规则、规则描述=测试key范围校验规则
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_key_range_test"，点击【编辑规则】进入规则配置页 |  |
| 3 | 找到已配置的key范围校验规则，点击规则行展开或查看参数详情卡片，逐项核对各参数字段 | 规则配置参数卡片完整展示以下所有字段且内容正确：规则类型=字段级；字段=info；统计函数=key范围校验；过滤条件=id > 0；校验方法=包含；校验内容=key1-key2;key11-key22；强弱规则=强规则；规则描述=测试key范围校验规则 |

#### 多数据源兼容性

##### 【P1】验证Doris3.x数据源的json字段支持key范围校验

> 前置条件
```
已在Doris3.x数据源中执行以下SQL：CREATE TABLE doris_json_test (id INT, data JSON) DISTRIBUTED BY HASH(id) BUCKETS 3 PROPERTIES("replication_num" = "1"); INSERT INTO doris_json_test VALUES (1, '{"key1":"A","key2":"B"}'), (2, '{"key1":"C"}');
已在通用配置中维护key1、key2，已创建关联该Doris3.x表的规则任务"task_doris_test"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_doris_test"，点击【编辑规则】，新增规则：统计函数=key范围校验、字段=data（json类型）、校验方法=包含、校验内容=key1和key2，保存 | 保存成功，无报错 |
| 3 | 立即执行"task_doris_test"，等待任务执行完成（状态变为"执行完成"） | 任务执行完成，状态=执行完成，无异常日志 |
| 4 | 查看质量报告中的执行结果 | id=1（含key1和key2）质检结果=校验通过；id=2（缺key2）质检结果=校验不通过 |

##### 【P1】验证SparkThrift2.x数据源的json字段支持key范围校验

> 前置条件
```
SparkThrift2.x数据源已配置并连接正常，已在该数据源中创建含json字段的表并插入数据：CREATE TABLE spark_json_test (id INT, data STRING); INSERT INTO spark_json_test VALUES (1, '{"key1":"X","key2":"Y"}'), (2, '{"key1":"Z"}');
已创建关联该SparkThrift2.x表的规则任务"task_spark_test"，已在通用配置中维护key1、key2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_spark_test"，点击【编辑规则】，新增规则：统计函数=key范围校验、字段=data（string类型）、校验方法=包含、校验内容=key1和key2，保存后立即执行，等待状态变为"执行完成" | 保存成功，任务执行完成，无异常日志 |
| 3 | 查看质量报告中的执行结果 | id=1质检结果=校验通过；id=2质检结果=校验不通过，与Doris数据源行为一致 |

##### 【P1】验证Hive2.x数据源的json字段支持key范围校验

> 前置条件
```
Hive2.x数据源已配置并连接正常，已在该数据源中创建含string类型字段的表并插入数据：CREATE TABLE hive_json_test (id INT, data STRING); INSERT INTO hive_json_test VALUES (1, '{"key1":"M","key2":"N"}'), (2, '{"key1":"P"}');
已创建关联该Hive2.x表的规则任务"task_hive_test"，已在通用配置中维护key1、key2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_hive_test"，点击【编辑规则】，新增规则：统计函数=key范围校验、字段=data（string类型）、校验方法=包含、校验内容=key1和key2，保存后立即执行，等待状态变为"执行完成" | 保存成功，任务执行完成，无异常日志 |
| 3 | 查看质量报告中的执行结果 | id=1质检结果=校验通过；id=2质检结果=校验不通过，与Doris/Spark数据源行为一致 |

#### 大数据量与层级校验

##### 【P1】验证key数量几千个时按层级校验逻辑正确执行

> 前置条件
```
已在【通用配置 → json格式校验管理】中维护含3000条key的数据（第一层级key_l1_001至key_l1_1000，第二层级key_l2_001至key_l2_2000）
已在Doris表中插入以下数据：INSERT INTO test_json_key_range VALUES (30, '{"key_l1_001":"v1","key_l2_001":"v2"}'), (31, '{"key_l1_001":"v3"}'), (32, '{"key_other":"v4"}');
已创建规则任务"task_json_large_key_test"并关联上述Doris表，校验内容选择第一层级key_l1_001和第二层级key_l2_001
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_large_key_test"，点击【编辑规则】，配置key范围校验规则：字段=info、校验方法=包含、校验内容=key_l1_001（第一层级）和key_l2_001（第二层级）；保存后立即执行，等待任务执行完成 | 保存成功，任务在合理时间内（5分钟内）执行完成，无超时或OOM报错 |
| 3 | 进入质量报告查看执行结果，分别查看id=30、31、32的记录校验结果 | id=30（含key_l1_001和key_l2_001）=校验通过；id=31（缺key_l2_001）=校验不通过；id=32（不含任何目标key）=校验不通过；层级匹配逻辑正确，第一层级key不与第二层级key混淆 |

#### 弱规则配置（P2）

##### 【P2】验证将key范围校验规则设为弱规则时执行和报告显示正常

> 前置条件
```
已在通用配置中维护key1、key2；Doris表test_json_key_range中含id=2的记录（仅含key1，缺key2）
已创建规则任务"task_json_weak_rule_test"
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ------------ | ------------ |
| 1 | 进入【数据质量 → 规则任务管理】页面，等待规则任务列表加载完成 |  |
| 2 | 找到"task_json_weak_rule_test"，点击【编辑规则】，新增规则：统计函数=key范围校验、字段=info、校验方法=包含、校验内容=key1和key2、强弱规则=【弱规则】，保存后立即执行，等待执行完成 | 保存成功，任务正常执行完成，无报错 |
| 3 | 进入质量报告，查看id=2记录（校验不通过）的展示和任务整体执行状态 | id=2记录质检结果=校验不通过，但任务整体状态不因弱规则未通过而标记为"失败"；质量报告中"强弱规则"列或规则标识显示"弱规则" |