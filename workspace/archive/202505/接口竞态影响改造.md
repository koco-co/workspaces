---
suite_name: "接口竞态影响改造"
description: "接口竞态影响改造用例归档"
tags:
  - "数据库"
  - "数据表"
  - "态问题"
  - "互独立"
  - "目录与"
  - "数据源"
  - "数据预览"
  - "级联请求"
  - "数据地图"
  - "数据表请求"
  - "数据库级联"
  - "元数据同步"
  - "源搜索功能"
  - "换任务类型"
  - "数据源类型"
prd_version: "v6.3.4"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 22
case_id: 8800
---

##### 【P1】验证多表左右表之间的数据表请求、分区列表请求、预览请求相互独立

> 前置条件

```
无
```

##### 【P1】验证多表的数据源、数据库、数据表、数据预览、分区搜索联动

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | trino |  |

##### 【P1】验证规则集面板与抽屉详情间的竞态问题

> 前置条件

```
无
```

##### 【P1】验证规则集数据源、数据库级联

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | trino |  |

##### 【P1】验证单表的数据源、数据库、数据表、数据预览、分区搜索联动

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | trino |  |

##### 【P1】验证标准匹配接口的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表，选择一个字段匹配标准，然后快速选择另一个字段的匹配标准 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证新建数据模型，数仓层级和模型元素之间的级联请求

> 前置条件

```
无
```

##### 【P1】验证新建数据模型，数据源和数据库的级联请求

> 前置条件

```
无
```

##### 【P1】验证规范设计模型元素与列表的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-规范设计，快速点击/切换模型元素，查看接口和列表数据 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证标准映射目录与列表的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据标准-标准映射，快速切换标准目录，查看接口和列表数据 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |
| 2 | 新增标准映射，选择标准，快速选择/取消标准目录，查看数据标准的接口和下拉框数据 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证码表管理目录与列表的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据标准-码表管理，快速切换代码目录，查看接口和列表数据 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证数据标准目录与列表的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据标准-标准定义，快速切换标准目录，查看接口和列表数据 | 1）数据正确2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证数据表下拉框交互

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-元数据同步，选择数据源，选择数据库，查看数据表返回 | 返回前200条数据 |
| 2 | 搜索，触发失焦，然后聚焦 | 返回前200条数据 |
| 3 | 搜索，选择表，查看数据表下拉框 | 返回前200条数据 |

##### 【P1】验证不同数据库请求数据表的接口是否相互独立

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-元数据同步，选择数据源，添加两条数据库，选择第一个数据库，然后不断切换第二个数据库，查看展示 | 1）第一个数据库接口正常返回，未被取消2）第二个数据库下拉框返回最近一次搜索的结果，只保留最后一次请求 |

##### 【P1】验证元数据同步，数据表搜索接口竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-元数据同步，选择数据源、数据库，不断输入内容调用数据表搜索接口，查看展示 | 1）下拉框返回最近一次搜索的结果2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证元数据同步接口竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-元数据同步，新增同步，切换成3G，快速切换数据源，查看接口和数据库下拉框 | 1）数据库下拉框返回最近一次选择的数据源下的数据库2）只保留最后一次请求的接口，先前的接口被cancel |
| 2 | 快速切换数据源，然后清空数据源，查看数据库下拉框内容 | 下拉框内容被清空 |
| 3 | 选择trino类型，快速切换数据源，查看接口和catalog下拉框 | 1）catalog下拉框返回最近一次选择的数据源下的catalog2）只保留最后一次请求的接口，先前的接口被cancel |
| 4 | 快速切换catalog，查看接口和数据库下拉框 | 1）数据库下拉框返回最近一次选择的数据源下的数据库2）只保留最后一次请求的接口，先前的接口被cancel |

##### 【P1】验证批量指定目录，数据源类型和数据源搜索功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据地图搜索页，点击数据目录标题右侧的批量指定目录按钮 | 弹窗批量指定目录弹窗 |
| 2 | 对数据源类型下拉框进行模糊搜索 | 结果正确 |
| 3 | 对数据源下拉框进行模糊搜索 | 结果正确 |

##### 【P1】验证数据目录的搜索与目录树间的竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 在数据地图搜索页，对目录快速进行多次搜索，查看列表接口和数据 | 只保留最近一次请求的接口，返回数据正确 |

##### 【P0】验证目录与、所有搜索项与表格列表间是否存在竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 在数据地图搜索页，选择目录，然后筛选项进行搜索，查看列表接口和数据 | 只保留最近一次请求的接口，返回数据正确 |
| 2 | 在数据地图搜索页，筛选项进行搜索，然后选择目录，查看列表接口和数据 | 只保留最近一次请求的接口，返回数据正确 |

##### 【P1】验证数据地图搜索页切换任务类型，接口竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 在数据地图搜索页，搜索框左侧切换任务类型，查看负责人接口和下拉框 | 只保留最近一次请求的接口，下拉框返回数据和任务类型对应 |
| 2 | 切换任务类型，查看标签接口和下拉框 | 只保留最近一次请求的接口，下拉框返回数据和任务类型对应 |

##### 【P1】验证数据源类型、数据源、数据库下拉框级联功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 在数据地图搜索页，切换成3G，快速切换数据源类型、数据源、数据库，然后清空数据源类型，查看数据源和数据库下拉框 | 下拉框内容被清空 |
| 2 | 在数据地图搜索页，切换成3G，快速切换数据源类型、数据源、数据库，然后清空数据源，查看数据库下拉框 | 下拉框内容被清空 |

##### 【P0】验证数据地图-表搜索接口竞态问题

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 在数据地图搜索页，切换成3G，快速切换数据源类型，查看接口和数据源下拉框 | 1）数据源下拉框返回最近一次选择的数据源类型的数据源2）只保留最后一次请求的接口，先前的接口被cancel |
| 2 | 快速切换数据源，查看接口和数据库下拉框 | 1）数据库下拉框返回最近一次选择的数据源下的数据库2）只保留最后一次请求的接口，先前的接口被cancel |
| 3 | 选择trino类型，快速切换数据源，查看接口和catalog下拉框 | 1）catalog下拉框返回最近一次选择的数据源下的catalog2）只保留最后一次请求的接口，先前的接口被cancel |
| 4 | 快速切换catalog，查看接口和数据库下拉框 | 1）数据库下拉框返回最近一次选择的数据源下的数据库2）只保留最后一次请求的接口，先前的接口被cancel |
