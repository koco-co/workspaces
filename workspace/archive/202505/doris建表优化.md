---
suite_name: "doris建表优化"
description: "doris建表优化用例归档"
tags:
  - "建表"
  - "源后"
  - "新建表"
  - "区字段"
  - "许修改"
  - "点功能"
  - "分区字段"
  - "批量解析"
  - "存储格式"
  - "据源类型"
  - "列表分区表"
  - "范围分区表"
  - "列表分区后"
  - "数据模建表"
  - "建表第一步"
prd_version: "v6.3.4"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 19
case_id: 8843
---

##### 【P1】验证范围分区编辑功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 编辑表模型第二步，修改范围分区的配置 | 不允许编辑 |

##### 【P1】验证新建表-列表分区有多个分区字段

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）选择2个字段作为分区字段（字段名：dt，city）;2) 配置分区- 分区名：p202504_California；分区内容：(“2025-04-01“, “Los Angeles“),(“2025-04-01“, “San Francisco“),(“2025-04-02“, “Los Angeles“),(“2025-04-02“, “San Francisco“)- 分区名：p202504_Texas；分区内容：(“2025-04-01“, “Houston“),(“2025-04-01“, “Dallas“),(“2025-04-02“, “Houston“),(“2025-04-02“, “Dallas“)3）点击【生成建表语句】，查看SQL；4）确认建表 | 1）建表语句SQL正确，分区语句如下：PARTITION BY LIST (dt,city) (   PARTITION p202504_California VALUES IN (       (“2025-04-01“, “Los Angeles“),       (“2025-04-01“, “San Francisco“),       (“2025-04-02“, “Los Angeles“),       (“2025-04-02“, “San Francisco“)    ),   PARTITION p202504_Texas VALUES IN (       (“2025-04-01“, “Houston“),       (“2025-04-01“, “Dallas“),       (“2025-04-02“, “Houston“),       (“2025-04-02“, “Dallas“)   ))2）表创建成功； |

##### 【P1】验证新建表-列表分区仅有一个分区字段

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）选择一个字段作为分区字段（字段名：city）;2) 配置分区- 分区名：pCalifornia；枚举值：“Los Angeles“,“San Francisco“,“San Diego“- 分区名：pTexas；枚举值：“Houston“,“Dallas“,“Austin“3）点击【生成建表语句】，查看SQL；4）确认建表 | 1）建表语句SQL正确，分区语句如下：PARTITION BY LIST (city) (   PARTITION pCalifornia VALUES IN (“Los Angeles“,“San Francisco“,“San Diego“),    PARTITION pTexas VALUES IN (“Houston“,“Dallas“,“Austin“))2）表创建成功； |

##### 【P0】验证分区类型选择“列表分区表”

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表-表结构，“分区类型”选择“列表分区” | 右侧显示列表分区配置按钮 |
| 2 | 1）勾选1个分区字段；2）“分区类型”选择“列表分区”，点击列表分区配置按钮 | 1）显示“列表分区设置”弹窗；2）显示1列枚举值列 |
| 3 | 1）勾选N个分区字段；2）“分区类型”选择“列表分区”，点击列表分区配置按钮 | 1）显示“列表分区设置”弹窗；2）显示N列枚举值列 |

##### 【P1】验证新建表-范围分区有多个分区字段

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）进入数据模型-新建表-表结构，选择2个字段作为分区字段（字段名：k1,k2）；2）配置分区- 分区名：p1；k1列值：2025-01-01；k2列值：10- 分区名：p2；k1列值：2025-02-01；k2列值：100- 分区名：p3；k1列值：2025-03-01；k2列值：10003）点击【生成建表语句】，查看SQL；4）确认建表 | 1）建表语句SQL正确，分区语句如下：PARTITION BY RANGE (k1, k2)(    PARTITION p1 VALUES LESS THAN (“2025-01-01“, “10“),    PARTITION p2 VALUES LESS THAN (“2025-02-01“, “100“),    PARTITION p3 VALUES LESS THAN (“2025-03-01“, “1000“)，)2）表创建成功； |

##### 【P1】验证新建表-范围分区仅有一个分区字段

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）进入数据模型-新建表-表结构，选择一个字段作为分区字段（字段名：k1）；2）配置分区- 分区名：p1；k1列值：2025-01-01- 分区名：p2；k1列值：2025-02-01- 分区名：p3；k1列值：2025-03-013）点击【生成建表语句】，查看SQL；4）确认建表 | 1）建表语句SQL正确，分区语句如下：PARTITION BY RANGE (k1)(    PARTITION p1 VALUES LESS THAN (“2025-01-01“),    PARTITION p2 VALUES LESS THAN (“2025-02-01“),    PARTITION p3 VALUES LESS THAN (“2025-03-01“))2）表创建成功； |

##### 【P0】验证分区类型选择“范围分区表”

> 前置条件

```
目前只做 LESS THAN 方式按照范围划分分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-新建表-表结构，“分区类型”选择“范围分区” | 右侧显示范围分区配置按钮 |
| 2 | 1）勾选1个分区字段（k1）；2）“分区类型”选择“范围分区”，点击范围分区配置按钮 | 1）显示“范围分区设置”弹窗；2）显示1列k1 |
| 3 | 1）勾选N个分区字段（k1,k2...kn）；2）“分区类型”选择“范围分区”，点击范围分区配置按钮 | 1）显示“范围分区设置”弹窗；2）显示N列k1,k2...kn |

##### 【P1】验证选择范围分区/列表分区后，key列自动勾选功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型，新建表，选择主键表，在第二步给字段id配置范围分区，查看主键是否勾选 | 主键自动勾选 |
| 2 | 进入数据资产-数据模型，新建表，选择主键表，在第二步给字段id配置列表分区，查看主键是否勾选 | 主键自动勾选 |
| 3 | 进入数据资产-数据模型，新建表，选择聚合表，在第二步给字段id配置范围分区，查看排序键是否勾选 | 排序键自动勾选 |
| 4 | 进入数据资产-数据模型，新建表，选择聚合表，在第二步给字段id配置列表分区，查看排序键是否勾选 | 排序键自动勾选 |
| 5 | 进入数据资产-数据模型，新建表，选择明细表，在第二步给字段id配置范围分区，查看排序键是否勾选 | 排序键自动勾选 |
| 6 | 进入数据资产-数据模型，新建表，选择明细表，在第二步给字段id配置列表分区，查看排序键是否勾选 | 排序键自动勾选 |

##### 【P1】验证doris数据模建表，字段新增分区类型

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型，新建表，选择主键表，在第二步给字段id勾选分区字段，查看分区类型下拉框 | 下拉框新增范围分区、列表分区 |

##### 【P0】验证建表-批量解析，字段中文名是否允许修改

> 前置条件

```
已存在标准：姓名、年龄
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表，批量解析填入“姓名“、“年龄“、“班级“，点击解析，查看右侧字段展示 | 1）“姓名“、“年龄“绑定标准，字段名、字段中文名不允许编辑2）“班级“未绑定标准，字段名、字段中文名都允许修改 |
| 2 | 修改“班级”的字段名和字段中文名，点击临时保存 | 保存成功，数据正确 |
| 3 | 修改“班级”的字段名和字段中文名，点击建表 | 建表成功，字段名、字段中文名正确 |

##### 【P1】验证建表第一步，存储格式/类型等信息的交互

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表，查看展示 | 表信息部分不展示【存储格式】、【表类型】、【hdfs存储路径】、【数据模型类型】 |
| 2 | “写入数据源”选择hive2.x/hive3.x数据库 | 1）展示【存储格式】、【表类型】、【hdfs存储路径】2）不展示【数据模型类型】 |
| 3 | “写入数据源”选择StarRocks2.x/3.x数据库 | 1）展示【数据模型类型】单选项，选项为：明细表、更新表、聚合表、主键表；默认选中“明细表”2)不展示【存储格式】、【表类型】、【hdfs存储路径】 |
| 4 | “写入数据源”选择mysq数据库 | 1）展示【存储格式】2)不展示【表类型】、【hdfs存储路径】、【数据模型类型】 |
| 5 | “写入数据源”选择doris2.x数据库 | 1）展示【数据模型类型】单选项，选项为：主键表、聚合表、明细表；默认选中“主键表”2)不展示【存储格式】、【表类型】、【hdfs存储路径】 |
| 6 | “写入数据源”选择AnalyticDB PostgreSQL数据库 | 1）展示【存储格式】，选项为：Row Storage Format、Column Storage Format，默认选择Row Storage Format2)不展示【表类型】、【hdfs存储路径】、【数据模型类型】 |
| 7 | “写入数据源”选择DM for Oracle数据库 | 1)不展示【存储格式】、【表类型】、【hdfs存储路径】、【数据模型类型】 |

##### 【P1】验证编辑模型时，数据回填正确

> 前置条件

```
资产已引入
hve2.x、hive3.x、AnalyticDB PostgreSQL、MySQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle数据源
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表-建表配置，取消勾选mysql类型 | 取消成功 |
| 2 | 点击编辑临时保存的mysql类型模型，查看数据回填 | 数据源未回填，其他数据回填正确 |
| 3 | 点击编辑已建表的mysql类型模型，查看数据回填 | 数据源及其他数据回填正确 |

##### 【P1】验证选中建表配置数据源后，建表功能正常

> 前置条件

```
资产已引入
hve2.x、hive3.x、AnalyticDB PostgreSQL、MySQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle数据源
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表-建表配置，勾选mysql类型 | 勾选成功 |
| 2 | 进入数据模型-新建表，选择mysql数据源进行建表 | 建表成功 |

##### 【P0】验证建表配置是否生效

> 前置条件

```
资产已引入
hve2.x、hive3.x、AnalyticDB PostgreSQL、MySQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle数据源
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表-建表配置，取消勾选mysql类型 | 取消成功 |
| 2 | 进入数据模型-新建表，点击写入数据源，查看数据 | 没有返回mysql类型的数据源 |
| 3 | 进入数据资产-数据模型-建表-建表配置，取消勾选所有类型 | 取消成功 |
| 4 | 进入数据模型-新建表，点击写入数据源，查看数据 | 数据源下拉框为空 |

##### 【P0】验证建表配置展示的数据源类型是否正确

> 前置条件

```
资产只引入
hve2.x、hive3.x、MySQL数据源
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表-建表配置，查看展示 | 1）小标题：建表支持数据源类型配置，2）下方只展示hive2.x、hive3.x、mysql3）默认全部勾选 |
| 2 | 引入数据源AnalyticDB PostgreSQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle数据源 | 1）建表配置数据源展示hve2.x、hive3.x、AnalyticDB PostgreSQL、MySQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle2）默认全部勾选 |
| 3 | 取消引入所有mysql类型的数据源，查看建表配置展示 | 建表配置不展示mysql类型 |

##### 【P1】验证建表配置支持的数据源类型

> 前置条件

```
资产已引入
hve2.x、hive3.x、AnalyticDB PostgreSQL、MySQL、StarRocks2.x、Starrocks3.x、Doris2.x、Inceptor、DM for Oracle数据源
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表-建表配置，查看展示 | 1）小标题：建表支持数据源类型配置，2）下方展示前置条件中的所有数据源类型“3）默认全部勾选 |

##### 【P1】验证新增建表配置功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表，查看右侧菜单 | 1）在【数据表引入记录】后新增【建表配置】2）【离线表】名称修改为【规范建表】，悬浮提示未展示 |

##### 【P1】验证新增建表配置权限点功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-平台管理-角色管理，查看数据模型-规范建表-建表下是否新增建表配置 | 1）新增建表配置权限点2）默认管理员勾选，数据开发和访客不勾选 |

##### 【P1】验证flink表功能是否展示

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据资产-数据模型-建表，查看右侧菜单是否有flink表 | 菜单中【flink表】被隐藏 |
