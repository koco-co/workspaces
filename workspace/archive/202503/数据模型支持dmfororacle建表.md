---
suite_name: "数据模型支持dm for oracle建表"
description: "数据模型支持dm for oracle建表用例归档"
tags:
  - "建表时"
  - "否展示"
  - "模型建表"
  - "字段功能"
  - "生命周期功能"
  - "规范设计功能"
  - "主键交互功能"
  - "类型的数据源"
  - "类型的字段属性"
  - "离线表提示新增"
  - "模型建表审批功能"
  - "建表语句解析功能"
  - "数据模型编辑功能"
  - "字段支持选择分区"
  - "类型的字段数据类"
prd_version: "v6.3.2"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 14
case_id: 8641
---

##### 【P1】验证DM For Oracle模型建表审批功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 管理员新建表，数据源选择DM For Oracle类型 | 建表成功，直接审批通过 |
| 2 | 非管理员新建表，数据源选择DM For Oracle类型 | 1）表状态为待审批2）审批通过后，建表成功3）审批拒绝后，建表失败，列表中不存在该表 |

##### 【P1】验证模型建表，DM For Oracle生命周期功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 数据模型新建表，生命周期为999 | 新建成功 |
| 2 | 编辑表，生命周期修改为99 | 修改成功 |
| 3 | 在元数据管理查看生命周期 | 生命周期也为99 |
| 4 | 元数据管理编辑，生命周期修改为9 | 修改成功 |
| 5 | 进入表详情查看生命周期 | 生命周期为9 |

##### 【P1】验证DM For Oracle建表时，规范设计功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择数仓层级-原始数据层，选择对应的规范设计选项 | 表名称和所选规范设计符合 |
| 2 | 填写字段信息，查看建表语句 | 建表语句正确 |
| 3 | 建表后，选择原始数据层，查看右侧表展示 | 有新建的表 |
| 4 | 查看表详情 | 数据正确 |

##### 【P1】验证建表语句解析功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表，第二步选择建表语句模式，写入建表语句，点击解析 | 解析结果正确 |

##### 【P0】验证数据模型编辑功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-编辑表，查看字段展示 | 原有字段不允许编辑和删除 |
| 2 | 新增字段 | 新增正常，允许编辑和删除 |

##### 【P0】验证字段支持选择分区字段功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，字段勾选分区字段 | 允许勾选，分区类型默认为范围分区 |
| 2 | 点击范围分区的设置按钮 | 1）弹出范围分区设置弹窗2）可以填写分区名和最大值，允许新增删除 |

##### 【P1】验证主键交互功能

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，字段不勾选主键 | 非null允许勾选 |
| 2 | 字段勾选主键 | 1）支持选择2）选择后非null自动勾选且不可编辑 |

##### 【P1】验证decimal类型的字段属性

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，选择decimal类型 | 支持设置数值长度和字段精度 |
| 2 | 修改数值长度，确定范围 | 允许的范围为1-38 |
| 3 | 修改精度，确定范围 | 最小值为0，最大值不超过数值长度 |

##### 【P1】验证char类型的字段属性

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，选择char类型，查看字符长度 | 默认为255 |
| 2 | 修改字符长度为256 | 最大值为255 |

##### 【P1】验证varchar类型的字段属性

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，选择varchar类型，查看字符长度 | 默认为255 |
| 2 | 修改字符长度为8189 | 最大值为8188 |

##### 【P1】验证模型建表DM for Oracle类型的字段数据类型

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，在第二步表结构，查看数据类型下拉框 | 有类型：TINYINT,SAMLLINT,INT,BIGINT,FLOAT,DOUBLE,DECIMAL,CHAR,VARCHAR,TEXT,DATE,TIME,TIMESTAMP,BLOB,CLOB |

##### 【P1】验证模型建表DM for Oracle类型的存储格式是否展示

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，选择DM for Oracle类型的数据源/数据库，查看表信息展示 | 表信息存储格式不展示 |

##### 【P0】验证写入数据源支持选择DM for Oracle类型的数据源

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表-新建表，查看写入数据源下拉框 | 展示DM for Oracle类型的数据源 |
| 2 | 选择DM for Oracle类型的数据源/数据库 | 回填正确 |

##### 【P1】验证离线表提示新增DM for Oracle

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入数据模型-建表，鼠标悬浮在离线表后的提示上 | 提示：支持创建Hive、AnalyticDB PostgreSQL、Mysql、StarRocks、Inceptor、DM for Oracle表 |
