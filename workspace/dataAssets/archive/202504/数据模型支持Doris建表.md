---
suite_name: "数据模型支持Doris建表"
description: "数据模型支持Doris建表用例归档"
tags:
  - "正确"
  - "更新"
  - "正常"
  - "聚合"
  - "建表"
  - "支持"
  - "类型"
  - "主键表"
  - "明细表"
  - "成功后"
  - "能正常"
  - "聚合表"
  - "指标列"
  - "盖正常"
  - "表类型"
prd_version: "v6.3.4"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 43
case_id: 8752
---

##### 【P1】验证审批中心，doris数据模型申请，审批详情信息是否正确

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 用户进入审批中心-审批授权，查看待我审批、我已审批、全部审批页面，数据资产-数据模型申请中doris模型的审批详情 | 1. 功能均正常2. 主键表结构-所属标准、主键、非null、分区字段、分桶字段、分桶数3. 聚合表结构-所属标准、指标列、聚合类型、非null、分区字段、分桶字段、分桶数4. 明细表结构-所属标准、排序键、非null、分区字段、分桶字段、分桶数 |
| 2 | 查看我的申请，数据资产-数据模型申请中doris模型的审批详情 | 1. 功能均正常2. 主键表结构-所属标准、主键、非null、分区字段、分桶字段、分桶数3. 聚合表结构-所属标准、指标列、聚合类型、非null、分区字段、分桶字段、分桶数4. 明细表结构-所属标准、排序键、非null、分区字段、分桶字段、分桶数 |

##### 【P2】验证【我的模型】更新-查看表信息更新正常

> 前置条件

```
已存在模型A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1.进入【数据模型】-【授权与审批】-【我的模型】页面2. 模型A属性均变更(非null，主键，标准信息等)，查看详情 | 1. 表类型展示为【doris2.x】2. 数据模型类型为【主键表】【聚合表】【明细表】3. 主键表结构-所属标准、主键、非null、分区字段、分桶字段、分桶数4. 聚合表结构-所属标准、指标列、聚合类型、非null、分区字段、分桶字段、分桶数5. 明细表结构-所属标准、排序键、非null、分区字段、分桶字段、分桶数6. 各个字段的值更新正确 |

##### 【P1】验证【我的模型】-查看表信息正常

> 前置条件

```
已存在模型A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1.进入【数据模型】-【授权与审批】-【我的模型】页面2. 选择模型A，查看详情 | 1. 表类型展示为【doris2.x】2. 数据模型类型为【主键表】【聚合表】【明细表】3. 主键表结构-所属标准、主键、非null、分区字段、分桶字段、分桶数4. 聚合表结构-所属标准、指标列、聚合类型、非null、分区字段、分桶字段、分桶数5. 明细表结构-所属标准、排序键、非null、分区字段、分桶字段、分桶数 |

##### 【P1】验证Doris删除源表功能正常

> 前置条件

```
已存在Doris主键表A、聚合表B、明细表C
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，分别选择表A、B、C2. 依次删除源表，确认删除 | 1. 数据模型列表不展示，数据地图不展示2. 底层库内表不存在 |

##### 【P1】验证Doris删除元数据表功能正常

> 前置条件

```
已存在Doris主键表A、聚合表B、明细表C
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，分别选择表A、B、C2. 依次删除元数据表，确认删除 | 1. 数据模型列表不展示，数据地图不展示2. 底层库内表仍存在 |

##### 【P1】验证非admin用户新增表-审批拒绝逻辑正常

> 前置条件

```
已存在表A为test用户创建并提交审批
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. admin用户进入业务中心审批中心2.选择【数据模型申请】模块，选择test用户提交的建表申请3.审批拒绝4. test用户登陆资产平台查看表A | 建表失败（底层没有表） |

##### 【P1】验证非admin用户新增表-审批通过逻辑正常

> 前置条件

```
已存在表A为test用户创建并提交审批
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. admin用户进入业务中心审批中心2.选择【数据模型申请】模块，选择test用户提交的建表申请3.审批通过4. test用户登陆资产平台查看表A | (审批前底层没有表)建表成功（底层有表） |

##### 【P0】验证非admin用户建表流程正常

> 前置条件

```
用户中心存在普通用户test，并在资产内加入成员
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 使用test用户进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 新增字段ID，勾选主键，分区字段，动态分区，分桶字段(分桶数1)7. 点击【生成建表语句】按钮，建表 | 提示进入审批阶段 |

##### 【P1】验证Doris建【主键表、聚合、明细表】成功后，数据地图表详情功能正常

> 前置条件

```
已存在Doris主键表A、聚合表B、明细表C
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 分别点击表A、B、C，查看表详情2. 点击【建表语句解析】按钮3. 点击【数据预览】按钮4. 查看字段标签(分桶字段，主键，标准等)5. 查看表来源6. 导出表功能7. 订阅功能 | 1. 功能均正常2. 主键表结构-所属标准、主键、非null、分区字段、分桶字段、分桶数3. 聚合表结构-所属标准、指标列、聚合类型、非null、分区字段、分桶字段、分桶数4. 明细表结构-所属标准、排序键、非null、分区字段、分桶字段、分桶数 |

##### 【P1】验证编辑Doris【明细表】-动态分区生效

> 前置条件

```
已存在Doris明细表A，动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
更新动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “HOUR“,
“dynamic_partition.start“ = “-24“,
“dynamic_partition.end“ = “24“,
“dynamic_partition.prefix“ = “h“,
“dynamic_partition.replication_num“ = “2“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 选择动态分区，按照备注内更新配置5. 点击【生成建表语句】，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证Doris建【明细表】-动态分区功能正确

> 前置条件

```
动态分区配置：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 新增字段ID，勾选主键，分区字段，动态分区，分桶字段(分桶数1)7. 编辑动态分区配置,如备注内配置8. 点击【生成建表语句】按钮，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证编辑Doris【明细表】-新增字段功能正常

> 前置条件

```
已存在Doris明细表A，存在id int 字段
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string5. 点击【生成建表语句】，建表 | 1. 原有字段不可编辑2. 新增字段成功 |

##### 【P1】验证Doris【明细表】新增字段限制逻辑正确

> 前置条件

```
已存在Doris明细表A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string | 原有字段不可编辑；新增字段只支持设置中文名、字段名、数据类型、动态分区的PROPERTIES设置属性、非null；不支持设置排序键、分桶信息 |

##### 【P0】验证Doris建【明细表】-全类型字段表正常

> 前置条件

```
CHAR,VARCHAR,STRING,BOOLEAN,TINTINT,SMALLINT,INT,BIGINT,LARGEINT,FLOAT,DOUBLE,DECIMAL,DATE,DATETIME,ARRAY,MAP,STRUCT,JSON,VARIANT,HLL,BITMAP,QUANTILE_STATE,AGG_STATE,IPv4,IPv6
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【明细表】5. 点击下一步进入【批量解析模式】页面6. 新增字段覆盖所有支持的字段类型7. 点击【生成建表语句】按钮，建表 | 建表成功 |

##### 【P1】验证编辑Doris【聚合表】-动态分区生效

> 前置条件

```
已存在Doris聚合表A，动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
更新动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “false“,
“dynamic_partition.time_unit“ = “HOUR“,
“dynamic_partition.start“ = “-24“,
“dynamic_partition.end“ = “24“,
“dynamic_partition.prefix“ = “h“,
“dynamic_partition.replication_num“ = “2“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 选择动态分区，按照备注内更新配置5. 点击【生成建表语句】，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证Doris建【聚合表】-动态分区功能正确

> 前置条件

```
动态分区配置：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 新增字段ID，勾选主键，分区字段，动态分区，分桶字段(分桶数1)7. 编辑动态分区配置,如备注内配置8. 点击【生成建表语句】按钮，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证编辑Doris【聚合表】-新增字段功能正常

> 前置条件

```
已存在Doris聚合表A，存在id int 字段
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string5. 点击【生成建表语句】，建表 | 1. 原有字段不可编辑2. 新增字段成功 |

##### 【P1】验证Doris【聚合表】新增字段限制逻辑正确

> 前置条件

```
已存在Doris聚合表A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string | 原有字段不可编辑；新增字段只支持设置中文名、字段名、数据类型、指标列、聚合函数、动态分区的PROPERTIES设置属性、非null；不支持设置排序键、分桶信息 |

##### 【P0】验证Doris建【聚合表】-全类型字段表正常

> 前置条件

```
CHAR,VARCHAR,STRING,BOOLEAN,TINTINT,SMALLINT,INT,BIGINT,LARGEINT,FLOAT,DOUBLE,DECIMAL,DATE,DATETIME,ARRAY,MAP,STRUCT,JSON,VARIANT,HLL,BITMAP,QUANTILE_STATE,AGG_STATE,IPv4,IPv6
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 新增字段覆盖所有支持的字段类型7. 点击【生成建表语句】按钮，建表 | 建表成功 |

##### 【P1】验证Doris建【聚合表】-全聚合类型新增正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 新增字段id,col_1.... 等7个字段覆盖全聚合类型7. 建表 | 建表成功，底层查看聚合字段正常 |

##### 【P2】验证Doris建【聚合表】-指标列/排序键冲突逻辑正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 新增字段id，勾选指标列/排序键 | 同一字段，不能同时作为指标列和排序键 |

##### 【P1】验证Doris建【聚合表】-聚合类型枚举值正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 在左侧文本框输入id,name,age 三个字段，点击【解析】按钮7. 勾选指标列，查看聚合类型 | SUM,REPLACE,MAX,MIN,PEPLACE_IF_NOT_NULL,HLL_UNION,BITMAP_UNION |

##### 【P1】验证编辑Doris【主键表】-动态分区生效

> 前置条件

```
已存在Doris主键表A，动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
更新动态分区配置如下：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “HOUR“,
“dynamic_partition.start“ = “-24“,
“dynamic_partition.end“ = “24“,
“dynamic_partition.prefix“ = “h“,
“dynamic_partition.replication_num“ = “2“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 选择动态分区，按照备注内更新配置5. 点击【生成建表语句】，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证Doris建【主键表】-动态分区功能正确

> 前置条件

```
动态分区配置：
PROPERTIES (
“dynamic_partition.enable“ = “true“,
“dynamic_partition.time_unit“ = “DAY“,
“dynamic_partition.start“ = “-3“,
“dynamic_partition.end“ = “3“,
“dynamic_partition.prefix“ = “p“,
“dynamic_partition.replication_num“ = “1“
);
底层库查看配置生效：
show dynamic partition tables;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 新增字段ID，勾选主键，分区字段，动态分区，分桶字段(分桶数1)7. 编辑动态分区配置,如备注内配置8. 点击【生成建表语句】按钮，建表 | 1. 新增表成功2. 底层命令查看配置正确 |

##### 【P1】验证编辑Doris【主键表】-新增字段功能正常

> 前置条件

```
已存在Doris主键表A，存在id int 字段
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string5. 点击【生成建表语句】，建表 | 1. 原有字段不可编辑2. 新增字段成功 |

##### 【P1】验证Doris【主键表】新增字段限制逻辑正确

> 前置条件

```
已存在Doris主键表A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 选择表A，点击编辑3. 点击下一步进入【批量解析模式】页面4. 新增字段col_B string | 原有字段不可编辑；新增字段只支持设置中文名、字段名、数据类型、动态分区的PROPERTIES设置属性、非null；不支持设置主键、分桶信息 |

##### 【P0】验证Doris建【主键表】-全类型字段表正常

> 前置条件

```
CHAR,VARCHAR,STRING,BOOLEAN,TINTINT,SMALLINT,INT,BIGINT,LARGEINT,FLOAT,DOUBLE,DECIMAL,DATE,DATETIME,ARRAY,MAP,STRUCT,JSON,VARIANT,HLL,BITMAP,QUANTILE_STATE,AGG_STATE,IPv4,IPv6
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 新增字段覆盖所有支持的字段类型7. 点击【生成建表语句】按钮，建表 | 建表成功 |

##### 【P2】验证Doris建【主键表】-分区字段与主键互动逻辑正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 新增字段id，name字段7. 勾选分区字段 | 勾选分区字段之后，不自动勾选主键 |

##### 【P2】验证Doris建表-非null默认值逻辑正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面6. 新增字段id，排序键，勾选非null，输入默认值7. 建表 | 默认值设置成功 |

##### 【P2】验证Doris建【主键表、聚合表、明细表】主键字段校验

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】【聚合表】【明细表】5. 点击下一步进入【批量解析模式】页面6. 在左侧文本框输入id,name,age 三个字段，点击【解析】按钮7. 不勾选主键，勾选分桶字段(分桶数1)7. 点击【生成建表语句】按钮 | 提示“必须选择一个字段作为主键字段” |

##### 【P2】验证Doris建【主键表、聚合表、明细表】分桶字段校验

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】【聚合表】【明细表】5. 点击下一步进入【批量解析模式】页面6. 在左侧文本框输入id,name,age 三个字段，点击【解析】按钮7. 勾选主键，不勾选分桶字段(分桶数1)7. 点击【生成建表语句】按钮 | 提示“必须选择一个字段作为分桶字段” |

##### 【P2】验证字段关联标准功能正常

> 前置条件

```
已存在标准ID
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 在左侧文本框输入id,name,age 三个字段，点击【解析】按钮 | 解析成功，ID字段自动关联标准信息 |

##### 【P2】验证字段名称解析功能正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面6. 在左侧文本框输入id,name,age 三个字段，点击【解析】按钮7. 勾选主键，分区字段，动态分区，分桶字段(分桶数1)7. 点击【生成建表语句】按钮，建表 | 解析成功，建表成功 |

##### 【P2】验证动态分区配置展示正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键】【聚合表】【明细表】5. 点击下一步进入【批量解析模式】页面，新增字段A6. 勾选【分区字段】，选择【分区类型】-【动态分区】7. 编辑【动态分区】 | 默认展示PROPERTIES()； |

##### 【P2】验证Doris建表分区字段暂只支持-动态分区

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键】【聚合表】【明细表】5. 点击下一步进入【批量解析模式】页面，新增字段A6. 勾选【分区字段】，选择【分区类型】查看 | 仅展示【动态分区】 |

##### 【P1】验证【明细表】类型-批量解析模式下列头展示正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【明细表】5. 点击下一步进入【批量解析模式】页面，查看列头 | 【中文名】【字段名】【数据类型】【所属标准】【排序键】【非null】【分区字段】【分区类型】【分桶字段】【分桶数】【操作】 |

##### 【P2】验证【聚合表】类型-批量解析模式下列头展示正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【聚合表】5. 点击下一步进入【批量解析模式】页面，查看列头 | 【中文名】【字段名】【数据类型】【所属标准】【指标列】【排序键】【非null】【分区字段】【分区类型】【分桶字段】【分桶数】【操作】 |

##### 【P2】验证【主键表】类型-批量解析模式下列头展示正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，选择【主键表】5. 点击下一步进入【批量解析模式】页面，查看列头 | 【中文名】【字段名】【数据类型】【所属标准】【主键】【非null】【分区字段】【分区类型】【分桶字段】【分桶数】【操作】 |

##### 【P0】验证Doris数据类型枚举值覆盖正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源4. 输入必填项后，点击下一步进入【批量解析模式】页面5. 查看【数据类型】 | CHAR,VARCHAR,STRING,BOOLEAN,TINTINT,SMALLINT,INT,BIGINT,LARGEINT,FLOAT,DOUBLE,DECIMAL,DATE,DATETIME,ARRAY,MAP,STRUCT,JSON,VARIANT,HLL,BITMAP,QUANTILE_STATE,AGG_STATE,IPv4,IPv6 |

##### 【P1】验证新增Doris表-数据模型类型枚举值展示正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框选择doris数据源 | 1. 展示【主键表】【聚合表】【明细表】三种类型2. 默认选择【主键表】 |

##### 【P0】验证【写入数据源】新增doris数据源类型

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【新建表】3. 点击【写入数据源】下拉框 | 展示所有平台已引入的doris数据源 |

##### 【P1】验证【表类型】新增Doris2.x

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 点击【表类型】筛选框3. 选择“Doris2.x”，确认 | 1. 新增Doris2.x2. 筛选功能正常3. 无匹配对象时，缺省页展示正常 |

##### 【P2】验证【离线表】“？”hover新增doris2.x类型

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入【数据模型】页面，点击建表-离线表2. 鼠标hover “？”号3. 查看描述文案信息 | 在starrocks后新增doris |
