---
suite_name: "【规则库管理】支持自定义sql模版"
description: "【规则库管理】支持自定义sql模版用例归档"
tags:
  - "模版"
  - "表级"
  - "多表"
  - "必选"
  - "面板"
  - "自定义"
  - "提示词"
  - "请选择"
  - "字段级"
  - "数据质量"
  - "验证新增"
  - "验证搜索"
  - "基本信息"
  - "选择枚举"
  - "为空提示"
prd_version: "v6.4.8"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 32
case_id: 10205
---

## 数据质量-规则库管理

### 自定义正则

##### 【P2】验证文案调整

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 查看原先的“自定义规则”tab名称 | 已成功修改为“自定义正则” |
| 2 | 查看原先“新增自定义规则”按钮名称 | 已成功修改为“新增自定义正则” |

##### 【P0】验证规则模式字段删除

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 查看自定义正则列表 | 「规则模式」字段已删除，不显示 |
| 2 | 点击「新增自定义正则」按钮，新增页面「规则模式」字段已删除不显示 | 新增自定义正则保存成功 |
| 3 | 点击「编辑」按钮，编辑页面「规则模式」字段已删除不显示 | 编辑自定义正则保存成功 |
| 4 | 点击编辑历史规则信息，编辑页面「规则模式」字段已删除不显示 | 编辑历史自定义正则保存成功 |

### 自定义SQL模版

##### 【P2】验证新增「自定义SQL模版」tab

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | “自定义正则”tab右侧，已新增tab“自定义sql模版” | 名称正确 |
| 2 | 点击“自定义sql模版”tab | 显示“自定义sql模版”列表 |

##### 【P2】验证搜索

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：请输入规则名称进行搜索 | 提示词正确 |
| 2 | 支持规则名称搜索输入模糊搜索 | 搜索结果正确 |

##### 【P1】验证列表展示

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 列表展示规则名称、规则分类、关联范围、关联规则数、规则描述、操作 | 列表字段显示正确 |
| 2 | 支持规则分类筛选，点击漏斗，枚举内容支持多选 | 筛选结果正确 |
| 3 | 支持规则范围筛选，点击漏斗，枚举内容支持多选 | 筛选结果正确 |
| 4 | 查看关联规则数 | 数量正确 |

##### 【P1】验证列表排序

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 按照修改时间倒排 | 排序正确 |

#### 新增

##### 【P0】验证新增自定义sql模版

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击「新增自定义sql模版」 | 成功进入新增页面，自定义sql模版  >  新建自定义sql模版 |
| 2 | 基本信息：输入合法的规则名称、规则分类、关联范围、规则描述 | 输入成功 |
| 3 | 自定义配置输入sql包含6个参数 | 选择成功 |
| 4 | 参数类型选择：6个参数分别选择数值、数组、逻辑关系、当前校验表、当前校验表字段、自定义参数 | 填写成功 |
| 5 | 参数名称：6个参数名称正常填写 | 填写成功 |
| 6 | 参数输入：6个参数说明正常填写 | 新增成功，列表数量+1 |
| 7 | 必填字段均正确填写，点击保存按钮 | 返回上一页，且已填写内容不保存 |
| 8 | 点击取消 |  |

##### 【P1】验证规则名称

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：请输入 | 提示词正确 |
| 2 | 必填 | 为空提示 |
| 3 | 最大支持输入100 | 超长无法输入 |
| 4 | 不支持重复 | 若重复，提示校验 |

##### 【P1】验证规则分类

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：请选择 | 提示词正确 |
| 2 | 选择枚举 | 完整性/唯一性/有效性/统计性/时效性/合理性 |
| 3 | 必选 | 为空提示 |
| 4 | 仅支持单选 | 选择成功 |

##### 【P1】验证关联范围 提示词：请选择 提示词正确

> 前置条件

```
无
```

##### 【P1】验证关联范围 选择枚举 字段级/表级/多表

> 前置条件

```
无
```

##### 【P1】验证关联范围 必选 为空提示

> 前置条件

```
无
```

##### 【P1】验证关联范围 仅支持单选 选择成功

> 前置条件

```
无
```

##### 【P1】验证规则描述

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：请输入 | 提示词正确 |
| 2 | 必填 | 为空提示 |
| 3 | 最大支持输入255 | 超长无法输入 |

##### 【P1】验证全局参数

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击全局参数 | 成功出现全局参数弹框 |
| 2 | 查看内容 | 内容正正确 |

##### 【P1】验证Sql面板

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 输入sql | 输入成功 |
| 2 | sql支持参数化 | 使用${xxxx}表示 |

##### 【P0】验证参数列表

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 若输入sql无参数化 | 参数列表显示暂无数据 |
| 2 | 输入sql包含3个参数化字段 | 3个参数化字段成功显示在参数列表 |
| 3 | 修改输入sql的3个参数化字段改为2个参数化字段 | 参数列表同步更新，仅展示2条 |
| 4 | 修改输入sql的2个参数化字段改为5个参数化字段 | 参数列表同步更新，展示5条 |
| 5 | 全局参数不显示在参数列表 |  |

##### 【P2】验证参数列表

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 参数来源sql输入的参数${xxxx} | 保存一致 |
| 2 | 排序 | 顺序，去重 |

##### 【P0】验证类型选择

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 必选 | 为空提示 |
| 2 | 选择枚举包含 | 支持选择数值、数组、逻辑关系、当前校验表、当前校验表字段、自定义参数 |
| 3 | 类型选择数值型，参数值只允许输入数值（bigint类型），适用于配置时间差值、计算差值等场景 | 配置自定义规则时，该参数值只能填写数值型增加校验 |
| 4 | 类型选择数组，参数值为数组配置框，适用于配置枚举范围 | 配置自定义规则时，该参数值支持选择，支持多选，输入选择框 |
| 5 | 类型选择逻辑关系，参数值支持选择支持配置>、=、<、>=、<=、!= | 配置自定义规则时，该参数值支持支持按照逻辑关系配置 |
| 6 | 类型选择当前校验表，不可编辑，系统会自动填充当前校验表名拼接到sql中 | 配置自定义规则时，该参数值显示-- |
| 7 | 类型选择校验表字段，支持选择当前校验表下的字段名称，只支持单选 | 配置自定义规则时，该参数值支持选择表下所有字段，且仅支持单选； 若表字段变更，此处选择内容同步变更 下一步校验 |
| 8 | 类型选择自定义类，支持输入 | 配置自定义规则时，该参数值支持输入 |

##### 【P1】验证参数名称

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 当类型选择当前校验表 | 参数名称置灰无需配置 |
| 2 | 当类型选择其他类型 | 支持输入 |
| 3 | 必填 | 为空提示 |
| 4 | 最大支持输入50 | 超长无法输入 |

##### 【P1】验证参数说明

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 非必填 | 支持为空 |
| 2 | 最大支持输入255 | 超长无法输入 |
| 3 | 支持在规则配置页面“?”悬浮查看 | 内容和配置一致 |

##### 【P0】验证详情

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击列表自定义sql模版名称 | 成功出现详情页面 |
| 2 | 查看详情字段及值 | 详情内容正确，和新增、编辑保存成功的内容一致 |

##### 【P0】验证编辑

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击编辑按钮 | 成功进入编辑页面，面包屑正确：自定义sql模版  >  编辑自定义sql模版 |
| 2 | 编辑规则和新增校验规则一致，必填信息正确修改，点击保存 | 修改成功 |
| 3 | 查看修改成功的内容 | 在自定义sql模版列表、详情同步更新、引用的地方同步更新 |
| 4 | 再次点击编辑 | 正确回显上次填写的内容 |
| 5 | 点击取消 | 返回上一页，且已填写内容不保存 |

##### 【P0】验证删除

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 未被自定义sql引用的模版，点击删除按钮，二次确认是否可以删除，点击确定 | 删除成功 |
| 2 | 已被被自定义sql引用的模版，点击删除按钮，二次确认是否可以删除，点击确定 | 删除失败，提示校验？ |

## 数据质量-规则任务配置

##### 【P0】验证新建监控配置-自定义sql

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击新建监控配置，「监控对象」配置页面正确填写，点击下一步 | 成功进入「监控规则」配置页面 |
| 2 | 点击添加规则，选择自定义SQL，规则类型选择：完整性，引用规则选择：自定义SQL模版xx | 填写正确 |
| 3 | 查看sql面板 | sql回显正确 |
| 4 | 查看参数列表 | 参数列表回显正确 |
| 5 | 选择校验字段，校验方法、期望值、强弱规则、规则描述 | 输入正确 |
| 6 | 「监控规则」配置页面所有信息正确填写，点击下一步 | 成功进入「调度属性」配置页面 |
| 7 | 「调度属性」配置页面所有信息均正确填写，点击新建 | 新增监控配置成功 |

##### 【P1】验证自定义sql-规则类型

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：选择规则类型 | 提示词正确 |
| 2 | 选择枚举 | 规则类型支持选择完整性/唯一性/准确性/及时性/一致性/规范性 |
| 3 | 必选 | 为空提示 |
| 4 | 仅支持单选 | 选择成功 |

##### 【P0】验证自定义sql-引用规则

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 提示词：选择引用规则 | 提示词正确 |
| 2 | 选择枚举 | 引用规则支持选择规则库配置-自定义规则模块创建的规则名称 |
| 3 | 必选 | 为空提示 |
| 4 | 仅支持单选 | 选择成功 |
| 5 | 选择自定义模版 | 成功可关联出定义的sql模版自动填入到sql框中，参数列表同步更新，显示正确 |
| 6 | 切换选择自定义模版 | sql模版更新，参数列表同步更新，显示正确 |

##### 【P1】验证自定义sql-sql面板

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 引用规则选择「自定义模版」 | sql面板正常回显sql模版填写的sql，且不支持编辑；若引用的sql模版填写SQL发生变更，规则编辑更新 |

##### 【P0】验证自定义sql：引用规则选择「自定义模版」-参数列表

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 引用规则选择「自定义模版」 | 参数列表根据模版展示；若模版发生变更，此处同步更新 |
| 2 | 参数、参数类型、参数名称，和模版配置的一样 | 回显正确，不支持编辑 |
| 3 | 鼠标覆盖到问号标识上 | 正常回显参数说明 |
| 4 | 参数值 | 根据所属类型配置 |

##### 【P1】验证校验字段

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 引入的自定义sql模版约定了“关联范围”为“字段级，多表” | 支持选择校验字段，反之不支持选择？ |
| 2 | 单表 | 不支持 |
| 3 | 校验字段 | 支持多选，非必填支持为空 |
| 4 | 枚举内容 | 表字段 |
| 5 | 选择校验字段后若存在不符合规则的明细数据 | 选择的字段进行标红展示 |

## 数据质量-任务实例查询

##### 【P0】验证自定义sql任务运行结果查看

> 前置条件

```
1、配置自定义sql模版，${自定义sql模版名称}
配置规则分类：完整性
配置sql如下：select count(*) from ${test_table} where
((unix_timestamp(${end_time1}) - unix_timestamp(${start_time1})) > ${d_value1} )
参数列表显示：
参数：${test_table}   类型：当前校验表   参数名称：当前校验表   参数说明：表名
参数：${end_time1}   类型：当前校验表字段   参数名称：校验字段   参数说明：结束时间
参数：${start_time1}   类型：当前校验表字段   参数名称：校验字段   参数说明：开始时间
参数：${d_value1}   类型：数值  参数名称：校验字段   参数说明：开始时间
2、配置自定义sql规则，引用规则${自定义sql模版名称} ，参数值列表参数值填写
参数：${test_table}   类型：当前校验表   参数名称：当前校验表   参数值：--
参数：${end_time1}   类型：当前校验表字段   参数名称：校验字段   参数值：字段end_time1
参数：${start_time1}   类型：当前校验表字段   参数名称：校验字段   参数值：字段start_time1
参数：${d_value1}   类型：数值  参数名称：校验字段   参数说明：参数值：60
校验字段：end_time1、start_time1
校验方法：固定值
期望值：<=0
强弱规则：弱规则
3、源表数据，一共有6条数据4条符合，2条不符合
4、周期任务运行成功
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 查看监控报告 | 支持校验未通过（数量）、校验通过（数量）tab切换 |
| 2 | 选中校验未通过「数量」tab | 根据自定义配置时的分类展示，正确显示：完整性（自定义sql)，显示正确 |
| 3 | 查看sql显示替换了参数信息之后的完整sql内容 | SQl展示完整正确；SELECT COUNT(*) FROM car_order WHERE ( UNIX_TIMESTAMP(end_time1) - UNIX_TIMESTAMP(start_time1) ) > 60; |
| 4 | 点击查看明细 | 成功打开明细弹框，明细内容正确，明细列表展示不符合要求的2条数据，展示数据表内的全部字段 |
| 5 | 支持通过运行时间筛选 | 筛选结果正确 |
| 6 | 若配置规则时选择“校验字段”：end_time1、start_time1 | 明细表end_time1、start_time1标红展示，显示正确 |
| 7 | 点击下载明细 | 受筛选条件影响，下载成功，下载结果正确，校验字段标红 |

## 数据质量-质量报告

##### 【P0】验证验证自定义sql质量报告-规则校验明细

> 前置条件

```
1、配置自定义sql模版，${自定义sql模版名称}
配置规则分类：完整性
配置sql如下：select count(*) from ${test_table} where
((unix_timestamp(${end_time1}) - unix_timestamp(${start_time1})) > ${d_value1} )
参数列表显示：
参数：${test_table}   类型：当前校验表   参数名称：当前校验表   参数说明：表名
参数：${end_time1}   类型：当前校验表字段   参数名称：校验字段   参数说明：结束时间
参数：${start_time1}   类型：当前校验表字段   参数名称：校验字段   参数说明：开始时间
参数：${d_value1}   类型：数值  参数名称：校验字段   参数说明：开始时间
2、配置自定义sql规则，引用规则${自定义sql模版名称} ，参数值列表参数值填写
参数：${test_table}   类型：当前校验表   参数名称：当前校验表   参数值：--
参数：${end_time1}   类型：当前校验表字段   参数名称：校验字段   参数值：字段end_time1
参数：${start_time1}   类型：当前校验表字段   参数名称：校验字段   参数值：字段start_time1
参数：${d_value1}   类型：数值  参数名称：校验字段   参数说明：参数值：60
校验字段：end_time1、start_time1
校验方法：固定值
期望值：<=0
强弱规则：弱规则
3、源表数据，一共有6条数据4条符合，2条不符合
4、周期任务运行成功
5、生成质量报告
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 查看质量报告-规则校验明细 | 列表显示：规则类型	规则名称	字段名称	字段类型	质检结果	未通过原因	详情说明	操作 |
| 2 | 查看规则类型 | 根据自定义配置时的分类展示，正确显示：完整性（自定义sql)，显示正确 |
| 3 | 查看规则名称悬浮内容，鼠标覆盖 | 正确显示规则描述；规则描述同步变更 |
| 4 | 查看字段名称，若配置规则时选择校验字段信息：end_time1、start_time1 | 显示字段end_time1、start_time1，显示正确 |
| 5 | 查看字段名称，若配置规则时选择校验字段信息”：为空 | 展位符--，显示正确 |
| 6 | 查看字段类型 | 根据字段类型展示，显示正确 |
| 7 | 查看未通过原因，质量结果为校验通过 | 未通过原因为空展示-- |
| 8 | 查看未通过原因，质量结果为校验失败 | 未通过原因显示“规则名称+未通过” |
| 9 | 查看详情说明，质量结果为校验通过 | 详情说明显示：查询结果的数据量为0，符合规则“期望值：<=0” |
| 10 | 查看详情说明，质量结果为校验失败 | 详情说明显示：查询结果的数据量为2（根据完整SQl查询来），不符合规则“期望值：<=0” |
