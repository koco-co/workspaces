---
suite_name: "inceptor支持存储过程血缘解析"
description: "inceptor支持存储过程血缘解析用例归档"
tags:
  - "客户"
  - "普通"
  - "外部表"
  - "内部表"
  - "事物表"
  - "存储过程"
  - "非事物表"
  - "元模型管理"
  - "支持采集视图表"
  - "支持采集普通表"
  - "非分区建表正常"
  - "血缘解析功能正常"
  - "视图表详情展示正"
  - "数据源新增技术属"
  - "视图表标签展示正"
prd_version: "v6.3.3"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 23
case_id: 8846
---

##### 【P0】验证inceptor客户sql血缘解析功能正常

> 前置条件

```
客户demo sql已提交
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 离线平台新增inceptor sql 2. 执行前提条件内的sql3. 进入资产查看result/source表血缘，字段血缘 | 正常展示source/result表上下游血缘关系 |

##### 【P0】验证inceptor存储过程create select 血缘解析功能正常

> 前置条件

```
create table if not exists source(id int,name varchar(255));

create or replace PROCEDURE procedure1() is
begin
EXECUTE IMMEDIATE ('create table result as select * from source');
END ;

call procedure1('test');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 离线平台新增inceptor sql 2. 执行前提条件内的sql3. 进入资产查看result/source表血缘，字段血缘 | 正常展示source/result表上下游血缘关系 |

##### 【P0】验证inceptor存储过程insert select 血缘解析功能正常

> 前置条件

```
create table  if not exists source(id int,name varchar(255));

create table if not exists result(id int,name varchar(255));

create or replace PROCEDURE procedure_1(id int) IS
begin
insert INTO result select * from source;
END ;

call maoyan_procedure_1(1);
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 离线平台新增inceptor sql 2. 执行前提条件内的sql3. 进入资产查看result/source表血缘，字段血缘 | 正常展示source/result表上下游血缘关系 |

##### 【P1】验证inceptor普通create select血缘解析功能正常

> 前置条件

```
create table  if not exists source(id int,name varchar(255));

create table result  as select * from source;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 离线平台新增inceptor sql 2. 执行前提条件内的sql3. 进入资产查看result/source表血缘，字段血缘 | 正常展示source/result表上下游血缘关系 |

##### 【P1】验证inceptor普通insert select血缘解析功能正常

> 前置条件

```
create table  if not exists result(id int,name varchar(255));

create table  if not exists source(id int,name varchar(255));

insert into result select * from source;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 离线平台新增inceptor sql 2. 执行前提条件内的sql3. 进入资产查看result/source表血缘，字段血缘 | 正常展示source/result表上下游血缘关系 |

##### 【P1】验证inceptor视图表详情展示正确

> 前置条件

```
数据地图存在inceptor视图表A
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入数据地图2. 选择表A，进入详情查看 | 1. 数据地图筛选需选到“视图”类型2. 数据地图页面展示表类型为“视图”3. 数据表详情内类型展示为“视图” |

##### 【P2】验证元模型管理-inceptor数据源新增技术属性

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入元数据-元模型管理页面2. 选择inceptor数据源，进入详情页3. 查看技术属性 | 新增view_description(视图特有)属性（varchar） |

##### 【P2】验证视图表标签展示正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入元数据同步页面2. 新增元数据同步任务3. 选择数据源A-数据库a4. 查看视图表标签 | 视图表均有视图标签 |

##### 【P0】验证inceptor支持采集视图表

> 前置条件

```
已存在inceptor数据源A，数据库a,及普通表和视图表N
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入元数据同步页面2. 新增元数据同步任务3. 选择数据源A-数据库a,添加全部表4. 临时同步 | 1. 元数据同步任务状态正常2. 视图表成功同步到数据地图 |

##### 【P1】验证inceptor支持采集普通表

> 前置条件

```
已存在inceptor数据源A，数据库a,及普通表和视图表N
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入元数据同步页面2. 新增元数据同步任务3. 选择数据源A-数据库a,添加全部表4. 临时同步 | 1. 元数据同步任务状态正常2. 表成功同步到数据地图 |

##### 【P0】验证inceptor-HOLODESK-外部表-范围分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: HOLODESK
生命周期:9999
表类型: 外部表
hdfs存储路径： hdfs://dtInsight/xx
字段1: id int ，勾选分区-范围分区，设置范围分区part1 ,分区值范围100
part2 ,分区值范围50
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor-HOLODESK-内部表-单值分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: HOLODESK
生命周期:9999
表类型: 内部表
字段1: id int ，勾选分区-单值分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor-HOLODESK-内部表-非分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: HOLODESK
生命周期:9999
表类型: 内部表
字段1: id int ，不勾选分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor数据源新增表类型HOLODESK

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 查看支持表类型 | 新增HOLODESK类型可选 |

##### 【P0】验证inceptor-CSV-外部表-范围分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: CSV
生命周期:9999
表类型: 外部表
hdfs存储路径： hdfs://dtInsight/xx
字段1: id int ，勾选分区-范围分区，设置范围分区part1 ,分区值范围100
part2 ,分区值范围50
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P1】验证inceptor-CSV-内部表-单值分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: CSV
生命周期:9999
表类型: 内部表
字段1: id int ，勾选分区-单值分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P1】验证inceptor-CSV-内部表-非分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: CSV
生命周期:9999
表类型: 内部表
字段1: id int ，不勾选分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P1】验证inceptor-TEXT-外部表-范围分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: TEXT
生命周期:9999
表类型: 外部表
hdfs存储路径： hdfs://dtInsight/xx
字段1: id int ，勾选分区-范围分区，设置范围分区part1 ,分区值范围100
part2 ,分区值范围50
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor-TEXT-内部表-单值分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: TEXT
生命周期:9999
表类型: 内部表
字段1: id int ，勾选分区-单值分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor-TEXT-内部表-非分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: TEXT
生命周期:9999
表类型: 内部表
字段1: id int ，不勾选分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P0】验证inceptor-ORC-非事物表-范围分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: ORC
表类型: 非事物表
生命周期:9999
表类型: 内部表
字段1: id int ，勾选分区-范围分区，设置范围分区part1 ,分区值范围100
part2 ,分区值范围50
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P2】验证inceptor-ORC-事物表-单值分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: ORC
表类型: 事物表
生命周期:9999
表类型: 内部表
字段1: id int ，勾选分区-单值分区
字段2: name String , 不勾选分区，设置分桶字段，10
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |

##### 【P1】验证inceptor-ORC-事物表-非分区建表正常

> 前置条件

```
任务配置如下：
表名： test_table
表中文名: 测试用表
存储格式: ORC
表类型: 事物表
生命周期:9999
表类型: 内部表
字段1: id int ，不勾选分区
字段2: name String , 不勾选分区
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1. 进入资产数据模型建表页面2. 点击【新建表】，写入数据源选择inceptor3. 如前提条件内配置4. 保存，新建表 | 1. 建表语句解析正常2. 建表成功3. 底层表查看表信息正常4. 数据地图查看表信息正常 |
