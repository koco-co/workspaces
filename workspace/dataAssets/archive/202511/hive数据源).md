---
suite_name: "hive数据源)"
description: "hive数据源)用例归档"
tags:
  - "正确"
  - "失败"
  - "成功"
  - "选项"
  - "选择一"
  - "分区值"
  - "示正确"
  - "表字段"
  - "后查看"
  - "支持输入"
  - "多级分区"
  - "功能正确"
  - "全局参数"
  - "数据质量"
  - "监控对象"
prd_version: "v6.4.5"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 13
case_id: 9695
---

## 选择动态分区

##### 【P0】验证选择一、二级分区校验结果正确

> 前置条件

```
1. 1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表 DDL\DML 如下：
drop table if exists my_table_sink;
create table if not EXISTS my_table_sink (
id int,
name string,
age int,
dt string,
hour string,
ts timestamp(3)
) USING paimon
partitioned by (dt,hour)
tblproperties (
'primary-key' = 'id'
);
INSERT INTO paimon_table VALUES
(1, 'Alice',21, '2025-11-20', '14', TIMESTAMP '2025-11-20 14:30:00.123'),
(2, 'Lily', 22, '2025-11-20', '15', TIMESTAMP '2025-11-20 14:30:00.123')
(3, 'Bob', 23, '2025-11-21', '14', TIMESTAMP '2025-11-20 14:30:00.123'),;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，一级分区选择「dt」，一级分区值选择「参数1」（2025-11-20），二级分区选择「hour」，二级分区值选择「参数2」(14) | 一、二级分区数据选择成功 |
| 3 | 点击「数据预览」 | 展示所有数据(需要确认是否合理) |
| 4 | 「规则名称」输入「test_rule」「选择数据源」选择「${DATASOURCE}」「选择数据库」选择「${DATABASE}」「选择数据表」选择「${TABLE}」 | 监控对象配置成功 |
| 5 | 「监控规则」新增「有效性校验」「字段」选择「age」「统计函数」选择「数值取值范围」「期望值」为「age <= 21」「过滤条件」设置为「id != 100」「强弱规则」选择为「弱规则」「规则描述」输入「测试规则」 | 监控规则配置完成 |
| 6 | 配置「调度属性」，保存规则 | 规则保存成功 |
| 7 | 立即运行规则，查看规则运行结果 | 规则校验通过 |
| 8 | 进入任务实例页面查看规则明细数据 | 没有脏数据 |

##### 【P0】验证仅选择一级分区校验结果正确

> 前置条件

```
1. 1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表 DDL\DML 如下：
drop table if exists my_table_sink;
create table if not EXISTS my_table_sink (
id int,
name string,
age int,
dt string,
hour string,
ts timestamp(3)
) USING paimon
partitioned by (dt,hour)
tblproperties (
'primary-key' = 'id'
);
INSERT INTO paimon_table VALUES
(1, 'Alice',21, '2025-11-20', '14', TIMESTAMP '2025-11-20 14:30:00.123'),
(2, 'Lily', 22, '2025-11-20', '15', TIMESTAMP '2025-11-20 14:30:00.123'),
(3, 'Bob', 23, '2025-11-21', '14', TIMESTAMP '2025-11-20 14:30:00.123');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，一级分区选择「dt」，一级分区值选择「参数1」（2025-11-20），不选择二级分区 | 一级分区数据选择成功 |
| 3 | 点击「数据预览」 | 展示所有数据(需要确认是否合理) |
| 4 | 「规则名称」输入「test_rule」「选择数据源」选择「${DATASOURCE}」「选择数据库」选择「${DATABASE}」「选择数据表」选择「${TABLE}」 | 监控对象配置成功 |
| 5 | 「监控规则」新增「有效性校验」「字段」选择「age」「统计函数」选择「数值取值范围」「期望值」为「age <= 21」「过滤条件」设置为「id != 100」「强弱规则」选择为「弱规则」「规则描述」输入「测试规则」 | 监控规则配置完成 |
| 6 | 配置「调度属性」，保存规则 | 规则保存成功 |
| 7 | 立即运行规则，查看规则运行结果 | 规则校验不通过 |
| 8 | 进入任务实例页面查看规则明细数据 | id 为2的那条数据被校验出来，age = 22，不符合期望值age<=21 |

##### 【P2】验证一二级分区字段不可重复选择

> 前置条件

```
1. 1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表 DDL如下：
drop table if exists my_table_sink;
create table if not EXISTS my_table_sink (
id int,
name string,
dt string,
hour string,
ts timestamp(3)
) USING paimon
partitioned by (dt,hour)
tblproperties (
'primary-key' = 'id'
);
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，一级分区选择「dt」，一级分区值选择「参数1」 | 选择成功 |
| 3 | 点击二级分区字段选择下拉框 | 不可选择「dt」字段 |
| 4 | 选择「hour」字段 | 可正常选择 |

##### 【P2】验证「分区值」全局参数下拉框展示正确

> 前置条件

```
1. 1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表 DDL如下：
drop table if exists my_table_sink;
create table if not EXISTS my_table_sink (
id int,
name string,
dt string,
hour string,
ts timestamp(3)
) USING paimon
partitioned by (dt,hour)
tblproperties (
'primary-key' = 'id'
);
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「请选择一级分区字段」下拉框 | 展示表A所有字段id,name,dt,hour,ts |
| 3 | 「一级分区字段」选择「dt」 | 一级分区字段选择成功 |
| 4 | 点击「请选择一级分区值」 | 展示全局参数中的参数名${参数名称（注释）} |
| 5 | 选择参数 | 「一级分区值」选择成功 |
| 6 | 点击「请选择二级分区」 | 展示「hour」字段 |
| 7 | 「二级分区字段」选择「hour」 | 二级分区字段选择成功 |
| 8 | 点击「请选择二级分区值」 | 展示全局参数中的参数名${参数名称（注释）} |
| 9 | 选择参数 | 「二级分区值」选择成功 |

##### 【P2】验证表字段-搜索功能正确

> 前置条件

```
1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表A包含字段 id,name,age ,ts,dt,pt
3. 表A未同步到数据地图内
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「请选择一级分区字段」下拉框 | 展示表A所有字段id,name,age,dt,ts,pt |
| 3 | 输入不存在的字段名“test” | 匹配失败，展示空白缺省页 |
| 4 | 输入特殊字符“！@#¥%……&” | 匹配失败，展示空白缺省页 |
| 5 | 输入“a” | 模糊匹配出字段「name，age」 |
| 6 | 输入“age” | 仅展示“age”字段 |
| 7 | 选择「一级分区字段」为「dt」,选择分区值 | 一级分区选择成功 |
| 8 | 点击「请选择二级分区字段」 | 展示「pt」字段 |
| 9 | 输入不存在的字段名“test” | 匹配失败，展示空白缺省页 |
| 10 | 输入特殊字符“！@#¥%……&” | 匹配失败，展示空白缺省页 |
| 11 | 输入“p” | 模糊匹配出字段「pt」 |
| 12 | 输入“pt” | 仅展示“pt”字段 |

##### 【P2】验证表元数据未同步-字段展示正确

> 前置条件

```
1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表A包含字段 id,name,age ,ts,dt,pt
3. 表A未同步到数据地图内
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「请选择一级分区字段」下拉框 | 展示表A分区字段，如果没有分区，则展示所有字段(直连底层库查询) |

##### 【P2】验证表元数据已同步-失败-字段展示正确

> 前置条件

```
1. 当前表A 已同步到数据地图
2. 表A包含字段 id,name,age ,ts,dt,pt
2. 当前表A dt/pt 字段为分区字段，但是采集失败(例如视图)
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「请选择一级分区字段」下拉框 | 展示表A所有字段id,name,age,dt,ts,pt |

##### 【P1】验证表元数据已同步-成功-分区字段过滤功能正确

> 前置条件

```
1. 当前表A已同步到数据地图
2. 且当前表A dt/pt 字段为分区字段
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「请选择一级分区字段」下拉框 | 仅仅展示分区字段「dt\pt」 |

##### 【P0】验证「手动输入分区」支持输入「多级分区」功能正确

> 前置条件

```
1. 1. 已存在${DATASOURCE}_${DATABASE}_${TABLE}数据源，并授权到当前测试项目
2. 表 DDL\DML 如下：
drop table if exists my_table_sink;
create table if not EXISTS my_table_sink (
id int,
name string,
age int,
dt string,
hour string,
ts timestamp(3)
) USING paimon
partitioned by (dt,hour)
tblproperties (
'primary-key' = 'id'
);
INSERT INTO paimon_table VALUES
(1, 'Alice',21, '2025-11-20', '14', TIMESTAMP '2025-11-20 14:30:00.123'),
(2, 'Lily', 22, '2025-11-20', '15', TIMESTAMP '2025-11-20 14:30:00.123'),
(3, 'Bob', 23, '2025-11-21', '14', TIMESTAMP '2025-11-20 14:30:00.123');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「手动输入分区」，输入「dt='2025-11-21',hour='14'」 | 手动输入分区成功 |
| 3 | 点击数据预览 | 数据预览功能正确 |
| 4 | 「规则名称」输入「test_rule」「选择数据源」选择「${DATASOURCE}」「选择数据库」选择「${DATABASE}」「选择数据表」选择「${TABLE}」 | 监控对象配置成功 |
| 5 | 「监控规则」新增「有效性校验」「字段」选择「age」「统计函数」选择「数值取值范围」「期望值」为「age = 21」「强弱规则」选择为「弱规则」「规则描述」输入「测试规则」 | 监控规则配置完成 |
| 6 | 配置「调度属性」，保存规则 | 规则保存成功 |
| 7 | 立即运行规则，查看规则运行结果 | 规则校验不通过 |
| 8 | 进入「任务实例查询」页面查看任务实例明细数据 | 二级分区下的age字段23 != 21 |

##### 【P2】验证「选择动态分区」后查看「全局参数」功能正确

> 前置条件

```
1. 控制台-全局配置内新增、编辑新的全局参数
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，点击「全局参数」 | 展示所有全局参数(包含新增和编辑的) |
| 3 | 点击复制参数名称按钮 | 参数名称复制成功 |

##### 【P2】验证「选择动态分区」选项必填校验正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」，不选择「一级分区字段」「一级分区字段值」，点击「下一步」 | 提示必填项未填 |

##### 【P2】验证「选择动态分区」后页面交互正确

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」-「监控对象」页面 | 进入成功 |
| 2 | 选择「选择动态分区」 | 展示「输入分区」右侧展示「全局参数」按钮必填项「请选择一级分区字段」「请选择一级分区值」下拉单选框非必填项「请选择二级分区字段」「请选择二级分区值」下拉单选框 |

## 数据质量

### 规则任务配置

##### 【P2】验证「监控对象」配置页新增「选择动态分区」选项

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入「数据质量」-「规则任务配置」页面 | 进入成功 |
| 2 | 点击「新建监控规则」按钮，进入「监控对象」配置页面 | 进入成功 |
| 3 | UI CHECK | 「输入分区」下新增「选择动态分区」可选项 |
