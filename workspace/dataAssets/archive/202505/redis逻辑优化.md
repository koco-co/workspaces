---
suite_name: "redis逻辑优化"
description: "redis逻辑优化用例归档"
tags:
  - "绝是否正常"
  - "过是否正常"
  - "功能是否正常"
  - "拒绝是否正常"
  - "通过是否正常"
  - "数据级别权限申请"
  - "审批功能是否正常"
  - "数据模型申请审批"
  - "数据标准申请审批"
  - "表权限申请审批拒"
  - "表权限申请审批通"
  - "redis逻辑优化"
prd_version: "v6.3.4"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 8
case_id: 8844
---

##### 【P1】验证数据级别权限申请审批功能是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据安全-级别管理，点击机密后的申请权限2）挂掉metadata，管理员审批拒绝3）重启metadata，查看分级数据 | 依旧未拥有分级数据为机密的查看权限 |

##### 【P1】验证数据级别权限申请审批功能是否正常

> 前置条件

```
普通用户级别为L1(用户中心修改)
级别管理-机密的用户级别为L2
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据安全-级别管理，点击机密后的申请权限2）挂掉metadata，管理员审批通过3）重启metadata，查看分级数据 | 拥有分级数据为机密的查看权限 |

##### 【P1】验证数据模型申请审批功能是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据模型，新建表，点击建表2）挂掉metadata，管理员审批拒绝3）重启metadata，查看数据模型状态 | 未建表 |
| 2 | 1）删除数据模型2）挂点metadata，管理员审批拒绝3）重启metadata，查看数据模型 | 表未被删除 |

##### 【P1】验证数据模型申请审批功能是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据模型，新建表，点击建表2）挂掉metadata，管理员审批通过3）重启metadata，查看数据模型状态 | 为已建表 |
| 2 | 1）删除数据模型2）挂点metadata，管理员审批通过3）重启metadata，查看数据模型 | 删除成功 |

##### 【P1】验证数据标准申请审批拒绝是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 普通用户进入数据标准，新建审批，点击发布，查看状态 | 待发布 |
| 2 | 1）普通用户进入数据标准，新建标准，发布2）挂掉metadata，管理员审批拒绝3）重启metadata，查看数据标准发布状态 | 为待发布 |
| 3 | 1）下线数据标准2）挂点metadata，管理员审批拒绝3）重启metadata，查看数据标准发布状态 | 为已发布 |

##### 【P0】验证数据标准申请审批通过是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据标准，新建标准，发布2）挂掉metadata，管理员审批通过3）重启metadata，查看数据标准发布状态 | 为已发布 |
| 2 | 1）下线数据标准2）挂点metadata，管理员审批通过3）重启metadata，查看数据标准发布状态 | 为已下线 |

##### 【P1】验证表权限申请审批拒绝是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据地图，申请表DQL权限2）挂掉metadata，管理员审批拒绝2）重启metadata，查看数据表权限 | 没有表的查询表权限 |

##### 【P0】验证表权限申请审批通过是否正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）普通用户进入数据地图，申请表DQL权限2）挂掉metadata，管理员审批通过2）重启metadata，查看数据表权限 | 有表的查询表权限 |
