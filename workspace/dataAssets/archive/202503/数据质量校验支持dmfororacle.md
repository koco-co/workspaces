---
suite_name: "数据质量校验支持dm for oracle"
description: "数据质量校验支持dm for oracle用例归档"
tags:
  - "概览"
  - "设置"
  - "运行"
  - "创建"
  - "文件"
  - "与显示"
  - "规则集"
  - "告警汇总"
  - "数据统计"
  - "列表数据"
  - "删除任务"
  - "次日更新"
  - "任务状态"
  - "手动停止"
  - "筛选正常"
prd_version: "v6.3.2"
dev_version:
  - "袋鼠云"
create_at: "2026-04-05"
status: "草稿"
origin: "csv"
case_count: 22
case_id: 8651
---

##### 【P2】验证概览-告警汇总-数据统计

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 告警次数 | 显示所选时间段内的告警次数统计 |
| 2 | 告警规则数 | 显示所选时间段内发生过告警的规则数量 |
| 3 | 告警率 | 所选时间段内告警规则数/所选时间段内总规则数 |

##### 【P2】验证概览-告警汇总-取消图标可点击下钻

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击告警汇总内的图标或信息无法进行跳转 | 展示更多告警信息 |
| 2 | 点击右上角查看更多 |  |

##### 【P2】验证概览-表质量排名-列表数据

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 默认展示当日质量分前10名数据 | 展示数据与实际质量项目内所【运行】的规则任务一致 |
| 2 | 筛选显示不同数据源信息 | 展示不同数据源下不同表信息且符合排名规则 |
| 3 | 点击查看质量报告 | 跳转具体表的质量报告信息 |

##### 【P2】验证概览-表质量排名-相似数据源同一张表

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 相同底层数据源，通过2个不同数据源名称引入资产（创建项目） | 记录为不同数据源下表分别的质量评分 |
| 2 | 使用2个不同的质量任务分别针对这两个数据源下同一张表进行质量校验 |  |
| 3 | 查看质量分排名 |  |

##### 【P2】验证概览-表质量排名-删除任务

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 创建多条质量任务，在表质量排名有数据展示后，删除任意一条质量任务 | 质量报告，概览中的表质量排名，都没有该任务评分 |

##### 【P1】验证概览-表质量排名-次日更新

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 创建一条质量任务，并使得其校验失败触发告警 | 每次运行结束后，表质量分都重新计算 |
| 2 | 多次运行该质量任务，查看概览中该表质量分情况 | 该表质量分恢复为--分 |
| 3 | 最后一次运行结束后，不再进行任务运行，等到第二天查看该表质量分 |  |

##### 【P2】验证任务状态（手动停止）筛选正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 准备一个手动停止的任务“状态”列筛选“手动停止” | 列表展示状态为“手动停止”的任务实例 |

##### 【P2】验证中途停止功能正常

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 运行一个质量任务等待任务为运行中状态时，点击【停止】 | 任务状态变为“手动停止”底层任务也被取消 |

## 任务停止-

##### 【P1】验证任务停止交互与显示

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 运行质量任务任务状态为“等待运行” | “操作”列无操作按钮 |
| 2 | 任务状态流转为“运行中” | “操作”列显示“停止”按钮 |
| 3 | 任务状态流转为非“运行中”或非“手动停止” | “操作”列无操作按钮 |
| 4 | 点击【停止】 | 任务状态流转为“手动停止”；“操作”列显示“运行”按钮；“任务名称”/“表”不可点击 |

##### 【P1】验证脏数据管理-到期删除脏数据表

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）配置dm for oracle数据源类型的数据源的脏数据库2）“数据存储时效”为1天3）运行dm for oracle数据源类型的单表校验任务，并使其校验不通过 | 脏数据表生成，且表数据正确 |
| 2 | 第二天查看该脏数据表 | 该脏数据表被删除 |

##### 【P1】验证编辑脏数据存储库

> 前置条件

```
1）进入【数据质量】-【脏数据管理】页面
2）准备dm for oracle数据源下的表单表校验任务
3）开启独立存储，开启“脏数据存储”
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 点击编辑 | 弹出编辑独立存储弹窗 |
| 2 | 1）点击编辑2）查看独立存储弹窗 | 1）检验数据源显示正确，置灰2）是否存储到源库默认选中“是”3）数据存储时效默认为90 |
| 3 | 1）点击编辑2)  点击否3）脏数据存储库选择xxx4）点击确定 | 1）弹窗关闭2）dm for oracle数据源下的脏数据库的脏数据存储库列显示xxx |

##### 【P0】验证脏数据管理-设置

> 前置条件

```
开启独立存储
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）开启“脏数据存储”2）异常任务，查看明细 | 显示异常数据并能下载成功 |
| 2 | 1）关闭“脏数据存储”2）异常任务，查看明细 | 不显示异常数据且不支持下载 |

##### 【P1】验证脏数据管理-查询

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 开启“独立存储” | 列表展示dm for oracle数据源记录 |
| 2 | 查看“数据源类型”下拉项 | 下拉项包含dm for oracle数据源类型 |
| 3 | “数据源类型”选择dm for oracle数据源类型 | 列表返回dm for oracle数据源类型的数据源记录 |

##### 【P1】验证单表校验-质量报告

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 运行dm for oracle数据源的单表校验任务 | 该任务质量评分数据以及其他统计信息正确 |
| 2 | 查看“最近30次表级统计” | 记录数：只统计每次表行数规则校验失败时的实际表行数；总告警数：只统计每次校验不通过的规则数 |

##### 【P1】验证单表校验-运行-校验不通过

> 前置条件

```
1）准备dm for oracle数据源下的表单表校验任务
2）规则配置完整性校验/准确性校验/规范性校验/唯一性校验/自定义SQL；且配置的规则会使任务校验不通过；
3）开启脏数据存储
4）运行该任务；
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入【任务实例查询】查看该任务实例 | 任务状态流转正确 |
| 2 | 1）进入【任务实例查询】查看该任务实例2）查看“监控报告”（点击表名） | 监控报告显示校验规则正确 |
| 3 | 1）进入【任务实例查询】查看该任务实例2）查看“监控报告“-查看明细 | 异常数据展示正确 |
| 4 | 1）进入【任务实例查询】查看该任务实例2）“监控报告”-“查看明细”-下载 | 1）下载文件成功；2）文件显示异常数据正确 |
| 5 | 1）进入【任务实例查询】查看该任务实例2）查看“表级报告”（点击表名） | 表级报告展示的数据正确 |

##### 【P0】验证单表校验-运行-校验通过

> 前置条件

```
1）准备dm for oracle数据源下的表单表校验任务
2）规则配置完整性校验/准确性校验/规范性校验/唯一性校验/自定义SQL；且配置的规则会使任务校验通过
3）运行该任务；
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入【任务实例查询】查看该任务实例 | 任务状态流转正确 |
| 2 | 1）进入【任务实例查询】查看该任务实例2)查看“监控报告”（点击表名） | 监控报告显示校验规则正确 |
| 3 | 1）进入【任务实例查询】查看该任务实例2)查看“表级报告”（点击表名） | 表级报告展示的数据正确 |

##### 【P1】验证单表校验-编辑

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）查看规则列表页2）点击单表校验规则表名3）修改规则名称 | 规则名称修改成功 |
| 2 | 1）查看规则列表页2）点击单表校验规则表名3）添加监控规则 | 规则添加成功 |
| 3 | 1）查看规则列表页2）点击单表校验规则表名3）修改监控规则 | 规则修改成功 |
| 4 | 1）查看规则列表页2）点击单表校验规则表名3）删除监控规则 | 规则删除成功 |
| 5 | 1）查看规则列表页2）点击单表校验规则表名3）编辑调度属性 | 调度属性编辑成功 |

##### 【P0】验证单表校验-创建

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）【规则任务配置】新建单表校验规则；2）数据表选择dm for oracle数据源类的表；3）规则配置完整性校验/准确性校验/规范性校验/唯一性校验/自定义SQL | 任务创建成功 |
| 2 | 1)查看规则列表页2)点击表名 | 1）弹出任务详情抽屉2）任务详情显示正确 |
| 3 | 1)查看规则列表页2)点击表名3)详情抽屉中点击“规则SQL”的【查看】 | 显示当前任务拼接的SQL |

##### 【P2】验证规则集-下载不同版本规则文件

> 前置条件

```
无
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）进入“查看详情”抽贴2）查看规则内容历史版本号-下载规则文件 | 1）下载功能正常，文件名格式：${版本}_rule_collection_${下载日期}.xls2）下载的文件内容正确，为对应版本的规则信息 |
| 2 | 下载另一个版本的规则文件 | 不同版本的对应规则文件内容正确 |

##### 【P2】验证规则集-查看详情

> 前置条件

```
1.校验平台管理引入dm for oracle数据源
2.dm for oracle数据源对质量项目test授权
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）数据质量页面，点击规则任务配置2）选择规则集A点击 | 1）顶部显示“规则集名称”2）基础信息展示正确：规则集名称、数据源、描述3）规则内容展示正确：规则名称、描述、表名、中文名、字段名、字段中文名、校验sql、分页展示4)调度配置展示正确：调度周期、执行时间、告警方式、接收人、规则拼接包5）规则历史内容版本展示正确：版本号、操作人、操作时间、操作 |

##### 【P0】验证规则集-运行

> 前置条件

```
1.校验平台管理引入dm for oracle数据源
2.dm for oracle数据源对质量项目test授权
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）创建规则集A，“校验数据源”选择dm for oracle2）导入规则集文件（文件中包含dm for oracle数据源下表的校验SQL，且SQL可执行）3）立即执行 | 规则集运行正常 |
| 2 | 1）创建规则集A，“校验数据源”选择dm for oracle2）导入两个规则集文件（文件中包含dm for oracle数据源下表的校验SQL，且第一个SQL可执行，第二个SQL不可执行）3）立即执行 | 任务校验失败 |

##### 【P0】验证规则集-创建

> 前置条件

```
1.校验平台管理引入dm for oracle数据源
2.dm for oracle数据源对质量项目test授权
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 1）创建规则集A，“校验数据源”选择dm for oracle2）导入规则集文件（文件中包含dm for oracle数据源下表的校验SQL） | 创建规则集流程正常 |
