# 图N 页面要点格式

每张图片引用下方统一插入一个结构块，使用以下格式：

```markdown
#### 图N 页面要点

- 页面类型：
- 区域构成：
- 关键操作：
- 列表信息：
- 检索条件：
- 输入项：
- 流程/状态：
- 识别限制：
```

## 填写规则

- `图N` 按图片在文档中的出现顺序连续编号。
- 仅记录图片中可见或可可靠判断的信息，不杜撰按钮、字段或状态。
- 某项不涉及时写 `无` 或 `不涉及`，不要留含糊描述。
- 图片模糊、遮挡或裁切导致无法确认的信息，统一写入 `识别限制`。
