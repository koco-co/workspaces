# Hotfix 用例生成指令

> 本提示词由 code-analysis skill 在模式 E 时加载。
> 根据禅道 Bug 信息和 git diff 生成单条 Hotfix 测试用例，输出为 Archive 格式 Markdown。

---

## 生成目标

针对一个已修复的线上 Bug，生成：

1. **一条主验证用例**：验证修复后 Bug 不再复现
2. **一条回归验证用例**（可选）：验证修复未引入新问题

---

## 输入信息

| 字段 | 来源 | 说明 |
|------|------|------|
| `bug_id` | zentao fetch 输出 | 禅道 Bug ID，如 `138845` |
| `title` | zentao fetch 输出 | Bug 标题 |
| `severity` | zentao fetch 输出 | 严重程度：critical / major / normal / minor |
| `fix_branch` | zentao fetch 输出 | 修复分支名 |
| `steps` | zentao fetch 输出 | 原始复现步骤（如有） |
| `git diff` | repo-sync 后执行 | 修复分支与主分支的代码差异 |

若 zentao fetch 返回 `partial: true`（API 不可达），则仅基于 Bug ID 和用户描述生成用例，在用例前置条件中注明"来源：用户描述"。

---

## 分析步骤

### 第一步：理解 Bug 本质

从 Bug 标题和复现步骤中提取：

- **触发场景**：在哪个页面 / 哪个操作下触发
- **现象描述**：出现了什么错误（报错 / 数据错误 / 功能不可用）
- **影响范围**：哪些用户 / 哪些数据受影响

### 第二步：分析代码变更（当 git diff 可用时）

从 git diff 中识别：

- **变更文件**：哪些文件被修改
- **变更类型**：空值防护 / 逻辑修复 / SQL 修正 / 配置修正 等
- **修复方式**：加了什么判断、改了什么逻辑

将变更类型映射到测试验证点，例如：
- 加了空值防护 → 验证该字段为空时页面不崩溃
- 修正了 SQL → 验证查询结果正确
- 修复了状态机 → 验证状态流转符合预期

### 第三步：设计验证步骤

按照以下原则设计步骤：

1. **前置条件**：复现 Bug 所需的最小环境配置和数据准备（参考 Bug 的原始复现步骤）
2. **主验证步骤**：执行触发原 Bug 的操作
3. **预期结果**：描述修复后应有的正确行为（而非错误行为）

---

## 输出格式

输出为 Archive Markdown 格式，遵循 `.claude/rules/archive-format.md` 规范：

```markdown
---
suite_name: "Hotfix 用例 - {{bug_title}}"
description: "验证 Bug #{{bug_id}} 修复效果"
tags:
  - hotfix
  - bug-{{bug_id}}
create_at: "{{YYYY-MM-DD}}"
status: 草稿
origin: hotfix
---

## {{模块名称}}

### {{页面名称}}

#### {{功能点名称}}

##### 【{{bugId}}】验证{{修复后的正确行为描述}}

> 前置条件
```
1、{{环境要求}}

2、{{数据准备（若涉及 SQL，给出具体建表和插入语句）}}

3、复现步骤（原始 Bug 的操作路径）：
{{复现步骤描述}}
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入【{{完整导航路径}}】页面 | 页面正常加载 |
| 2 | {{执行触发原 Bug 的操作}} | {{修复后的正确预期}} |
| 3 | {{补充验证步骤（如有）}} | {{预期}} |

##### 【{{bugId}}-回归】验证{{相关功能未受影响}}

> 前置条件
```
同上
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| ---- | ---- | ---- |
| 1 | 进入【{{完整导航路径}}】页面 | 页面正常加载 |
| 2 | {{执行正常业务操作}} | {{原有功能正常}} |
```

---

## 用例标题规范

Hotfix 用例**不使用** `【P0】`/`【P1】`/`【P2】` 优先级前缀，改用 Bug ID 作为标识：

- 主验证：`【{{bugId}}】验证{{修复后的正确行为}}`
- 回归验证：`【{{bugId}}-回归】验证{{相关功能未受影响}}`

示例：
- `【138845】验证商品列表页在品类为空时不显示 500 报错`
- `【138845-回归】验证品类有值时商品列表页正常加载`

---

## 前置条件规范

参考 `.claude/rules/test-case-writing.md` 中「前置条件数据准备规则」：

- 若 Bug 涉及特定数据状态，**必须给出可执行的 SQL 语句**（建表 + 插入）
- 若 Bug 仅涉及操作流程，前置条件描述操作路径即可
- 不得写"确保数据存在"这类模糊描述

---

## 注意事项

- 用例目标是**验证修复效果**，不是重新描述 Bug 现象；预期结果应写修复后的正确行为。
- 若 git diff 不可用（API 不可达），从 Bug 标题和复现步骤推断，并在用例备注中注明"代码变更信息不可用，步骤基于 Bug 描述推断"。
- 若 Bug 标题或复现步骤信息极少（partial: true），仍需生成用例框架，将不确定的部分用 `{{待补充}}` 标注。
- 回归用例仅在 git diff 显示修改了核心业务逻辑时才生成；若仅是加了空值防护等防御性修复，可省略回归用例。
