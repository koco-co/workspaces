# 测试用例编写规范

> 适用范围：所有自动生成的测试用例（Writer Subagent 和 Reviewer Subagent 必须遵守）

## XMind 层级结构

```
Root (项目名)
  └── L1: 【版本】需求名称（prd_id 写入 L1 labels）
       └── L2: 菜单/模块名称          ← 如「质量问题台账」
            └── L3: 页面名称           ← 如「列表页」「新增页」「详情页」
                 └── [L4: 功能子组]    ← 如「搜索」「字段校验」（可选）
                      └── 用例标题
                           └── 步骤
                                └── 预期结果
```

- L2 必须对应系统中的菜单或独立功能模块名称
- L3 按页面维度划分（列表页、新增页、编辑页、详情页等）
- L4 为可选层级，当页面内功能较多时按功能分组（搜索、导出、字段校验等）

## Archive MD 层级结构

Archive Markdown 从 `##` 开始，需求名称（`suite_name`）在 frontmatter 中，不再有 `#` 标题：

```
## 模块名称        ← 如「数据质量」（对应 XMind L2）
### 菜单名称       ← 如「规则集配置」（对应 XMind L3）
#### 功能点名称    ← 如「一致性校验」（对应 XMind L4，可选）
##### 【P0】验证xxxx  ← 用例标题（优先级前缀）
```

## 用例标题规范

- **优先级前缀**：`【P0】`/`【P1】`/`【P2】`，置于用例标题**前面**，如：`【P0】验证质量问题台账列表页默认加载`
- 「验证」二字紧跟优先级标记，如：`【P1】验证填写完整表单后成功提交`
- 标题应概括测试目的，不包含具体操作步骤

## 步骤格式规范

- **禁止**步骤编号前缀：不允许出现「步骤1:」「步骤2:」「Step1:」等字样
- 第一个步骤**必须**以「进入【完整导航路径】页面」开头
  - **单层菜单**：`进入【JSON格式配置】页面`
  - **多层菜单**：`进入【数据质量 → 通用配置 → JSON格式配置】页面`（菜单路径放在【】内，用 → 连接，导航路径**不放在括号注释中**）
  - **禁止格式**：`进入【JSON格式配置】页面（导航路径：数据质量 → 通用配置 → JSON格式配置）` ❌
- 步骤描述必须为可执行的具体操作，包含明确的操作对象和操作行为
- 步骤表格前**必须**有 `> 用例步骤` 标签

```markdown
> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 通用配置 → JSON格式配置】页面 | 页面正常加载 |
```

## 用例设计原则

| 类型     | 条件组合规则           | 示例                                                                  |
| -------- | ---------------------- | --------------------------------------------------------------------- |
| 正常用例 | 可包含多个正向条件组合 | 验证填写完整表单后成功提交（问题名称 + 问题分类 + 责任人 + 整改期限） |
| 异常用例 | **仅允许一个逆向条件** | 验证问题名称为空时无法提交                                            |
| 边界用例 | 针对边界值的单一验证   | 验证问题名称输入 100 个字符时正常保存                                 |

### 正向用例合并规则（强制）

同一功能区（sub_group 或 page）内，步骤数少于 4 条的正向用例必须与其他正向用例合并：

- 将该功能区内所有步骤数 < 4 的正向用例整合为 1 个综合正向用例
- 综合用例步骤按用户操作流顺序衔接，前置条件取各子用例的并集
- **异常用例不适用此规则**：每条异常用例仍只测试一个逆向条件，步骤数即使只有 1 条也不与其他用例合并

## 测试数据规范

- **必须使用真实业务数据**，给出具体的输入值和选择项
- **禁止模糊词**：

  | 禁止词                     | 正确写法                                                         |
  | -------------------------- | ---------------------------------------------------------------- |
  | 尝试提交表单               | 点击【提交】按钮                                                 |
  | 输入像"测试数据"这样的内容 | 在「问题名称」输入框输入"2026年3月产线温度异常"                  |
  | 选择如"已完成"等状态       | 在「处理状态」下拉框选择"已完成"                                 |
  | 输入比如手机号             | 在「联系电话」输入框输入"13800138000"                            |
  | 填写相关信息               | 在「问题名称」输入"原料批次不合格"，在「问题分类」选择"质量缺陷" |

## 预期结果规范

- 必须精确描述系统行为，包含可观测的具体表现
- **禁止空洞表述**：

  | 禁止写法 | 正确写法                                                                         |
  | -------- | -------------------------------------------------------------------------------- |
| 操作成功 | 页面提示"新增成功"，列表页新增一条记录，问题名称显示为"原料批次不合格"           |
| 显示正确 | 列表第一行显示：行动编号"QA-2026-001"，问题分类"质量缺陷"，状态"待处理"          |
| 提交失败 | 「问题名称」输入框下方显示红色提示"请输入问题名称"，【提交】按钮保持不可点击状态 |

## 源码优先规则（当 config.repos 非空时启用）

> 以下规则仅在 config.json 中 `repos` 字段为非空对象时适用。若 `repos: {}` 则跳过本节。

- **源码优先**：用例编写前必须先确认 `.repos/` 中相关仓库已切到 config.json 的 `branchMapping` 字段解析出的目标分支。
- **前置条件补全**：若需求涉及数据库操作、规则集、调度等能力，前置条件应尽量包含：
  - 数据源类型（如 `${datasource_type}`）
  - 数据库 / schema / 表名
  - 关键字段
  - 必要时补充建表或准备数据说明，格式示例：

```
1、xxx前置环境说明

2、${datasource_type} SQL语句准备:
DROP TABLE IF EXISTS ${schema}.${table};
CREATE TABLE ${schema}.${table} (...);
INSERT INTO ${schema}.${table} VALUES (...);
```
- **步骤格式**：表单类步骤优先写成结构化块，如：

```text
点击【新建规则集】按钮，基础信息如下：
- 选择数据源: ${数据源}
- 选择数据库: ${数据库}
- 选择数据表: ${数据表}
```

- **禁止”堆句子”**：不得将多个字段输入动作压缩为”填写相关信息””勾选对应字段”这类概括句。

## 表单多字段步骤合并规则（强制）

**当一个步骤涉及同一表单/面板内的多个配置项时，必须合并为一个步骤节点，按字段在页面上的实际顺序逐项列出。**

**正确写法：**

```text
在【监控规则】表单页面，点击【添加规则-合理性校验】，配置如下：
- 字段: xx
- 统计函数: 多表字段值对比
- 校验表主键: xx
- 过滤条件: xxx（非必填，可填写无）
- 关联表1（关联表1主键）: xx（xx）
- 计算逻辑配置: xx
- 对比方法: xx
- 强弱规则: 弱规则
- 规则描述: xxx
```

**禁止写法（每个字段单独一个步骤节点）：**

```text
❌ 步骤1: 在「统计函数」下拉框选择「key范围校验」
❌ 步骤2: 在「字段」下拉框选择「json_field」字段
❌ 步骤3: 在「校验方法」下拉框选择「包含」
```

**适用规则：**

1. **字段顺序**：按前端页面实际渲染顺序排列，优先参考前端源码（表单组件定义顺序）
2. **非必填字段**：保留在列表中，在值后加注 `（非必填，可填写无）`
3. **嵌套配置**：若某个字段展开后有子配置项，用缩进或换行继续列举（如「计算逻辑配置」下有「对比方法」）
4. **多步骤拆分时机**：仅在以下情况允许拆分为单独步骤：
   - 该字段的选择会触发页面联动/刷新，且联动后需要单独验证
   - 该步骤本身就是该用例的核心验证动作（如验证某个字段选项是否存在）

## 质量控制

Reviewer 采用 3 级质量阈值自动决策：

| 问题率    | 行为                                 |
| --------- | ------------------------------------ |
| < 15%     | 自动修正，无需通知用户               |
| 15% - 40% | 自动修正 + 质量警告，建议用户核查    |
| > 40%     | 立即停止，输出阻断报告，等待用户决策 |

问题率 = 含 F01-F09 任意问题的用例数 / 总用例数（一条用例多个问题只计 1 次）。
