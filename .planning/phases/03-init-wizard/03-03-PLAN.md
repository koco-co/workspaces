---
phase: 03-init-wizard
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - .claude/skills/using-qa-flow/SKILL.md
autonomous: true
requirements: [INIT-01, INIT-02, INIT-03, INIT-04, INIT-05]

must_haves:
  truths:
    - "SKILL.md routes 'init' / '初始化' / '0' to Step 0 config wizard before Step 1-5"
    - "Step 0 calls init-wizard.mjs --command scan and displays results as Markdown table (D-03)"
    - "Step 0 supports Q&A correction flow for inaccurate inference (D-04)"
    - "Step 0 includes history file detection prompt (D-05) and module key confirmation (D-06)"
    - "Step 0 walks through 5 functional groups in order: basic → modules → repos → integrations → confirm (D-09)"
    - "Step 0 shows text summary before writing and asks confirmation (D-10)"
    - "Step 0 detects re-init and asks user intent: full reconfigure vs incremental (D-14)"
    - "Step 0 asks whether to also update CLAUDE.md on re-init (D-16)"
  artifacts:
    - path: ".claude/skills/using-qa-flow/SKILL.md"
      provides: "Complete Step 0 config wizard dialog flow + preserved Step 1-5"
      contains: "Step 0"
  key_links:
    - from: ".claude/skills/using-qa-flow/SKILL.md"
      to: ".claude/skills/using-qa-flow/scripts/init-wizard.mjs"
      via: "node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan/write/load-existing/render-template"
      pattern: "init-wizard\\.mjs"
    - from: ".claude/skills/using-qa-flow/SKILL.md"
      to: ".claude/skills/using-qa-flow/templates/CLAUDE.md.template"
      via: "Referenced in render-template command call"
      pattern: "CLAUDE\\.md\\.template"
---

<objective>
Update SKILL.md with the complete Step 0 config wizard dialog orchestration. This plan wires the init-wizard.mjs sub-commands (scan, parse-file, write, load-existing, render-template) into a Claude dialog flow that guides the user through project inference, history file parsing, grouped config Q&A, confirmation, and file writing.

Purpose: INIT-01 is the user-facing interactive init. The script (init-wizard.mjs) handles I/O, but the Claude dialog flow in SKILL.md orchestrates the conversation: displaying inference tables, asking questions, collecting answers, and calling scripts at the right moments. This plan connects all pieces into a coherent init experience.

Output: Updated SKILL.md with Step 0 inserted before existing Step 1-5, full dialog flow covering D-01 through D-16.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-init-wizard/03-CONTEXT.md
@.planning/phases/03-init-wizard/03-RESEARCH.md
@.planning/phases/03-init-wizard/03-01-SUMMARY.md
@.planning/phases/03-init-wizard/03-02-SUMMARY.md

@.claude/skills/using-qa-flow/SKILL.md
@.claude/skills/using-qa-flow/scripts/init-wizard.mjs

<interfaces>
<!-- init-wizard.mjs CLI commands available for SKILL.md to call -->

Sub-commands (all output JSON to stdout):
```bash
# Scan project directory, returns { modules: [], signals: {} }
node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan [--root-dir <path>]

# Parse single history file, returns { candidates: [], source: 'csv'|'xmind' }
node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command parse-file --path <file-path>

# Load existing config (re-init), returns config JSON or null
node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command load-existing [--root-dir <path>]

# Write config.json and optionally CLAUDE.md
node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command write --config-json '<json>' [--claude-md '<content>'] [--root-dir <path>]

# Render CLAUDE.md template with replacements
node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command render-template --template-path <path> --replacements '<json>'
```

Scan output format:
```json
{
  "modules": [
    { "key": "data-assets", "versioned": true, "inferredFrom": "cases/xmind", "paths": {} }
  ],
  "signals": {
    "hasCasesDir": true,
    "hasReposDir": false,
    "hasImages": true,
    "historyFiles": [{ "path": "cases/history/test.csv", "type": ".csv" }],
    "prdVersionPatterns": ["v1.2.3"],
    "existingConfig": null
  }
}
```

Functional groups for config Q&A (D-09):
1. basic: project.name, displayName, casesRoot
2. modules: module keys, versioned flag, zh name (already inferred from scan)
3. repos: repos paths, branchMapping YAML path, stackTrace config
4. integrations: lanhuMcp (runtimePath, envFile, etc.)
5. confirm: text summary → write

mergeConfigGroups(existing, updated, selectedGroups) — selectedGroups is array of group names from: ['basic', 'modules', 'repos', 'integrations', 'shortcuts']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md with Step 0 config wizard dialog flow</name>
  <files>
    .claude/skills/using-qa-flow/SKILL.md
  </files>
  <read_first>
    .claude/skills/using-qa-flow/SKILL.md
    .claude/skills/using-qa-flow/scripts/init-wizard.mjs
    .claude/skills/using-qa-flow/templates/CLAUDE.md.template
    .planning/phases/03-init-wizard/03-CONTEXT.md
  </read_first>
  <action>
    Update `.claude/skills/using-qa-flow/SKILL.md` with the following changes. **Preserve all existing content** (功能菜单, routing logic, Step 1-5, 快速示例) — only insert Step 0 and update the init routing.

    **Change 1: Update 功能菜单 table** — change item 0 description:
    ```
    | **0** | 项目配置 + 环境初始化 | 首次使用时执行：项目结构推断、config.json 生成、CLAUDE.md 创建 + 环境初始化 |
    ```

    **Change 2: Update init routing block** — the existing block `如果 $ARGUMENTS 包含 init 或 初始化 或 0：` should now route to Step 0 FIRST, then offer Step 1-5 afterwards:
    ```markdown
    如果 `$ARGUMENTS` 包含 `init` 或 `初始化` 或 `0`：

    - 先执行下方「Step 0: 项目配置向导」
    - Step 0 完成后，询问用户是否继续执行「环境初始化（Step 1-5）」
    ```

    **Change 3: Insert Step 0 section** — add BEFORE the existing `## 环境初始化（5 步流程）` section. The new section:

    ```markdown
    ## Step 0: 项目配置向导（首次使用必须完成）

    仅在 `$ARGUMENTS` 包含 `init` / `初始化` / `0` 时执行。此步骤生成 `config.json` 和 `CLAUDE.md`，是所有其他 Skill 正常工作的前提。

    ### 0.1 扫描项目结构

    执行目录扫描：

    ```bash
    node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan
    ```

    解析返回的 JSON 结果。

    **Re-init 检测（D-14）：** 如果 `signals.existingConfig` 不为 null，说明已有 config.json：
    - 询问用户：「检测到已有项目配置。请选择：(1) 完整重新配置 (2) 只更新部分配置」
    - 选择 (2) 时，展示五个功能组让用户勾选要重新配置的组（D-15）：
      - ① 基础信息（项目名、显示名、用例根目录）
      - ② 模块配置
      - ③ 源码仓库
      - ④ 集成工具
      - ⑤ 快捷方式和目录
    - 未勾选的组保持现有值不变
    - 如果选择 (1)，执行完整的 0.1~0.5 流程

    **新项目流程：** 如果 `signals.existingConfig` 为 null，直接进入推断展示。

    ### 0.2 展示推断结果（D-03）

    将 `modules[]` 数组格式化为 Markdown 表格展示：

    ```
    ### 📋 项目结构推断结果

    | 模块 key | 是否版本化 | 路径 | 推断来源 |
    |----------|-----------|------|----------|
    | {key}    | {versioned ? '✅ 是' : '❌ 否'} | {paths.xmind || 'cases/xmind/' + key + '/'} | {inferredFrom} |
    ```

    同时展示检测到的信号摘要：
    - `.repos/` 目录: {hasReposDir ? '已检测到 → 建议配置源码仓库' : '未检测到'}
    - 历史文件: {historyFiles.length} 个 ({historyFiles.map(f => f.path).join(', ')})
    - PRD 版本号: {prdVersionPatterns.join(', ') || '无'}
    - 图片目录: {hasImages ? '已检测到' : '未检测到'}

    如果 `modules` 为空且 `historyFiles` 也为空（完全空白项目）：
    - 提示：「未检测到现有结构，将通过问答方式引导你配置项目。」
    - 直接跳到 0.4 全量问答

    **用户确认：** 询问「以上推断是否正确？(y/n)」
    - 如果 n：逐项询问修正（D-04）——对每个模块询问 key 是否正确、versioned 是否正确，用户输入新值后更新，重新展示表格直到用户确认

    ### 0.3 历史文件解析（D-05/D-06/D-07）

    **自动检测部分：** 如果 `signals.historyFiles` 非空：
    - 对每个历史文件调用：
      ```bash
      node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command parse-file --path {filePath}
      ```
    - 展示解析结果：「从 {filename} 检测到模块候选名：{candidates.join(', ')}」
    - 对每个候选名，询问用户确认（D-06）：「检测到模块名 "{candidate}"，请确认或输入正确的英文 key：」
    - 用户输入后，还需确认该模块是否版本化

    **主动追问部分：** 完成自动检测后，追问（D-05）：「还有其他历史文件要导入吗？如有请提供文件路径，没有请回复 n」
    - 如果用户提供路径，继续调用 parse-file 解析

    **合并展示（D-07）：** 将目录扫描推断结果 + 历史文件解析结果分别展示：
    ```
    ### 来自目录扫描的模块：
    | 模块 key | 是否版本化 | 来源 |
    ...

    ### 来自历史文件的新增模块：
    | 模块 key | 是否版本化 | 来源文件 |
    ...

    ### 冲突项（同名模块，不同来源）：
    | 模块 key | 目录扫描结果 | 历史文件结果 | 请选择保留哪个 |
    ...
    ```

    用户逐项决定冲突项后，合并为最终模块列表。

    ### 0.4 功能分组问答（D-08/D-09）

    按五个功能组逐一询问所有配置字段：

    **① 基础信息**
    ```
    ### ① 基础信息
    请提供以下信息：
    - 项目英文标识（project.name）：（用于内部标识，如 my-project）
    - 项目显示名（displayName）：（用于展示，如「我的项目」）
    - 用例根目录（casesRoot）：（默认 cases/）
    ```

    **② 模块配置**
    ```
    ### ② 模块配置
    以下是当前确认的模块列表（来自扫描 + 历史文件解析）：
    {展示模块表格}

    需要添加新模块吗？(y/n)
    如果 y：询问模块 key、是否版本化、中文名（可选）

    对每个模块最终确认 versioned 状态。
    ```

    **③ 源码仓库**
    ```
    ### ③ 源码仓库配置
    {如果 signals.hasReposDir: '检测到 .repos/ 目录，建议配置源码仓库。'}

    是否需要配置源码仓库分析能力？(y/n)
    如果 y：
    - 询问仓库名和本地路径（可多次添加）
    - 是否配置分支映射文件？（默认路径 .claude/repo-branch-mapping.yaml）
    - 是否配置 stackTrace 分析？（Java 包名等）
    如果 n：repos = {}, branchMapping = null, stackTrace = {}
    ```

    **④ 集成工具**
    ```
    ### ④ 集成工具
    是否需要配置蓝湖 MCP 集成？(y/n)
    如果 y：逐一确认各字段（展示默认值，用户可直接回车接受）
    - runtimePath（默认 tools/lanhu-mcp/）
    - envFile（默认 tools/lanhu-mcp/.env）
    - ...其余字段展示默认值
    如果 n：使用默认 lanhuMcp 配置
    ```

    **⑤ 最终确认写入（D-10）**
    ```
    ### ⑤ 确认写入

    以下是将要写入的配置摘要：

    **基础信息**
    - 项目标识：{project.name}
    - 显示名：{displayName}
    - 用例根目录：{casesRoot}

    **模块配置（{moduleCount} 个）**
    {模块列表}

    **源码仓库**
    {repos 摘要或「未配置」}

    **集成工具**
    {lanhuMcp 摘要}

    确认写入吗？(y/n)
    ```

    不展示完整 JSON（D-10），只展示纯文字分组摘要。

    ### 0.5 写入文件

    用户确认后：

    1. **构建 config JSON** — 将所有收集到的字段组装为完整 config 对象（脚本端调用 buildConfigObject）
    2. **写入 config.json：**
       ```bash
       node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command write --config-json '{完整JSON}' --root-dir .
       ```

    3. **生成 CLAUDE.md** — 询问用户是否生成/更新 CLAUDE.md（re-init 时 D-16 要求询问）：
       ```bash
       node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command render-template \
         --template-path .claude/skills/using-qa-flow/templates/CLAUDE.md.template \
         --replacements '{"{{PROJECT_NAME}}": "{displayName}", "{{MODULE_KEY_EXAMPLE}}": "{firstModuleKey}", "{{CASES_ROOT}}": "{casesRoot}"}'
       ```
       然后将渲染结果作为 --claude-md 参数传给 write 命令，或者分两步：先 render 获取内容，再 write。

    4. **完成提示：**
       ```
       ✅ 项目配置完成！
       - config.json 已写入 .claude/config.json
       - CLAUDE.md 已写入项目根目录（如已选择生成）

       是否继续执行环境初始化（Step 1-5）？
       ```
    ```

    **Change 4: Update existing Step 1-5 section title** — change from `## 环境初始化（5 步流程）` to:
    ```markdown
    ## 环境初始化（Step 1-5）
    ```
    And add a note at the top:
    ```markdown
    > 前提：Step 0（项目配置向导）已完成。如未完成，请先执行 `/using-qa-flow init`。
    ```
    Keep all existing Step 1-5 content unchanged.

    **CRITICAL: Preserve all non-init routing logic exactly as-is** (功能 1-5 routing, 快速示例, etc.)
  </action>
  <verify>
    <automated>grep -c 'Step 0' .claude/skills/using-qa-flow/SKILL.md && grep -c 'init-wizard.mjs' .claude/skills/using-qa-flow/SKILL.md && grep -c '功能分组' .claude/skills/using-qa-flow/SKILL.md && grep 'Step 1' .claude/skills/using-qa-flow/SKILL.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `## Step 0: 项目配置向导` section
    - SKILL.md contains `### 0.1 扫描项目结构` subsection
    - SKILL.md contains `### 0.2 展示推断结果` subsection
    - SKILL.md contains `### 0.3 历史文件解析` subsection
    - SKILL.md contains `### 0.4 功能分组问答` subsection
    - SKILL.md contains `### 0.5 写入文件` subsection
    - SKILL.md contains `init-wizard.mjs --command scan` (scan call)
    - SKILL.md contains `init-wizard.mjs --command parse-file` (parse call)
    - SKILL.md contains `init-wizard.mjs --command write` (write call)
    - SKILL.md contains `init-wizard.mjs --command render-template` (template render call)
    - SKILL.md contains `init-wizard.mjs --command load-existing` or `signals.existingConfig` (re-init detection)
    - SKILL.md contains `完整重新配置` and `只更新部分配置` (D-14 re-init intent question)
    - SKILL.md contains `还有其他历史文件要导入吗` (D-05 follow-up prompt)
    - SKILL.md contains `确认写入吗` (D-10 confirmation prompt)
    - SKILL.md contains `是否同时更新 CLAUDE.md` or `是否生成` (D-16 CLAUDE.md update question)
    - SKILL.md still contains `### Step 1：Python 环境` (original Step 1 preserved)
    - SKILL.md still contains `### Step 5：验证并输出状态汇总` (original Step 5 preserved)
    - SKILL.md still contains all routing blocks for features 1-5 (unchanged)
    - SKILL.md still contains `## 快速示例` section (preserved)
  </acceptance_criteria>
  <done>SKILL.md has complete Step 0 config wizard with all 5 sub-steps, references all init-wizard.mjs sub-commands, implements D-01 through D-16, preserves all existing Step 1-5 and routing logic</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end validation — full test suite green + SKILL.md structure check</name>
  <files><!-- validation-only: no files created; runs test suite and CLI smoke tests against artifacts from Plans 01-03 --></files>
  <read_first>
    .claude/skills/using-qa-flow/SKILL.md
    .claude/tests/test-init-wizard.mjs
    .claude/tests/run-all.mjs
  </read_first>
  <action>
    Run the complete test suite to ensure nothing is broken:

    1. **Run init-wizard tests:**
       ```bash
       node .claude/tests/test-init-wizard.mjs
       ```
       Ensure all tests pass (exit 0, no ❌).

    2. **Run full test suite:**
       ```bash
       node .claude/tests/run-all.mjs
       ```
       Note: Some pre-existing test failures may exist (Phase 2 noted 7 pre-existing failures). The init-wizard tests specifically must all pass. If run-all.mjs shows failures, verify they are pre-existing (not introduced by Phase 3 work) by checking the failing test file names against known pre-existing failures.

    3. **SKILL.md structure validation:**
       - Verify Step 0 section exists and has all 5 sub-steps (0.1 through 0.5)
       - Verify Step 1-5 section is preserved intact
       - Verify all init-wizard.mjs sub-command references are present (scan, parse-file, write, load-existing, render-template)
       - Verify routing logic routes init/初始化/0 to Step 0 first

    4. **Init wizard CLI smoke test:**
       ```bash
       # Scan current project
       node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); print('modules:', len(d['modules']), 'signals:', list(d['signals'].keys()))"

       # Load existing config
       node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command load-existing 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); print('project:', d['project']['name'])"
       ```

    5. **Template presence check:**
       ```bash
       test -f .claude/skills/using-qa-flow/templates/CLAUDE.md.template && echo "✅ Template exists" || echo "❌ Template missing"
       ```

    If any init-wizard test fails, fix the issue in the relevant file before proceeding. Do NOT modify pre-existing tests.
  </action>
  <verify>
    <automated>node .claude/tests/test-init-wizard.mjs && echo "---ALL-INIT-TESTS-PASS---"</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/tests/test-init-wizard.mjs` exits with code 0
    - `node .claude/tests/test-init-wizard.mjs` output contains no `❌`
    - `node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan` outputs valid JSON
    - `node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command load-existing` outputs valid JSON
    - .claude/skills/using-qa-flow/templates/CLAUDE.md.template file exists
    - SKILL.md contains both `Step 0` and `Step 1` sections
  </acceptance_criteria>
  <done>Full test suite validates init wizard functionality, SKILL.md has complete Step 0 orchestration, all init-wizard CLI commands produce valid output, no regressions introduced</done>
</task>

</tasks>

<verification>
1. `grep -c 'Step 0' .claude/skills/using-qa-flow/SKILL.md` returns >= 1
2. `grep -c 'init-wizard.mjs' .claude/skills/using-qa-flow/SKILL.md` returns >= 4 (scan, parse-file, write, render-template)
3. `node .claude/tests/test-init-wizard.mjs` exits 0
4. `node .claude/skills/using-qa-flow/scripts/init-wizard.mjs --command scan` outputs valid JSON
5. SKILL.md preserves Step 1 through Step 5 intact
</verification>

<success_criteria>
- SKILL.md has Step 0 with 5 sub-steps (0.1-0.5) covering the complete init wizard dialog flow
- All D-01 through D-16 locked decisions are reflected in Step 0 orchestration
- Step 0 calls all 5 init-wizard.mjs sub-commands at appropriate points
- Existing Step 1-5, routing logic, and 快速示例 are fully preserved
- All init-wizard unit tests pass
- CLI smoke tests produce valid JSON output
</success_criteria>

<output>
After completion, create `.planning/phases/03-init-wizard/03-03-SUMMARY.md`
</output>
