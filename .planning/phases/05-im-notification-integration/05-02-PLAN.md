---
phase: 05-im-notification-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .claude/skills/test-case-generator/prompts/step-notify.md
  - .claude/skills/code-analysis-report/SKILL.md
  - .claude/skills/archive-converter/SKILL.md
autonomous: true
requirements: [NOTF-06]

must_haves:
  truths:
    - "test-case-generator step-notify calls notify.mjs with --event case-generated after successful completion"
    - "code-analysis-report calls notify.mjs with --event bug-report after Step 9 report output"
    - "archive-converter calls notify.mjs with --event archive-converted in section 八 after completion stats"
    - "All three Skills pass --dry-run suggestion in their notify instructions for debugging"
  artifacts:
    - path: ".claude/skills/test-case-generator/prompts/step-notify.md"
      provides: "Notify.mjs invocation in test-case-generator flow"
      contains: "node .claude/shared/scripts/notify.mjs --event case-generated"
    - path: ".claude/skills/code-analysis-report/SKILL.md"
      provides: "Notify.mjs invocation in code-analysis-report flow"
      contains: "node .claude/shared/scripts/notify.mjs --event bug-report"
    - path: ".claude/skills/archive-converter/SKILL.md"
      provides: "Notify.mjs invocation in archive-converter flow"
      contains: "node .claude/shared/scripts/notify.mjs --event archive-converted"
  key_links:
    - from: ".claude/skills/test-case-generator/prompts/step-notify.md"
      to: ".claude/shared/scripts/notify.mjs"
      via: "node CLI call with --event case-generated"
      pattern: "node .claude/shared/scripts/notify\\.mjs --event case-generated"
    - from: ".claude/skills/code-analysis-report/SKILL.md"
      to: ".claude/shared/scripts/notify.mjs"
      via: "node CLI call with --event bug-report"
      pattern: "node .claude/shared/scripts/notify\\.mjs --event bug-report"
    - from: ".claude/skills/archive-converter/SKILL.md"
      to: ".claude/shared/scripts/notify.mjs"
      via: "node CLI call with --event archive-converted"
      pattern: "node .claude/shared/scripts/notify\\.mjs --event archive-converted"
---

<objective>
Integrate notify.mjs into three Skill workflows so that QA events automatically trigger notifications to all configured IM channels.

Purpose: Delivers NOTF-06 — the final wiring that connects the notification module (Plan 01) to actual QA workflow completion events.
Output: Updated step-notify.md, code-analysis-report/SKILL.md, and archive-converter/SKILL.md with concrete notify.mjs invocations.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-im-notification-integration/05-CONTEXT.md
@.planning/phases/05-im-notification-integration/05-01-SUMMARY.md

@.claude/shared/scripts/notify.mjs
@.claude/skills/test-case-generator/prompts/step-notify.md
@.claude/skills/code-analysis-report/SKILL.md
@.claude/skills/archive-converter/SKILL.md

<interfaces>
<!-- notify.mjs CLI interface from Plan 01 -->
CLI invocation pattern:
```bash
node .claude/shared/scripts/notify.mjs --event <event-type> --data '<json>' [--dry-run]
```

Supported event types:
- `case-generated` — data: { count, file, duration }
- `bug-report` — data: { reportFile, summary }
- `workflow-failed` — data: { step, reason }
- `archive-converted` — data: { fileCount, caseCount }

From step-notify.md (current content, lines 36-46):
```markdown
## 完成通知
输出生成摘要：
- XMind 路径、模式（新建/追加）、用例统计
- 归档 MD 路径
- 主验收快捷链接：latest-output.xmind
- 上游 PRD 快捷链接：latest-prd-enhanced.md
- Reviewer 质量评分
- 提示用户可执行后续操作
```

From code-analysis-report/SKILL.md (Step 9, lines 490-510):
```markdown
### Step 9：完成报告（固定模版）
任务完成后，在终端输出以下报告：
📋 Hotfix 用例生成完成
```

From archive-converter/SKILL.md (section 八, lines 155-175):
```markdown
## 八、完成通知
转化完成后，向用户输出：
✅ 历史用例归档转化完成
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notify.mjs calls to test-case-generator step-notify.md</name>
  <files>.claude/skills/test-case-generator/prompts/step-notify.md</files>
  <read_first>
    - .claude/skills/test-case-generator/prompts/step-notify.md (full current content — must preserve all existing behavior)
    - .claude/skills/test-case-generator/SKILL.md (understand step 11 context: notify is the terminal step after archive)
    - .claude/shared/scripts/notify.mjs (verify CLI interface: --event, --data, --dry-run flags)
  </read_first>
  <action>
Edit `.claude/skills/test-case-generator/prompts/step-notify.md` to add a notify.mjs call in the "完成通知" section (around line 36). The existing terminal output behavior MUST be preserved — the notify.mjs call is ADDED, not replacing the console summary.

**Insert after the existing "完成通知" section content (after line 46, before "错误处理"):**

Add a new subsection `## IM 通知` with the following exact content:

```markdown
## IM 通知

在终端输出完成通知后，调用通知模块向已配置的 IM 渠道发送通知：

\`\`\`bash
node .claude/shared/scripts/notify.mjs \
  --event case-generated \
  --data '{"count":<用例总数>,"file":"<xmind输出文件路径>","duration":"<耗时>"}'
\`\`\`

参数说明：
- `count`：生成的用例总数（数字）
- `file`：XMind 输出文件的相对路径（如 `latest-output.xmind`）
- `duration`：工作流总耗时（如 `3m21s`，可从 execution_log 首尾时间差计算）

> 💡 调试提示：添加 `--dry-run` 参数可打印完整 payload 而不发送网络请求。
> ⚠️ 若 notify.mjs 执行失败（如 .env 未配置），仅 console.error 记录，不阻断流程。流程的用例文件已经生成完毕。
```

Also update the "「确认通过」" section (line 13-15) to add the notify call AFTER step 3 ("向用户输出最终完成通知"):

Insert as step 4:
```
4. 调用 IM 通知（见「IM 通知」章节）
```

And update the "「已修改，请同步」" section (line 19-24) to add after step 4 ("输出完成通知"):

Insert as step 5:
```
5. 调用 IM 通知（见「IM 通知」章节）
```
  </action>
  <verify>
    <automated>grep -c "notify.mjs" .claude/skills/test-case-generator/prompts/step-notify.md</automated>
  </verify>
  <acceptance_criteria>
    - step-notify.md contains `node .claude/shared/scripts/notify.mjs --event case-generated`
    - step-notify.md contains `--data '{"count":`
    - step-notify.md contains `--dry-run`
    - step-notify.md still contains `## 完成通知` (original section preserved)
    - step-notify.md still contains `## 错误处理` (original section preserved)
    - step-notify.md contains `## IM 通知` (new section added)
    - step-notify.md contains `调用 IM 通知` at least twice (in 「确认通过」 and 「已修改，请同步」 sections)
    - `grep -c "notify.mjs" .claude/skills/test-case-generator/prompts/step-notify.md` returns at least 1
  </acceptance_criteria>
  <done>
    step-notify.md has IM notification call after terminal output in both the 「确认通过」 and 「已修改，请同步」 flows, with --dry-run hint and failure-tolerance note. All existing step behavior preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add notify.mjs calls to code-analysis-report and archive-converter SKILL.md</name>
  <files>.claude/skills/code-analysis-report/SKILL.md, .claude/skills/archive-converter/SKILL.md</files>
  <read_first>
    - .claude/skills/code-analysis-report/SKILL.md (full file — find Step 9 completion section at ~line 490)
    - .claude/skills/archive-converter/SKILL.md (full file — find section 八 completion at ~line 155)
    - .claude/shared/scripts/notify.mjs (verify CLI interface matches the calls being added)
  </read_first>
  <action>
**A) Edit `.claude/skills/code-analysis-report/SKILL.md`:**

Find the Step 9 completion section (around line 490, after `### Step 9：完成报告（固定模版）`). After the existing completion report template block (after the closing ``` of the report template), add:

```markdown

### Step 10：IM 通知（自动）

在终端输出完成报告后，调用通知模块：

\`\`\`bash
node .claude/shared/scripts/notify.mjs \
  --event bug-report \
  --data '{"reportFile":"<报告文件路径>","summary":"<报告摘要，如：发现 N 个 P1 Bug>"}'
\`\`\`

参数说明：
- `reportFile`：生成的 HTML 报告文件相对路径（如 `reports/bug-analysis-xxx.html`）
- `summary`：简短摘要，包含 Bug 数量和严重级别

> ⚠️ 若 notify.mjs 执行失败，仅 console.error 记录，不影响已生成的报告文件。
> 💡 调试：添加 `--dry-run` 查看发送内容。
```

Also update the step table in SKILL.md (if one exists near the top) to add Step 10.

**B) Edit `.claude/skills/archive-converter/SKILL.md`:**

Find section 八 "完成通知" (around line 155). After the existing completion output template (after the error files template block), add:

```markdown

### IM 通知

在终端输出转化统计后，调用通知模块：

\`\`\`bash
node .claude/shared/scripts/notify.mjs \
  --event archive-converted \
  --data '{"fileCount":<成功转化文件数>,"caseCount":<总用例数>}'
\`\`\`

参数说明：
- `fileCount`：成功转化的文件数量（数字）
- `caseCount`：总用例条数（数字）

> ⚠️ 若 notify.mjs 执行失败，仅 console.error 记录，不影响已转化的归档文件。
> 💡 调试：添加 `--dry-run` 查看发送内容。
```
  </action>
  <verify>
    <automated>grep -l "notify.mjs" .claude/skills/code-analysis-report/SKILL.md .claude/skills/archive-converter/SKILL.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - code-analysis-report/SKILL.md contains `node .claude/shared/scripts/notify.mjs --event bug-report`
    - code-analysis-report/SKILL.md contains `--data '{"reportFile":`
    - code-analysis-report/SKILL.md contains `Step 10` or `### Step 10`
    - code-analysis-report/SKILL.md contains `--dry-run`
    - archive-converter/SKILL.md contains `node .claude/shared/scripts/notify.mjs --event archive-converted`
    - archive-converter/SKILL.md contains `--data '{"fileCount":`
    - archive-converter/SKILL.md contains `### IM 通知`
    - archive-converter/SKILL.md contains `--dry-run`
    - `grep -l "notify.mjs" .claude/skills/code-analysis-report/SKILL.md .claude/skills/archive-converter/SKILL.md | wc -l` returns 2
  </acceptance_criteria>
  <done>
    Both code-analysis-report and archive-converter SKILL.md files have notify.mjs calls with correct event types, data schemas, dry-run hints, and failure-tolerance notes. Existing completion output preserved in both files.
  </done>
</task>

</tasks>

<verification>
1. `grep "notify.mjs" .claude/skills/test-case-generator/prompts/step-notify.md` — contains case-generated call
2. `grep "notify.mjs" .claude/skills/code-analysis-report/SKILL.md` — contains bug-report call
3. `grep "notify.mjs" .claude/skills/archive-converter/SKILL.md` — contains archive-converted call
4. `grep -r "notify.mjs" .claude/skills/ | wc -l` — at least 3 skill files reference notify.mjs
5. All three files still contain their original completion output sections (no removed content)
</verification>

<success_criteria>
- test-case-generator step-notify.md calls `notify.mjs --event case-generated` with count, file, duration data
- code-analysis-report SKILL.md calls `notify.mjs --event bug-report` with reportFile, summary data
- archive-converter SKILL.md calls `notify.mjs --event archive-converted` with fileCount, caseCount data
- All three have --dry-run debugging hints
- All three have failure-tolerance notes (notify failure doesn't block workflow)
- No existing SKILL.md content was removed — notify calls are additive
</success_criteria>

<output>
After completion, create `.planning/phases/05-im-notification-integration/05-02-SUMMARY.md`
</output>
