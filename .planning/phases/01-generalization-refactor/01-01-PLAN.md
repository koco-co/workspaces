---
phase: 01-generalization-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/config.json
  - .claude/shared/scripts/load-config.mjs
  - .claude/tests/test-load-config.mjs
  - .claude/shared/scripts/load-config.test.mjs
  - .planning/tests/rules-generalization.test.mjs
  - .planning/tests/prompts-generalization.test.mjs
  - .planning/tests/branch-migration.test.mjs
autonomous: true
requirements:
  - GEN-01
  - GEN-02

must_haves:
  truths:
    - "loadConfig() returns a config object with no DTStack-specific field names (no zentaoId, repoBranchMapping, dataAssetsVersionMap)"
    - "loadConfig() throws a descriptive error naming the missing field when project.name or modules is absent"
    - "loadConfig() succeeds with minimal blank config (project.name + modules only)"
    - "getDtstackModules() is removed from load-config.mjs and replaced with getModuleKeys()"
    - "getRepoBranchMappingPath() reads from config.branchMapping instead of config.repoBranchMapping"
  artifacts:
    - path: ".claude/config.json"
      provides: "Generalized config schema with no DTStack module names"
      contains: "trackerId"
    - path: ".claude/shared/scripts/load-config.mjs"
      provides: "Config loading with validation and generalized exports"
      exports: ["loadConfig", "getModuleMap", "getModuleKeys", "getWorkspaceRoot", "resolveWorkspacePath", "getBranchMappingPath", "getIntegrationConfig"]
    - path: ".claude/shared/scripts/load-config.test.mjs"
      provides: "Unit tests for GEN-01 and GEN-02"
      min_lines: 50
    - path: ".planning/tests/rules-generalization.test.mjs"
      provides: "Smoke test grepping rules files for hardcoded DTStack terms"
      min_lines: 20
    - path: ".planning/tests/prompts-generalization.test.mjs"
      provides: "Smoke test grepping prompts for Doris/Hive/SparkThrift outside conditional blocks"
      min_lines: 20
    - path: ".planning/tests/branch-migration.test.mjs"
      provides: "Smoke test verifying DTStack data dirs absent from main"
      min_lines: 15
  key_links:
    - from: ".claude/shared/scripts/load-config.mjs"
      to: ".claude/config.json"
      via: "readFileSync + JSON.parse"
      pattern: "readFileSync.*config\\.json"
    - from: ".claude/shared/scripts/load-config.mjs"
      to: "caller scripts"
      via: "exported getModuleKeys() replaces getDtstackModules()"
      pattern: "export function getModuleKeys"
---

<objective>
Redesign config.json schema and add loadConfig() validation, plus create all Wave 0 test scaffolds for Phase 1.

Purpose: GEN-01 requires removing all DTStack-specific field names from config. GEN-02 requires loadConfig() to throw descriptive errors for missing fields. All subsequent plans depend on these test files existing (Nyquist compliance).
Output: Generalized config.json, validated loadConfig(), and 4 test files covering all Phase 1 requirements.
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-generalization-refactor/01-CONTEXT.md
@.planning/phases/01-generalization-refactor/01-RESEARCH.md
@.planning/phases/01-generalization-refactor/01-VALIDATION.md

<interfaces>
<!-- Current load-config.mjs exports that callers depend on -->
From .claude/shared/scripts/load-config.mjs:
```javascript
export function loadConfig()          // returns cached config object
export function getModuleMap()        // returns { zh: moduleDir, key: moduleDir } map
export function getDtstackModules()   // WILL BE REMOVED — returns { zh: string[], en: string[] }
export function getWorkspaceRoot()    // returns absolute workspace root path
export function resolveWorkspacePath(relativePath) // resolves path relative to workspace root
export function getRepoBranchMappingPath()         // WILL BE RENAMED to getBranchMappingPath()
export function getIntegrationConfig(name)         // returns integration config or null
```

Call sites for getDtstackModules() (must all be updated in Plan 02):
- .claude/skills/archive-converter/scripts/json-to-archive-md.mjs (line 13, 445)
- .claude/skills/archive-converter/scripts/convert-history-cases.mjs (line 44, 574)
- .claude/skills/xmind-converter/scripts/json-to-xmind.mjs (line 37, 41)
- .claude/tests/test-load-config.mjs (line 10, 56-57)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffolds for all Phase 1 requirements</name>
  <files>
    .claude/shared/scripts/load-config.test.mjs,
    .planning/tests/rules-generalization.test.mjs,
    .planning/tests/prompts-generalization.test.mjs,
    .planning/tests/branch-migration.test.mjs
  </files>
  <read_first>
    .claude/shared/scripts/load-config.mjs,
    .claude/tests/test-load-config.mjs,
    .claude/shared/scripts/output-naming-contracts.mjs,
    .planning/phases/01-generalization-refactor/01-VALIDATION.md,
    .planning/phases/01-generalization-refactor/01-RESEARCH.md
  </read_first>
  <behavior>
    load-config.test.mjs (node:test framework):
    - Test: loadConfig() with generalized config returns object with "project.name" field
    - Test: loadConfig() with generalized config has no "zentaoId" in any module
    - Test: loadConfig() with generalized config has no "repoBranchMapping" top-level key
    - Test: loadConfig() with generalized config has no "dataAssetsVersionMap" top-level key
    - Test: loadConfig() with generalized config has "trackerId" instead of "zentaoId" in modules that had it
    - Test: loadConfig() with generalized config has "branchMapping" instead of "repoBranchMapping"
    - Test: loadConfig() throws error containing "project.name" when project.name missing
    - Test: loadConfig() throws error containing "modules" when modules missing
    - Test: loadConfig() succeeds with minimal config { project: { name: "test" }, modules: {} }
    - Test: getModuleKeys() returns array of module key strings
    - Test: getDtstackModules is NOT exported (typeof === "undefined")
    - Test: getBranchMappingPath() reads config.branchMapping (not config.repoBranchMapping)

    rules-generalization.test.mjs (smoke test):
    - Test: No occurrence of literal "DTStack" (case-sensitive) outside conditional guard blocks in .claude/rules/*.md
    - Test: No occurrence of "Doris" or "Hive" or "SparkThrift" outside conditional guard blocks in .claude/rules/*.md

    prompts-generalization.test.mjs (smoke test):
    - Test: No occurrence of "Doris" or "Hive" or "SparkThrift" outside conditional blocks in .claude/skills/test-case-generator/prompts/*.md

    branch-migration.test.mjs (smoke test):
    - Test: cases/archive/data-assets/ directory does not exist on current branch (will pass after GEN-06)
    - Test: cases/archive/batch-works/ directory does not exist on current branch
    - Test: config/repo-branch-mapping.yaml does not contain DTStack-specific repo profiles
  </behavior>
  <action>
    Create `.planning/tests/` directory first.

    Create `.claude/shared/scripts/load-config.test.mjs` using `node:test` framework (`import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'`). This test file must:
    1. Import from a mock config mechanism (use `node:test` `mock` or read a test fixture JSON file from `.claude/shared/scripts/test-fixtures/config-generalized.json`). The fixture should be the generalized config.json content (see Task 2 for exact content).
    2. For validation tests, import `loadConfig` after temporarily replacing the CONFIG_PATH (or use a wrapper that accepts a path argument). If the module uses a hardcoded path, create a helper `loadConfigFromPath(path)` that is also exported for testing, and have `loadConfig()` call it with the default path.
    3. Write all behavior tests listed above. Tests for GEN-01 (field names) and GEN-02 (validation errors) should initially FAIL (RED phase) since the implementation has not changed yet.

    Create `.planning/tests/rules-generalization.test.mjs`:
    1. Read all `.md` files in `.claude/rules/` directory using `fs.readdirSync` + `fs.readFileSync`
    2. For each file, extract text outside conditional guard blocks (lines between `> 以下规则仅` ... next `---` or `##` heading)
    3. Assert no matches for `/\bDTStack\b/`, `/\bDoris\b/`, `/\bHive\b/`, `/\bSparkThrift\b/` in the non-conditional text
    4. These tests should initially FAIL since rules have not been generalized yet.

    Create `.planning/tests/prompts-generalization.test.mjs`:
    1. Read all `.md` files in `.claude/skills/test-case-generator/prompts/`
    2. Extract text outside conditional blocks (same pattern as rules test)
    3. Assert no matches for `/\bDoris\b/`, `/\bHive\b/`, `/\bSparkThrift\b/`, `/\bdt-center-assets\b/`, `/\bdt-center-metadata\b/` in non-conditional text
    4. These should initially FAIL.

    Create `.planning/tests/branch-migration.test.mjs`:
    1. Use `fs.existsSync` to check that `cases/archive/data-assets/`, `cases/archive/batch-works/`, `cases/archive/data-query/`, `cases/archive/variable-center/`, `cases/archive/public-service/`, `cases/xmind/data-assets/`, `cases/xmind/batch-works/`, `cases/xmind/data-query/`, `cases/xmind/variable-center/`, `cases/history/` do NOT exist
    2. Read `config/repo-branch-mapping.yaml` if it exists and assert it does NOT contain "dt-insight" or "DTStack" strings
    3. These should initially FAIL since migration has not happened.

    All test files use `import { describe, it } from 'node:test'` and `import assert from 'node:assert/strict'`.
  </action>
  <verify>
    <automated>node --test .claude/shared/scripts/load-config.test.mjs 2>&1 | head -30; node --test .planning/tests/rules-generalization.test.mjs 2>&1 | head -20; node --test .planning/tests/prompts-generalization.test.mjs 2>&1 | head -20; node --test .planning/tests/branch-migration.test.mjs 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - .claude/shared/scripts/load-config.test.mjs exists and contains `import { describe, it } from 'node:test'`
    - .claude/shared/scripts/load-config.test.mjs contains `assert` calls for "project.name", "zentaoId", "repoBranchMapping", "dataAssetsVersionMap", "trackerId", "branchMapping", "getModuleKeys", "getBranchMappingPath"
    - .planning/tests/rules-generalization.test.mjs exists and contains grep patterns for "DTStack", "Doris", "Hive", "SparkThrift"
    - .planning/tests/prompts-generalization.test.mjs exists and contains grep patterns for "Doris", "Hive", "SparkThrift"
    - .planning/tests/branch-migration.test.mjs exists and contains `existsSync` checks for "cases/archive/data-assets", "cases/archive/batch-works"
    - `node --test .claude/shared/scripts/load-config.test.mjs` runs without syntax errors (tests may fail = RED phase, that's expected)
    - `node --test .planning/tests/rules-generalization.test.mjs` runs without syntax errors
  </acceptance_criteria>
  <done>All 4 test files exist, import node:test correctly, run without syntax errors (failures expected at RED phase), and cover all GEN-01 through GEN-06 validation behaviors from the VALIDATION.md matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Redesign config.json schema and add loadConfig() validation</name>
  <files>
    .claude/config.json,
    .claude/shared/scripts/load-config.mjs
  </files>
  <read_first>
    .claude/config.json,
    .claude/shared/scripts/load-config.mjs,
    .claude/shared/scripts/load-config.test.mjs,
    .claude/tests/test-load-config.mjs,
    .planning/phases/01-generalization-refactor/01-RESEARCH.md
  </read_first>
  <behavior>
    - loadConfig() with blank minimal config { project: { name: "x" }, modules: {} } succeeds
    - loadConfig() without project.name throws "config.json missing required field: \"project.name\""
    - loadConfig() without modules throws "config.json missing required field: \"modules\""
    - getModuleKeys() returns Object.keys(config.modules)
    - getBranchMappingPath() returns resolved path from config.branchMapping (or null if not set)
    - getRepoBranchMappingPath still exists as deprecated alias for getBranchMappingPath during transition
  </behavior>
  <action>
    **Step 1: Rewrite .claude/config.json** to the generalized schema. The exact new content:

    ```json
    {
      "project": {
        "name": "qa-flow",
        "displayName": "qa-flow"
      },
      "modules": {},
      "repos": {},
      "stackTrace": {},
      "branchMapping": null,
      "trash": {
        "dir": ".trash/",
        "retentionDays": 30
      },
      "assets": {
        "images": "assets/images/"
      },
      "reports": {
        "bugs": "reports/bugs/",
        "conflicts": "reports/conflicts/"
      },
      "integrations": {
        "lanhuMcp": {
          "runtimePath": "tools/lanhu-mcp/",
          "envFile": "tools/lanhu-mcp/.env",
          "setupScript": "tools/lanhu-mcp/setup-env.sh",
          "quickstartScript": "tools/lanhu-mcp/quickstart.sh",
          "entryScript": "tools/lanhu-mcp/lanhu_mcp_server.py",
          "serverHost": "127.0.0.1",
          "serverPort": 8000,
          "serverUrl": "http://127.0.0.1:8000",
          "mcpPath": "/mcp",
          "logFile": ".claude/tmp/lanhu-mcp.log",
          "cookieRefreshScript": ".claude/skills/using-qa-flow/scripts/refresh-lanhu-cookie.py"
        }
      },
      "shortcuts": {
        "latestXmind": "latest-output.xmind",
        "latestEnhancedPrd": "latest-prd-enhanced.md",
        "latestBugReport": "latest-bug-report.html",
        "latestConflictReport": "latest-conflict-report.html"
      }
    }
    ```

    Key changes from current:
    - All 6 DTStack modules removed from `modules` (empty object — users add their own)
    - All 18 repo paths removed from `repos` (empty object)
    - All 8 Java package mappings removed from `stackTrace` (empty object)
    - `zentaoId` → removed (users add `trackerId` to their modules)
    - `repoBranchMapping` → `branchMapping` (null default)
    - `dataAssetsVersionMap` → removed entirely
    - `type` field removed from module definitions

    **Step 2: Rewrite .claude/shared/scripts/load-config.mjs:**

    1. Add `loadConfigFromPath(configPath)` function that accepts an explicit path (for testability). `loadConfig()` calls `loadConfigFromPath(CONFIG_PATH)`.
    2. Add `assertRequiredFields(config)` that checks:
       - `config.project` exists and is an object
       - `config.project.name` exists and is a non-empty string
       - `config.modules` exists and is an object
       For each missing field, throw: `new Error('config.json missing required field: "${fieldPath}". Please check ${configPath}.')`
       Collect ALL missing fields and throw one error listing them all (user-friendly: fix everything in one pass).
    3. Remove `getDtstackModules()` entirely.
    4. Add `getModuleKeys()`: `return Object.keys(loadConfig().modules ?? {});`
    5. Rename `getRepoBranchMappingPath()` to `getBranchMappingPath()`. Change it to read `config.branchMapping` instead of `config.repoBranchMapping`. Return `null` if `config.branchMapping` is null/undefined (no fallback to hardcoded path). Keep `getRepoBranchMappingPath` as a deprecated alias that calls `getBranchMappingPath()`.
    6. Keep `loadConfig()`, `getModuleMap()`, `getWorkspaceRoot()`, `resolveWorkspacePath()`, `getIntegrationConfig()` — update `getModuleMap()` to not reference `type` field.
    7. Export `loadConfigFromPath` for testing.
    8. Add a `resetConfigCache()` exported function that sets `_config = null` (for test isolation).

    **Step 3: Update .claude/tests/test-load-config.mjs** to remove references to `getDtstackModules`, `dataAssetsVersionMap`, and `repoBranchMapping`. Update assertions to test the new schema. This is the legacy test file; update it to not break.
  </action>
  <verify>
    <automated>node --test .claude/shared/scripts/load-config.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .claude/config.json contains `"branchMapping": null` and does NOT contain `"repoBranchMapping"`
    - .claude/config.json contains `"modules": {}` and does NOT contain `"zentaoId"` or `"dataAssetsVersionMap"`
    - .claude/config.json does NOT contain `"data-assets"`, `"batch-works"`, `"dt-center-assets"`, `"com.dtstack"`
    - .claude/shared/scripts/load-config.mjs contains `export function getModuleKeys()`
    - .claude/shared/scripts/load-config.mjs contains `export function getBranchMappingPath()`
    - .claude/shared/scripts/load-config.mjs contains `export function loadConfigFromPath(`
    - .claude/shared/scripts/load-config.mjs contains `export function resetConfigCache()`
    - .claude/shared/scripts/load-config.mjs does NOT contain `export function getDtstackModules()`
    - .claude/shared/scripts/load-config.mjs contains `config.branchMapping` (not `config.repoBranchMapping` except in deprecated alias)
    - .claude/shared/scripts/load-config.mjs contains `assertRequiredFields`
    - `node --test .claude/shared/scripts/load-config.test.mjs` exits with code 0 (all tests pass = GREEN phase)
  </acceptance_criteria>
  <done>config.json has zero DTStack-specific content. loadConfig() validates required fields and throws descriptive errors. getDtstackModules() removed. getModuleKeys() and getBranchMappingPath() exported. All load-config.test.mjs tests pass GREEN.</done>
</task>

</tasks>

<verification>
- `node --test .claude/shared/scripts/load-config.test.mjs` — all unit tests pass
- `grep -c "zentaoId\|repoBranchMapping\|dataAssetsVersionMap\|getDtstackModules" .claude/config.json .claude/shared/scripts/load-config.mjs` — returns 0 for config.json, near-zero for load-config.mjs (only deprecated alias)
- `node -e "import('./.claude/shared/scripts/load-config.mjs').then(m => { m.resetConfigCache(); console.log(m.getModuleKeys()); })"` — prints `[]`
</verification>

<success_criteria>
- config.json contains zero DTStack module names, repo paths, or Java package mappings
- loadConfig() throws descriptive error when required fields missing
- loadConfig() succeeds with minimal blank config
- getDtstackModules() removed, getModuleKeys() available
- All Wave 0 test scaffolds exist and run without syntax errors
- load-config.test.mjs passes GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-generalization-refactor/01-01-SUMMARY.md`
</output>
