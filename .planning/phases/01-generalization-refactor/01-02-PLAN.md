---
phase: 01-generalization-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .claude/shared/scripts/output-naming-contracts.mjs
  - .claude/shared/scripts/output-naming-contracts.test.mjs
  - .claude/shared/scripts/unify-directory-structure.mjs
  - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs
  - .claude/skills/archive-converter/scripts/convert-history-cases.mjs
  - .claude/skills/xmind-converter/scripts/json-to-xmind.mjs
  - .claude/skills/xmind-converter/scripts/patch-xmind-roots.mjs
  - .claude/skills/archive-converter/scripts/convert-data-assets-v2.mjs
autonomous: true
requirements:
  - GEN-05

must_haves:
  truths:
    - "deriveArchiveBaseName() works without DTStack-specific meta fields (source_standard, archive_file_name)"
    - "getDtstackPreferredArchiveBaseName is renamed to getPreferredArchiveBaseName and works generically"
    - "All scripts importing getDtstackModules are updated to use getModuleKeys or config.modules directly"
    - "zentaoId references in scripts are replaced with trackerId"
    - "dataAssetsVersionMap references in scripts are removed or made config-driven"
    - "versionMap field absence in config is handled gracefully (no crash)"
  artifacts:
    - path: ".claude/shared/scripts/output-naming-contracts.mjs"
      provides: "Generalized archive naming without DTStack-specific function names"
      exports: ["deriveArchiveBaseName", "getPreferredArchiveBaseName"]
    - path: ".claude/shared/scripts/output-naming-contracts.test.mjs"
      provides: "Unit tests for GEN-05 intermediate JSON generalization"
      min_lines: 40
  key_links:
    - from: ".claude/skills/archive-converter/scripts/json-to-archive-md.mjs"
      to: ".claude/shared/scripts/load-config.mjs"
      via: "import getModuleKeys (replaces getDtstackModules)"
      pattern: "import.*getModuleKeys.*load-config"
    - from: ".claude/skills/xmind-converter/scripts/json-to-xmind.mjs"
      to: ".claude/shared/scripts/load-config.mjs"
      via: "import getModuleKeys or loadConfig (replaces getDtstackModules)"
      pattern: "import.*load-config"
    - from: ".claude/shared/scripts/unify-directory-structure.mjs"
      to: ".claude/config.json"
      via: "reads config.versionMap with fallback to empty object"
      pattern: "config\\.versionMap.*\\|\\|.*\\{\\}"
---

<objective>
Generalize all shared scripts and skill scripts that reference DTStack-specific functions, field names, or data structures.

Purpose: GEN-05 requires intermediate JSON schema generalization and all script call sites for getDtstackModules(), zentaoId, dataAssetsVersionMap, and getDtstackPreferredArchiveBaseName must be updated to use generic equivalents.
Output: All .mjs scripts reference only generalized config fields and exported functions.
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-generalization-refactor/01-CONTEXT.md
@.planning/phases/01-generalization-refactor/01-RESEARCH.md
@.planning/phases/01-generalization-refactor/01-01-SUMMARY.md

<interfaces>
<!-- New exports from load-config.mjs (from Plan 01) -->
From .claude/shared/scripts/load-config.mjs (after Plan 01):
```javascript
export function loadConfig()               // returns validated config
export function loadConfigFromPath(path)   // for testing
export function resetConfigCache()         // for test isolation
export function getModuleMap()             // { zh: moduleDir, key: moduleDir }
export function getModuleKeys()            // Object.keys(config.modules)
export function getWorkspaceRoot()         // absolute workspace root
export function resolveWorkspacePath(rel)  // resolve relative to workspace
export function getBranchMappingPath()     // config.branchMapping resolved, or null
export function getRepoBranchMappingPath() // deprecated alias for getBranchMappingPath
export function getIntegrationConfig(name) // integration config or null
```

Call sites that need updating (from grep):
- .claude/skills/archive-converter/scripts/json-to-archive-md.mjs:13 — `import { getDtstackModules }`
- .claude/skills/archive-converter/scripts/json-to-archive-md.mjs:445 — `getDtstackModules()`
- .claude/skills/archive-converter/scripts/convert-history-cases.mjs:44 — `import { getModuleMap, getDtstackModules }`
- .claude/skills/archive-converter/scripts/convert-history-cases.mjs:574 — `getDtstackModules()`
- .claude/skills/xmind-converter/scripts/json-to-xmind.mjs:37 — `import { getDtstackModules, loadConfig }`
- .claude/skills/xmind-converter/scripts/json-to-xmind.mjs:41 — `getDtstackModules()`
- .claude/skills/xmind-converter/scripts/json-to-xmind.mjs:66-67 — `mod?.zentaoId`
- .claude/skills/xmind-converter/scripts/patch-xmind-roots.mjs:30,64 — `mod.zentaoId`
- .claude/skills/archive-converter/scripts/convert-data-assets-v2.mjs:347 — `config.dataAssetsVersionMap`
- .claude/shared/scripts/unify-directory-structure.mjs:478-692 — `dataAssetsVersionMap` (7 refs)
- .claude/shared/scripts/output-naming-contracts.mjs:100 — `getDtstackPreferredArchiveBaseName`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize output-naming-contracts.mjs and create its test file</name>
  <files>
    .claude/shared/scripts/output-naming-contracts.mjs,
    .claude/shared/scripts/output-naming-contracts.test.mjs
  </files>
  <read_first>
    .claude/shared/scripts/output-naming-contracts.mjs,
    .claude/shared/scripts/load-config.mjs,
    .planning/phases/01-generalization-refactor/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: deriveArchiveBaseName("input.json", { archive_file_name: "My Feature" }) returns "My-Feature" (no source_standard check)
    - Test: deriveArchiveBaseName("input.json", { requirement_title: "Feature X" }) returns "Feature-X"
    - Test: deriveArchiveBaseName("input.json", {}) falls through to input basename
    - Test: deriveArchiveBaseName("PRD-26-Quality.json", {}) returns "PRD-26-Quality"
    - Test: getPreferredArchiveBaseName exists as exported function name (not getDtstackPreferredArchiveBaseName)
  </behavior>
  <action>
    **Step 1: Create .claude/shared/scripts/output-naming-contracts.test.mjs** using `node:test`:
    - Import `deriveArchiveBaseName`, `sanitizeFileName`, `stripKnownOutputSuffixes` from `output-naming-contracts.mjs`
    - Write tests for the behaviors listed above
    - Tests should initially FAIL (RED phase)

    **Step 2: Modify .claude/shared/scripts/output-naming-contracts.mjs:**
    1. Rename `getDtstackPreferredArchiveBaseName(meta)` to `getPreferredArchiveBaseName(meta)`.
    2. Remove the `source_standard !== "dtstack"` guard at line 101. The function should now work for ANY project:
       ```javascript
       function getPreferredArchiveBaseName(meta = {}) {
         const preferredTitle = meta.archive_file_name || meta.requirement_title || meta.page_title || "";
         if (!preferredTitle.trim()) {
           return null;
         }
         return sanitizeFileName(preferredTitle.trim());
       }
       ```
    3. Update `deriveArchiveBaseName()` at line 187 to call `getPreferredArchiveBaseName` instead of `getDtstackPreferredArchiveBaseName`:
       ```javascript
       const preferredBaseName = getPreferredArchiveBaseName(meta);
       if (preferredBaseName) {
         return preferredBaseName;
       }
       ```
    4. All other functions in this file remain unchanged.

    **Step 3: Run tests** — should pass GREEN.
  </action>
  <verify>
    <automated>node --test .claude/shared/scripts/output-naming-contracts.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .claude/shared/scripts/output-naming-contracts.mjs does NOT contain `getDtstackPreferredArchiveBaseName`
    - .claude/shared/scripts/output-naming-contracts.mjs does NOT contain `source_standard !== "dtstack"`
    - .claude/shared/scripts/output-naming-contracts.mjs contains `function getPreferredArchiveBaseName(`
    - .claude/shared/scripts/output-naming-contracts.test.mjs exists and contains `import { deriveArchiveBaseName`
    - `node --test .claude/shared/scripts/output-naming-contracts.test.mjs` exits with code 0
  </acceptance_criteria>
  <done>getDtstackPreferredArchiveBaseName renamed to getPreferredArchiveBaseName with source_standard check removed. deriveArchiveBaseName works for any project. Test file passes GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Update all script call sites for getDtstackModules, zentaoId, and dataAssetsVersionMap</name>
  <files>
    .claude/skills/archive-converter/scripts/json-to-archive-md.mjs,
    .claude/skills/archive-converter/scripts/convert-history-cases.mjs,
    .claude/skills/xmind-converter/scripts/json-to-xmind.mjs,
    .claude/skills/xmind-converter/scripts/patch-xmind-roots.mjs,
    .claude/skills/archive-converter/scripts/convert-data-assets-v2.mjs,
    .claude/shared/scripts/unify-directory-structure.mjs,
    .claude/tests/test-load-config.mjs
  </files>
  <read_first>
    .claude/skills/archive-converter/scripts/json-to-archive-md.mjs,
    .claude/skills/archive-converter/scripts/convert-history-cases.mjs,
    .claude/skills/xmind-converter/scripts/json-to-xmind.mjs,
    .claude/skills/xmind-converter/scripts/patch-xmind-roots.mjs,
    .claude/skills/archive-converter/scripts/convert-data-assets-v2.mjs,
    .claude/shared/scripts/unify-directory-structure.mjs,
    .claude/tests/test-load-config.mjs,
    .claude/shared/scripts/load-config.mjs
  </read_first>
  <action>
    Update every script that imports `getDtstackModules` or references `zentaoId`/`dataAssetsVersionMap`. Changes per file:

    **json-to-archive-md.mjs (line 13, 445):**
    - Line 13: Change `import { getDtstackModules }` to `import { getModuleKeys, loadConfig }`
    - Line 445: Replace `const { zh: _dtstackZh, en: _dtstackEn } = getDtstackModules();` with `const _allModuleKeys = getModuleKeys();`. Update any downstream usage of `_dtstackZh` / `_dtstackEn` — if they were used for filtering, replace with `_allModuleKeys` (all modules are treated equally now).

    **convert-history-cases.mjs (line 44, 574):**
    - Line 44: Change `import { getModuleMap, getDtstackModules }` to `import { getModuleMap, getModuleKeys }`
    - Line 574: Replace `const { zh: DTSTACK_MODULES_ZH, en: DTSTACK_MODULES_EN } = getDtstackModules();` with `const ALL_MODULE_KEYS = getModuleKeys();`. Update downstream usage.

    **json-to-xmind.mjs (line 37, 41, 66-67):**
    - Line 37: Change `import { getDtstackModules, loadConfig }` to `import { getModuleKeys, loadConfig }`
    - Line 41: Replace `const { zh: DTSTACK_ZH_MODULES, en: DTSTACK_EN_MODULES } = getDtstackModules()` with `const ALL_MODULE_KEYS = getModuleKeys()`
    - Lines 66-67: Change `mod?.zentaoId` to `mod?.trackerId`. Change `const idSuffix = zentaoId ? ...` to `const idSuffix = trackerId ? \`(#\${trackerId})\` : ''`

    **patch-xmind-roots.mjs (line 11, 30, 64):**
    - Line 11 comment: Change "zentaoId" to "trackerId"
    - Line 30: Change `if (!mod.zentaoId) continue` to `if (!mod.trackerId) continue`
    - Line 64: Change `mod.zentaoId` to `mod.trackerId`

    **convert-data-assets-v2.mjs (line 347):**
    - Line 347: Change `const xmindVersionMap = config.dataAssetsVersionMap || {};` to `const xmindVersionMap = config.versionMap || {};` (or remove if this script is DTStack-specific and will move to dtstack-data branch). Since this script is specifically for data-assets conversion (a DTStack-specific operation), add a comment: `// NOTE: This script is DTStack-specific. It will be moved to dtstack-data branch in GEN-06.` Keep functional for now.

    **unify-directory-structure.mjs (lines 479-692):**
    - Line 479 comment: Change "dataAssetsVersionMap" to "versionMap"
    - Line 481-484: Change parameter name from `dataAssetsVersionMap` to `versionMap`, change `config.dataAssetsVersionMap` reads to `config.versionMap || {}`
    - Lines 637, 663, 678, 692: All references to `dataAssetsVersionMap` -> `versionMap`
    - IMPORTANT: Ensure that when `config.versionMap` is undefined/null, the code defaults to `{}` and does not crash. This is critical for blank config support.

    **test-load-config.mjs:**
    - Remove import of `getDtstackModules`
    - Remove test for `dataAssetsVersionMap` (line 41)
    - Remove `getDtstackModules` test block (lines 56-57)
    - Remove `repoBranchMapping` string literal test (line 78) — update to test `branchMapping`
    - Add tests for `getModuleKeys()` and `getBranchMappingPath()`

    **Add versionMap graceful-absence test to load-config.test.mjs:**
    - In `.claude/shared/scripts/load-config.test.mjs` (created in Plan 01), add a test case:
      ```javascript
      it('loadConfig() with missing versionMap returns undefined for versionMap (no crash)', () => {
        const config = loadConfigFromPath(MINIMAL_FIXTURE_PATH);
        assert.equal(config.versionMap, undefined);
      });
      ```
    - This verifies that a blank config (which has no versionMap field) does not cause crashes when scripts read `config.versionMap || {}`.
  </action>
  <verify>
    <automated>grep -rn "getDtstackModules\|getDtstackPreferred" .claude/skills/ .claude/shared/scripts/ .claude/tests/ 2>/dev/null | grep -v "node_modules" | grep -v ".test.mjs" | wc -l; echo "---"; node --test .claude/shared/scripts/load-config.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "getDtstackModules" .claude/skills/ .claude/shared/scripts/ .claude/tests/` returns 0 matches (excluding test scaffolds)
    - .claude/skills/xmind-converter/scripts/json-to-xmind.mjs contains `trackerId` (not `zentaoId`)
    - .claude/skills/xmind-converter/scripts/patch-xmind-roots.mjs contains `trackerId` (not `zentaoId`)
    - .claude/skills/archive-converter/scripts/json-to-archive-md.mjs contains `getModuleKeys` (not `getDtstackModules`)
    - .claude/shared/scripts/unify-directory-structure.mjs does NOT contain `dataAssetsVersionMap`
    - .claude/shared/scripts/unify-directory-structure.mjs contains `config.versionMap || {}`
    - .claude/tests/test-load-config.mjs does NOT contain `getDtstackModules`
    - `node .claude/tests/test-load-config.mjs` exits without error
    - `node --test .claude/shared/scripts/load-config.test.mjs` passes (including versionMap absence test)
  </acceptance_criteria>
  <done>All script call sites updated. Zero references to getDtstackModules, zentaoId (in scripts), or dataAssetsVersionMap remain in the codebase. versionMap absence handled gracefully. Legacy test file updated and passing. load-config.test.mjs versionMap test passes.</done>
</task>

</tasks>

<verification>
- `grep -rn "getDtstackModules\|zentaoId\|dataAssetsVersionMap" .claude/skills/ .claude/shared/scripts/ .claude/tests/ | grep -v node_modules` — returns zero matches
- `node --test .claude/shared/scripts/output-naming-contracts.test.mjs` — passes
- `node --test .claude/shared/scripts/load-config.test.mjs` — passes (including versionMap absence test)
- `node .claude/tests/test-load-config.mjs` — passes
</verification>

<success_criteria>
- Zero references to getDtstackModules, getDtstackPreferredArchiveBaseName, zentaoId, or dataAssetsVersionMap in any .mjs file
- deriveArchiveBaseName() works without source_standard check
- All scripts import getModuleKeys or loadConfig instead of getDtstackModules
- zentaoId replaced with trackerId in xmind-converter scripts
- versionMap field absence in config does not crash any script
- output-naming-contracts.test.mjs passes GREEN
- load-config.test.mjs passes GREEN (including versionMap test)
</success_criteria>

<output>
After completion, create `.planning/phases/01-generalization-refactor/01-02-SUMMARY.md`
</output>
