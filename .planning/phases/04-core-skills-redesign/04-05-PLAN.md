---
phase: 04-core-skills-redesign
plan: 05
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - .claude/skills/using-qa-flow/SKILL.md
  - CLAUDE.md
autonomous: true
requirements: [SKIL-06]

must_haves:
  truths:
    - "using-qa-flow feature menu reflects all 6 Skills with current trigger words and descriptions"
    - "using-qa-flow routing examples use generic module names, not DTStack-specific ones"
    - "Function 3 description mentions frontend+backend error support (reflects Mode C addition)"
    - "CLAUDE.md Skill index table is synchronized with updated Skill descriptions"
    - "DTStack-specific examples are conditionally preserved (not deleted) per anti-pattern guidance"
  artifacts:
    - path: ".claude/skills/using-qa-flow/SKILL.md"
      provides: "Generalized feature menu and routing with generic examples"
    - path: "CLAUDE.md"
      provides: "Updated Skill index table matching redesigned Skills"
  key_links:
    - from: ".claude/skills/using-qa-flow/SKILL.md"
      to: ".claude/skills/test-case-generator/SKILL.md"
      via: "Function 1 routing references test-case-generator trigger words"
      pattern: "生成测试用例"
    - from: "CLAUDE.md"
      to: ".claude/skills/using-qa-flow/SKILL.md"
      via: "Skill index table references using-qa-flow"
      pattern: "using-qa-flow"
---

<objective>
Update using-qa-flow menu and CLAUDE.md to reflect all Phase 4 Skill redesigns: generalized examples, updated descriptions, frontend error support mention, and synchronized trigger words.

Purpose: using-qa-flow is the entry point for all users. It must accurately reflect the current state of all Skills. CLAUDE.md is the project's authoritative handbook and must stay in sync.
Output: Generalized using-qa-flow SKILL.md + updated CLAUDE.md Skill index
</objective>

<execution_context>
@/Users/poco/.claude/get-shit-done/workflows/execute-plan.md
@/Users/poco/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-skills-redesign/04-CONTEXT.md
@.planning/phases/04-core-skills-redesign/04-RESEARCH.md
@.planning/phases/04-core-skills-redesign/04-01-SUMMARY.md
@.planning/phases/04-core-skills-redesign/04-02-SUMMARY.md
@.planning/phases/04-core-skills-redesign/04-03-SUMMARY.md
@.planning/phases/04-core-skills-redesign/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize using-qa-flow SKILL.md — menu, routing, and examples</name>
  <files>.claude/skills/using-qa-flow/SKILL.md</files>
  <read_first>
    - .claude/skills/using-qa-flow/SKILL.md (full file — current menu, routing logic, quick examples)
    - .claude/skills/test-case-generator/SKILL.md (verify current trigger words for Function 1 routing)
    - .claude/skills/prd-enhancer/SKILL.md (verify current trigger words for Function 2 routing)
    - .claude/skills/code-analysis-report/SKILL.md (verify current trigger words and Mode C for Function 3 routing)
    - .claude/skills/archive-converter/SKILL.md (verify current trigger words for Function 4 routing)
    - .claude/skills/xmind-converter/SKILL.md (verify current trigger words for Function 5 routing)
    - .planning/phases/04-core-skills-redesign/04-RESEARCH.md (Pitfall 5: menu numbering sync, anti-pattern on DTStack examples)
  </read_first>
  <action>
    **Edit `.claude/skills/using-qa-flow/SKILL.md`:**

    1. **Feature menu table:** Update Function 3 description to mention frontend support:
       ```markdown
       | **3** | 分析代码报错 | 粘贴报错日志，定位问题根因并生成 HTML 报告（支持前端/后端/冲突分析） |
       ```
       Verify all other menu items match current Skill descriptions. Keep existing menu structure.

    2. **Function 1 routing (when user selects 1/用例/test):** Replace DTStack-specific examples:
       - Change `为 data-assets v6.4.10 生成测试用例` → `为 ${module_key} v${version} 生成测试用例`（保留为格式说明）
       - Add a generic concrete example: `例如：为 orders v2.0 生成测试用例`
       - Change `为 data-assets v6.4.10 --quick 生成测试用例` → `为 ${module_key} v${version} --quick 生成测试用例`
       - Remove `继续 data-assets v6.4.10` → `继续 ${module_key} v${version} 的用例生成`
       - Remove `重新生成 data-assets v6.4.10 的「列表页」模块用例` → `重新生成 ${module_key} v${version} 的「${page}」模块用例`
       - Keep 蓝湖 URL example (it's generic, not DTStack-specific)
       - Change PRD path hint from `cases/requirements/<module>/v{version}/` (already generic, keep)

    3. **Function 2 routing (when user selects 2/PRD/增强):** Replace example path:
       - Change `cases/requirements/custom/xyzh/数据质量-质量问题台账.md` → `cases/requirements/${module_key}/v${version}/需求文档名.md`
       - Add concrete example: `例如：帮我增强这个 PRD：cases/requirements/orders/v2.0/商品管理需求.md`

    4. **Function 4 routing (when user selects 4/转换/归档):** Replace:
       - Change `转化离线开发的历史用例` → `转化 ${module_key} 的历史用例`
       - Add example: `例如：转化 orders 的历史用例`

    5. **Quick examples section (if exists):** Update to use generic examples. Per anti-pattern rule from RESEARCH.md: "示例改为通用示例 + 可选条件 DTStack 注释" — DTStack-specific hints MAY be preserved inside HTML comments for backward compatibility with existing DTStack users. Example:
       ```markdown
       <!-- DTStack 用户示例：为 data-assets v6.4.10 生成测试用例 -->
       ```
       These HTML comments are ALLOWED and expected. Only bare (non-comment) DTStack-specific examples with version numbers must be replaced with generic `${module_key}` format.

    6. **description YAML:** Keep current generic description. Verify no DTStack references.

    7. **Step 0 (init wizard):** KEEP entirely unchanged — this was built in Phase 3 and is already generic.
  </action>
  <verify>
    <automated>grep -v '<!--' .claude/skills/using-qa-flow/SKILL.md | grep -c "data-assets v[0-9]\|DTStack\|信永中和\|数据质量\|离线开发\|质量问题台账" | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>using-qa-flow menu is accurate, routing uses generic examples, Function 3 mentions frontend support. No bare DTStack-specific examples with version numbers outside HTML comments. Conditional HTML comments preserving DTStack hints for existing users are allowed per RESEARCH.md anti-pattern guidance.</done>
  <acceptance_criteria>
    - `grep -v '<!--' .claude/skills/using-qa-flow/SKILL.md | grep -c 'data-assets v[0-9]'` returns 0 (no bare versioned DTStack examples outside comments)
    - `grep -v '<!--' .claude/skills/using-qa-flow/SKILL.md | grep -c '信永中和'` returns 0
    - HTML comment lines containing DTStack hints (e.g. `<!-- DTStack 用户示例：... -->`) are permitted and do NOT count as violations
    - .claude/skills/using-qa-flow/SKILL.md contains '前端/后端/冲突分析' in Function 3 description
    - .claude/skills/using-qa-flow/SKILL.md contains '${module_key}' in routing examples
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Synchronize CLAUDE.md Skill index table with redesigned Skills</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — Skill index table, skill descriptions, trigger words)
    - .claude/skills/test-case-generator/SKILL.md (line 1-5 for current name/description)
    - .claude/skills/prd-enhancer/SKILL.md (line 1-5 for current name/description)
    - .claude/skills/code-analysis-report/SKILL.md (line 1-5 for current name/description)
    - .claude/skills/archive-converter/SKILL.md (line 1-5 for current name/description)
    - .claude/skills/xmind-converter/SKILL.md (line 1-5 for current name/description)
    - .claude/skills/using-qa-flow/SKILL.md (line 1-5 for current name/description)
  </read_first>
  <action>
    **Edit `CLAUDE.md`:**

    1. **Skill 索引 table:** Update each row to match the current Skill description YAML. Specifically:
       - `code-analysis-report` description: update to mention frontend+backend support. Change `帮我分析这个报错` trigger to include frontend context
       - Verify all other Skill descriptions match their SKILL.md description field
       - Verify all trigger words match current SKILL.md description field

    2. **Quick start examples:** If any DTStack-specific examples remain (like `为 Story-20260322 生成测试用例`), these are acceptable as they reference a generic Story ID format, not DTStack products. BUT if examples reference `data-assets` or specific DTStack modules, replace with generic `${module_key}`.

    3. **"源码仓库项目规则" section:** This section already has a config.repos guard (`> 以下规则仅在 config.json 中 repos 字段为非空对象时适用。若 repos: {} 则跳过本节。`). Verify this is intact. Do NOT remove it.

    4. **Any other DTStack references in CLAUDE.md:** Scan for `DTStack`, `数据资产`, `离线开发`, `批量运维` etc. If found outside of conditional blocks, replace with generic references.

    5. Ensure CLAUDE.md's code-analysis-report row says: `帮我分析这个报错` (keep existing trigger) with description updated to: `报错日志 → HTML 分析报告（支持前端/后端/冲突分析）`
  </action>
  <verify>
    <automated>grep -c "code-analysis-report" CLAUDE.md | grep -qv '^0$' && grep -q "前端" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md Skill index table synchronized with all Phase 4 redesigned Skills, code-analysis-report description mentions frontend support</done>
  <acceptance_criteria>
    - CLAUDE.md contains 'code-analysis-report' in Skill index table
    - CLAUDE.md Skill index table for code-analysis-report contains '前端' or '前端/后端'
    - grep -c 'data-assets v6.4.10' CLAUDE.md returns 0 (no specific DTStack version references)
    - CLAUDE.md still contains 'config.repos' guard in 源码仓库项目规则 section
    - `node --test .claude/tests/` exits 0 (no regressions)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- using-qa-flow menu Function 3 mentions frontend support
- All routing examples use generic ${module_key} format
- CLAUDE.md Skill index is synchronized
- `grep -v '<!--' .claude/skills/using-qa-flow/SKILL.md | grep -c 'data-assets v[0-9]\|DTStack\|信永中和'` returns 0
- HTML comments with conditional DTStack hints are preserved (not stripped)
- `node --test .claude/tests/` passes
</verification>

<success_criteria>
- A new user reading using-qa-flow menu sees accurate descriptions for all 6 Skills
- CLAUDE.md handbook correctly reflects all Phase 4 changes
- No bare DTStack-specific module names with version numbers in user-facing entry points (HTML comment hints for existing DTStack users are allowed)
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-skills-redesign/04-05-SUMMARY.md`
</output>
