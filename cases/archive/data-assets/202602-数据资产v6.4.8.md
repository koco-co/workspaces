---
suite_name: 202602-数据资产v6.4.8（XMind）
description: 202602-数据资产v6.4.8（XMind）
prd_version: v6.4.8
prd_path: ""
product: data-assets
tags:
  - 数据资产
  - spark任务调参
  - 规则任务管理
  - "Driver程序使用的CPU核数,默认为1"
  - "Driver程序使用内存大小,默认1g"
  - 对Spark每个action结果集大小的限制，最少是1M，若设为0则不限制大小。
  - 若Job结果超过限制则会异常退出，若结果集限制过大也可能造成OOM问题，默认1g
  - 启动的executor的数量，默认为1
  - 每个executor使用的CPU核数，默认为1
  - "每个executor内存大小,默认1g"
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 227
origin: xmind
---
# 202602-数据资产v6.4.8（XMind）
> 来源：zentao-cases/XMind/数据资产/202602-数据资产v6.4.8.xmind

---

## 【规则调度设置】spark任务调参(#10190)

### 规则任务管理❯

#### 验证调度属性新增环境参数配置(SparkThrift2.x)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 检查调度配置信息

- 新增配置: 环境参数配置
##### 点击【环境参数配置】

- 进入【环境参数配置】页面, 可进行编辑
##### 取消/确定

- 配置页面关闭 / 配置内容保存成功
#### 验证调度属性编辑环境参数配置(SparkThrift2.x)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.executor.cores=2 后保存

- 规则任务保存成功
##### 进入规则任务详情, 点击「环境参数」

- 进入「配置环境参数」界面, 配置参数回显正确
##### 设置spark.executor.cores=3 后保存

- 修改成功, 配置参数回显正确
#### 验证规则任务详情-环境参数显示正常(SparkThrift2.x)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】

- 进入【环境参数配置】页面, 可进行编辑, 默认内容:
## Driver程序使用的CPU核数,默认为1
# spark.driver.cores=1

## Driver程序使用内存大小,默认1g
# spark.driver.memory=1g

## 对Spark每个action结果集大小的限制，最少是1M，若设为0则不限制大小。
## 若Job结果超过限制则会异常退出，若结果集限制过大也可能造成OOM问题，默认1g
# spark.driver.maxResultSize=1g

## 启动的executor的数量，默认为1
# spark.executor.instances=1

## 每个executor使用的CPU核数，默认为1
# spark.executor.cores=1

## 每个executor内存大小,默认1g
# spark.executor.memory=1g

## spark 日志级别可选ALL, DEBUG, ERROR, FATAL, INFO, OFF, TRACE, WARN
# logLevel = INFO

## spark中所有网络交互的最大超时时间
# spark.network.timeout=120s

## executor的OffHeap内存，和spark.executor.memory配置使用
# spark.yarn.executor.memoryOverhead=

## 设置spark sql shuffle分区数，默认200
# spark.sql.shuffle.partitions=200

## 开启spark推测行为，默认false
# spark.speculation=false
##### 保存后, 进入规则任务详情页中, 点击【环境参数】

- 展开环境参数抽屉, 参数显示正常
##### 运行规则任务

- 运行成功, 校验结果正常
#### 验证Spark环境参数配置生效(spark.executor.instances)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.executor.instances=3 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【Running Containers】字段下, 对应任务的值

- 1) 计算公式【Running Containers = spark.executor.instances + 1 (ApplicationMaster/Driver)】 
2) 对应结果: 4 = 3 + 1
#### 验证Spark环境参数配置生效(spark.executor.cores)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.executor.cores=2 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【Allocated CPU Vcores】字段下, 对应任务的值

- 1) 计算公式：Allocated CPU VCores = (spark.executor.instances * spark.executor.cores) + spark.driver.cores
2) 对应结果：7 = (3 * 2) + 1
#### 验证Spark环境参数配置生效(spark.executor.memory)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.executor.memory=2g 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【Allocated Memory MB】字段下, 对应任务的值

- 1) 计算公式：Allocated Memory MB = (spark.executor.instances * (spark.executor.memory + max(384MB, spark.executor.memory * 0.1))) + (spark.driver.memory + max(384MB, spark.driver.memory * 0.1)) 
(注：计算结果需按 YARN 最小分配步长向上取整)

2) 对应结果：7168MB = (1 * (2048MB + 384MB = 2432MB -> 取整为 3072MB)) + (1024MB + 384MB = 1408MB -> 取整为 2048MB)
(默认 instances=1，driver.memory=1g)
#### 验证Spark环境参数配置生效(spark.driver.cores)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.driver.cores=2 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【Allocated CPU Vcores】字段下, 对应任务的值

- 1) 计算公式: Allocated CPU VCores = spark.driver.cores + (spark.executor.instances * spark.executor.cores)
2) 对应结果：3 = 2 + (1 * 1) (默认spark.executor.instances=1 且 spark.executor.cores=1)
#### 验证Spark环境参数配置生效(spark.driver.memory)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.driver.memory=2g 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【 Allocated Memory MB】字段下, 对应任务的值

- 1) 计算公式: Allocated Memory MB = (spark.driver.memory + max(384MB, spark.driver.memory * 0.1)) + (spark.executor.instances * (spark.executor.memory + max(384MB, spark.executor.memory * 0.1)))
(注：各容器计算结果需分别按 YARN 最小分配步长向上取整后再求和)
2) 对应结果：3072MB = (2048MB + 384MB = 2432MB -> 取整为 2560MB) + (1 * (1024MB + 384MB = 1408MB -> 取整为 1536MB))
(默认 instances=1，executor.memory=1g)
#### 验证Spark环境参数配置生效(spark.yarn.executor.memoryOverhead)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.yarn.executor.memoryOverhead=1024(1g) 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 检查【 Allocated Memory MB】字段下, 对应任务的值

- 1) 计算公式: Allocated Memory MB = (spark.executor.instances * (spark.executor.memory + spark.yarn.executor.memoryOverhead)) + (spark.driver.memory + max(384MB, spark.driver.memory * 0.1))
(注：由于显式设置了 memoryOverhead，Executor 部分不再使用 10% 隐式计算；各容器结果需按 YARN 最小分配步长向上取整)

2) 对应结果：3584MB = (1 * (1024MB + 1024MB = 2048MB -> 取整为 2048MB)) + (1024MB + 384MB = 1408MB -> 取整为 1536MB)
(默认 instances=1, executor.memory=1g, driver.memory=1g)
#### 验证Spark环境参数配置生效(spark.network.timeout)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.network.timeout=300s 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 点击 Tracking UI: ApplicationMaster，在 Environment 页签搜索该参数

- 确认 Value 为 300s
#### 验证Spark环境参数配置生效(spark.speculation)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 依次配置监控对象(SparkThrift2.x), 监控规则后, 点击下一步

- 进入【新建单表校验规则 ❯ 调度属性】配置页面
##### 点击【环境参数配置】, 设置spark.speculation=true 后保存

- 规则任务保存成功
##### 运行规则任务

- 任务运行成功, 校验结果正常
##### 进入Apache Hadoop YARN界面, 点击 Tracking UI: ApplicationMaster，在 Environment 页签确认 spark.speculation

- 应为true
## 【质量规则库】内置规则增加规则项

### 规则库配置❯

#### 验证新增内置规则

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 检查内置规则列表

- 新增4个内置规则
##### 检查新增内置规则: 及时性校验

- 1) 规则名称: 及时性校验
2) 规则解释: 多字段时间差校验
3) 规则分类: 时效性校验
4) 关联范围: 字段
5) 规则描述: 比较两个时间类型字段之间的时间差是否符合要求，支持校验多个字段组时间差
##### 检查新增内置规则: 周期性校验

- 1) 规则名称: 周期性校验
2) 规则解释: 单字段时间差校验
3) 规则分类: 时效性校验
4) 关联范围: 字段
5) 规则描述: 比较时间字段内相邻两行数据的时间差是否符合要求
##### 检查新增内置规则: 数据变化趋势

- 1) 规则名称: 数据变化趋势
2) 规则解释: 单调递增、单调递减校验
3) 规则分类: 合理性校验
4) 关联范围: 字段
5) 规则描述: 比较字段内数据排序后是否符合单调递增/单调递减的逻辑
#### 验证重复内置规则隐藏

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 检查内置规则列表

- 1) 规则分类为「有效性校验」，规则名称为「重复值检测」、「null值检测」的规则隐藏，不做展示
2) 规则状态默认为关闭状态
##### 进入【规则集管理】, 新建规则集-监控规则配置页面, 添加「有效性校验」规则

- 添加成功
##### 检查有效性校验块的配置项

- 不再支持选择重复值检测、NULL值检测的规则
#### 验证新增规则分类正确

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 点击规则分类筛选项

- 规则分类新增"时效性校验"、"合理性校验"
#### 验证【时效性校验】筛选功能正常

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 规则分类分类筛选: 时效性校验

- 筛选成功, 结果正确
##### 规则分类分类筛选: 时效性校验 + 唯一性校验

- 组合筛选成功, 结果正确
##### 重置后, 确定

- 筛选成功, 结果正确
#### 验证【合理性校验】筛选功能正常

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 规则分类分类筛选: 合理性校验

- 筛选成功, 结果正确
##### 规则分类分类筛选: 合理性校验 + 有效性校验

- 组合筛选成功, 结果正确
##### 重置后, 确定

- 筛选成功, 结果正确
#### 验证【导出规则库】中的内置规则正确

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 点击导出规则库并保存

- 保存成功
##### 检查导出文件内容

- 包含新增内置规则: 
1) 及时性校验
2) 周期性校验
3) 数据变化趋势

不再显示隐藏规则:
1) 重复值检测
2) NULL值检测
##### 检查内置规则信息

- 规则信息正确
#### 验证【内置规则-多字段时间差校验&单字段时间差校验】规则状态变更正常

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 选择【内置规则-多字段时间差校验&单字段时间差校验】, 开启规则状态

- 开启成功
##### 进入【规则集管理】, 新建规则集-监控规则配置页面

- 进入成功
##### 点击【添加规则】

- 新增三个内置规则: 
1) 时效性校验
2) 合理性校验
##### 添加时效性校验后, 检查校验规则配置项

- 1) 支持配置规则: 多字段时间差校验、单字段时间差校验
2) 选择该规则包的任务也不支持配置该内置规则
##### 返回规则库配置, 关闭【内置规则-多字段时间差校验、单字段时间差校验】规则状态

- 关闭成功
##### 重新进入规则集管理, 配置监控规则, 选择时效性校验

- 1) 时效性校验中: 不再支持配置规则(多字段时间差校验、单字段时间差校验)
2) 选择该规则包的任务也不支持配置该内置规则
##### 返回规则库配置, 筛选出所有【规则分类】为时效性校验的内置规则, 并关闭规则状态

- 关闭成功
##### 重新进入规则集管理, 配置监控规则

- 1) 添加规则中不再支持: 时效性校验
2) 选择该规则包的任务也不支持配置该规则类型
#### 验证【内置规则-单调递增、单调递减校验】规则状态变更正常

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 选择【内置规则-单调递增、单调递减校验】, 开启规则状态

- 开启成功
##### 进入【规则集管理】, 新建规则集-监控规则配置页面

- 进入成功
##### 选择合理性校验后, 检查校验规则配置项

- 1) 支持配置规则: 单调递增、单调递减校验
2) 选择该规则包的任务也不支持配置该内置规则
##### 返回规则库配置, 关闭【内置规则-单调递增、单调递减校验】规则状态

- 关闭成功
##### 重新进入规则集管理, 配置监控规则, 选择一致性校验

- 1) 合理性校验中: 不再支持配置规则(单调递增、单调递减校验)
2) 选择该规则包的任务也不支持配置该内置规则
##### 返回规则库配置, 筛选出所有【规则分类】为合理性校验的内置规则, 并关闭规则状态

- 关闭成功
##### 重新进入规则集管理, 配置监控规则

- 1) 添加规则中不再支持: 合理性校验
2) 选择该规则包的任务也不支持配置该规则类型
#### 验证【其它历史内置规则】规则状态变更正常

##### 进入【数据资产】-【数据质量】-【规则库配置】页面

- 进入成功
##### 选择【其它历史内置规则】, 开启所有规则状态

- 开启成功
##### 进入【规则集管理】, 新建规则集-监控规则配置页面

- 进入成功
##### 点击「添加规则」, 检查下拉框数据

- 历史规则分类正常选择
##### 选择完整性校验, 检查校验规则块中可选的内置规则

- 所有内置规则均可选择
##### 依次选择有效性校验、唯一性校验、统计性校验, 检查规则块中的可选内置规则

- 所有内置规则均可选择
##### 在「规则库配置」中, 关闭完整性校验~统计性校验的内置规则

- 规则状态全部关闭
##### 重新进入【规则集管理】, 新建规则集-监控规则配置页面, 检查添加规则后的可选内置规则

- 已关闭的内置规则不再支持选择
## 【规则任务配置优化】质量规则任务支持编辑分区信息(#10192)

### 规则任务管理❯

#### 验证规则任务支持编辑分区操作

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 选择规则任务A, 进入编辑页

- 1) 分区配置处于可编辑状态
2) 分区配置信息回显正常
#### 运行方式

##### 验证规则任务支持「实例生成方式」

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建规则任务A, 配置监控对象、监控规则后, 检查调度属性页面
  - 调度配置中新增字段: 实例生成方式
- 检查实例生成方式配置项
  - 1) 枚举: T+1生成(默认)、立即生成
2) 悬浮提示: 选中「T+1生成」，代表实例生成按照配置项设置的时间生成实例，默认是22：00；
选中「立即生成」，代表提交后立即生成当天开始未来时间内的实例，之后的实例生成还是按照配置项设置的时间生成实例；
- 保存规则任务, 检查规则任务详情页
  - 新增字段: 实例生成方式, 回显内容正确
- 选择规则任务A, 进入任务详情页-「编辑调度属性」配置页面
  - 调度配置中存在字段: 实例生成方式
- 检查实例生成方式配置项
  - 1) 枚举: T+1生成(默认)、立即生成
2) 悬浮提示: 选中「T+1生成」，代表实例生成按照配置项设置的时间生成实例，默认是22：00；
选中「立即生成」，代表提交后立即生成当天开始未来时间内的实例，之后的实例生成还是按照配置项设置的时间生成实例；
##### 验证规则任务正常运行(T+1生成)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「T+1生成」
  - 配置成功
- 等待T+1生成规则任务实例
  - 实例生成成功, 具体流程如下:
1) 假设今天 14:00 提交任务
2) 今天 22:00（T+1触发时间）调度引擎自动生成【明天】24条小时调度任务(00:00、01:00、.......、23:00), 但不会显示在资产页面
3) 明天 00:00 开始, 校验结果查询中, 可以看到实例记录并运行, 每小时新增一条实例
4) 之后每天 22:00, 生成下一天的全天实例（同 T+1）
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次等待T+1生成规则任务实例
  - 实例生成成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验通过
- 进入规则任务详情「编辑调度属性」配置页面, 将「T+1生成」, 改为「立即生成」后, 保存
  - 配置变更成功
- 立即生成规则任务实例
  - 生成成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验通过
##### 验证规则任务正常运行(立即生成)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 立即生成规则任务实例
  - 实例生成成功, 具体流程如下:
1) 假设今天 14:00 提交任务
2) 约 30 秒后, 生成【今天 14:15 之后】到【23:59】的实例, 取整后得到的预期实例：15:00, 16:00, 17:00, 18:00, 19:00, 20:00, 21:00, 22:00, 23:00
3) 今天 15:00 生成当天的第一个实例, 之后每小时新增一条实例
4) 今天 22:00 （T+1触发时间）, 生成【明天】全天 24 个实例 （同 T+1, 包括后续流程）
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次立即生成任务
  - 生成成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验通过
##### 验证规则任务正常运行(关闭检测)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: T+1生成
  - 配置成功
- 选择规则任务rule01, 关闭检测
  - 不生成任何实例, 具体流程如下:
1) 假设今天 14:00 提交任务（关闭状态）
2) 调度引擎不会为暂停状态的任务生成实例
#### 一级分区 ❯ 一级分区

##### 验证分区信息不变, 校验结果不变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260202 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置不变
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果不变: 校验不通过
- 检查后续生成的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果依旧不变: 校验不通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date = bdp.system.bizdate , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115, 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区:  factory_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区:  factory_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息
- 检查后续rule01实例的校验结果
  - 后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date = bdp.system.bizdate
 , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
#### 一级分区 ❯ 二级分区

##### 验证分区信息不变, 校验结果不变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260202/sale_date=20260202 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置不变
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验不通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date/sale_date = bdp.system.bizdate , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date/sale_date = bdp.system.bizdate
 , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
#### 二级分区 ❯ 二级分区

##### 验证分区信息不变, 校验结果不变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260202/sale_date=20260202 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置不变
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果不变: 校验不通过
- 检查后续生成的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果依旧不变: 校验不通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date/sale_date = bdp.system.bizdate , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date/sale_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date/sale_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115/sale_date=20260201 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date/sale_date = bdp.system.bizdate
 , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
#### 二级分区 ❯ 一级分区

##### 验证分区信息不变, 校验结果不变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260202 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置不变
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果不变: 校验不通过
- 检查后续生成的rule01实例的详情页
  - 分区配置不变, 实例继续运行, 结果依旧不变: 校验不通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date = bdp.system.bizdate , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择已有分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择已有分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date/sale_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(选择动态分区 ❯ 手动输入分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择动态分区: factory_date/sale_date = bdp.system.bizdate

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 手动输入分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择已有分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择已有分区: factory_date=20260115 , 点击数据预览
  - 数据预览成功: 3条数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
##### 验证分区信息改变, 任务实例信息改变(手动输入分区 ❯ 选择动态分区)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1)监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 手动输入分区: factory_date=20260202/sale_date=20260202

2) 监控规则: 

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 运行规则任务rule01, 立即执行
  - 运行成功
- 进入【校验结果查询】, 检查实例详情页面
  - 校验结果: 校验不通过
- 进入【规则任务管理】, 编辑rule01, 修改分区: 选择动态分区: factory_date = bdp.system.bizdate
 , 点击数据预览
  - 数据预览成功: 暂无数据
- 后面配置不变, 保存后再次运行任务
  - 1) 任务运行成功
2) 规则任务rule01详情页中, 分区配置变更成功
- 进入【校验结果查询】, 检查【运行中、等待运行】的rule01实例的详情页
  - 实例停止运行, 实例状态依次变更: 【停止中】、【中途停止】
- 检查后续生成的rule01实例的详情页
  - 分区配置变更成功, 变更范围:
1) 表名悬浮提示中的分区配置信息
2) 规则详情页中, 表级报告中的表名和分区信息

后续生成的实例正常运行, 校验结果: 校验通过
## 【岚图】【规则集管理】支持每个数据表的规则集管理(#10193)

### 规则集管理 ❯

#### 验证规则集管理查询功能

##### 进入【数据资产】-【数据质量】-【规则集管理】页面

- 进入成功
##### 检查查询框

- 1、提示: 输入表名搜索
2、支持回车查询、搜索icon查询
##### 输入表部分名查询

- 模糊查询成功
##### 输入表全名查询

- 查询成功
#### 验证规则集管理页面

##### 进入【数据资产】-【数据质量】-【规则集管理】页面

- 进入成功
##### 检查页面UI

- 1、支持规则集列表展示, 包含表名、所属数据库、所属数据源、规则包数量、规则数量、规则集描述、更新人、更新时间、操作
2、支持对规则集的增删改查
3、支持对规则集分页列表展示
##### 检查列表数据

- 1) 表名: ${最后一次编辑规则集时的表名}
2) 所属数据库:  ${最后一次编辑规则集时的数据库}
3) 所属数据源:  ${最后一次编辑规则集时的数据源}
4) 规则包数量:  ${最后一次编辑规则集时的规则包数量, 不超过20}
5) 规则数量:  ${最后一次编辑规则集时的规则数量}
6) 规则集描述:  ${最后一次编辑规则集时的规则集描述}
7) 更新人:  ${最后一次编辑规则集时的账号}
8) 更新时间:  ${最后一次编辑规则集时的时间}
#### 验证规则集管理删除功能

##### 进入【数据资产】-【数据质量】-【规则集管理】页面

- 进入成功
##### 选择规则集1(未关联任务), 点击删除

- 二次确认弹窗, 提示: 删除规则集不会影响已经建好的规则任务，若需要删除已有规则任务，需要前往规则任务模块进行任务删除或关闭检测。请确认是否删除?
##### 确认删除

- Toast提示: 删除成功
##### 选择规则集2(关联任务), 点击删除

- 二次确认弹窗提示
##### 确认删除

- 1) Toast提示: 删除成功
2) 其关联的规则任务不会受影响
#### 验证分页组件功能正常

##### 进入【数据资产】-【数据质量】-【规则集管理】页面

- 进入成功
##### 分页组件UICHECK

- 1) 共x条数据, 每页显示xx条
2) 支持数字页码切换
3) 支持前/后页箭头页码切换
4) 支持10、20、50、100 页数切换
##### 切换数字页码

- 页码切换成功, 数据显示正常
##### 切换前后页箭头页码

- 页码切换成功, 数据显示正常
##### 切换10、20、50、100页数

- 页数切换成功, 数据显示正常
#### 新建规则集 ❯

##### 验证新建规则集配置页面

- 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
- 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面, 支持配置基础信息和监控规则
- 基础信息UICHECK
  - 支持配置选择数据表、规则包:
1) 选择数据表: 选择数据源(必填)、选择数据库(必填)、选择数据表(必填)、规则集描述
2) 规则包: 支持对规则包名称进行增删改, 必填
3) 按钮: 取消/下一步
- 正常配置基础信息内容, 点击下一步
  - 进入【新建规则集 ❯ 监控规则】配置页面
- 监控规则UICHECK
  - 1)支持对每一个规则包配置不同的校验规则
2)支持规则包的增删改操作
3) 支持对规则包中的校验规则增删改和克隆操作
4) 支持查看全局参数
5) 按钮: 下一步/保存
##### 基础信息 ❯

- 验证选择数据表配置功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
  - 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 配置选择数据源
  - 1)下拉显示在平台管理中已授权的数据源
2)支持选择的数据源类型包括hive2.x/sparkthrift2.x/doris3.x
  - 配置选择数据库
  - 下拉显示所选数据源下的数据库配置
  - 配置选择数据表
  - 下拉显示所选数据库下的数据表配置, 已配置过规则的表被过滤
  - 配置规则集描述
  - 规则集描述最大支持255字符, 超过会置红提示
- 验证选择数据表选项过滤已配置的表
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 选择已配置过规则的数据源(hive2.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的hive表tableA
  - 选择已配置过规则的数据源(sparkthrift2.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的sparkthrift表tableA
  - 选择已配置过规则的数据源(doris3.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的doris表tableA
- 验证规则包名称增删改功能
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置选择数据表内容
  - 配置成功
  - 在规则包名称配置中, 点击增加按钮, 添加规则包名称
  - 添加成功, 从第二个规则包名称开始均提供删除按钮
  - 添加至第21个规则包名称
  - 最多支持增加20个规则包名称
  - 删除至1个规则包名称
  - 删除按钮消失
  - 规则包名称输入框输入51字符
  - 规则包名称最大50个字符
- 验证规则包名称不可重复
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置选择数据表内容
  - 配置成功
  - 在规则包配置中, 点击增加按钮, 添加规则包
  - 添加成功, 从第二个规则包开始均提供删除按钮
  - 配置内容:
1) 规则包1: rule01
2) 规则包2: rule01
  - 置红提示: 规则包名称不可重复
##### 监控规则 ❯

- 验证规则包下拉框数据正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入监控规则配置页面
  - 展开规则包下拉框
  - 下拉框数据为基础信息配置页面中添加的规则包名称
  - 选择规则包rule01,  点击增加
  - 新增一个规则包模块
  - 检查规则包下拉框数据
  - 已选择过的规则包(rule01)不支持再次选择
- 验证规则包增删改功能
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入监控规则配置页面
  - 在规则包配置中, 点击增加按钮, 添加规则包
  - 1)添加成功, 从第二个规则包开始均提供删除按钮
2) 每个规则包都支持查看全局参数和添加规则按钮
3) 规则包支持折叠和展开, 默认展开
  - 折叠/展开规则包
  - 操作成功
  - 添加至20个规则包
  - 增加按钮消失
  - 删除任一规则包
  - 二次弹窗确认: 删除时进行二次确认提示: 删除规则包后该规则包下已经配置好的规则会同步被删除，请确认是否删除?
  - 二次确认删除
  - 1) Toast提示: 删除成功
2) 可再次添加规则包
  - 删除至1个规则包
  - 删除按钮消失
  - 编辑规则包中的校验规则内容, 保存
  - 保存成功
- 验证查看全局参数功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入监控规则配置页面
  - 在规则包配置中, 点击【查看全局参数】按钮
  - 进入全局参数弹窗, 展示全局参数名称列表
- 验证校验规则增删改功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入监控规则配置页面
  - 在规则包配置中, 点击【添加规则】按钮
  - 支持完整性校验、有效性校验、唯一性校验、统计性校验、自定义SQL、时效性校验、合理性校验
  - 添加完整性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加有效性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加唯一性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加统计性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加自定义SQL, 正常配置后, 点击保存
  - 配置保存成功
  - 添加时效性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加合理性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加至10个校验规则
  - 上限10个, 无法再次添加
  - 选择校验规则A, 点击删除
  - 删除成功, 可再次添加
  - 选择校验规则B, 编辑内容
  - 编辑成功
  - 选择校验规则C, 点击克隆
  - 克隆成功, 配置内容保持一致
- 验证【完整性校验】字段变更(规则类型 ❯ 生效范围)
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入监控规则配置页面
  - 添加【完整性校验】, 检查配置字段
  - 原配置字段名称(规则类型)变更为: 生效范围
- 验证更换规则包名称后, 校验规则配置不变
  - 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集
  - 进入【新建规则集 ❯ 基础信息】配置页面
  - 正常配置基础信息内容, 点击下一步
  - 进入「监控规则」配置页面
  - 选择规则包1, 规则包2
  - 配置成功
  - 进入规则任务管理中, 引入规则包1并引入, 检查校验规则配置信息
  - 校验规则引入成功
  - 切换为规则包2但不引入
  - 规则配置不变: 更换选择不同的规则包名称不影响已经配置的规则
  - 点击引入
  - 提示: 引入后会对已有规则进行覆盖引入，请确认是否引入
  - 确定引入
  - 仅保留规则包2中的规则
#### 编辑规则集 ❯

##### 验证编辑规则集配置页面

- 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
- 选择规则集记录rule01, 点击编辑
  - 进入【编辑规则集 ❯ 基础信息】配置页面, 所有字段均可编辑
- 基础信息UICHECK
  - 1) 选择数据表: ${最近一次编辑保存时的配置记录}
2) 规则包: ${最近一次编辑保存时的规则包名称}
3) 按钮: 取消/下一步
- 点击下一步
  - 进入【编辑规则集 ❯ 监控规则】配置页面
- 监控规则UICHECK
  - 1) 规则包&校验规则: ${最近一次编辑保存时的规则包&校验规则}
2) 按钮: 下一步/保存
##### 基础信息 ❯

- 验证选择数据表配置功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
  - 选择规则集记录rule01, 点击编辑
  - 进入【编辑规则集 ❯ 基础信息】配置页面
  - 切换选择数据源配置后, 正常配置规则集并保存
  - 配置成功
  - 切换选择数据库配置后, 正常配置规则集并保存
  - 配置成功
  - 切换选择数据表配置后, 正常配置规则集并保存
  - 配置成功
  - 切换选择规则集描述后, 正常配置规则集并保存
  - 配置成功
- 验证选择数据表选项过滤已配置的表
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01
  - 进入【编辑规则集 ❯ 基础信息】配置页面
  - 选择已配置过规则的数据源(hive2.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的hive表tableA
  - 选择已配置过规则的数据源(sparkthrift2.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的sparkthrift表tableA
  - 选择已配置过规则的数据源(doris3.x)、数据库后, 查看选择数据表下拉选项
  - 过滤已配置的doris表tableA
- 验证规则包名称增删改功能
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01
  - 进入【编辑规则集 ❯ 基础信息】配置页面
  - 在规则包名称配置中, 点击增加按钮, 添加规则包名称
  - 添加成功, 从第二个规则包名称开始均提供删除按钮
  - 添加至20个规则包名称
  - 增加按钮消失
  - 删除任一规则包名称
  - 可再次添加
  - 删除至1个规则包名称
  - 删除按钮消失
  - 规则包名称输入框输入51字符
  - 置红提示: 规则包名称不可重复
  - 输入50字符, 点击下一步
  - 配置成功
- 验证规则包名称不可重复
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01
  - 进入【编辑规则集 ❯ 基础信息】配置页面
  - 在规则包配置中, 点击增加按钮, 添加规则包
  - 添加成功, 从第二个规则包开始均提供删除按钮
  - 配置内容:
1) 规则包1: rule01
2) 规则包2: rule01
  - 置红提示: 规则包名称不可重复
##### 监控规则 ❯

- 验证规则包下拉框数据正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01, 点击下一步
  - 进入【编辑规则集 ❯ 监控规则】配置页面
  - 展开规则包下拉框
  - 下拉框数据为基础信息配置页面中添加的规则包名称
  - 检查规则包下拉框数据
  - 已选择过的规则包(rule01)不支持再次选择
- 验证规则包增删改功能
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01, 点击下一步
  - 进入【编辑规则集 ❯ 监控规则】配置页面
  - 折叠/展开规则包
  - 操作成功
  - 添加至20个规则包
  - 增加按钮消失
  - 删除任一规则包
  - 二次弹窗确认: 删除时进行二次确认提示: 删除规则包后该规则包下已经配置好的规则会同步被删除，请确认是否删除?
  - 二次确认删除
  - 1) Toast提示: 删除成功
2) 可再次添加规则包
  - 删除至1个规则包
  - 删除按钮消失
  - 编辑规则包中的校验规则内容, 并保存
  - 保存并配置成功
- 验证查看全局参数功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01, 点击下一步
  - 进入【编辑规则集 ❯ 监控规则】配置页面
  - 在规则包配置中, 点击【查看全局参数】按钮
  - 进入全局参数弹窗, 展示全局参数名称列表
- 验证校验规则增删改功能正常
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01, 点击下一步
  - 进入【编辑规则集 ❯ 基础信息】配置页面
  - 选择规则包rule01, 点击【添加规则】按钮
  - 支持完整性校验、有效性校验、唯一性校验、统计性校验、自定义SQL
  - 添加完整性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加有效性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加唯一性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加统计性校验, 正常配置后, 点击保存
  - 配置保存成功
  - 添加自定义SQL, 正常配置后, 点击保存
  - 配置保存成功
  - 添加至10个校验规则
  - 上限10个, 无法再次添加
  - 选择校验规则A, 点击删除
  - 删除成功, 可再次添加
  - 选择校验规则B, 编辑内容
  - 编辑成功
  - 选择校验规则C, 点击克隆
  - 克隆成功, 配置内容保持一致
- 验证更换规则包名称后, 校验规则配置不变
  - 进入【数据资产】-【数据质量】-【规则集管理】页面,  编辑规则集rule01, 点击下一步
  - 进入【编辑规则集 ❯ 监控规则】配置页面
  - 将原规则包1更换为规则包2(不引入)
  - 规则包下的校验规则配置内容不变
#### 规则集详情 ❯

##### 验证规则集详情数据正确

- 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
- 选择规则集rule01, 点击表名
  - 右侧抽屉形式展开详情页, 包含基本信息和规则详情, 具体如下:
1) 标题: ${数据表名称}规则集详情
2) 基本信息: 包含表名、所属数据库、所属数据源、规则包数量、规则数量、规则集描述、更新人、更新时间
3) 规则详情: 
- 展示该规则集记录中已配置的规则包内容, 包含规则包名称及关联的校验规则
- 每个规则包支持折叠, 默认展开
- 检查【规则集详情页】中的基本信息&规则详情数据
  - 1) 基本信息与规则集列表记录保持一致
2) 规则详情与规则集记录中配置的规则包内容一致
- 进入【规则任务管理】中, 配置规则任务引入该规则包并保存
  - 保存成功
- 检查【规则任务详情页】
  - 显示规则任务关联的规则包中, 所有的校验规则
- 运行规则任务后, 进入【校验结果查询】, 检查【校验结果详情页】
  - 显示规则任务关联的规则包中, 所有的校验规则
##### 验证规则集详情页面显示正常(20规则包 * 10校验规则)

- 进入【数据资产】-【数据质量】-【规则集管理】页面
  - 进入成功
- 选择规则集rule01, 点击表名
  - 右侧展开详情页, 包含基本信息和规则详情
- 检查【规则集详情页】中的基本信息
  - 数据显示正确, 规则包数量、规则数量分别为20、200
- 检查【规则集详情页】中的规则详情
  - 数据显示正确, 页面显示正常, 溢出数据通过列表下滑查看
- 进入【规则任务管理】中, 配置规则任务引入所有规则包、规则类型并保存
  - 保存成功
- 检查【规则任务详情页】
  - 显示规则任务关联的规则包中, 所有的校验规则
- 运行规则任务后, 进入【校验结果查询】, 检查【校验结果详情页】
  - 显示规则任务关联的规则包中, 所有的校验规则
### 规则任务管理 ❯

#### 验证列表字段名称变更(规则名称 ❯ 任务名称)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 检查规则任务管理列表字段

- 原列表字段名称(规则名称)变更为: 任务名称
#### 验证监控规则页面变更

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 点击新建监控规则, 配置监控对象后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 检查【监控规则】页面变更

- 1) 新增规则包(必填)、规则类型(非必填)下拉框、引入按钮
2) 原右上角按钮(添加规则、查看全局参数)隐藏
#### 验证规则集引用功能正常(规则包单选)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 新建监控规则, 配置监控对象(hive_table)后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 检查规则包下拉框数据

- 支持选择全部、hive_rulePkg01~04共5个选项
##### 选择hive_rulePkg01规则包, 检查规则类型下拉框数据

- 存在级联关系, 仅筛选出该规则包下的规则类型: 完整性校验
##### 勾选【完整性校验】后, 点击【引入】

- 成功引入一个【完整性校验】, 配置正确
##### 正常配置调度属性后, 保存

- 配置保存成功
##### 重新配置监控规则页面: 选择hive_rulePkg02规则包, 检查规则类型下拉框数据

- 筛选结果: 唯一性校验
##### 勾选【唯一性校验】后, 点击【引入】

- 成功引入十个【唯一性校验】, 配置正确
##### 正常配置调度属性后, 保存

- 配置保存成功
##### 重新配置监控规则页面: 选择hive_rulePkg03规则包, 检查规则类型下拉框数据

- 筛选结果: 完整性校验~合理性校验共8项
##### 勾选所有校验类型后, 点击【引入】

- 成功引入所有校验类型, 配置正确
##### 正常配置调度属性后, 保存

- 配置保存成功
#### 验证规则集引用功能正常(规则包多选)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 新建监控规则, 配置监控对象(hive_table)后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 选择hive_rulePkg01、02规则包, 检查规则类型下拉框数据

- 仅支持选择: 完整性校验和唯一性校验
##### 勾选所有规则类型后, 点击【引入】

- 引入完整性校验 * 1 + 唯一性校验 * 10 , 引入配置正确
#### 验证规则集引用功能正常(规则包选择全部)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 新建监控规则, 配置监控对象(hive_table)后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 选择hive_rulePkg01、02、03规则包, 检查规则类型下拉框数据

- 支持选择完整性校验~合理性校验共8项
##### 勾选所有规则类型后, 点击【引入】

- 引入成功, 不同规则包间的相同校验规则均被引入至规则任务: 完整性校验*2 + 唯一性校验*11 + 其它6种校验*1
#### 验证规则集引用功能正常(覆盖引入)

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 新建监控规则, 配置监控对象(hive_table)后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 选择hive_rulePkg01规则包, 完整性校验, 并引入

- 引入成功
##### 选择hive_rulePkg02规则包, 完整性校验, 并引入

- 提示: 引入后会对已有规则进行覆盖引入，请确认是否引入
##### 确认引入后, 检查校验规则配置

- 引入后对已有规则配置进行覆盖
##### 保存规则任务后, 检查规则任务详情页

- 规则配置为: 引入后的规则, 原规则被删除
#### 验证更换规则包但不引入, 校验规则配置不变

##### 进入【数据资产】-【数据质量】-【规则集管理】页面, 点击新增规则集

- 进入【新建规则集 ❯ 基础信息】配置页面
##### 正常配置基础信息内容, 点击下一步

- 进入监控规则配置页面
##### 选择已配置校验规则的规则包1并引入

- 规则包1中的校验规则引入成功, 配置信息正确
##### 更换为规则包2 (不引入)

- 规则包下的校验规则配置内容不变
##### 保存后, 检查规则任务配置信息

- 保存成功, 配置内容不变:
1) 规则包: 规则包1
2) 校验规则: 规则包1中的校验规则
#### 验证规则集引用规则仅支持删除

##### 进入【数据资产】-【数据质量】-【规则任务管理】页面

- 进入成功
##### 新建监控规则, 配置监控对象(hive_table)后点击下一步

- 进入【新建单表校验规则 ❯ 监控规则】配置页面
##### 选择hive_rulePkg03规则包, 勾选所有校验规则, 并引入

- 引入成功
##### 检查完整性校验规则块

- 规则块仅支持删除按钮, 其余编辑操作全部禁用(置灰), 包括克隆按钮
##### 点击删除, 并二次确认删除

- 删除成功
##### 依次检查后面7个校验规则块

- 仅支持删除(蓝色按钮), 其余编辑操作、克隆操作均不再支持
##### 依次删除并二次确认

- 删除成功
##### 再次引入hive_rulePkg03规则包中的所有规则类型, 保存

- 引入、保存成功
### 校验结果查询 ❯

#### SparkThrift2.x ❯

##### 验证规则任务配置规则包后校验正常(1规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE final_price < 0 OR final_price IS NULL;
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 变更分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 3校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

有效性校验:
- 字段: vin
- 统计规则: 字符串长度 
- 校验方法: 固定值
- 期望值> 0

唯一性校验
- 字段: order_id
- 统计函数: 重复数
- 校验方法: 固定值
- 期望值: =0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE 
    -- 1. 完整性校验异常 (final_price < 0 或为 NULL)
    (final_price < 0 OR final_price IS NULL)
    
    OR
    
    -- 2. 有效性校验异常 (vin 长度 <= 0，包含 NULL 或 纯空格字符串)
    (length(trim(vin)) = 0 OR vin IS NULL)
    
    OR
    
    -- 3. 唯一性校验异常 (order_id 重复)
    order_id IN (
        SELECT order_id 
        FROM dwd_voyah_vehicle_sales_dates 
        GROUP BY order_id 
        HAVING COUNT(1) > 1
    );
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(2规则包 * 2校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_sales_time_quality
- 选择分区: pt_date='20260204' / data_type='abnormal_monotonicity'

2) 监控规则: 引入规则包1(完整性校验、合理性校验)、规则包2(时效性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: order_id
- 统计函数: 字段取值校验
- 期望值: >= 0

合理性校验:
- 字段: order_id
- 统计函数: 数据变化趋势
- 过滤条件: /
- 排序字段: order_id
- 校验方法: 单调递增

有效性校验:
- 字段: vin
- 统计规则: 字符串长度 
- 校验方法: 固定制
- 期望值> 0

唯一性校验
- 字段: order_id
- 统计函数: 重复数
- 校验方法: 固定值
- 期望值: =0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- 编辑规则任务rule01, 分区: pt_date='20260201' / data_type='normal'
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 10校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 唯一性校验: 计算 order_id 的重复次数
        COUNT(1) OVER(PARTITION BY order_id) as order_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- ---------------------------------------------------------
    -- 1. 完整性校验 (字段级)
    -- ---------------------------------------------------------
    -- 规则: vin 空串数=0 (即: 找出 vin 为空串的数据)
    (trim(vin) = '')
    
    OR 
    
    -- 规则: car_model 空值数=0 (即: 找出 car_model 为 NULL 的数据)
    (car_model IS NULL)
    
    OR 
    
    -- 规则: final_price 期望值 >= 0 (即: 找出 < 0 的数据)
    (final_price < 0)

    -- ---------------------------------------------------------
    -- 2. 有效性校验
    -- ---------------------------------------------------------
    -- 规则: vin 字符串长度 > 0 (即: 找出长度 <= 0 或 NULL 的数据)
    -- 注: 此规则与上面的"空串数"、"空值数"有重叠，此处做兜底
    OR (length(vin) <= 0 OR vin IS NULL)
    
    -- 规则: guide_price 取值范围 [0, 1000000] (即: 找出在此范围之外的数据)
    OR (guide_price < 0 OR guide_price > 1000000)
    
    -- 规则: car_model 枚举个数 = 3 (即: 找出不属于这3个标准车型的数据)
    -- 假设标准枚举值为: 岚图FREE, 岚图梦想家, 岚图追光
    -- 注: NULL值已被前面的完整性校验捕获，此处主要校验"未知枚举值"
    OR (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- ---------------------------------------------------------
    -- 3. 唯一性校验
    -- ---------------------------------------------------------
    -- 规则: order_id 重复数/重复率期望为0 (即: 找出重复出现的订单)
    OR (order_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(20规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 针对规则 17,19,20: 计算 order_id 重复数
        COUNT(1) OVER(PARTITION BY order_id) as calc_order_dup_count,
        -- [辅助计算] 针对规则 18: 计算 vin 重复数
        COUNT(1) OVER(PARTITION BY vin) as calc_vin_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- =========================================================
    -- A. 完整性校验
    -- =========================================================
    -- 规则1: vin 空串
    (trim(vin) = '') OR
    -- 规则2-10: 各关键字段判空
    (vin IS NULL) OR
    (order_id IS NULL) OR
    (car_model IS NULL) OR
    (guide_price IS NULL) OR
    (final_price IS NULL) OR
    (dealer_name IS NULL) OR
    (order_status IS NULL) OR
    (factory_date IS NULL) OR
    (sale_date IS NULL)

    -- =========================================================
    -- B. 取值范围与有效性
    -- =========================================================
    OR 
    -- 规则11: final_price < 0
    (final_price < 0)
    
    OR
    -- 规则14: vin 长度 <= 0 (注: 空串/NULL已被上面捕获，此处兜底)
    (length(vin) <= 0)

    OR
    -- 规则15: guide_price 超出 [0, 1000000] 范围
    (guide_price < 0 OR guide_price > 1000000)

    OR
    -- 规则16: car_model 枚举校验 (假设标准值为3个: FREE, 梦想家, 追光)
    -- 如果当前值不在这3个里面，且不是NULL，则视为脏数据
    (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- =========================================================
    -- C. 唯一性与重复性
    -- =========================================================
    OR
    -- 规则17,19,20
    (calc_order_dup_count > 1)

    OR
    -- 规则18
    (calc_vin_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证编辑规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果: 校验不通过
- 修改【规则集-规则包rule01】中的校验规则:
- 期望值: >= -200
  - 配置成功
- 新建规则任务task02并立即执行:
1) 监控对象: t2 (与dwd_voyah_vehicle_sales_dates同表结构同数据)
2) 监控规则: rule01中的校验规则
  - 任务运行成功, 校验结果: 校验通过
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果: 校验不通过
##### 验证删除规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 新建监控规则, 配置监控对象(dwd_voyah_vehicle_sales_dates)后点击下一步
  - 进入【新建单表校验规则 ❯ 监控规则】配置页面
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果正确: 校验不通过
- 删除规则集1
  - 删除成功
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果正确: 校验不通过
#### Doris3.x ❯

##### 验证规则任务配置规则包后校验正常(1规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE final_price < 0 OR final_price IS NULL;
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 3校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

有效性校验:
- 字段: vin
- 统计规则: 字符串长度 
- 校验方法: 固定制
- 期望值> 0

唯一性校验
- 字段: order_id
- 统计函数: 重复数
- 校验方法: 固定值
- 期望值: =0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE 
    -- 1. 完整性校验异常 (final_price < 0 或为 NULL)
    (final_price < 0 OR final_price IS NULL)
    
    OR
    
    -- 2. 有效性校验异常 (vin 长度 <= 0，包含 NULL 或 纯空格字符串)
    (length(trim(vin)) = 0 OR vin IS NULL)
    
    OR
    
    -- 3. 唯一性校验异常 (order_id 重复)
    order_id IN (
        SELECT order_id 
        FROM dwd_voyah_vehicle_sales_dates 
        GROUP BY order_id 
        HAVING COUNT(1) > 1
    );
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(2规则包 * 2校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_sales_time_quality
- 选择分区: pt_date='20260204' / data_type='abnormal_monotonicity'

2) 监控规则: 引入规则包1(一致性校验、合理性校验)、规则包2(时效性校验)

一致性校验: 
- 校验类型: 多表数据一致性对比
- 校验字段: /
- 校验表主键: order_id
- 对比表1: dwd_voyah_sales_time_quality02
- 输入分区: pt_date=20260203/data_type='normal'
- 对比表主键: order_id

合理性校验:
- 字段: order_id
- 统计函数: 数据变化趋势
- 过滤条件: /
- 排序字段: order_id
- 校验方法: 单调递增

时效性校验
- 字段: order_id
- 统计函数: 周期性校验（单字段时间差校验）
- 过滤条件: /
- 排序字段: order_id
- 时间差: <= 1 秒

时效性校验
- 字段: order_id
- 统计函数: 及时性校验（多字段时间差校验）
- 对比字段组1: order_time, payment_time
- 时间差: <= 1 秒
- 大小关系: order_time < payment_time

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
WITH iqr_stats AS (
    -- 计算 guide_price 的四分位数和 IQR 阈值
    SELECT 
        percentile_approx(guide_price, 0.25) as q1,
        percentile_approx(guide_price, 0.75) as q3,
        (percentile_approx(guide_price, 0.75) - percentile_approx(guide_price, 0.25)) * 1.5 as iqr_range
    FROM dwd_voyah_vehicle_sales_dates
),
duplicate_orders AS (
    -- 识别 order_id 重复的数据
    SELECT order_id
    FROM dwd_voyah_vehicle_sales_dates
    GROUP BY order_id
    HAVING COUNT(1) > 1
)

SELECT 
    t.*,
    CASE 
        WHEN t.final_price < 0 OR t.final_price IS NULL THEN '完整性校验异常:成交价<0或为空'
        WHEN length(trim(t.vin)) = 0 OR t.vin IS NULL THEN '有效性校验异常:VIN长度<=0'
        WHEN d.order_id IS NOT NULL THEN '唯一性校验异常:订单号重复'
        WHEN t.guide_price < (i.q1 - i.iqr_range) OR t.guide_price > (i.q3 + i.iqr_range) THEN '统计性校验异常:指导价IQR离群'
        ELSE NULL 
    END AS error_reason
FROM 
    dwd_voyah_vehicle_sales_dates t
CROSS JOIN iqr_stats i
LEFT JOIN duplicate_orders d ON t.order_id = d.order_id
WHERE 
    -- 1. 完整性校验: final_price < 0 或 NULL
    (t.final_price < 0 OR t.final_price IS NULL)
    
    -- 2. 有效性校验: vin 长度 <= 0 (含纯空格或NULL)
    OR (length(trim(t.vin)) = 0 OR t.vin IS NULL)
    
    -- 3. 唯一性校验: order_id 重复
    OR (d.order_id IS NOT NULL)
    
    -- 4. 统计性校验: guide_price 为 IQR 离群点
    OR (t.guide_price < (i.q1 - i.iqr_range) OR t.guide_price > (i.q3 + i.iqr_range));
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 10校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 唯一性校验: 计算 order_id 的重复次数
        COUNT(1) OVER(PARTITION BY order_id) as order_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- ---------------------------------------------------------
    -- 1. 完整性校验 (字段级)
    -- ---------------------------------------------------------
    -- 规则: vin 空串数=0 (即: 找出 vin 为空串的数据)
    (trim(vin) = '')
    
    OR 
    
    -- 规则: car_model 空值数=0 (即: 找出 car_model 为 NULL 的数据)
    (car_model IS NULL)
    
    OR 
    
    -- 规则: final_price 期望值 >= 0 (即: 找出 < 0 的数据)
    (final_price < 0)

    -- ---------------------------------------------------------
    -- 2. 有效性校验
    -- ---------------------------------------------------------
    -- 规则: vin 字符串长度 > 0 (即: 找出长度 <= 0 或 NULL 的数据)
    -- 注: 此规则与上面的"空串数"、"空值数"有重叠，此处做兜底
    OR (length(vin) <= 0 OR vin IS NULL)
    
    -- 规则: guide_price 取值范围 [0, 1000000] (即: 找出在此范围之外的数据)
    OR (guide_price < 0 OR guide_price > 1000000)
    
    -- 规则: car_model 枚举个数 = 3 (即: 找出不属于这3个标准车型的数据)
    -- 假设标准枚举值为: 岚图FREE, 岚图梦想家, 岚图追光
    -- 注: NULL值已被前面的完整性校验捕获，此处主要校验"未知枚举值"
    OR (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- ---------------------------------------------------------
    -- 3. 唯一性校验
    -- ---------------------------------------------------------
    -- 规则: order_id 重复数/重复率期望为0 (即: 找出重复出现的订单)
    OR (order_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(20规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 针对规则 17,19,20: 计算 order_id 重复数
        COUNT(1) OVER(PARTITION BY order_id) as calc_order_dup_count,
        -- [辅助计算] 针对规则 18: 计算 vin 重复数
        COUNT(1) OVER(PARTITION BY vin) as calc_vin_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- =========================================================
    -- A. 完整性校验
    -- =========================================================
    -- 规则1: vin 空串
    (trim(vin) = '') OR
    -- 规则2-10: 各关键字段判空
    (vin IS NULL) OR
    (order_id IS NULL) OR
    (car_model IS NULL) OR
    (guide_price IS NULL) OR
    (final_price IS NULL) OR
    (dealer_name IS NULL) OR
    (order_status IS NULL) OR
    (factory_date IS NULL) OR
    (sale_date IS NULL)

    -- =========================================================
    -- B. 取值范围与有效性 (规则 11, 14, 15, 16)
    -- =========================================================
    OR 
    -- 规则11: final_price < 0
    (final_price < 0)
    
    OR
    -- 规则14: vin 长度 <= 0 (注: 空串/NULL已被上面捕获，此处兜底)
    (length(vin) <= 0)

    OR
    -- 规则15: guide_price 超出 [0, 1000000] 范围
    (guide_price < 0 OR guide_price > 1000000)

    OR
    -- 规则16: car_model 枚举校验 (假设标准值为3个: FREE, 梦想家, 追光)
    -- 如果当前值不在这3个里面，且不是NULL，则视为脏数据
    (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- =========================================================
    -- C. 唯一性与重复性 (规则 17, 18, 19, 20)
    -- =========================================================
    OR
    -- 规则17,19,20: order_id 出现重复 (统计数 > 1)
    (calc_order_dup_count > 1)

    OR
    -- 规则18: vin 出现重复 (统计数 > 1)
    (calc_vin_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证编辑规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 新建监控规则, 配置监控对象(dwd_voyah_vehicle_sales_dates)后点击下一步
  - 进入【新建单表校验规则 ❯ 监控规则】配置页面
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果: 校验不通过
- 修改【规则集-规则包rule01】中的校验规则:
- 期望值: >= -200
  - 配置成功
- 新建规则任务task02并立即执行:
1) 监控对象: t2 (与dwd_voyah_vehicle_sales_dates同表结构同数据)
2) 监控规则: rule01中的校验规则
  - 任务运行成功, 校验结果: 校验通过
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果: 校验不通过
##### 验证删除规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 新建监控规则, 配置监控对象(dwd_voyah_vehicle_sales_dates)后点击下一步
  - 进入【新建单表校验规则 ❯ 监控规则】配置页面
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果正确: 校验不通过
- 删除规则集1
  - 删除成功
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果正确: 校验不通过
#### Hive2.x ❯

##### 验证规则任务配置规则包后校验正常(1规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE final_price < 0 OR final_price IS NULL;
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 3校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验、有效性校、唯一性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

有效性校验:
- 字段: vin
- 统计规则: 字符串长度 
- 校验方法: 固定制
- 期望值> 0

唯一性校验
- 字段: order_id
- 统计函数: 重复数
- 校验方法: 固定值
- 期望值: =0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
  - 配置成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 1) 显示表数据中未通过的数据
2) 状态: 只要有一个未通过, 状态为: 校验异常
- SQL验证:
SELECT * FROM dwd_voyah_vehicle_sales_dates WHERE 
    -- 1. 完整性校验异常 (final_price < 0 或为 NULL)
    (final_price < 0 OR final_price IS NULL)
    
    OR
    
    -- 2. 有效性校验异常 (vin 长度 <= 0，包含 NULL 或 纯空格字符串)
    (length(trim(vin)) = 0 OR vin IS NULL)
    
    OR
    
    -- 3. 唯一性校验异常 (order_id 重复)
    order_id IN (
        SELECT order_id 
        FROM dwd_voyah_vehicle_sales_dates 
        GROUP BY order_id 
        HAVING COUNT(1) > 1
    );
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(2规则包 * 2校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_sales_time_quality
- 选择分区: pt_date='20260204' / data_type='abnormal_monotonicity'

2) 监控规则: 引入规则包1(一致性校验、合理性校验)、规则包2(时效性校验)

一致性校验: 
- 校验类型: 多表数据一致性对比
- 校验字段: /
- 校验表主键: order_id
- 对比表1: dwd_voyah_sales_time_quality02
- 输入分区: pt_date=20260203/data_type='normal'
- 对比表主键: order_id

合理性校验:
- 字段: order_id
- 统计函数: 数据变化趋势
- 过滤条件: /
- 排序字段: order_id
- 校验方法: 单调递增

时效性校验
- 字段: order_id
- 统计函数: 周期性校验（单字段时间差校验）
- 过滤条件: /
- 排序字段: order_id
- 时间差: <= 1 秒

时效性校验
- 字段: order_id
- 统计函数: 及时性校验（多字段时间差校验）
- 对比字段组1: order_time, payment_time
- 时间差: <= 1 秒
- 大小关系: order_time < payment_time

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- 编辑规则任务rule01, 分区: pt_date='20260201' / data_type='normal'
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(1规则包 * 10校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 唯一性校验: 计算 order_id 的重复次数
        COUNT(1) OVER(PARTITION BY order_id) as order_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- ---------------------------------------------------------
    -- 1. 完整性校验 (字段级)
    -- ---------------------------------------------------------
    -- 规则: vin 空串数=0 (即: 找出 vin 为空串的数据)
    (trim(vin) = '')
    
    OR 
    
    -- 规则: car_model 空值数=0 (即: 找出 car_model 为 NULL 的数据)
    (car_model IS NULL)
    
    OR 
    
    -- 规则: final_price 期望值 >= 0 (即: 找出 < 0 的数据)
    (final_price < 0)

    -- ---------------------------------------------------------
    -- 2. 有效性校验
    -- ---------------------------------------------------------
    -- 规则: vin 字符串长度 > 0 (即: 找出长度 <= 0 或 NULL 的数据)
    -- 注: 此规则与上面的"空串数"、"空值数"有重叠，此处做兜底
    OR (length(vin) <= 0 OR vin IS NULL)
    
    -- 规则: guide_price 取值范围 [0, 1000000] (即: 找出在此范围之外的数据)
    OR (guide_price < 0 OR guide_price > 1000000)
    
    -- 规则: car_model 枚举个数 = 3 (即: 找出不属于这3个标准车型的数据)
    -- 假设标准枚举值为: 岚图FREE, 岚图梦想家, 岚图追光
    -- 注: NULL值已被前面的完整性校验捕获，此处主要校验"未知枚举值"
    OR (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- ---------------------------------------------------------
    -- 3. 唯一性校验
    -- ---------------------------------------------------------
    -- 规则: order_id 重复数/重复率期望为0 (即: 找出重复出现的订单)
    OR (order_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证规则任务配置规则包后校验正常(20规则包 * 1校验规则)

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 选择任务rule01, 立即执行
  - 执行成功, 校验结果: 校验不通过
- 进入「校验结果查询」, 检查规则任务详情页
  - 显示表数据中未通过的数据
- SQL验证:
SELECT 
    *
FROM (
    SELECT 
        *,
        -- [辅助计算] 针对规则 17,19,20: 计算 order_id 重复数
        COUNT(1) OVER(PARTITION BY order_id) as calc_order_dup_count,
        -- [辅助计算] 针对规则 18: 计算 vin 重复数
        COUNT(1) OVER(PARTITION BY vin) as calc_vin_dup_count
    FROM 
        dwd_voyah_vehicle_sales_dates
) t
WHERE 
    -- =========================================================
    -- A. 完整性校验 (规则 1-10: 这里的逻辑是"找出为空或NULL的数据")
    -- =========================================================
    -- 规则1: vin 空串
    (trim(vin) = '') OR
    -- 规则2-10: 各关键字段判空
    (vin IS NULL) OR
    (order_id IS NULL) OR
    (car_model IS NULL) OR
    (guide_price IS NULL) OR
    (final_price IS NULL) OR
    (dealer_name IS NULL) OR
    (order_status IS NULL) OR
    (factory_date IS NULL) OR
    (sale_date IS NULL)

    -- =========================================================
    -- B. 取值范围与有效性 (规则 11, 14, 15, 16)
    -- =========================================================
    OR 
    -- 规则11: final_price < 0
    (final_price < 0)
    
    OR
    -- 规则14: vin 长度 <= 0 (注: 空串/NULL已被上面捕获，此处兜底)
    (length(vin) <= 0)

    OR
    -- 规则15: guide_price 超出 [0, 1000000] 范围
    (guide_price < 0 OR guide_price > 1000000)

    OR
    -- 规则16: car_model 枚举校验 (假设标准值为3个: FREE, 梦想家, 追光)
    -- 如果当前值不在这3个里面，且不是NULL，则视为脏数据
    (car_model IS NOT NULL AND car_model NOT IN ('岚图FREE', '岚图梦想家', '岚图追光'))

    -- =========================================================
    -- C. 唯一性与重复性 (规则 17, 18, 19, 20)
    -- =========================================================
    OR
    -- 规则17,19,20: order_id 出现重复 (统计数 > 1)
    (calc_order_dup_count > 1)

    OR
    -- 规则18: vin 出现重复 (统计数 > 1)
    (calc_vin_dup_count > 1);
  - 返回的数据与规则过滤出的数据一致
- 编辑规则任务rule01, 变更分区: factory_date=20260115/sale_date=20260201
其它配置不变, 保存后重新执行任务
  - 执行成功, 校验结果: 校验通过
##### 验证编辑规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 新建监控规则, 配置监控对象(dwd_voyah_vehicle_sales_dates)后点击下一步
  - 进入【新建单表校验规则 ❯ 监控规则】配置页面
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果: 校验不通过
- 修改【规则集-规则包rule01】中的校验规则:
- 期望值: >= -200
  - 配置成功
- 新建规则任务task02并立即执行:
1) 监控对象: t2 (与dwd_voyah_vehicle_sales_dates同表结构同数据)
2) 监控规则: rule01中的校验规则
  - 任务运行成功, 校验结果: 校验通过
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果: 校验不通过
##### 验证删除规则集后, 对已配置过历史规则的任务不生效

- 进入【数据资产】-【数据质量】-【规则任务管理】页面
  - 进入成功
- 新建监控规则, 配置如下:
1) 监控对象: 
- 规则名称: rule01
- 数据表: dwd_voyah_vehicle_sales_dates
- 选择分区: /

2) 监控规则: 引入规则包、规则类型(完整性校验)

完整性校验: 
- 生效范围: 字段级
- 字段: final_price
- 统计函数: 字段取值校验
- 期望值: >= 0

3) 调度属性:
- 调度周期: 时
- 生效日期: T~T+1
- 间隔时间: 1小时
- 其它默认
- 实例生成方式: 「立即生成」
  - 配置成功
- 新建监控规则, 配置监控对象(dwd_voyah_vehicle_sales_dates)后点击下一步
  - 进入【新建单表校验规则 ❯ 监控规则】配置页面
- 引入规则包rule01中所有校验规则
  - 引入成功, 配置参数正确
- 保存规则任务task01后, 立即执行
  - 任务运行成功, 校验结果正确: 校验不通过
- 删除规则集1
  - 删除成功
- 重新运行历史规则任务task01, 在【校验结果查询】中查看运行状态
  - 任务运行成功, 校验结果正确: 校验不通过
