# 【数据质量】质量校验不通过时支持发送邮件，邮件信息需要包含未通过的明细数据(#10307)（XMind）
> 来源：zentao-cases/XMind/数据资产/202603-数据资产v6.4.9.xmind

---

### 验证「调度属性配置」页面-告警方式选择「邮箱」时显示「是否发送明细数据」选项

#### 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错

#### 点击【新建监控规则-单表校验规则】按钮，
- 「规则名称」字段输入「测试规则A」
- 「选择数据源」字段选择「${datasource}」
- 「选择数据库」字段选择「${database}」
- 「选择数据表」字段选择「${table}」
- 成功进入「监控规则配置」页面，监控对象配置完成

#### 点击【下一步】按钮，添加完整性校验规则，点击【下一步】按钮进入「调度属性配置」页面
- 成功进入「调度属性配置」页面

#### 在「告警配置」区域，勾选「邮箱」告警方式
- 「邮箱」告警方式勾选成功

#### 查看「邮箱」告警方式下方显示内容
- 「邮箱」告警方式下方显示「是否发送明细数据」单选框，包含「是」「否」两个选项，默认选择「是」

#### 勾选其它告警方式，查看下方显示内容
- 其它告警方式下方不显示「是否发送明细数据」选项

#### 检查多表比对规则和规则集的选项
- 显示正常

### 验证「是否发送明细数据」支持切换选择

#### 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错

#### 点击【新建监控规则】按钮，配置监控对象，点击【下一步】按钮
- 成功进入「监控规则配置」页面

#### 添加完整性校验规则，点击【下一步】按钮进入「调度属性配置」页面
- 成功进入「调度属性配置」页面

#### 在「告警配置」区域，勾选「邮箱」告警方式
- 「邮箱」告警方式勾选成功

#### 在「是否发送明细数据」字段选择「否」
- 「否」选项选中成功，选项高亮显示

#### 在「是否发送明细数据」字段选择「是」
- 「是」选项选中成功，选项高亮显示

#### 点击【完成】按钮保存规则
- 规则保存成功，相关页面/数据状态更新为最新

#### 检查多表比对规则和规则集的选项切换
- 正常切换

### 单表

#### 验证单表规则校验不通过时邮件附件发送功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 点击【新建监控规则】按钮，配置监控对象为test_table，点击【下一步】按钮
- 成功进入「监控规则配置」页面
- 添加完整性校验规则：
- 「字段」字段选择「name」
- 「统计函数」字段选择「空值数」
- 「校验方法」字段选择「固定值」
- 「期望值」字段输入「=0」，点击【保存】按钮
- 完整性校验规则添加成功
- 点击【下一步】按钮，在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「是」
- 点击【完成】按钮保存规则
- 规则保存成功，系统给出成功反馈
- 选择该规则，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- ZIP附件命名为「不符合规则的明细数据」，解压后包含一个Excel文件，命名为「完整性校验_name_空值数.xlsx」，Excel中包含name字段为NULL的明细数据（id=2的记录）

#### 验证单表规则多个子规则校验不通过时邮件附件生成正确(不同判断条件)
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 点击【新建监控规则】按钮，配置监控对象为test_table，点击【下一步】按钮
- 成功进入「监控规则配置」页面
- 添加完整性校验规则1：在「字段」字段选择「name」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=0」，点击【保存】按钮
- 完整性校验规则1添加成功
- 添加完整性校验规则2：在「字段」字段选择「id」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=0」，点击【保存】按钮
- 完整性校验规则2添加成功
- 添加完整性校验规则3：在「字段」字段选择「age」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=0」，点击【保存】按钮
- 完整性校验规则3添加成功
- 点击【下一步】按钮，在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「是」
- 点击【完成】按钮保存规则
- 规则保存成功
- 选择该规则，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件包含附件
- 下载邮件附件，查看ZIP包内容
- ZIP附件命名为「不符合规则的明细数据」，解压后包含2个Excel文件：
- 文件1命名为「完整性校验_name_空值数.xlsx」（包含name为NULL的数据）
- 文件2命名为「完整性校验_id_空值数.xlsx」（包含id为NULL的数据）
- age字段无空值故不生成对应Excel

#### 验证单表规则多个子规则校验不通过时邮件附件生成正确(相同判断条件, 不同期望值)
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 点击【新建监控规则】按钮，配置监控对象为test_table，点击【下一步】按钮
- 成功进入「监控规则配置」页面
- 添加完整性校验规则1：在「字段」字段选择「name」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=0」，点击【保存】按钮
- 完整性校验规则1添加成功
- 添加完整性校验规则2：在「字段」字段选择「name」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=1」，点击【保存】按钮
- 完整性校验规则2添加成功
- 添加完整性校验规则3：在「字段」字段选择「name」，在「统计函数」字段选择「空值数」，在「校验方法」字段选择「固定值」，在「期望值」字段输入「=2」，点击【保存】按钮
- 完整性校验规则3添加成功
- 点击【下一步】按钮，在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「是」
- 点击【完成】按钮保存规则
- 规则保存成功
- 选择该规则，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件包含附件
- 下载邮件附件，查看ZIP包内容
- ZIP附件命名为「不符合规则的明细数据」，解压后包含3个Excel文件：
- 文件1命名为「完整性校验_name_空值数_xxxx.xlsx」（包含name为NULL的数据）
- 文件2命名为「完整性校验_name_空值数_xxxx.xlsx」（包含name为NULL的数据）

#### 验证单表规则「是否发送明细数据」选择「否」时不发送附件
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 新建单表校验规则，配置完整性校验规则，使规则校验不通过
- 规则配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「否」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「否」
- 保存规则并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含附件

#### 验证单表规则脏数据存储关闭时不发送明细数据附件
- 进入【数据质量】-【脏数据管理】页面
- 成功进入【脏数据管理】页面
- 选择${datasource}数据源，确认脏数据存储状态为「关闭」
- ${datasource}数据源脏数据存储状态为「关闭」
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面
- 新建单表校验规则，配置完整性校验规则，使规则校验不通过
- 规则配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功
- 保存规则并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含明细数据附件（因脏数据存储未开启）

#### 验证编辑单表规则-告警配置回显「是否发送明细数据」正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则A，点击【编辑调度属性】按钮
- 弹出「编辑调度属性」弹窗
- 查看「告警配置」区域「是否发送明细数据」回显内容
- 「是否发送明细数据」回显为「是」，与原配置一致

#### 验证编辑单表规则修改「是否发送明细数据」后功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则A，点击【编辑调度属性】按钮
- 弹出「编辑调度属性」弹窗
- 在「是否发送明细数据」字段选择「是」
- 「是否发送明细数据」修改为「是」，选项高亮显示
- 点击【保存】按钮
- 保存成功，系统给出成功反馈
- 选择规则A，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件包含ZIP附件，附件命名为「不符合规则的明细数据」

### 多表

#### 验证多表规则校验不通过时邮件附件发送功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 点击【新建监控规则】按钮，选择「多表比对」规则类型
- 进入多表比对规则配置页面
- 配置源表和目标表，选择比对字段和比对规则
- 源表和目标表配置成功
- 点击【下一步】按钮，在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「是」
- 点击【完成】按钮保存规则
- 规则保存成功
- 选择该规则，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- ZIP附件命名为「不符合规则的明细数据」，解压后包含Excel文件，每个异常类型生成一个Excel，命名格式为异常类型名称（如「逻辑主键匹配，但数据不匹配.xlsx」等）

#### 验证多表规则「是否发送明细数据」选择「否」时不发送附件
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 新建多表比对规则，配置源表和目标表及比对规则
- 规则配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「否」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「否」
- 保存规则并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含附件

#### 验证多表规则脏数据存储关闭时不发送明细数据附件
- 进入【数据质量】-【脏数据管理】页面
- 成功进入【脏数据管理】页面
- 选择${datasource}数据源，确认脏数据存储状态为「关闭」
- ${datasource}数据源脏数据存储状态为「关闭」
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面
- 新建多表比对规则，配置源表和目标表及比对规则，使规则校验不通过
- 规则配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功
- 保存规则并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含明细数据附件

#### 验证编辑多表规则-告警配置回显「是否发送明细数据」正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则B，点击【编辑调度属性】按钮
- 弹出「编辑调度属性」弹窗
- 查看「告警配置」区域「是否发送明细数据」回显内容
- 「是否发送明细数据」回显为「否」，与原配置一致

#### 验证编辑多表规则修改「是否发送明细数据」后功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则A，点击【编辑调度属性】按钮
- 弹出「编辑调度属性」弹窗
- 在「是否发送明细数据」字段选择「是」
- 「是否发送明细数据」修改为「是」，选项高亮显示
- 点击【保存】按钮
- 保存成功，系统给出成功反馈
- 选择规则A，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- ZIP附件命名为「不符合规则的明细数据」，解压后包含Excel文件，每个异常类型生成一个Excel，命名格式为异常类型名称（如「逻辑主键匹配，但数据不匹配.xlsx」等）

### 规则集

#### 验证规则集校验不通过时邮件附件发送功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 点击【新建规则集】按钮，配置基础信息
- 进入规则集配置页面
- 导入规则集文件，点击【下一步】按钮
- 规则集文件导入成功，显示规则内容
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「是」
- 点击【完成】按钮保存规则集
- 规则集保存成功
- 选择该规则集，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- ZIP附件命名为「不符合规则的明细数据」，解压后包含Excel文件，每个校验不通过的规则生成一个Excel，命名格式为「{规则名称}.xlsx」

#### 验证规则集「是否发送明细数据」选择「否」时不发送附件
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 新建规则集，配置基础信息并导入规则集文件
- 规则集配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「否」，选择告警接收人为用户A
- 告警配置成功，「是否发送明细数据」选择「否」
- 保存规则集并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含附件

#### 验证规则集脏数据存储关闭时不发送明细数据附件
- 进入【数据质量】-【脏数据管理】页面
- 成功进入【数据源管理】页面
- 选择${datasource}数据源，确认脏数据存储状态为「关闭」
- ${datasource}数据源脏数据存储状态为「关闭」
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面
- 新建规则集，配置基础信息并导入规则集文件，使规则校验不通过
- 规则集配置成功
- 在「告警配置」区域勾选「邮箱」告警方式，在「是否发送明细数据」字段选择「是」，选择告警接收人为用户A
- 告警配置成功
- 保存规则集并立即运行，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- 用户A收到告警邮件，邮件内容为文本告警信息，不包含明细数据附件

#### 验证编辑规则集-告警配置回显「是否发送明细数据」正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则集C，点击右上角【…】-【编辑】按钮
- 进入规则集编辑页面
- 点击【下一步】按钮进入「调度配置」页面
- 成功进入「调度配置」页面
- 查看「告警配置」区域「是否发送明细数据」回显内容
- 「是否发送明细数据」回显为「是」，与原配置一致

#### 验证编辑规则集修改「是否发送明细数据」后功能正确
- 进入【数据资产】-【数据质量】-【规则任务配置】页面
- 成功进入【规则任务配置】页面，页面内容正常加载显示，无报错
- 选择规则A，点击【编辑调度属性】按钮
- 弹出「编辑调度属性」弹窗
- 在「是否发送明细数据」字段选择「是」
- 「是否发送明细数据」修改为「是」，选项高亮显示
- 点击【保存】按钮
- 保存成功，系统给出成功反馈
- 选择规则A，点击【立即运行】按钮，等待任务执行完成
- 任务执行完成，校验结果为「不通过」
- 登录用户A邮箱，查看收到的告警邮件
- ZIP附件命名为「不符合规则的明细数据」，解压后包含Excel文件，每个校验不通过的规则生成一个Excel，命名格式为「{规则名称}.xlsx」
