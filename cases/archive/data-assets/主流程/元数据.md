---
suite_name: 元数据（XMind）
description: 元数据（XMind）
prd_path: ""
product: data-assets
tags:
  - 数据资产
  - 数据地图
  - 元数据同步
  - 元模型管理
  - 元数据管理
  - 元数据
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 0
origin: xmind
---
# 元数据（XMind）
> 来源：zentao-cases/XMind/数据资产/数据资产-主流程用例.xmind

---

### 数据地图

#### 首页【资产类型】功能验证
- 验证【数据表】表数量统计正确
- 1. 新增元数据同步-周期同步任务
2. 选择${DataSourceType}数据源，选择catalog||schema||database
3. 选择数据表-table1
4. 勾选【全部内容】
5. 点击【临时同步】按钮
- 元数据同步任务创建成功，任务状态-运行中(实例状态运行中)
- 任务运行完成,实例运行成功
- 【数据表】统计数量+1
- 数据地图选择table1，删除元表
- 【数据表】统计数量-1
- 验证【离线任务】任务数量统计正确
- 1. 离线平台新增SQL 建表任务
2. 临时运行/提交
- 【离线任务】任务统计数量+1

#### 【数据地图】-二级页面【筛选过滤】功能验证
- 验证筛选条件组合查询功能正常
- 选择【查询结果类型】为【数据表】
- 选择成功
- 选择【数据源类型】为${DATASOURCE_TYPE}
- 1. 筛选框展示资产平台引入的所有数据源类型
2. 数据源类型数量统计正确
3. 选择成功
- 选择【数据源】为${DATASOURCE_NAME}
- 1. 展示资产平台已引入${DATASOURCE_TYPE}类型的所有数据源名称
2. 选择成功
- 选择【数据库】为${DATABASE}
- 1. 如果当前数据源-数据库下没有同步任何表到资产平台，则展示为空
2. 如果当前数据源-数据库下已经同步${TABLE_NAME}到资产平台，则【数据库】选择${DATABASE}成功
- 选择【负责人】为${TABLE_OWNER}
- 1. 如果存在表指定负责人，则【负责人】选择${TABLE_OWNER}成功
2. 如果没有表指定负责人，则【负责人】展示为空
- 选择【表标签】为${TABLE_TAG}
- 1. 如果存在表设置表标签，则【表标签】选择${TABLE_TAG}成功
2. 如果没有表设置表标签，则【表标签】展示为空
- 设置完所有筛选条件后，筛选
- 1. 过滤出所有符合组合条件的数据
2. 没有匹配条件的数据则展示“暂无数据”缺省页

#### 【数据地图】-【数据表】详情页面功能验证
- 验证删除表功能正常
- 点击【删除表】按钮
- 弹二次确认弹窗
- 删除表二次弹窗UI CHECK
- 确认删除表${TABLE}吗？
选择删除元数据表，删除后将影响子产品引用该表的所有任务，且资产平台中相关信息也会删除。
删除方式 - 【删除元数据表】/【删除源表】
表名输入框
取消，删除按钮
- 选择【删除方式】为“删除元数据表”，输入表名${TABLE}, 点击删除按钮
- 删除成功，资产平台不展示${TABLE}表，底层表不删除
- 选择【删除方式】为“删除源表”，输入表名${TABLE}, 点击删除按钮
- 删除成功，资产平台不展示${TABLE}表，底层表也删除
- 验证导出表功能正常
- 点击【导出表】按钮
- 弹二次确认弹窗
- 导出元数据弹窗UI CHECK
- 确认导出元数据？
取消，确定按钮
- 点击【确定】按钮
- 元数据下载_日期.csv文件导出成功
- 查看CSV文件内容
- 表名，字段名，字段中文名，字段描述，数据类型，字段标签，SQL信息，技术属性，业务属性，表描述信息，表标签，任务依赖，服务依赖 信息展示正确
- 验证表订阅功能正常
- 点击【订阅】按钮
- 弹【订阅】弹窗
- 【订阅】弹窗UI CHECK
- title 展示“订阅”，鼠标hover“？”提示“完成订阅后，当元数据发生变更时，系统会按订阅的提醒方式发出通知。”
告警方式，鼠标hover“？”提示“各个告警通道需要先在公共管理配置默认告警通道，否则配置后无效”
展示【邮箱】【钉钉】【自定义】通道选择
取消，确定，关闭弹窗“X”按钮
- 选择【邮箱】/【钉钉】告警，如果钉钉输入【webhook】地址
- 配置成功
- 点击确认
- 订阅成功，订阅按钮展示为“取消订阅”
- 进入【元数据】-【订阅的数据】页面
- 展示表A订阅信息
- 点击【取消订阅】按钮，二次确认
- 取消订阅成功，右侧订阅按钮展示为“订阅”
- 进入【元数据】-【订阅的数据】页面
- 隐藏表A订阅信息
- 验证【表结构】-【建表语句】模块功能正常
- 点击【建表语句】按钮
- 建表语句显示正确
- 复制建表语句，去底层执行SQL
- 执行成功
- 验证【数据预览】功能正常
- 点击表详情【数据预览】按钮
- 表所有字段信息，以及表内前10条数据展示正确
- 空白页展示
- 展示“暂无数据”
- 验证【血缘关系】功能正常
- 点击表详情【血缘关系】按钮
- 展示【表级血缘】【字段级血缘】【展示文字信息】【资产类型】【导航器】【表按钮】
- 点击【表级血缘】按钮
- 展示当前表的上下游表
- ！文案校验
- 每个节点都可右击查看该节点的全链路血缘，存在逆向可展开节点时建议右击查看~
- 点击中央【表按钮】
- 弹表详情弹窗
- 表详情弹窗内容校验
- 展示“表名””复制按钮”“技术属性模块”“业务属性模块“字段信息”“非分区字段”“分区字段””字段名称字段中文名搜索框“
- 输入存在的字段名称，字段中文名，点击搜索
- 返回匹配的字段信息
- 输入不存在的字段名称，字段中文名，点击搜索
- 展示“暂无数据”
- 点击分页，切换pagesize
- pagesize切换成功，数据展示正确，跳转正确
- 点击【字段级血缘】
- 展示当前表所有字段及血缘关系
- 点击【字段名称】
- 存在血缘字段时，弹血缘字段，没有血缘字段时，提示“字段血缘关系为空”
- 点击【展示文字信息】按钮
- 展示当前表全名
- 点击【居中】按钮
- 图居中展示
- 点击【放大】按钮
- 图放大
- 点击【放小】按钮
- 图放小
- 点击【下载】按钮
- 下载血缘PNG成功，查看内容正确
- 点击【展示/隐藏】按钮
- 展示/不展示导航器

### 元数据同步

#### 验证元数据同步任务创建流程正常
- 1）点击【元数据】-【元数据同步】-【新增周期同步任务】
2）选择数据源${DATASOURCE_TYPE}
3）选择数据库${DATABASE}
4）选择数据表${TABLE}
5）依次点击【添加】_【下一步】_【新增】按钮
- “新增周期同步任务”成功，任务列表展示同步任务，列信息正确，同步状态展示正常

#### 验证【元数据同步-临时运行】功能正常
- 新增「元数据同步任务」，选择doris库表，库表选择情况见前置条件，点击「临时运行」
- 新增一条元数据同步实例，运行中
- 结束后查看同步结果
- 同步成功，同步结果与前置结果一致
- 进入数据地图首页查看
- 数据源类型表数量及数据表数量统计新增正确
- 在数据地图查看表
- 存在元数据同步选择的表，表信息正确

#### 验证【元数据同步-周期同步】功能正常
- 新增「元数据同步任务」，选择doris数据源-全部数据库，配置调度周期：天，00:00
- 新增一条周期同步实例
- 底层创建前置中的doris表，插入数据
- 建表成功
- 第二天0点后查看结果
- 同步成功
- 进入数据地图首页查看
- 数据源类型表数量及数据表数量统计新增正确
- 在数据地图查看表
- 存在前置中的数据表，表详情正确

#### 验证【元数据同步】_【常规表】_【全类型字段】同步正常
- 1）点击【元数据】-【元数据同步】-【新增周期同步任务】
2）选择数据源${DATASOURCE_TYPE}
3）选择数据库${DATABASE}
4）选择数据表${TABLE}
5）依次点击【添加】_【下一步】_【新增】按钮
- 表同步成功， 且表详情信息展示正确， 数据预览正确

#### 验证【元数据同步】_【视图表】同步正常
- 元数据同步任务，同步常规视图表；
- 同步任务成功，视图表详情页元数据信息正确

#### 验证【元数据同步】_【异常场景】
- 1） 新增完元数据同步任务A，点击立即运行
2） agentShell sidercar 服务不健康
- 运行失败，报错agentsidercar服务不通
- 1） 新增完元数据同步任务A，点击立即运行
2） 运行过程中，删除同步任务A中选择的表
- 运行失败，报错表找不到

#### 验证离线修改Hive表的生命周期同步至资产
- 离线SparkSQL任务修改表生命周期：
ALTER TABLE student_lifecycle SET TBLPROPERTIES ('lifecycle'='12');
等待同步；
查看资产-数据地图-该表表详情-基本信息
- 生命周期更新正确

#### 验证离线建表【自动同步】到资产功能正常
- 进入离线，执行前置中的doris建表语句
- 建表成功
- 进入资产数据地图，搜索表test_001
- 能搜索到新建的表
- 查看表详情
- 信息正确

### 元模型管理

#### 验证通用业务属性-新增功能-逻辑正常
- 新增枚举属性：
1）属性类型为“枚举”
2）输入表单内容
3）点击【确定】
- 列表中显示新建的业务属性，且数据正确
- 新增string类型文本框属性：
1）属性类型为“文本框”，字段类型为“string”
2）输入表单内容
3）点击【确定】
- 列表中显示新建的业务属性，且数据正确
- 新增bigint类型文本框属性：
1）属性类型为“文本框”，字段类型为“bigint”
2）输入表单内容
3）点击【确定】
- 列表中显示新建的业务属性，且数据正确
- 新增树形目录属性：
1）属性类型为“树形目录”
2）输入表单内容
3）点击【确定】
- 列表中显示新建的业务属性，且数据正确
- 进入${DATASOURCE_TYPE}数据源类型的数据表详情页，查看「业务属性」
- 显示此新增的业务属性名

#### 验证通用业务属性-生效功能正常
- 进入${DATASOURCE_TYPE}数据源类型的数据表详情页，查看右侧「业务属性」
- 业务属性展示包含通用业务属性

#### 验证通用业务属性-删除功能-逻辑正常
- 删除业务属性${SX}
- 列表不显示业务属性${SX}
- 进入${DATASOURCE_TYPE}数据源类型的数据表详情页，查看「业务属性」
- 该业务属性不显示

### 元数据管理

#### 验证数据表列表-数据展示正确
- 位置：元数据-元数据管理-${DATASOURCE _TYPE}数据源-某数据库
查看列表
- 列表展示当前数据库下数据表+视图信息正确（数据与表详情页一直即可）：
		表名、表中文名、创建时间、存储大小、更新时间

#### 验证数据库列表-导入元数据功能正确
- 点击【导入元数据】
- 原“导入元数据”弹窗去除“选择数据源”，只保留“上传文件”组件；
- 准备元数据文件（只包含当前数据源的元数据表信息）；
上传文件；
点击确定
- 导入元数据成功；
数据库列表数据刷新
- 准备元数据文件（包含其他数据源的元数据表信息）；
上传文件；
点击确定
- 导入元数据失败；
错误文件内容，错误信息提示正确

#### 验证数据库列表-导出元数据功能正确
- 点击【导出元数据】
- 原“导入元数据”弹窗去除“选择数据源”，只保留“数据库”选择项；
- 选择具体数据库；
点击【确定】
- 导出文件内容为所选数据库下，所有元数据（表+视图）信息
- 选择“全部”；
点击【确定】
- 导出文件内容为当前数据源下，所有元数据（表+视图）信息

#### 验证数据库列表-生命周期应用功能正确
- tableB（未同步至资产）所属数据源和所属数据库为：datasourceB、schemaB
只配置datasourceB的生命周期为3天；schemaB未设置生命周期；
将tableB同步至资产；
查看tableB的生命周期
- tableB的生命周期为3天
- tableC（未同步至资产）所属数据源和所属数据库为：datasourceC、schemaC
datasourceC未设置生命周期；schemaC设置生命周期为10天；
将tableC同步至资产；
查看tableC的生命周期
- tableC的生命周期为10天
- tableA（未同步至资产）所属数据源和所属数据库为：datasourceA、schemaA
先配置datasourceA的生命周期为3天；后配置schemaA的生命周期为10天；
将tableA同步至资产；
查看tableA的生命周期
- tableA的生命周期为10天
