# 数据模型（XMind）
> 来源：zentao-cases/XMind/数据资产/数据资产-主流程用例.xmind

---

### 建表

#### 验证创建不同类型sparkThrift表功能正常
- 创建Hive PARQUET非事务内部表：
	- “表类型”选择“内部表”
	- “hdfs存储路径”为空
- 生成的建表语句正确；
PARQUET表创建成功；
- 创建Hive ORC非事务内部表：
	- “表类型”选择“内部表”
	- “hdfs存储路径”为空
- 生成的建表语句正确；
ORC表创建成功；
- 创建Hive TEXTFILE非事务内部表：
	- “表类型”选择“内部表”
	- “hdfs存储路径”为指定路径（非默认路径）
- 生成的建表语句正确；
TEXTFILE表创建成功；
- 创建Hive外部表：
	- “表类型”选择“外部表”
	- “hdfs存储路径”为指定路径（非默认路径）
- 生成的建表语句正确；
事务表创建成功；
该表存储路径正确
- 创建Hive分区表
- 生成的建表语句正确；
分区表创建成功；
该表分区字段正确

#### 验证数据模型-doris主键表功能正常
- 1）进入数据模型-新建表，选择doris数据源、数据库、数仓层级、模型元素
表名：自定义内容：「T_doris_A」
中文名：doris表A
数据模型类型：【主键表】
2）点击下一步
- 1）进入数据模型新建页面
2）进入表结构页面
- 新增字段，数据类型为前置中的类型
- 添加成功
- 新增字段ID，勾选主键，分区字段，动态分区，分桶字段(分桶数1)，编辑动态分区配置：同前置条件
- 新增成功
- 点击【生成建表语句】按钮，建表
- 建表语句正确，建表成功
- 点击表名
- 表详情展示正确
- 编辑模型，新增字段name
- 1）基础信息只有中文名和生命周期可以编辑，其他都不可编辑
2）表结构页面原有字段置灰不可编辑
3）新增字段只支持设置中文名、字段名、数据类型、动态分区的PROPERTIES设置属性、非null；不支持设置主键、分桶信息。
- 编辑动态分区的属性，建表
- 编辑成功，建表成功，表详情正确
- 点击模型的删除按钮-删除元数据表
- 删除成功，资产平台上表不存在，底层表还存在

#### 验证数据模型-doris聚合表功能正常
- 1）进入数据模型-新建表，选择doris数据源、数据库、数仓层级、模型元素
表名：自定义内容：「T_doris_B」
中文名：doris表B
数据模型类型：【聚合表】
2）点击下一步
- 1）进入数据模型新建页面
2）进入表结构页面
- 新增字段，数据类型为前置中的类型
- 添加成功
- 勾选字段id的指标列，查看展示
- 展示聚合类型下拉框
- 1）字段id，勾给排序键、非null（默认值1），分区字段（列表分区）、分桶数3
2）列表分区：log1:“log1_test1","log1_test2”、log2:“log2_test1","log2_test2”,"log3_test2”,点击保存
- 1）勾选成功
2）列表分区保存成功
- 根据前置条件2，选择所有聚合函数
- 选择成功
- 点击【生成建表语句】按钮，建表
- 建表语句正确，建表成功
- 点击表名
- 表详情展示正确
- 编辑模型，新增字段name
- 1）进入编辑页面，基础信息只有中文名和生命周期可以编辑，其他都不可编辑
2）表结构页面原有字段置灰不可编辑
3）新增字段只支持设置中文名、字段名、数据类型、指标列、聚合函数、非null；不支持设置排序键、分桶信息。
- 建表
- 建表成功，表详情正确
- 点击模型的删除按钮-删除源表
- 删除成功，资产平台和底层表都不存在

#### 验证数据模型-doris明细表功能正常
- 1）进入数据模型-新建表，选择doris数据源、数据库、数仓层级、模型元素
表名：自定义内容：「T_doris_C」
中文名：doris表C
2）点击下一步
- 1）进入数据模型新建页面
2）进入表结构页面
- 新增字段，数据类型为前置中的类型
- 添加成功
- 1）字段id，勾给排序键、非null（默认值1），分区字段（范围分区）、分桶数3
2）查看范围分区
3）添加分区名：log1:1、log2：2、log3：3
- 1）勾选成功
2）范围分区只有分区名和字段id两个列
3）添加成功
- 点击【生成建表语句】按钮，建表
- 建表语句正确，建表成功
- 点击表名
- 表详情展示正确
- 编辑模型，新增字段name
- 1）进入编辑页面，基础信息只有中文名和生命周期可以编辑，其他都不可编辑
2）表结构页面原有字段置灰不可编辑
3）新增字段只支持设置中文名、字段名、数据类型、非null；不支持设置排序键、分桶信息
- 建表
- 建表成功，表详情正确

#### 验证Mysql-CSV表-创建功能
- 新建模型，存储格式选择“CSV”；
- 点击【生成建表语句】
- 建表语句正确
- 确认建表
- 建表成功
- 进入表详情页
- 元数据信息展示正确

#### 验证建表语句解析功能正确
- 主键表建表语句解析
- 字段名、字段中文名、字段类型、字符长度、数值长度、字段精度、主键、分区字段、分区类型等解析成功
- 范围分区表语句解析
- 1）字段名、字段中文名、字段类型、字符长度、数值长度、字段精度、主键、分区字段、分区类型等解析成功
2）范围分区信息解析成功
- 各数据类型字段解析
- 所有支持的字段类型均解析

#### 验证「规范设计」-新增数仓层级功能正常
- 进入「资产」-「数据模型」-「规范建表」页面
- 进入成功
- 点击「规范设计」按钮
- 进入「规范设计」页面成功
- 点击「新增数据层级」按钮
- 弹「新增数据层级」弹窗
- 「中文名称」输入「新增层级」
「英文名称」输入「add_catalog」
「描述」输入「test _ desc」
- 配置完成
- 点击「确定」按钮
- 新增数仓层级成功
- 选择「新增层级」，点击「规范设计」按钮
- 弹「规范设计」弹窗
- 「模型元素」配置「业务系统」「主题域」，点击「确定」按钮
- 配置成功
- 进入「建表」页面，「数仓层级」选择「新增层级」
- 展示「新增层级」下的「规范设计内容」
- 新增表流程结束
- 新增表成功
- 查看表所属数仓层级
- 该表存储在「新增层级」下

#### 验证数据模型-审批功能
- 数据开发角色用户，进入数据模型-新建表，选择doris数据源、数据库、数仓层级、模型元素
表名：自定义内容：「T_doris_A」
中文名：doris表A
数据模型类型：【主键表】
2）点击下一步
- 1）进入数据模型新建页面
2）进入表结构页面
- 新增多个字段，其中包含字段ID
字段id勾选主键，分区字段，动态分区，分桶字段(分桶数1)，编辑动态分区配置：同前置条件
- 新增成功
- 点击【生成建表语句】按钮，建表
- 1）提示：提交审批成功
2）我的模型-已审批页面记录新增一条记录为审批中
- 管理员用户进入「审批中心」，审批拒绝
- 1）表未新建，数据模型新增一条记录，为未建表状态
2）数据开发用户，我的模型-已审批页面记录状态变为已拒绝
- 管理员用户进入「审批中心」，审批通过
- 1）数据模型新增一条记录，为已建表状态
2）表详情正确
3）数据开发用户，我的模型-已审批页面记录状态变为已通过
