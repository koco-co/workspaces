# 数据质量（XMind）
> 来源：zentao-cases/XMind/数据资产/数据资产-主流程用例.xmind

---

### 规则任务配置

#### 验证单表校验-【表行数-过滤条件】校验结果正确
- 新建单表规则，选择doris表
规则选择表行数，过滤条件：part_date='${utc0yyyy_MM_dd_1d}'，配置固定值=5
- 校验通过
- 立即运行查看结果
- 校验通过
- 修改规则，配置固定值>5
- 编辑成功
- 立即运行查看结果
- 校验不通过，展示每天写入的5条数据

#### 验证单表校验-【完整性校验-字段级-单字段】结果正确
- 新建单表规则，选择doris表
规则选择完整性校验-字段级，分别创建空值数、空值率、空串数、空串率的子规则，配置使校验通过
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其都校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证单表校验-【完整性校验-字段级-多字段】校验结果正确
- 新建单表规则，选择doris表
规则选择完整性校验-字段级，选择字段id、age
- 新增【字段间规则逻辑】下拉框，值为and/or
- 选择and-空值数-固定值>1,保存后立即运行
- 运行不通过
- 修改规则and-空值数-固定值>=1,保存后立即运行
- 运行通过
- 修改规则or-空值数-固定值>1,保存后立即运行
- 运行通过

#### 验证单表校验-【准确性校验】结果正确
- 新建单表规则，选择doris表
规则选择准确性校验，分别创建求和、求平均、负值比、零值比、正值比的子规则，配置使校验通过
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其都校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证单表校验-【规范性校验】结果正确
- 新建单表规则，选择doris表
规则选择规范性校验，分别创建取值范围、枚举范围、枚举个数、身份证号、手机号、邮箱、最大长度、最小长度、字符串长度、数据精度、空值数、重复数、枚举值的子规则，配置使校验通过
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其都校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证单表校验-【唯一性校验】结果正确
- 新建单表规则，选择doris表
规则选择唯一性校验，分别创建重复数、重复率、非重复个数、非重复占比的子规则，配置使校验通过
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其都校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证单表校验-【自定义sql-单表逻辑】结果正确
- 新建单表规则，选择前置中的doris表
规则选择自定义sql，输入样例sql，固定值<10000
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证单表校验-【自定义sql-多表逻辑】结果正确
- 新建单表规则，选择前置中的doris表
规则选择自定义sql，输入sql使校验通过，如：select * from doris;固定值<10000
- 新建成功
- 立即运行查看结果
- 结果正确
- 修改规则，配置使其校验不通过
- 编辑成功
- 立即运行查看结果
- 结果正确

#### 验证【单表-数据预览】结果正确
- 新建单表校验，选择前置中的doris表，点击数据预览，查看结果
- 数据预览展示三条数据，数据正确

#### 验证周期调度-【立即生成】功能正常
- 新建单表规则，选择doris表
规则选择完整性校验-字段级，涵盖字段级下的所有子规则类型，配置使校验通过
调度时间为17分钟后，调度方式：立即生成
- 新建成功
- 17分钟后，查看质量实例
- 生成一条实例，运行中
- 运行后查看结果
- 运行结果正确
- 第二天查看实例
- 周期时间生成一条实例，运行正确

#### 验证周期调度-【T+1】功能正常
- 新建单表规则，选择doris表
规则选择完整性校验-字段级，涵盖字段级下的所有子规则类型，配置使校验通过
调度时间为17分钟后，调度方式：T+1
- 新建成功
- 17分钟后，查看质量实例
- 没有生成实例
- 第二天查看实例
- 周期时间生成一条实例
- 运行后查看结果
- 运行结果正确

#### 验证【规则集】功能正常
- 新建规则集，选择前置的doris表，点击下一步
- 进入规则配置
- 下载规则模板，填写前置中的规则sql，上传
- 上传成功,规则导入正确
- 配置周期调度，点击创建
- 创建成功
- 立即执行，查看运行结果
- 运行结果正确

#### 验证【多表-同源比对】功能正常
- 新建多表比对，左表选择前置中的表doris_demo_data_types_source
- 右表数据源默认回填左表选择的数据源，置灰不可修改
- 右表选择前置中的表doris_demo1_data_types_source，点击下一步
- 进入选择字段步骤
- 同名映射
- 1）字段类型一致的进行映射
2）不一致的不映射并提示：主表与对照表字段类型不一致，无法比对
- 勾选user_id为主键
- 勾选成功
- 勾选【记录数百分比差异】-0%
- 勾选成功
- 创建规则
- 创建成功
- 立即运行规则，查看结果
- 质量实例校验不通过
- 编辑规则，修改【记录数百分比差异】-100%
- 修改成功
- 立即运行规则，查看结果
- 质量实例校验通过

#### 验证【多表-doris-mysql跨源比对】功能正常
- 新建多表比对，左表选择前置中的表doris_demo_data_types_source,过滤条件为part_date='${utc0yyyy_MM_dd_1d}'
- 右表数据源不会默认回填左表选择的数据源，下拉框允许选择所有授权给质量项目的数据源
- 右表选择前置中的表mysql_demo_data_types_source，点击下一步
- 同名映射
- 1）字段类型一致的进行映射
2）不一致的不映射并提示：主表与对照表字段类型不一致，无法比对
- 勾选user_id为主键
- 勾选成功
- 勾选【记录数百分比差异】-0%
- 勾选成功
- 创建规则
- 创建成功
- 立即运行规则，查看结果
- 质量实例校验不通过
- 编辑规则，修改【记录数百分比差异】-50%
- 修改成功
- 立即运行规则，查看结果
- 质量实例校验通过

#### 验证【多表-数据预览】结果正确
- 新建多表比对，左表选择前置中的表doris_demo_data_types_source，点击数据预览查看结果
- 数据预览展示三条数据，数据正确
- 右表选择前置中的表doris_demo_data_types_source，点击数据预览查看结果
- 数据预览展示三条数据，数据正确

#### 验证【多表-doris-doris跨源比对】功能正常
- 新建多表比对，选择前置中的表doris1、doris2,过滤条件都为part_date='${utc0yyyy_MM_dd_1d}'
- 进入选择字段页面
- 同名映射，勾选id为主键
- 勾选成功
- 勾选【记录数百分比差异】-34%
- 勾选成功
- 创建规则
- 创建成功
- 立即运行规则，查看结果
- 质量实例校验不通过
- 编辑规则，修改【记录数百分比差异】-50%
- 修改成功
- 立即运行规则，查看结果
- 质量实例校验通过

#### 【联动】验证质量和离线绑定，单规则包-【表行数-校验通过】结果正确
- 新建单表校验，选择表doris2
规则：选择表行数固定值=3，弱规则
调度：自动关联离线调度周期。规则包个数1
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线实例运行，查看质量实例
- 质量新增一条实例，状态为运行中
- 运行结束后，查看离线和质量实例的运行结果
- 1）质量实例：校验通过
2）离线实例：运行成功
- 进入离线，对doris2doris任务补数据-当前任务及下游，查看结果
- 同步骤2-3

#### 【联动】验证质量和离线绑定，单规则包-【表行数-弱规则-校验不通过】结果正确
- 新建单表校验，选择表doris2
规则：选择表行数固定值固定值>3,弱规则
调度：自动关联离线调度周期。规则包个数1
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线实例运行，查看质量实例
- 质量新增一条实例，状态为运行中
- 运行结束后，查看离线和质量实例的运行结果
- 1）质量实例：校验未通过，查看明细展示doris1中的三条数据
2）离线实例：运行成功
- 进入「离线开发-运维中心-周期任务」，对doris2doris任务补当前数据及下游，查看结果
- 1）补数据质量工作流中有1个规则包，任务都运行成功
2）质量生成一条实例，校验未通过

#### 【联动】验证质量和离线绑定，单规则包-【表行数-强规则-校验不通过】结果正确
- 修改质量规则，固定值>3,强规则
- 新建成功
- 等待离线实例运行，查看质量实例
- 质量新增一条实例，状态为运行中
- 运行结束后，查看离线和质量实例的运行结果
- 1）质量实例：校验未通过，查看明细展示doris1中的三条数据
2）离线实例：运行失败，失败原因：上游质量任务失败
- 进入「离线开发-运维中心-周期任务」，对doris2doris任务补当前数据及下游，查看结果
- 1）补数据质量工作流中有1个规则包，离线任务运行失败，报错质量任务校验不通过
2）质量生成一条实例，校验不通过

#### 【联动】验证质量和离线绑定，单规则包-【表行数-添加过滤条件】-校验结果正确
- 新建单表校验，选择表doris2
规则：选择表行数固定值>0，强规则
过滤条件：
time >= '${utc0yyyy_MM_dd_HH_3H_00_00}'
and time <  '${utc0yyyy_MM_dd_HH_00_00}'
调度：自动关联离线调度周期。规则包个数1
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线实例运行，查看质量实例
- 质量新增一条实例，状态为运行中
- 运行结束后，查看离线和质量实例的运行结果
- 1）质量实例：校验通过
2）离线实例：运行成功
- 进入离线，对doris2doris任务补数据-当前任务及下游，查看结果
- 同步骤2-3

#### 【联动】验证质量和离线绑定，多规则包-【表行数-校验通过】-校验结果正确
- 进入「数据资产-数据质量」，新建单表校验，选择表doris2
规则：选择表行数固定值=3，弱规则。再任意添加其他两种类型的规则，使校验通过
调度：自动关联离线调度周期。规则包个数3
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线周期实例运行，进入「数据资产-数据质量-任务实例查询」
- 页面生成一条实例，校验通过
- 进入「离线开发-运维中心-周期任务实例」
- 离线任务运行成功
- 进入「离线开发-运维中心-周期任务」，对doris2doris任务补当前数据及下游，查看结果
- 1）补数据质量工作流中有3个规则包，任务都运行成功
2）质量生成一条实例，校验通过

#### 【联动】验证质量和离线绑定，多规则包-【表行数-弱规则-校验不通过】-校验结果正确
- 进入「数据资产-数据质量」，新建单表校验，选择表doris2
规则：选择表行数，固定值>3,弱规则。再任意添加其他两种类型的规则，使校验通过
调度：自动关联离线调度周期。规则包个数3
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线周期实例运行，进入「数据资产-数据质量-任务实例查询」
- 1）页面生成一条实例，校验未通过
2）查看明细展示doris1中的三条数据
- 进入「离线开发-运维中心-周期任务实例」
- 离线任务运行成功
- 进入「离线开发-运维中心-周期任务」，对doris2doris任务补当前数据及下游，查看结果
- 1）补数据质量工作流中有3个规则包，任务都运行成功
2）质量生成一条实例，校验未通过

#### 【联动】验证质量和离线绑定，多规则包-【表行数-强规则-校验不通过】-校验结果正确
- 进入「数据资产-数据质量」，新建单表校验，选择表doris2
规则：选择表行数，固定值>3,强规则。再任意添加其他两种类型的规则，使校验通过
调度：自动关联离线调度周期。规则包个数3
绑定任务：前置条件doris2doris
- 新建成功
- 等待离线周期实例运行，进入「数据资产-数据质量-任务实例查询」
- 1）页面生成一条实例，校验未通过
2）查看明细展示doris1中的三条数据
- 进入「离线开发-运维中心-周期任务实例」
- 离线任务运行失败
- 进入「离线开发-运维中心-周期任务」，对doris2doris任务补当前数据及下游，查看结果
- 1）补数据质量工作流中有3个规则包，离线任务运行失败，报错质量任务校验不通过
2）质量生成一条实例，校验不通过

### 项目管理

#### 项目信息
- 验证项目增删改查功能
- 点击「创建项目」按钮，配置如下
「项目名称」输入「超64字符文本」
「项目标识」输入「超64字符文本&特殊字符」
「管理员」不选择
「项目描述」输入「测试项目描述信息」
点击「确定」按钮
- 提示「项目名称」输入框64字符限制；
提示「项目标识」输入框64字符限制以及特殊字符不支持提示；
「管理员」必填项未填提示；
- 点击「创建项目」按钮，配置如下
「项目名称」输入「测试项目」
「项目标识」输入「测试项目标识」
「管理员」选择${已引入到资产平台的用户,例如admin@dtstack.com}
「项目描述」输入「测试项目描述信息」
点击「确定」按钮
- 质量项目创建成功
- 选择新增项目，点击「置顶」按钮
- 项目置顶成功，提示“操作成功”
- 删除项目，二次确认
- 项目删除成功
