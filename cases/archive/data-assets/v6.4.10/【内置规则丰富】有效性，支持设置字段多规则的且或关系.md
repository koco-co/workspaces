---
suite_name: 【内置规则丰富】有效性，支持设置字段多规则的且或关系
description: 【内置规则丰富】有效性，支持设置字段多规则的且或关系
prd_id: 15695
prd_version: v6.4.10
prd_path: cases/requirements/data-assets/v6.4.10/【内置规则丰富】有效性，支持设置字段多规则的且或关系.md
prd_url: "https://lanhuapp.com/web/#/item/project/product?tid=24a1c6b2-a52e-454c-8d51-8aff866598b1&pid=7de90493-e80f-4592-a263-38fb2d2e98c0&versionId=7991bb05-6f97-4b29-8ad6-de18b5869a4d&docId=fc0fee93-74f5-4eff-a769-99e68506b296&docType=axure&pageId=3035f5b47bed47fcb8a7a4a26fa7a701&image_id=fc0fee93-74f5-4eff-a769-99e68506b296&parentId=493e80b4-33c3-44cb-b880-42bee51dba19"
product: data-assets
dev_version: 6.3岚图定制化分支
tags:
  - 数据资产
  - 内置规则
  - 有效性校验
  - 取值范围
  - 枚举值
  - 且或关系
  - 字段级规则
  - 数据质量报告
  - 岚图定制化
  - 内置规则丰富
create_at: 2026-03-30
update_at: 2026-03-30
status: ""
health_warnings: []
repos:
  - .repos/CustomItem/dt-center-assets
  - .repos/CustomItem/dt-insight-studio
case_count: 17
case_types:
  normal: 14
  abnormal: 3
  boundary: 0
origin: json
---

## 规则库配置

### 列表页

#### 规则展示与说明

##### 【P0】验证内置规则列表展示取值范围&枚举范围条目及规则解释

> 前置条件
```
当前账号具备【数据质量 → 规则库配置】访问权限；已在当前版本导入 15695 需求对应的内置规则模板。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则库配置 → 列表页】页面 | 页面正常加载，展示规则列表和规则名称搜索框。 |
| 2 | 在规则名称搜索框输入“取值范围&枚举范围”并执行搜索 | 列表返回名称为“取值范围&枚举范围”的内置规则条目。 |
| 3 | 查看该条目的规则分类、关联范围和规则描述 | 规则分类显示“有效性校验”，关联范围显示“字段”，规则描述显示“校验字段值取值范围和枚举范围是否符合要求，支持配置规则且或关系”。 |
| 4 | 将鼠标悬浮在该条目的【规则解释】内容上 | 悬浮提示完整展示“取值范围和枚举范围的联合校验”。 |

## 规则任务管理

### 新建单表校验规则页

#### 规则创建

##### 【P0】验证新建取值范围&枚举范围且关系规则保存成功并回显

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载，展示三步向导。 |
| 2 | 在【监控对象】步骤填写如下信息：<br>- 规则名称：RANGE_ENUM_AND_001<br>- 选择数据源：Doris3.x测试源<br>- 选择数据库：qa_valid_combo_doris<br>- 选择数据表：tb_range_enum_combo<br>- 选择分区：dt=2026-03-30 | 监控对象加载成功，【下一步】按钮可点击。 |
| 3 | 点击【下一步】进入【监控规则】步骤，配置如下：<br>- 强弱规则：强规则<br>- 规则描述：校验 month_wild 必须大于1且小于10并且属于 1,2,3<br>- 字段：month_wild<br>- 统计函数：取值范围&枚举范围<br>- 第一行取值范围：> 1 且 < 10<br>- 第二行枚举值：in 1,2,3<br>- 第三行关系：且<br>- 过滤条件：无 | 页面成功保存当前规则行，第一行、第二行和第三行配置区域同时展示；关系值回显为“且”。 |
| 4 | 点击【下一步】进入【调度属性】步骤并点击【新建】 | 页面提示“新建规则成功！”，编辑页回显字段“month_wild”、联合规则配置“>1 且 <10”“in 1,2,3”“且”。 |

##### 【P0】验证新建取值范围&枚举范围或关系规则保存成功并回显

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载，展示三步向导。 |
| 2 | 在【监控对象】步骤填写如下信息：<br>- 规则名称：RANGE_ENUM_OR_001<br>- 选择数据源：SparkThrift2.x测试源<br>- 选择数据库：qa_valid_combo_spark<br>- 选择数据表：tb_range_enum_combo<br>- 选择分区：dt=2026-03-30 | 监控对象加载成功，【下一步】按钮可点击。 |
| 3 | 进入【监控规则】步骤后配置如下：<br>- 强弱规则：弱规则<br>- 规则描述：校验 month_wild 取值范围与枚举值满足或关系<br>- 字段：month_wild<br>- 统计函数：取值范围&枚举范围<br>- 第一行取值范围：> 1 且 < 10<br>- 第二行枚举值：in 1,2,3<br>- 第三行关系：或<br>- 过滤条件：无 | 页面成功保存当前规则行，第三行关系值显示为“或”。 |
| 4 | 点击【下一步】进入【调度属性】步骤并点击【新建】 | 页面提示“新建规则成功！”，编辑页回显联合规则配置与关系“或”。 |

#### 配置能力

##### 【P1】验证取值范围配置支持双操作符和双阈值组合

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控规则】步骤选择字段 `month_wild` 和统计函数“取值范围&枚举范围” | 页面展开联合规则的三行配置区域。 |
| 3 | 在第一行依次选择 `>=`、输入 `1`、选择 `且`、选择 `<=`、输入 `10` | 第一行同时支持两个操作符和两个阈值输入，展示效果与原有取值范围规则一致。 |
| 4 | 切换当前规则到其他字段后再切回 `month_wild` | 页面仍保留已设置的双操作符与双阈值内容。 |

##### 【P1】验证枚举值配置支持in和not in选项

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控规则】步骤选择统计函数“取值范围&枚举范围”后，在第二行点击【点击配置】 | 页面打开枚举值配置区域或弹窗。 |
| 3 | 先选择 `in` 并录入 `1,2,3`，再切换为 `not in` 并确认 | 枚举值配置支持在 `in` 与 `not in` 之间切换，已选值标签随之刷新。 |
| 4 | 关闭枚举值配置区域返回规则配置页 | 页面保留最后一次选择的 `not in` 状态和值集合。 |

##### 【P1】验证取值范围和枚举值关系支持且和或切换

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控规则】步骤选择统计函数“取值范围&枚举范围”并完成第一行与第二行配置 | 页面展示第三行关系配置位。 |
| 3 | 在第三行先选择“且”，再切换为“或” | 关系控件支持在“且”和“或”之间自由切换，当前选中值实时回显。 |
| 4 | 查看第三行中的过滤条件区域 | 关系配置与过滤条件处于同一行，切换关系不会影响过滤条件配置入口。 |

##### 【P1】验证string类型字段可创建联合规则并按double逻辑参与判断

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控对象】步骤选择表 `tb_range_enum_combo` 并进入【监控规则】步骤 | 字段下拉中可见 string 类型字段 `month_wild`。 |
| 3 | 选择字段 `month_wild`，配置联合规则为：第一行 `> 1 且 < 10`，第二行 `in 1,2,3`，第三行关系 `且` | 页面允许对 string 类型字段完成上述数值规则配置，且无字段类型限制错误。 |
| 4 | 保存当前规则 | 页面保存成功，说明 string 类型字段能够以 double 逻辑参与联合判断。 |

##### 【P1】验证数值字段可创建联合规则并保存成功

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控规则】步骤选择数值字段 `sale_qty` 和统计函数“取值范围&枚举范围” | 页面允许继续配置联合规则。 |
| 3 | 配置如下：<br>- 第一行取值范围：> 1 且 <= 20<br>- 第二行枚举值：not in 5,7<br>- 第三行关系：且<br>- 规则描述：校验 sale_qty 不在禁用枚举内且处于合法区间 | 联合规则配置内容展示完整，枚举值支持 `not in`。 |
| 4 | 点击【新建】保存当前规则 | 页面提示保存成功，编辑页回显字段 `sale_qty`、取值范围、枚举值和关系“且”。 |

##### 【P1】验证原枚举值规则同步支持not in配置

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 新建单表校验规则页】页面 | 页面正常加载。 |
| 2 | 在【监控规则】步骤新增一条规则并选择统计函数“枚举值” | 页面展示原枚举值规则的配置区域。 |
| 3 | 打开枚举值配置并查看可选关系 | 枚举值规则除原有 `in` 外，同时提供 `not in` 选项。 |
| 4 | 选择 `not in` 并录入 `5,7` 后保存当前规则 | 页面允许保存，说明原枚举值规则已同步支持 `not in`。 |

### 编辑单表校验规则页

#### 配置回显

##### 【P1】验证编辑页正确回显联合规则的范围阈值枚举值和关系设置

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 规则任务管理 → 编辑单表校验规则页】页面 | 页面加载成功，已打开规则 `RANGE_ENUM_AND_001` 的编辑页面并展示当前规则配置。 |
| 2 | 查看第一行取值范围、第二行枚举值和第三行关系配置 | 第一行回显 `> 1 且 < 10`，第二行回显 `in 1,2,3`，第三行关系回显为“且”。 |
| 3 | 将第三行关系从“且”切换为“或”并点击保存 | 页面提示保存成功。 |
| 4 | 刷新页面再次打开当前规则 | 关系值回显为“或”，其余范围和枚举值配置保持不变。 |

## 数据质量报告

### 列表页

#### 报告定位

##### 【P1】验证已生成报告列表可定位取值范围&枚举范围报告并进入详情

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 列表页】页面 | 页面正常加载，展示报告列表和搜索区域。 |
| 2 | 切换到【已生成报告】页签，在“报告名称”输入框中输入 `Doris_取值范围枚举范围报告_20260330` 并搜索 | 列表返回对应的已生成报告记录。 |
| 3 | 核对返回记录的数据源、数据表与执行时间 | 报告数据源与数据表对应 `qa_valid_combo_doris.tb_range_enum_combo`，执行时间为本次任务生成时间。 |
| 4 | 点击该记录的【报告详情】 | 页面成功跳转到该报告详情页。 |

### 详情页

#### 结果展示

##### 【P0】验证详情页展示且关系联合规则校验通过结果

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Doris_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在规则校验明细中定位字段 `month_wild`、规则名称“取值范围&枚举范围”的通过记录 | 该记录对应样本值 `2`，展示在“有效性校验”分类下。 |
| 3 | 查看该记录的“质检结果”“未通过原因”“详情说明” | 该记录显示“质检结果=校验通过”“未通过原因=--”“详情说明=符合规则“取值范围>1且<10”且“枚举值in 1,2,3””。 |
| 4 | 观察该记录的操作列 | 操作列不展示【查看详情】按钮。 |

##### 【P0】验证详情页展示且关系联合规则校验不通过结果并提供查看详情入口

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Doris_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在规则校验明细中定位字段 `month_wild`、规则名称“取值范围&枚举范围”的失败记录 | 失败记录对应样本值 `8`，其取值范围满足但枚举值不满足。 |
| 3 | 查看该记录的“质检结果”“未通过原因”“详情说明”与操作列 | 该记录显示“质检结果=校验不通过”“未通过原因=不符合有效性规则”“详情说明=不符合规则“取值范围>1且<10”且“枚举值in 1,2,3””，并展示【查看详情】按钮。 |

##### 【P0】验证详情页展示或关系联合规则在单侧满足时仍判定通过

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Spark_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在规则校验明细中定位字段 `month_wild`、关系为“或”的联合规则记录 | 该记录对应样本值 `8`，取值范围满足、枚举值不满足。 |
| 3 | 查看该记录的“质检结果”“未通过原因”“详情说明” | 该记录显示“质检结果=校验通过”“未通过原因=--”“详情说明=符合规则“取值范围>1且<10”或“枚举值in 1,2,3””。 |
| 4 | 观察该记录的操作列 | 操作列不展示【查看详情】按钮。 |

##### 【P1】验证原枚举值规则not in场景在质量报告中展示新增说明文案

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Hive_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在规则校验明细中定位统计函数为“枚举值”、配置为 `not in 5,7` 的记录 | 页面成功定位到原枚举值规则的回归记录。 |
| 3 | 查看该记录的详情说明 | 详情说明使用 `not in` 文案，例如“字段枚举值不存在约定范围内的值，符合规则“枚举值not in 5,7””或对应失败文案，不再只保留 in 场景描述。 |
| 4 | 观察该记录的操作列 | 该记录的操作入口与原枚举值规则保持一致。 |

#### 失败明细与日志

##### 【P1】验证联合规则失败记录支持查看日志

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Doris_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在失败记录所在行点击【查看日志】 | 页面弹出执行日志面板。 |
| 3 | 查看日志中的规则名称、执行时间和失败信息 | 日志能定位到联合规则执行记录，并包含与失败样本相关的执行信息。 |
| 4 | 关闭日志面板返回详情页 | 页面返回详情页，失败记录仍可继续查看。 |

##### 【P1】验证查看详情弹窗中不符合规则的字段值被标红展示

> 前置条件
```
1、环境说明：当前账号具备【规则库配置】【规则任务管理】【数据质量报告】权限；当前环境已具备 string 字段与数值字段的单表校验能力。

2、Doris3.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_doris;
DROP TABLE IF EXISTS qa_valid_combo_doris.tb_range_enum_combo;
CREATE TABLE qa_valid_combo_doris.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_doris.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

3、Hive2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_hive;
CREATE TABLE IF NOT EXISTS qa_valid_combo_hive.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_hive.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');

4、SparkThrift2.x SQL语句准备:
CREATE DATABASE IF NOT EXISTS qa_valid_combo_spark;
CREATE TABLE IF NOT EXISTS qa_valid_combo_spark.tb_range_enum_combo (
  id BIGINT,
  month_wild STRING,
  sale_qty DOUBLE,
  dt STRING
);
INSERT INTO qa_valid_combo_spark.tb_range_enum_combo VALUES
(1, '2', 2.0, '2026-03-30'),
(2, '8', 8.0, '2026-03-30'),
(3, '0', 0.0, '2026-03-30'),
(4, '20', 20.0, '2026-03-30');5、已基于上述数据创建并执行以下规则：
- 规则A：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=且。
- 规则B：字段=month_wild，统计函数=取值范围&枚举范围，取值范围=>1 且 <10，枚举值=in 1,2,3，取值范围和枚举值关系=或。
- 规则C：字段=sale_qty，统计函数=取值范围&枚举范围，取值范围=>1 且 <=20，枚举值=not in 5,7，取值范围和枚举值关系=且。
- 规则D：字段=sale_qty，统计函数=枚举值，枚举值=not in 5,7（用于回归原枚举值规则）。

6、已生成以下报告：
- Doris_取值范围枚举范围报告_20260330
- Hive_取值范围枚举范围报告_20260330
- Spark_取值范围枚举范围报告_20260330
其中 Doris 报告存在 1 条且关系失败记录，Spark 报告存在 1 条或关系通过记录。
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据质量 → 数据质量报告 → 详情页】页面 | 页面成功进入 `Doris_取值范围枚举范围报告_20260330` 的报告详情。 |
| 2 | 在失败记录所在行点击【查看详情】 | 页面打开失败明细抽屉。 |
| 3 | 查看失败明细中的字段值展示效果 | 明细表保留全部字段，不符合联合规则的目标字段值以红色高亮展示。 |
| 4 | 关闭失败明细抽屉返回详情页 | 页面关闭抽屉后返回报告详情。 |
