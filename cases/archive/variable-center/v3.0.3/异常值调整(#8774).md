---
suite_name: "异常值调整(#8774)（XMind）"
description: "异常值调整(#8774)（XMind）"
prd_id: 8774
prd_version: v3.0.3
prd_path: ""
product: variable-center
tags:
  - 变量中心
  - 变量中心-变量管理-
  - 业务查询-变量空间-
  - 异常值调整
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 0
origin: xmind
---
# 异常值调整(#8774)（XMind）
> 来源：zentao-cases/XMind/变量中心/202506-变量中心v3.0.3.xmind

---

### 变量中心-变量管理-
#### 标准变量-
- 新建-设置标准规则
- 验证标准变量页面测试功能正常_原始变量测试值为空
- 进入【变量中心-变量管理-新建标准变量】页面-【设置标准规则】步骤，「Java Shell」输入正确脚本（脚本中引用的变量仅涉及1个数据产品），点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：${注册原始变量时保存的值}
2）「测试结果」：注册原始变量时保存的值对应的计算结果，例：
standard1：${注册原始变量时保存的值对应的计算结果}
- 修改脚本的数据类型为String，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Integer，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Long，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Float，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Double，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Boolean，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 验证完成功能正常_页面测试已通过
- 进入【变量中心-变量管理-新建标准变量】页面-【设置标准规则】步骤，填写必填项，输入所有正确的原始变量值，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：${输入的原始变量值}
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 点击“完成”
- 1）跳转至【变量管理】列表，提示：标准变量创建成功
2）【变量管理】列表新增一条标准变量记录
3）dv_variable库，dv_variable_test.example_value=${原始变量的值为注册时保存的值所对应的标准变量计算结果}
- 编辑-设置标准规则
- 验证标准变量页面测试功能正常_原始变量测试值为空
- 进入【变量中心-变量管理-编辑标准变量】页面-【设置标准规则】步骤，「Java Shell」输入正确脚本（脚本中引用的变量仅涉及1个数据产品），点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：${注册原始变量时保存的值}
2）「测试结果」：注册原始变量时保存的值对应的计算结果，例：
standard1：${注册原始变量时保存的值对应的计算结果}
- 修改脚本的数据类型为String，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Integer，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Long，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Float，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Double，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 修改脚本的数据类型为Boolean，修改原始变量的测试值为空，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 验证完成功能正常_页面测试已通过
- 进入【变量中心-变量管理-编辑标准变量】页面-【设置标准规则】步骤，填写必填项，输入所有正确的原始变量值，点击“测试”
- 测试通过
1）「原始变量」：
原始变量名称：${标准加工规则中的原始变量名称}
原始变量值：${输入的原始变量值}
2）「测试结果」：输入的原始变量值对应的计算结果，例：
standard1：${输入的原始变量值对应的计算结果}，若为空，则显示为standard1：
- 点击“完成”
- 1）跳转至【变量管理】列表，提示：标准变量保存成功
2）dv_variable库，dv_variable_test.example_value=${保存时输入的原始变量的值所对应的标准变量计算结果}
#### 衍生变量-
- 新建-设置衍生规则
- 验证测试功能正常_标准变量测试值为空
- 进入【变量中心-变量管理-新建衍生变量】页面-【设置衍生规则】步骤，衍生规则加工输入正确公式（公式中引用的变量仅涉及1个数据产品），点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：${衍生加工规则中的标准变量在dv_variable.dv_variable.example_value的值}
2）「测试结果」：标准变量保存时的值对应的计算结果，例：
standard1：${标准变量保存时的值对应的计算结果}
- 修改公式引用的标准变量（标准变量的数据类型为String），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Integer），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Long），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Float），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Double），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Boolean），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 验证完成功能正常_页面测试已通过
- 进入【变量中心-变量管理-新建衍生变量】页面-【设置衍生规则】步骤，填写必填项，输入所有正确的标准变量值，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：${输入的标准变量值}
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 点击“完成”
- 1）跳转至【变量管理】列表，提示：衍生变量创建成功
2）【变量管理】列表新增一条衍生变量记录
- 编辑-设置衍生规则
- 验证测试功能正常_标准变量测试值为空
- 进入【变量中心-变量管理-编辑衍生变量】页面-【设置衍生规则】步骤，衍生规则加工输入正确公式（公式中引用的变量仅涉及1个数据产品），点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：${衍生加工规则中的标准变量在dv_variable.dv_variable.example_value的值}
2）「测试结果」：标准变量保存时的值对应的计算结果，例：
standard1：${标准变量保存时的值对应的计算结果}
- 修改公式引用的标准变量（标准变量的数据类型为String），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Integer），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Long），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Float），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Double），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 修改公式引用的标准变量（标准变量的数据类型为Boolean），修改标准变量的测试值为空，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 验证完成功能正常_页面测试已通过
- 进入【变量中心-变量管理-编辑衍生变量】页面-【设置衍生规则】步骤，填写必填项，输入所有正确的标准变量值，点击“测试”
- 测试通过
1）「标准变量」：
标准变量名称：${衍生加工规则中的标准变量名称}
标准变量值：${输入的标准变量值}
2）「测试结果」：输入的标准变量值对应的计算结果，例：
standard1：${输入的标准变量值对应的计算结果}，若为空，则显示为standard1：
- 点击“完成”
- 1）跳转至【变量管理】列表，提示：衍生变量保存成功

### 业务查询-变量空间-
#### 决策变量空间-
- 测试
- 验证决策变量空间页面测试功能正常_原始变量返回空值
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空值，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证决策变量空间页面测试功能正常_原始变量没有返回对应的字段
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”（修改调用的api使得出参中没有原始变量A1），点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证决策变量空间页面测试功能正常_原始变量返回空字符串
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空字符串（即返回结果为：""），点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: 
标准变量C对应的查询结果: 1
标准变量D对应的查询结果: ${原始变量A2的值}
衍生变量A1对应的查询结果: 
衍生变量A2对应的查询结果: 1
衍生变量B1对应的查询结果: 
衍生变量B2对应的查询结果: 1
衍生变量C1对应的查询结果: 1
衍生变量C2对应的查询结果: 11
衍生变量D1对应的查询结果: ${原始变量A2的值}
衍生变量D2对应的查询结果: ${原始变量A2+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "",
            "标准变量C": "1",
            "标准变量D": "${原始变量A2的值}",
            "衍生变量A1": "",
            "衍生变量A2": "1",
            "衍生变量B1": "",
            "衍生变量B2": "1",
            "衍生变量C1": "1",
            "衍生变量C2": "11",
            "衍生变量D1": "${原始变量A2的值}",
            "衍生变量D2": "${原始变量A2+1的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证决策变量空间页面测试功能正常_标准变量返回空值
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: ${nullCode对应的值}
衍生变量B对应的查询结果: ${nullCode对应的值}
衍生变量C对应的查询结果: ${nullCode对应的值}
衍生变量D对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${原始变量A的值},
            "衍生变量A": ${nullCode对应的值},
            "衍生变量B": ${nullCode对应的值},
            "衍生变量C": ${nullCode对应的值},
            "衍生变量D": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证决策变量空间页面测试功能正常_标准变量返回空字符串
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: 
衍生变量B对应的查询结果: 
衍生变量C对应的查询结果: 1
衍生变量D对应的查询结果: ${原始变量A的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "${原始变量A的值}",
            "衍生变量A": "",
            "衍生变量B": "",
            "衍生变量C": "1",
            "衍生变量D": "${原始变量A的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证决策变量空间页面测试功能正常_api调用异常
- 进入【业务查询-变量空间-测试】页面，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”，修改数据产品A引用的api，使得api调用异常（返回状态码非200），点击“测试”
- errorCode对应的值：${dt_variable.dv_system_property.property_name=global_error_code的值}
1）查询结果：
标准变量A对应的查询结果: ${errorCode对应的值}
标准变量B对应的查询结果: ${errorCode对应的值}
标准变量C对应的查询结果: ${errorCode对应的值}
标准变量D对应的查询结果: ${errorCode对应的值}
衍生变量A1对应的查询结果: ${errorCode对应的值}
衍生变量A2对应的查询结果: ${errorCode对应的值}
衍生变量B1对应的查询结果: ${errorCode对应的值}
衍生变量B2对应的查询结果: ${errorCode对应的值}
衍生变量C1对应的查询结果: ${errorCode对应的值}
衍生变量C2对应的查询结果: ${errorCode对应的值}
衍生变量D1对应的查询结果: ${errorCode对应的值}
衍生变量D2对应的查询结果: ${errorCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${errorCode对应的值},
            "标准变量B": ${errorCode对应的值},
            "标准变量C": ${errorCode对应的值},
            "标准变量D": ${errorCode对应的值},
            "衍生变量A1": ${errorCode对应的值},
            "衍生变量A2": ${errorCode对应的值},
            "衍生变量B1": ${errorCode对应的值},
            "衍生变量B2": ${errorCode对应的值},
            "衍生变量C1": ${errorCode对应的值},
            "衍生变量C2": ${errorCode对应的值},
            "衍生变量D1": ${errorCode对应的值},
            "衍生变量D2": ${errorCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 调用
- 验证决策变量空间调用功能正常_原始变量返回空值
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回空值
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回非空值
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证决策变量空间调用功能正常_原始变量没有返回对应的字段
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”（修改调用的api使得出参中没有原始变量A1）
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证决策变量空间调用功能正常_原始变量返回空字符串
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回空字符串（即返回结果为：""）
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "",
            "标准变量C": "1",
            "标准变量D": "${原始变量A2的值}",
            "衍生变量A1": "",
            "衍生变量A2": "1",
            "衍生变量B1": "",
            "衍生变量B2": "1",
            "衍生变量C1": "1",
            "衍生变量C2": "11",
            "衍生变量D1": "${原始变量A2的值}",
            "衍生变量D2": "${原始变量A2+1的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回非空字符串
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证决策变量空间调用功能正常_标准变量返回空值
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、衍生变量A、B、C、D），postman调用，输入“参数值”使得原始变量A返回非空值
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${原始变量A的值},
            "衍生变量A": ${nullCode对应的值},
            "衍生变量B": ${nullCode对应的值},
            "衍生变量C": ${nullCode对应的值},
            "衍生变量D": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证决策变量空间调用功能正常_标准变量返回空字符串
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、衍生变量A、B、C、D），postman调用，输入“参数值”使得原始变量A返回非空字符串
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "${原始变量A的值}",
            "衍生变量A": "",
            "衍生变量B": "",
            "衍生变量C": "1",
            "衍生变量D": "${原始变量A的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证决策变量空间调用功能正常_api调用异常
- 进入【业务查询-变量空间】页面，选择一个决策变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”（修改数据产品A引用的api，使得api调用异常（返回状态码非200））
- errorCode对应的值：${dt_variable.dv_system_property.property_name=global_error_code的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${errorCode对应的值},
            "标准变量B": ${errorCode对应的值},
            "标准变量C": ${errorCode对应的值},
            "标准变量D": ${errorCode对应的值},
            "衍生变量A1": ${errorCode对应的值},
            "衍生变量A2": ${errorCode对应的值},
            "衍生变量B1": ${errorCode对应的值},
            "衍生变量B2": ${errorCode对应的值},
            "衍生变量C1": ${errorCode对应的值},
            "衍生变量C2": ${errorCode对应的值},
            "衍生变量D1": ${errorCode对应的值},
            "衍生变量D2": ${errorCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
#### 模型变量空间-
- 新建
- 验证新建模型变量空间页面测试功能正常_原始变量返回空值
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空值，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证新建模型变量空间页面测试功能正常_原始变量没有返回对应的字段
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”（修改调用的api使得出参中没有原始变量A1），点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证新建模型变量空间页面测试功能正常_原始变量返回空字符串
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空字符串（即返回结果为：""），点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: 
标准变量C对应的查询结果: 1
标准变量D对应的查询结果: ${原始变量A2的值}
衍生变量A1对应的查询结果: 
衍生变量A2对应的查询结果: 1
衍生变量B1对应的查询结果: 
衍生变量B2对应的查询结果: 1
衍生变量C1对应的查询结果: 1
衍生变量C2对应的查询结果: 11
衍生变量D1对应的查询结果: ${原始变量A2的值}
衍生变量D2对应的查询结果: ${原始变量A2+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "",
            "标准变量C": "1",
            "标准变量D": "${原始变量A2的值}",
            "衍生变量A1": "",
            "衍生变量A2": "1",
            "衍生变量B1": "",
            "衍生变量B2": "1",
            "衍生变量C1": "1",
            "衍生变量C2": "11",
            "衍生变量D1": "${原始变量A2的值}",
            "衍生变量D2": "${原始变量A2+1的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证新建模型变量空间页面测试功能正常_标准变量返回空值
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: ${nullCode对应的值}
衍生变量B对应的查询结果: ${nullCode对应的值}
衍生变量C对应的查询结果: ${nullCode对应的值}
衍生变量D对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${原始变量A的值},
            "衍生变量A": ${nullCode对应的值},
            "衍生变量B": ${nullCode对应的值},
            "衍生变量C": ${nullCode对应的值},
            "衍生变量D": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证新建模型变量空间页面测试功能正常_标准变量返回空字符串
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: 
衍生变量B对应的查询结果: 
衍生变量C对应的查询结果: 1
衍生变量D对应的查询结果: ${原始变量A的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "${原始变量A的值}",
            "衍生变量A": "",
            "衍生变量B": "",
            "衍生变量C": "1",
            "衍生变量D": "${原始变量A的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证新建模型变量空间页面测试功能正常_api调用异常
- 进入【业务查询-变量空间-新建模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”，修改数据产品A引用的api，使得api调用异常（返回状态码非200），点击“测试”
- errorCode对应的值：${dt_variable.dv_system_property.property_name=global_error_code的值}
1）查询结果：
标准变量A对应的查询结果: ${errorCode对应的值}
标准变量B对应的查询结果: ${errorCode对应的值}
标准变量C对应的查询结果: ${errorCode对应的值}
标准变量D对应的查询结果: ${errorCode对应的值}
衍生变量A1对应的查询结果: ${errorCode对应的值}
衍生变量A2对应的查询结果: ${errorCode对应的值}
衍生变量B1对应的查询结果: ${errorCode对应的值}
衍生变量B2对应的查询结果: ${errorCode对应的值}
衍生变量C1对应的查询结果: ${errorCode对应的值}
衍生变量C2对应的查询结果: ${errorCode对应的值}
衍生变量D1对应的查询结果: ${errorCode对应的值}
衍生变量D2对应的查询结果: ${errorCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${errorCode对应的值},
            "标准变量B": ${errorCode对应的值},
            "标准变量C": ${errorCode对应的值},
            "标准变量D": ${errorCode对应的值},
            "衍生变量A1": ${errorCode对应的值},
            "衍生变量A2": ${errorCode对应的值},
            "衍生变量B1": ${errorCode对应的值},
            "衍生变量B2": ${errorCode对应的值},
            "衍生变量C1": ${errorCode对应的值},
            "衍生变量C2": ${errorCode对应的值},
            "衍生变量D1": ${errorCode对应的值},
            "衍生变量D2": ${errorCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 编辑
- 验证编辑模型变量空间页面测试功能正常_原始变量返回空值
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空值，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证编辑模型变量空间页面测试功能正常_原始变量没有返回对应的字段
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”（修改调用的api使得出参中没有原始变量A1），点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${nullCode对应的值}
标准变量C对应的查询结果: ${nullCode对应的值}
标准变量D对应的查询结果: ${nullCode对应的值}
衍生变量A1对应的查询结果: ${nullCode对应的值}
衍生变量A2对应的查询结果: ${nullCode对应的值}
衍生变量B1对应的查询结果: ${nullCode对应的值}
衍生变量B2对应的查询结果: ${nullCode对应的值}
衍生变量C1对应的查询结果: ${nullCode对应的值}
衍生变量C2对应的查询结果: ${nullCode对应的值}
衍生变量D1对应的查询结果: ${nullCode对应的值}
衍生变量D2对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证编辑模型变量空间页面测试功能正常_原始变量返回空字符串
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回空字符串（即返回结果为：""），点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: 
标准变量C对应的查询结果: 1
标准变量D对应的查询结果: ${原始变量A2的值}
衍生变量A1对应的查询结果: 
衍生变量A2对应的查询结果: 1
衍生变量B1对应的查询结果: 
衍生变量B2对应的查询结果: 1
衍生变量C1对应的查询结果: 1
衍生变量C2对应的查询结果: 11
衍生变量D1对应的查询结果: ${原始变量A2的值}
衍生变量D2对应的查询结果: ${原始变量A2+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "",
            "标准变量C": "1",
            "标准变量D": "${原始变量A2的值}",
            "衍生变量A1": "",
            "衍生变量A2": "1",
            "衍生变量B1": "",
            "衍生变量B2": "1",
            "衍生变量C1": "1",
            "衍生变量C2": "11",
            "衍生变量D1": "${原始变量A2的值}",
            "衍生变量D2": "${原始变量A2+1的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”使得原始变量A1返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1的值}
标准变量B对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值}
标准变量C对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+1的值}
标准变量D对应的查询结果: ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值}
衍生变量A1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A的值}
衍生变量A2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量A+1的值}
衍生变量B1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B的值}
衍生变量B2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量B+1的值}
衍生变量C1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C的值}
衍生变量C2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量C+1的值}
衍生变量D1对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D的值}
衍生变量D2对应的查询结果: ${对应标准变量加工后的计算结果，标准变量D+1的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
	"${变量对应的数据产品英文名称}":{
	${对应数据产品请求返回的json报文}
	}
}
- 验证编辑模型变量空间页面测试功能正常_标准变量返回空值
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空值，点击“测试”
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
1）查询结果：
标准变量A对应的查询结果: ${nullCode对应的值}
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: ${nullCode对应的值}
衍生变量B对应的查询结果: ${nullCode对应的值}
衍生变量C对应的查询结果: ${nullCode对应的值}
衍生变量D对应的查询结果: ${nullCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${原始变量A的值},
            "衍生变量A": ${nullCode对应的值},
            "衍生变量B": ${nullCode对应的值},
            "衍生变量C": ${nullCode对应的值},
            "衍生变量D": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证编辑模型变量空间页面测试功能正常_标准变量返回空字符串
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、衍生变量A、B、C、D，「输入参数」输入“参数值”使得原始变量A返回非空字符串，点击“测试”
- 1）查询结果：
标准变量A对应的查询结果: 
标准变量B对应的查询结果: ${原始变量A的值}
衍生变量A对应的查询结果: 
衍生变量B对应的查询结果: 
衍生变量C对应的查询结果: 1
衍生变量D对应的查询结果: ${原始变量A的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "${原始变量A的值}",
            "衍生变量A": "",
            "衍生变量B": "",
            "衍生变量C": "1",
            "衍生变量D": "${原始变量A的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 验证编辑模型变量空间页面测试功能正常_api调用异常
- 进入【业务查询-变量空间-编辑模型变量空间】页面-【模型变量空间测试及创建】步骤，添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2，「输入参数」输入“参数值”，修改数据产品A引用的api，使得api调用异常（返回状态码非200），点击“测试”
- errorCode对应的值：${dt_variable.dv_system_property.property_name=global_error_code的值}
1）查询结果：
标准变量A对应的查询结果: ${errorCode对应的值}
标准变量B对应的查询结果: ${errorCode对应的值}
标准变量C对应的查询结果: ${errorCode对应的值}
标准变量D对应的查询结果: ${errorCode对应的值}
衍生变量A1对应的查询结果: ${errorCode对应的值}
衍生变量A2对应的查询结果: ${errorCode对应的值}
衍生变量B1对应的查询结果: ${errorCode对应的值}
衍生变量B2对应的查询结果: ${errorCode对应的值}
衍生变量C1对应的查询结果: ${errorCode对应的值}
衍生变量C2对应的查询结果: ${errorCode对应的值}
衍生变量D1对应的查询结果: ${errorCode对应的值}
衍生变量D2对应的查询结果: ${errorCode对应的值}
2）返回报文（JSON）：
{
    "data": {
        "result": {
            "标准变量A": ${errorCode对应的值},
            "标准变量B": ${errorCode对应的值},
            "标准变量C": ${errorCode对应的值},
            "标准变量D": ${errorCode对应的值},
            "衍生变量A1": ${errorCode对应的值},
            "衍生变量A2": ${errorCode对应的值},
            "衍生变量B1": ${errorCode对应的值},
            "衍生变量B2": ${errorCode对应的值},
            "衍生变量C1": ${errorCode对应的值},
            "衍生变量C2": ${errorCode对应的值},
            "衍生变量D1": ${errorCode对应的值},
            "衍生变量D2": ${errorCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message}
}
3）原始响应（JSON）：
{
    "${变量对应的数据产品英文名称}":{
    ${对应数据产品请求返回的json报文}
    }
}
- 调用
- 验证模型变量空间调用功能正常_原始变量返回空值
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回空值
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回非空值
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证模型变量空间调用功能正常_原始变量没有返回对应的字段
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”（修改调用的api使得出参中没有原始变量A1）
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${nullCode对应的值},
            "标准变量C": ${nullCode对应的值},
            "标准变量D": ${nullCode对应的值},
            "衍生变量A1": ${nullCode对应的值},
            "衍生变量A2": ${nullCode对应的值},
            "衍生变量B1": ${nullCode对应的值},
            "衍生变量B2": ${nullCode对应的值},
            "衍生变量C1": ${nullCode对应的值},
            "衍生变量C2": ${nullCode对应的值},
            "衍生变量D1": ${nullCode对应的值},
            "衍生变量D2": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证模型变量空间调用功能正常_原始变量返回空字符串
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回空字符串（即返回结果为：""）
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "",
            "标准变量C": "1",
            "标准变量D": "${原始变量A2的值}",
            "衍生变量A1": "",
            "衍生变量A2": "1",
            "衍生变量B1": "",
            "衍生变量B2": "1",
            "衍生变量C1": "1",
            "衍生变量C2": "11",
            "衍生变量D1": "${原始变量A2的值}",
            "衍生变量D2": "${原始变量A2+1的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”使得原始变量A1返回非空字符串
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${对应原始变量加工后的计算结果，原始变量A1的值},
            "标准变量B": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A1的值},
            "标准变量C": ${对应原始变量加工后的计算结果，原始变量A1+1的值},
            "标准变量D": ${对应原始变量加工后的计算结果，原始变量A1+原始变量A2的值},
            "衍生变量A1": ${对应标准变量加工后的计算结果，标准变量A的值},
            "衍生变量A2": ${对应标准变量加工后的计算结果，标准变量A+1的值},
            "衍生变量B1": ${对应标准变量加工后的计算结果，标准变量B的值},
            "衍生变量B2": ${对应标准变量加工后的计算结果，标准变量B+1的值},
            "衍生变量C1": ${对应标准变量加工后的计算结果，标准变量C的值},
            "衍生变量C2": ${对应标准变量加工后的计算结果，标准变量C+1的值},
            "衍生变量D1": ${对应标准变量加工后的计算结果，标准变量D的值},
            "衍生变量D2": ${对应标准变量加工后的计算结果，标准变量D+1的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证模型变量空间调用功能正常_标准变量返回空值
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、衍生变量A、B、C、D），postman调用，输入“参数值”使得原始变量A返回非空值
- nullCode对应的值：${dt_variable.dv_system_property.property_name=global_null_value的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${nullCode对应的值},
            "标准变量B": ${原始变量A的值},
            "衍生变量A": ${nullCode对应的值},
            "衍生变量B": ${nullCode对应的值},
            "衍生变量C": ${nullCode对应的值},
            "衍生变量D": ${nullCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证模型变量空间调用功能正常_标准变量返回空字符串
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、衍生变量A、B、C、D），postman调用，输入“参数值”使得原始变量A返回非空字符串
- 调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": "",
            "标准变量B": "${原始变量A的值}",
            "衍生变量A": "",
            "衍生变量B": "",
            "衍生变量C": "1",
            "衍生变量D": "${原始变量A的值}"
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
- 验证模型变量空间调用功能正常_api调用异常
- 进入【业务查询-变量空间】页面，选择一个模型变量空间（添加标准变量A、B、C、D、衍生变量A1、A2、B1、B2、C1、C2、D1、D2），postman调用，输入“参数值”（修改数据产品A引用的api，使得api调用异常（返回状态码非200））
- errorCode对应的值：${dt_variable.dv_system_property.property_name=global_error_code的值}
调用成功，展示大接口返回报文，返回报文样例：
{
    "data": {
        "result": {
            "标准变量A": ${errorCode对应的值},
            "标准变量B": ${errorCode对应的值},
            "标准变量C": ${errorCode对应的值},
            "标准变量D": ${errorCode对应的值},
            "衍生变量A1": ${errorCode对应的值},
            "衍生变量A2": ${errorCode对应的值},
            "衍生变量B1": ${errorCode对应的值},
            "衍生变量B2": ${errorCode对应的值},
            "衍生变量C1": ${errorCode对应的值},
            "衍生变量C2": ${errorCode对应的值},
            "衍生变量D1": ${errorCode对应的值},
            "衍生变量D2": ${errorCode对应的值}
        },
        "error_var_count": ${请求响应失败的变量数},
        "cache_var": [${本次请求使用缓存的变量名称}],
        "interface_names": [${本次请求调用的数据产品名称}]
    },
    "success": ${请求结果，true/false},
    "code": ${请求结果对应的code},
    "message": ${请求结果对应的message},
    "origin_flow_id": ${全局流水号},
    "response_time": ${接口响应时间}
}
