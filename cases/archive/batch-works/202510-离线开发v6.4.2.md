---
suite_name: 202510-离线开发v6.4.2（XMind）
description: 202510-离线开发v6.4.2（XMind）
prd_version: v6.4.2
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - 离线Doris3.x适配
  - 本地数据导入- oracle
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 0
origin: xmind
---
# 202510-离线开发v6.4.2（XMind）
> 来源：zentao-cases/XMind/离线开发/202510-离线开发v6.4.2.xmind

---

## 【岚图+标品】离线Doris3.x适配(#9404)

### 验证「数据源」页面正确展示Doris3.x版本信息

#### 进入「离线开发-数据源」页面

- 进入成功
#### 点击「引入数据源」

- 弹出可用数据源弹窗
#### 点击「数据源类型」下拉框

- 新增可选项「Doris3.x」
#### 选择「Doris3.x」数据源并确定

- 数据源成功引入至离线平台
#### 检查Doris 3.x数据源记录的类型字段值

- 显示为「Doris3.x」
#### 点击「数据源类型」下拉框

- 新增可选项「Doris3.x」
#### 选择Doris3.x

- 过滤出类型为「Doris3.x」的数据源记录
### 验证整库同步功能正常(Doris3.x → Other)

#### 进入「离线开发-数据源」页面

- 进入成功
#### 选择一条Doris3.x类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableA}
目标: SparkThrift2.x
Schema: 选择${schemaA}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建SparkSQL任务, 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询成功, 表记录与Doris3.x表记录一致
#### 再次配置Doris3.x「整库同步」: 
源表: 勾选${tableB}
目标: Hive2.x
Schema: ${schemaB}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建HiveSQL任务, 执行SQL: SELECT * FROM ${schemaB}.${tableB};

- 查询成功, 表记录与Doris3.x表记录一致
#### 再次配置Doris3.x「整库同步」: 
源表: 勾选${tableC}
目标: Doris2.x
Schema: ${schemaC}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建DorisSQL任务, 执行SQL: SELECT * FROM ${schemaC}.${tableC};

- 查询成功, 表记录与Doris3.x表记录一致
### 验证整库同步功能正常(Doris2.x → Other)

#### 进入「离线开发-数据源」页面

- 进入成功
#### 选择一条Doris2.x类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableA}
目标: SparkThrift2.x
Schema: 选择${schemaA}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建SparkSQL任务, 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询成功, 表记录与Doris2.x表记录一致
#### 再次配置Doris2.x「整库同步」: 
源表: 勾选${tableB}
目标: Hive2.x
Schema: ${schemaB}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建HiveSQL任务, 执行SQL: SELECT * FROM ${schemaB}.${tableB};

- 查询成功, 表记录与Doris2.x表记录一致
#### 再次配置Doris2.x「整库同步」: 
源表: 勾选${tableC}
目标: Doris3.x
Schema: ${schemaC}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建DorisSQL任务, 执行SQL: SELECT * FROM ${schemaC}.${tableC};

- 查询成功, 表记录与Doris2.x表记录一致
### 验证整库同步功能正常(Other → Doris3.x)

#### 进入「离线开发-数据源」页面

- 进入成功
#### 选择一条Starrocks类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableA}
目标: Doris3.x
Schema: 选择${schemaA}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建SR SQL任务, 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询成功, 表记录与SR表记录一致
#### 选择一条MySQL类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableB}
目标: Doris3.x
Schema: 选择${schemaB}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建MySQL任务, 执行SQL: SELECT * FROM ${schemaB}.${tableB};

- 查询成功, 表记录与MySQL表记录一致
### 验证整库同步功能正常(Other → Doris2.x)

#### 进入「离线开发-数据源」页面

- 进入成功
#### 选择一条Starrocks类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableA}
目标: Doris2.x
Schema: 选择${schemaA}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建SR SQL任务, 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询成功, 表记录与SR表记录一致
#### 选择一条MySQL类型的数据源记录, 点击「整库同步」

- 进入整库同步页面
#### 配置如下: 
源表: 勾选${tableB}
目标: Doris2.x
Schema: 选择${schemaB}
方式: 全量
日期、时间: 默认
并发配置: 整批上传
保存目录: 默认

- 配置成功
#### 点击发布任务

- 发布成功, 「同步历史」新增同步记录
#### T+1后, 新建MySQL任务, 执行SQL: SELECT * FROM ${schemaB}.${tableB};

- 查询成功, 表记录与MySQL表记录一致
### 验证数据同步功能正常(Doris3.x → Other、session模式)

#### 进入「离线开发-数据开发」页面

- 进入成功
#### 新建周期任务:
名称: Doris2Other
类型: 数据同步
配置模式: 向导模式
向导模式: 无增量标识

- 新增成功
#### 数据来源配置:
数据源: Doris3.x
Schema: ${schemaA}
表名: ${tableA}

- 配置成功, 点击「数据预览」可查看表数据
#### 选择目标配置: 
数据源: SparkThrift2.x
Schema: ${schemaB}
表名: 一键生成目标表
写入模式: 覆盖

- 配置成功
#### 其他配置: 
字段映射: 同名映射
通道控制: 默认

- 配置成功
#### 保存任务后运行

- 运行成功
#### 新建SparkSQL任务, 执行SQL: SELECT * FROM ${schemaB}.${tableA};

- 查询成功, 表记录与Doris3.x表记录一致
#### 编辑数据同步任务Doris2Other的选择目标配置:
数据源: Hive2.x
Schema: ${schemaC}
表名: 一键生成目标表
写入模式: 覆盖

- 配置成功
#### 其他配置保持不变, 保存任务后运行

- 编辑后的任务运行成功
#### 新建HiveSQL任务, 执行SQL: SELECT * FROM ${schemaC}.${tableA};

- 查询成功, 表记录与Doris3.x表记录一致
#### 编辑数据同步任务Doris2Other的选择目标配置:
数据源: Doris2.x
Schema: ${schemaD}
表名: 一键生成目标表
写入模式: 覆盖

- 配置成功
#### 其他配置保持不变, 保存任务后运行

- 编辑后的任务运行成功
#### 新建DorisSQL任务, 执行SQL: SELECT * FROM ${schemaD}.${tableA};

- 查询成功, 表记录与Doris3.x表记录一致
### 验证数据同步功能正常(Other → Doris3.x、per_job模式)

#### 进入「离线开发-数据开发」页面

- 进入成功
#### 新建周期任务:
名称: Other2Doris
类型: 数据同步
配置模式: 向导模式
向导模式: 无增量标识

- 新增成功
#### 数据来源配置:
数据源: SparkThrift2.x
Schema: ${schemaA}
表名: ${tableA}

- 配置成功, 点击「数据预览」可查看表数据
#### 选择目标配置: 
数据源: Doris3.x
Schema: ${schemaB}
表名: 一键生成目标表
写入模式: 覆盖

- 配置成功
#### 其他配置: 
字段映射: 同名映射
通道控制: 默认
环境参数: 开启per_job模式(取消注释: flinkTaskRunMode=per_job)

- 配置成功
#### 保存任务后运行

- 运行成功
#### 执行DorisSQL: SELECT * FROM ${schemaB}.${tableA};

- 查询成功, 表记录与SparkThrift2.x表记录一致
#### 编辑数据同步任务Other2Doris的数据来源配置:
数据源: Hive2.x
Schema: ${schemaC}
表名: ${tableC}

- 配置成功, 点击「数据预览」可查看表数据
#### 其他配置保持不变, 保存任务后运行

- 编辑后的任务运行成功
#### 执行DorisSQL: SELECT * FROM ${schemaB}.${tableC};

- 查询成功, 表记录与Hive2.x表记录一致
#### 编辑数据同步任务Other2Doris的数据来源配置:
数据源: Doris2.x
Schema: ${schemaD}
表名: ${tableD}

- 配置成功, 点击「数据预览」可查看表数据
#### 其他配置保持不变, 保存任务后运行

- 编辑后的任务运行成功
#### 执行DorisSQL: SELECT * FROM ${schemaB}.${tableD};

- 查询成功, 表记录与Doris2.x表记录一致
### 验证 Doris3.x 同步任务导入前后准备语句执行有效

#### 进入「离线开发-数据开发」页面

- 进入成功
#### 新建数据同步任务

- 新增成功
#### 源端配置：
数据源: Doris2.x
Schema: ${schemaA}
表名: ${tableA}

- 配置成功
#### 目标端配置：
数据源: Doris3.x
Schema: ${schemaB}
表:  一键生成目标表
导入前准备语句：TRUNCATE TABLE IF EXISTS ${schemaB}.${tableB};
导入后准备语句:  INSERT INTO ${schemaB}.${tableB} VALUES(2, 'done');

- 配置成功
#### 其他配置: 
字段映射: 同名映射
通道控制: 默认

- 配置成功
#### 保存任务后运行

- 运行成功
#### 执行SQL: SELECT * FROM ${schemaB}.${tableA};

- 查询成功, 表记录与Doris2.x表记录一致
### 验证「控制台」新增Doris3.x组件版本

#### 进入「控制台-多集群管理-集群配置-计算组件」

- 进入成功
#### 组件配置添加Doris

- 添加成功
#### 检查配置项

- 新增「组件版本」配置项, 位于组件名称下方
#### 展开「组件版本」

- 默认选择2.x, 下拉支持选择2.x和3.x
### 验证「创建项目」添加Doris 数据源显示正常(初始化方式: 创建)

#### 进入「离线开发」页面

- 进入成功
#### 点击「创建项目」

- 进入创建项目页面
#### 配置如下: 
项目标识&项目名称: ${nameA}
计算引擎配置: 
No.01: Doris2.x集群${ClusterA}、初始化方式: 「创建」
No.02: Doris3.x集群${ClusterB}、初始化方式: 「创建」

- 配置成功
#### 点击「创建项目」

- 创建成功
#### 进入「数据源」页面

- 进入成功, 数据源列表存在如下数据源记录:
1) 类型为Doris2.x, 数据源名称为${nameA}_DORIS_${ClusterA}的数据源记录
2) 类型为Doris3.x, 数据源名称为${nameA}_DORIS_${ClusterB}的数据源记录
### 验证「创建项目」添加Doris 数据源显示正常(初始化方式: 对接)

#### 进入「离线开发」页面

- 进入成功
#### 点击「创建项目」

- 进入创建项目页面
#### 配置如下: 
项目标识&项目名称: ${nameA}
计算引擎配置: 
No.01: Doris2.x集群${ClusterA}、初始化方式: 「对接已有Doris Schema」、${schemaA}、${表类目A}
No.02: Doris3.x集群${ClusterB}、初始化方式: 「对接已有Doris Schema」、${schemaB}、${表类目A}

- 配置成功
#### 点击「创建项目」

- 创建成功
#### 进入「数据源」页面

- 进入成功, 数据源列表存在如下数据源记录:
1) 类型为Doris2.x, 数据源名称为${nameA}_DORIS_${ClusterA}的数据源记录
2) 类型为Doris3.x, 数据源名称为${nameA}_DORIS_${ClusterB}的数据源记录
### 验证「创建项目」添加Doris数据源显示正常(初始化方式: 不创建且不对接)

#### 进入「离线开发」页面

- 进入成功
#### 点击「创建项目」

- 进入创建项目页面
#### 配置如下: 
项目标识&项目名称: ${nameA}
计算引擎配置: 
No.01: Doris2.x集群${ClusterA}、初始化方式:  「不创建且不对接 Schema」
No.02: Doris3.x集群${ClusterB}、初始化方式: 「不创建且不对接 Schema」

- 配置成功
#### 点击「创建项目」

- 创建成功
#### 进入「数据源」页面

- 进入成功, 数据源列表存在如下数据源记录:
1) 类型为Doris2.x, 数据源名称为${nameA}_DORIS_${ClusterA}的数据源记录
2) 类型为Doris3.x, 数据源名称为${nameA}_DORIS_${ClusterB}的数据源记录
### 验证「数据开发」Doris SQL任务创建功能正常

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 右键「任务开发」, 选择Doris SQL任务

- 「新建周期任务-集群名称」中支持选择创建项目时对接的doris3.x集群名称
#### 新建周期任务:
任务名称: ${nameA}
任务类型: Doris SQL
集群名称: ${ClusterA}

- 周期任务创建成功
#### 进入「手动任务」, 新建手动任务:
任务名称: ${nameB}
任务类型: Doris SQL
集群名称: ${ClusterA}

- 手动任务创建成功
#### 进入「临时查询」, 新建临时查询:
查询名称: ${nameC}
任务类型: Doris SQL
集群名称: ${ClusterA}

- 临时查询创建成功
### 验证 DorisSQL 任务可正常运行并返回结果

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建Doris SQL任务, 输入测试SQL语句并运行

- 执行成功, 返回结果显示正常
#### 进入「离线开发-数据开发-手动任务」页面

- 进入成功
#### 新建Doris SQL任务, 输入测试SQL语句并运行

- 执行成功, 返回结果显示正常
#### 进入「离线开发-数据开发-临时查询」页面

- 进入成功
#### 新建Doris SQL任务, 输入测试SQL语句并运行

- 执行成功, 返回结果显示正常
### 验证 DorisSQL 语法提示功能正常

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建 DorisSQL 任务

- 创建成功
#### 在编辑器中输入 SEL

- 自动提示补全 SELECT
#### 输入 SELECT * FROM dor

- 提示补全表名 doris3_sql_run
#### 输入系统函数前缀 CUR

- 自动提示 CURDATE() 、 CURTIME()
### 验证 DorisSQL 任务支持停止运行中的任务

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建 DorisSQL 任务

- 创建成功
#### 输入SQL测试语句并执行

- 运行成功, 控制台开始打印日志
#### 点击「停止」

- 运行终止, 日志停止打印
### 验证 DorisSQL 支持系统函数调用

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建 DorisSQL 任务

- 创建成功
#### 输入测试语句: SELECT id, CURRENT_DATE() AS cur_date, NOW() AS cur_time FROM doris3_sql_run;

- 执行成功, 返回两行数据
#### 验证输出

- cur_date 与 cur_time 均为系统当前日期和时间
### 验证 DorisSQL 支持自定义函数

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建 DorisSQL 任务

- 创建成功
#### 输入测试语句: select addStr("test");并执行

- 执行成功, 返回结果: hello test
### 验证Doris3.x表查询功能正常

#### 进入「离线开发-数据开发-表查询」页面

- 进入成功
#### 表类型中选择Doris

- 进入数据源名称列表页, 展示当前项目中已对接的Meta Doris集群列表
#### 选择Doris3.x集群${ClusterA}

- 展示该集群下的Schema列表
#### 选择${schemaA}

- 展示该schema下的table列表
#### 选择${tableA}

- 展示该表的字段信息和数据预览
#### 点击数据预览

- 展示表${tableA}的表记录
### 验证Doris3.x同步元数据功能正常

#### 进入「离线开发-数据开发-表查询」页面

- 进入成功
#### 选择Doris-${schemaA}, 查询表doris_meta_sync_test

- 不存在表doris_meta_sync_test
#### 进入「数据源」页面, 选择Meta Doris3.x数据源, 点击同步元数据

- 弹出同步元数据配置弹窗
#### 配置如下:
选择表: 部分表, doris_meta_sync_test
表类目: ${资产数据地图下的表类目}

- 同步成功
#### 再次进入表查询页面, 查询表doris_meta_sync_test

- 查询成功
#### 进入「数据源」页面, 再次选择Doris3.x, 同步元数据, 配置如下:
选择表: 全部表
表类目: ${资产数据地图下的表类目}

- 同步成功
#### 再次进入表查询页面, 查询表doris_meta_sync_test2

- 查询成功
#### 底层删除两张表doris_meta_sync_test, doris_meta_sync_test2后, 再次同步元数据, 配置如下:
选择表: 全部表
表类目: ${资产数据地图下的表类目}

- 同步成功
#### 进入表查询页面, 查询表doris_meta_sync_test和doris_meta_sync_test2

- 不存在
### 验证 DorisSQL任务调度功能运行正常(DDL语法)

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 并临时运行

- 运行成功
#### 保存并提交到运维中心

- 提交成功
#### 周期运行, 查看结果

- 运行成功, 周期任务实例中新增该任务的实例记录
#### 补数据运行, 查看结果

- 运行成功, 补数据任务实例中新增该任务的实例记录
#### 进入「离线开发-数据开发-手动任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 提交到运维中心后手动运行

- 运行成功, 手动任务实例中新增该任务的实例记录
### 验证 DorisSQL任务调度功能运行正常(DML语法)

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 并临时运行

- 运行成功
#### 保存并提交到运维中心

- 提交成功
#### 周期运行, 查看结果

- 运行成功, 周期任务实例中新增该任务的实例记录
#### 补数据运行, 查看结果

- 运行成功, 补数据任务实例中新增该任务的实例记录
#### 进入「离线开发-数据开发-手动任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 提交到运维中心后手动运行

- 运行成功, 手动任务实例中新增该任务的实例记录
### 验证 DorisSQL任务调度功能运行正常(DQL语法)

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 并临时运行

- 运行成功
#### 保存并提交到运维中心

- 提交成功
#### 周期运行, 查看结果

- 运行成功, 周期任务实例中新增该任务的实例记录
#### 补数据运行, 查看结果

- 运行成功, 补数据任务实例中新增该任务的实例记录
#### 进入「离线开发-数据开发-手动任务」页面

- 进入成功
#### 创建Doris SQL任务${task01}、${task02}、${task03}, 提交到运维中心后手动运行

- 运行成功, 手动任务实例中新增该任务的实例记录
### 验证项目维度数据库绑定账号功能正常

#### 进入「离线开发-数据开发-周期任务」页面

- 进入成功
#### 新建 DorisSQL 任务

- 创建成功
#### 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询成功, 当前为root用户在执行
#### 进入项目管理-项目设置-开发设置-Doris配置中，开启项目绑定数据库账号, 新增test_user账号

- 新增成功, 数据回显正常
#### 执行SQL: SELECT * FROM ${schemaA}.${tableA};

- 查询失败
#### 执行SQL: SELECT * FROM lantu_test.${tableB};

- 查询成功, 当前为test_user用户在执行
## 【江苏银行】本地数据导入- oracle(#9405)

### 验证导入模式为追加，导入数据正确

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写，导入模式选择覆盖

- 导入数据成功
#### 查询表test中的数据

- test表中追加写入此次导入的数据
### 验证导入模式为覆盖，导入数据正确

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 选择一张表test（表里已存在数据）
#### 其它数据默认填写，导入模式选择覆盖

- 导入数据成功
#### 查询表test中的数据

- test表中只有此次导入的数据
### 验证按位置匹配，导入数据正确

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 选择按位置匹配

- sex的源字段对应name, name的源字段对应sex
#### 其它数据默认填写，导入模式选择覆盖

- 导入数据成功
#### 查询表test中的数据

- test文件中的sex，name分别对应test表中的name，sex字段
### 验证按名称匹配，导入数据正确

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写，选择按名称匹配

- 导入数据成功
#### 查询表test中的数据

- test文件中的sex，name分别对应test表中的sex，name字段
### 验证导入数据页面功能正确

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 分割符默认选中逗号，自定义按钮可选

- 分割符下拉列表可选择，数据列表随选择同步更新
#### 原始字符集：下拉列表，默认utf-8

- 填写数据大于列表数据条数，列表不展示数据
#### 导入起始行：默认1，所支持的数字大于等于1的正整数

- 显示的数据即为导入的数据
#### 首行为标题勾选项，默认勾选

- 默认勾选：第一行数据为标题
去掉勾选，使用默认标题
### 验证.log文件导入数据成功

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.log文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test表中只有此次导入的数据
### 验证.txt文件导入数据成功

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.txt文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test表中只有此次导入的数据
### 验证.csv文件导入数据成功

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.csv文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test表中只有此次导入的数据
### 验证.xls文件导入数据成功(多个sheet)

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.xls文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test中第一个sheet中的数据被写入
### 验证.xls文件导入数据成功(单个sheet)

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.xls文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test.xls中的数据文件正常导入
### 验证.xlsx文件导入数据成功(多个sheet)

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.xlsx文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test中第一个sheet中的数据被写入
### 验证.xlsx文件导入数据成功(单个sheet)

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.xls文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test.xlsx中的数据文件正常导入
### 验证上传文件大小上限正常(1000列+500M)

#### 进入离线开发-数据开发页面

- 进入成功
#### 点击导入, 上传test.xls文件

- 上传成功
#### 其它值为默认填写

- 点击下一步
#### 选择表类型为Oracle

- 新建一张包含文件中字段的表test
#### 其它数据默认填写

- 导入数据成功
#### 查询表test中的数据

- test.xlsx中的数据文件正常导入
