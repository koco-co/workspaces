---
suite_name: "【Gate】告警规则触发条件新增未按计划时间运行(#9644)（XMind）"
description: "【Gate】告警规则触发条件新增未按计划时间运行(#9644)（XMind）"
prd_id: 9644
prd_version: v6.4.3
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - Gate
  - 告警规则触发条件新增未按计划时间运行
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 48
origin: xmind
---
## 验证告警规则配置 新增触发方式「未按计划时间运行」 与 输入框交互校验

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【“创建告警规则”弹窗】页面 |  |

##### 【P2】在“触发方式”区域勾选 “未按计划时间运行”

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 该选项底下出现两输入框：“计划时间后 [小时] [分钟] 未运行” |  |

##### 【P2】小时输入框保留默认0，分钟输入框保留默认30，点击保存

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 规则保存成功，能看到新规则列表中触发方式包括“未按计划时间运行” |  |

##### 【P2】在小时输入框输入 –1，分钟输入框输入 1，失焦后

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 小时自动调整为0，分钟自动调整为2 |  |

##### 【P2】小时输入框输入 48，分钟输入框输入 70，失焦后

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 小时自动调整为48（最大值），分钟自动调整为59（最大值） |  |

##### 【P2】小时输入框输入 abc（非数字），分钟输入框输入 xyz（非数字），失焦后

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 小时自动调整为0，分钟自动调整为2 |  |

## 验证告警规则(未按计划时间运行)支持对象仅为周期任务

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【“创建告警规则”弹窗】页面 |  |

##### 【P2】告警对象选择「周期任务」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 触发条件中可以选择到「未按计划时间运行」 |  |

##### 【P2】告警对象选择「手动任务」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 触发条件中没有「未按计划时间运行」 |  |

##### 【P2】告警对象选择「基线」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 触发条件中没有「未按计划时间运行」 |  |

##### 【P2】配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: TaskA
触发条件: 未按计划时间运行(0h5min)
告警方式: 钉钉、邮箱、短信、自定义通道全部勾选

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 配置成功 |  |

##### 【P2】保存规则后，列表展示该规则且告警对象显示为周期任务

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 保存规则后，列表展示该规则且告警对象显示为周期任务 | 告警规则列表中“告警对象”列显示“周期任务”且对应触发方式 「未按计划时间运行」 |

## 验证 修改已有告警规则，新增触发方式「未按计划时间运行」

##### 【P2】进入运维中心 → 监控告警 → 告警规则

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 | 存在告警规则A，触发方式仅为 「完成」 |

##### 【P2】点击「编辑」，勾选触发方式 「未按计划时间运行」，点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「编辑」，勾选触发方式 「未按计划时间运行」，点击「确认」 | 修改成功 |

##### 【P2】查看 列表告警规则的触发方式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 查看 列表告警规则的触发方式 | 触发方式 存在 「完成」「未按计划时间运行」 |

## 验证 修改已有告警规则，去除触发方式「未按计划时间运行」

##### 【P2】进入运维中心 → 监控告警 → 告警规则

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 | 存在告警规则A，触发方式为全部 |

##### 【P2】操作「编辑」，去除勾选触发方式 「未按计划时间运行」，点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 修改成功 |  |

##### 【P2】查看 列表告警规则的触发方式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 查看 列表告警规则的触发方式 | 触发方式 不存在 「未按计划时间运行」 |

## 验证告警信息页面支持筛选 「未按计划时间运行」

##### 【P2】进入运维中心 → 监控告警 → 告警信息页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】在“触发方式”筛选框中勾选“未按计划时间运行”

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 列表仅显示触发方式包含“未按计划时间运行”的记录 |  |

##### 【P2】在“触发方式”筛选框中勾选“失败”,“停止”,“未按计划时间运行”

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 列表显示触发方式包含“失败”或“停止”或“未按计划时间运行”的记录 |  |

## 验证告警触发逻辑正常

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】任务A (上游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

任务B (下游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 11:30

保存并提交任务A, B到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 运维中心-周期任务实例中生成A, B的任务实例<br>上游任务A计划开始时间: 12:00<br>下游任务B计划开始时间: 11:30 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”
配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h5min)
告警方式: 钉钉、邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则” | 配置成功 |

##### 【P2】等待11:35左右, 检查告警触发结果

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 11:35左右告警信息发送成功<br>2) 告警信息列表新增一条11:35左右的告警记录 |  |

##### 【P2】检查告警记录的告警内容

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 告警内容格式如下:<br><br>[BatchWorks]离线任务：${任务名称} 超过计划时间 ${配置小时数} 小时 ${配置分钟数} 分钟未运行告警<br>任务：${当前任务名称}<br>项目：${当前项目名称}<br>租户：${当前租户名称}<br>调度类型：周期调度<br>计划时间：${当前任务在调度内运行的计划时间}<br>开始时间：-<br>结束时间：-<br>运行时长：-<br>当前状态：等待运行<br>责任人：${创建告警规则配置的接收人}<br>请及时处理! |  |

## 验证配置多个相同告警规则(未按计划时间运行), 告警正常触发

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】任务A (上游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

任务B (下游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 11:30

保存并提交任务A, B到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 运维中心-周期任务实例中生成A, B的任务实例<br>上游任务A计划开始时间: 12:00<br>下游任务B计划开始时间: 11:30 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”
告警规则A -> 配置如下:
告警规则名称: test_alarm_ruleA
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h2min)
告警方式: 钉钉

告警规则B -> 配置如下:
告警规则名称: test_alarm_ruleB
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h3min)
告警方式: 邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则” | 配置成功 |

##### 【P2】等待11:30左右, 进入「告警信息」中检查告警触发结果

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 11:31左右告警信息成功发送至钉钉, 告警信息列表新增一条11:31左右的告警记录<br>2) 11:32左右告警信息成功发送至邮箱, 告警信息列表新增一条11:32左右的告警记录 |  |

##### 【P2】检查告警记录的告警内容

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 告警内容格式如下:<br><br>[BatchWorks]离线任务：${任务名称} 超过计划时间 ${配置分钟数} 分钟未运行告警<br>任务：${当前任务名称}<br>项目：${当前项目名称}<br>租户：${当前租户名称}<br>调度类型：周期调度<br>计划时间：${当前任务在调度内运行的计划时间}<br>开始时间：-<br>结束时间：-<br>运行时长：-<br>当前状态：等待运行<br>责任人：${创建告警规则配置的接收人}<br>请及时处理! |  |

## 验证调度服务重启后, 告警正常触发

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】任务A (上游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

任务B (下游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 11:00

保存并提交任务A, B到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 提交成功, 运维中心-周期任务实例中生成A, B的任务实例 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”
配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h30min)
告警方式: 钉钉、邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则” | 配置成功 |

##### 【P2】在计划开始时间11:00~未按计划时间运行11:30之间重启调度服务

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 重启成功, 11:30左右发送告警信息<br>2) 告警信息列表新增一条11:35左右的告警记录 |  |

## 验证周期任务取消后, 告警正常触发

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】任务A (上游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

任务B (下游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 11:00

保存并提交任务A, B到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 运维中心-周期任务实例中生成A, B的任务实例 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”
配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h30min)
告警方式: 钉钉、邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则” | 配置成功 |

##### 【P2】进入周期任务实例, 杀死任务B实例

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【周期任务实例】页面, 杀死任务B实例 | 1) 取消成功, 11:30左右发送告警信息<br>2) 告警信息列表新增一条11:30左右的告警记录 |

## 验证周期任务冻结后, 告警正常触发

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】任务A (上游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

任务B (下游) → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 11:00

保存并提交任务A, B到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 运维中心-周期任务实例中生成A, B的任务实例 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则”
配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: 任务B (下游) 
触发条件: 未按计划时间运行(0h30min)
告警方式: 钉钉、邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入运维中心 → 监控告警 → 告警规则 → 点击 “创建告警规则” | 配置成功 |

##### 【P2】进入周期任务B-调度属性, 冻结任务B

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【周期任务B-调度属性】页面, 冻结任务B | 1) 冻结成功, 11:30左右发送告警信息<br>2) 告警信息列表新增一条11:30左右的告警记录 |

## 验证yarn 资源队列阻塞，触发【未按计划时间】告警

##### 【P2】进入离线开发 → 周期任务页面

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入成功 |  |

##### 【P2】配置多个Shell任务, 并设置最短调度周期, 配置如下:
实例生成方式: 立即生成
调度周期: 分钟
开始时间: 00:00
间隔时间: 5分钟
结束时间: 23:59

保存并提交这些任务到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 运维中心-周期任务实例中生成多个「等待提交」的任务实例<br>2) Yarn资源队列阻塞, 后续任务实例状态一直处于「等待提交」 |  |

##### 【P2】配置周期任务A → 调度属性配置如下:
实例生成方式: 立即生成
调度周期: 天
开始时间: 12:00

保存并提交任务A到运维中心

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 运维中心-周期任务实例中生成任务A的实例, 状态为「等待提交」 |  |

##### 【P2】进入运维中心 → 监控告警 → 告警规则, 创建告警规则, 配置如下:
告警规则名称: test_alarm_rule
告警对象: 周期任务
监控任务: 任务A
触发条件: 未按计划时间运行(0h2min)
告警方式: 钉钉、邮箱

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【运维中心 → 监控告警 → 告警规则】页面, 创建告警规则, 配置如下: | 配置成功 |

##### 【P2】等待12:02左右, 进入「告警信息」中检查告警触发结果

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 12:02左右告警信息成功发送, 告警信息列表新增告警记录 |  |