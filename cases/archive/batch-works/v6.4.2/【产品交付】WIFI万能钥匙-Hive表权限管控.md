---
suite_name: 【产品交付】WIFI万能钥匙-Hive表权限管控 v6.4.2
description: 【产品交付】WIFI万能钥匙-Hive表权限管控 v6.4.2
prd_version: v6.4.2
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - hive表权限管控
  - 产品交付
  - WIFI万能钥匙
  - Hive表权限管控
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 26
origin: csv
---
# 【产品交付】WIFI万能钥匙-Hive表权限管控 v6.4.2
> 来源：zentao-cases/dtstack-platform/离线开发/archive-cases/v6.4.2/【产品交付】WIFI万能钥匙-Hive表权限管控.csv
> 用例数：26

---

## hive表权限管控

##### 验证【通用信息】页面展示正确 「P2」

> 前置条件
```
无
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 1）【select查询结果限制】模块下方新增【数据权限管理】模块 |
| 2 |  | 2）【数据权限管理】模块展示字段：数据地图外部表权限管控 |

##### 验证【数据权限管控】模块显示&交互正确 「P2」

> 前置条件
```
1）当前存在多个集群：集群A、集群B
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入集群A的【多集群管理-绑定租户-通用信息】页面，查看【数据地图外部表权限管控】 | 1）默认不勾选 |
| 2 | 鼠标移至“问号icon” | 2）【数据地图外部表权限管控】右侧存在“问号icon” |
| 3 | 勾选【数据地图外部表权限管控】 | hover展示：用于管控未同步至离线数据地图的数据表操作权限。开启后，任务中不能直接操作数据地图外部表 |
| 4 | 点击“编辑” | 1）toast提示：切换成功！ |
| 5 | 查看【数据地图外部表权限管控】-【生效表类型】 | 2）【数据地图外部表权限管控】为勾选状态 |
| 6 | 【数据地图外部表权限管控】-【生效表类型】勾选“Hive”后，点击“取消” | 3）【数据地图外部表权限管控】右侧新增“编辑”按钮 |
| 7 | 【数据地图外部表权限管控】-【生效表类型】勾选“Hive”后，点击“确定” | 进入【数据地图外部表权限管控】弹窗 |
| 8 |  | 1）title：数据地图外部表权限管控 |
| 9 |  | 2）展示字段：生效表类型 |
| 10 |  | 3）“取消、确定、X”按钮 |
| 11 |  | 1）【生效表类型】默认不勾选 |
| 12 |  | 2）【生效表类型】枚举：Hive |
| 13 |  | 3）【生效表类型】勾选后，为选中状态 |
| 14 |  | “Hive”仍为未勾选状态 |
| 15 |  | 保存成功，“Hive”为勾选状态 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_create 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务，并进行周期&补数据运行 | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交hivesql任务 | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 7 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 8 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 9 |  | 1）任务提交失败 |
| 10 |  | 2）进入异常弹窗，提示文案： |
| 11 |  | “以下schema当前用户无操作权限 |
| 12 |  | ${schema名称} |
| 13 |  | 以下表当前用户无权限 |
| 14 |  | ${schema名称.表名称}” |
| 15 |  | 3）点击“知道了”关闭弹窗，任务不提交 |
| 16 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 17 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 18 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 19 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 20 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_insert 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务 | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 1）任务提交失败 |
| 5 | 提交hivesql任务，并进行周期&补数据运行 | 2）进入异常弹窗，提示文案： |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | “以下schema当前用户无操作权限 |
| 7 | 提交hivesql任务 | ${schema名称} |
| 8 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 以下表当前用户无权限 |
| 9 | 提交hivesql任务，并进行周期&补数据运行 | ${schema名称.表名称，，table_a}” |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 15 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 16 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 17 |  | 1）任务提交失败 |
| 18 |  | 2）进入异常弹窗，提示文案： |
| 19 |  | “以下schema当前用户无操作权限 |
| 20 |  | ${schema名称} |
| 21 |  | 以下表当前用户无权限 |
| 22 |  | ${schema名称.表名称，，table_a}” |
| 23 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 24 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 25 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 26 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 27 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_select 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务 | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_b; | 1）任务提交失败 |
| 5 | 提交hivesql任务，并进行周期&补数据运行 | 2）进入异常弹窗，提示文案： |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | “以下schema当前用户无操作权限 |
| 7 | 提交hivesql任务 | ${schema名称} |
| 8 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 以下表当前用户无权限 |
| 9 | 提交hivesql任务，并进行周期&补数据运行 | ${schema名称.表名称，，table_a}” |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 15 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 16 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 17 |  | 1）任务提交失败 |
| 18 |  | 2）进入异常弹窗，提示文案： |
| 19 |  | “以下schema当前用户无操作权限 |
| 20 |  | ${schema名称} |
| 21 |  | 以下表当前用户无权限 |
| 22 |  | ${schema名称.表名称，，table_a}” |
| 23 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 24 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 25 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 26 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 27 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_create 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务，并进行周期&补数据运行 | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交hivesql任务，并进行周期&补数据运行 | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 7 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 8 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 9 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 10 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 11 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 12 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 13 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 14 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 15 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 16 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_insert 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务，并进行周期&补数据运行 | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交hivesql任务，并进行周期&补数据运行 | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 7 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 8 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 9 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 10 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 11 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 12 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 13 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 14 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 15 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 16 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 17 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 18 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 19 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 20 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 21 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_select 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交hivesql任务，并进行周期&补数据运行 | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_b; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交hivesql任务，并进行周期&补数据运行 | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 7 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 8 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 9 | 提交hivesql任务，并进行周期&补数据运行 | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 10 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 11 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 12 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 13 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 14 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 15 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 16 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 17 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 18 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 19 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 20 |  | 2）周期任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 21 |  | 3）补数据运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：创建
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：对接已有schema
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：不创建且不对接
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-周期任务】页面，新建hivesql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证sparksql任务权限正确_开启数据权限管控_hive表生效_create 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交sparksql任务 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 | 提交sparksql任务，并进行手动运行 | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 8 |  | 1）任务提交失败 |
| 9 |  | 2）进入异常弹窗，提示文案： |
| 10 |  | “以下schema当前用户无操作权限 |
| 11 |  | ${schema名称} |
| 12 |  | 以下表当前用户无权限 |
| 13 |  | ${schema名称.表名称}” |
| 14 |  | 3）点击“知道了”关闭弹窗，任务不提交 |
| 15 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 16 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 17 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 18 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_hive表生效_insert 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务 | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 1）任务提交失败 |
| 5 | 提交sparksql任务，并进行手动运行 | 2）进入异常弹窗，提示文案： |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | “以下schema当前用户无操作权限 |
| 7 | 提交sparksql任务 | ${schema名称} |
| 8 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 以下表当前用户无权限 |
| 9 | 提交sparksql任务，并进行手动运行 | ${schema名称.表名称，，table_a}” |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 15 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 16 |  | 1）任务提交失败 |
| 17 |  | 2）进入异常弹窗，提示文案： |
| 18 |  | “以下schema当前用户无操作权限 |
| 19 |  | ${schema名称} |
| 20 |  | 以下表当前用户无权限 |
| 21 |  | ${schema名称.表名称，，table_a}” |
| 22 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 23 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 24 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 25 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_hive表生效_select 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务 | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_b; | 1）任务提交失败 |
| 5 | 提交sparksql任务，并进行手动运行 | 2）进入异常弹窗，提示文案： |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | “以下schema当前用户无操作权限 |
| 7 | 提交sparksql任务 | ${schema名称} |
| 8 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 以下表当前用户无权限 |
| 9 | 提交sparksql任务，并进行手动运行 | ${schema名称.表名称，，table_a}” |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 15 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 16 |  | 1）任务提交失败 |
| 17 |  | 2）进入异常弹窗，提示文案： |
| 18 |  | “以下schema当前用户无操作权限 |
| 19 |  | ${schema名称} |
| 20 |  | 以下表当前用户无权限 |
| 21 |  | ${schema名称.表名称，，table_a}” |
| 22 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 23 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 24 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 25 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_hive表生效_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证sparksql任务权限正确_开启数据权限管控_无生效表类型_create 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交sparksql任务，并进行手动运行 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 8 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 9 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_无生效表类型_insert 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交sparksql任务，并进行手动运行 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 8 | 修改sparksql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 9 | 提交sparksql任务，并进行手动运行 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 15 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 16 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 17 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_无生效表类型_select 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_b; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 5 | 提交sparksql任务，并进行手动运行 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 6 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 | 提交sparksql任务，并进行手动运行 | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 8 | 修改sparksql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 9 | 提交sparksql任务，并进行手动运行 | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 10 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 11 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 12 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 13 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |
| 14 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 15 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 16 |  | 1） 系统提示任务提交成功，任务状态更新为【待运行/调度中/运行中】 |
| 17 |  | 2）手动任务运行成功，状态更新为【成功】，运行/执行结果符合预期，日志无报错 |

##### 验证sparksql任务权限正确_开启数据权限管控_无生效表类型_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：对接已有schema
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：不创建且不对接
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：创建
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-手动任务】页面，新建sparksql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_create 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_insert 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 8 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 9 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_select 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_b; | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行失败，日志异常提示“以下schema当前用户无操作权限 \n ${schema名称} \n 以下表当前用户无权限 \n ${schema名称.表名称，table_a}” |
| 8 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 9 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_hive表生效_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目A的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_create 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_1 (id int, name string); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_a; | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：create table if not exists table_c_2 as select * from table_b; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行成功，表创建成功，系统给出创建成功提示，列表中出现新创建的记录 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_insert 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：insert into table table_a values (1,'qq'); | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table table_b values (1,'qq'); | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 | 修改hivesql任务，保存并临时运行，sql脚本如下：insert into table_c select * from table_b; | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 8 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 9 |  | 2）临时运行成功，数据写入成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_select 「P1」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b、table_c，其中table_a没有同步至数据地图，table_b、table_c已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：select * from table_a; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_b; | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 4 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_a on table_c.id = table_a.id; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 5 | 修改hivesql任务，保存并临时运行，sql脚本如下：select * from table_c left join table_b on table_c.id = table_b.id; | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 6 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 7 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |
| 8 |  | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 9 |  | 2）临时运行成功，数据查询成功，下载成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

##### 验证hivesql任务权限正确_开启数据权限管控_无生效表类型_show tables 「P2」

> 前置条件
```
1）集群A：已开启hive表的数据权限管控；已创建项目A，初始化方式：不创建且不对接
2）集群B：已开启数据权限管控，但生效表类型为空；已创建项目B，初始化方式：创建
3）集群C：未开启数据权限管控；已创建项目C，初始化方式：对接已有schema
4）已存在table_a 、table_b，其中table_a没有同步至数据地图，table_b已同步至数据地图
sql如下：
create table if not exists table_a (id int, name string);
insert into table table_a values (1,'qq');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息 | 成功进入【控制台-多集群管理】页面，选择集群A进入【绑定租户-通用信息】页面，查看页面展示信息页面，页面内容正常加载显示，无报错 |
| 2 | 数据开发用户进入项目B的【数据开发-临时查询】页面，新建hivesql任务，保存并临时运行，sql脚本如下：show tables; | 1） 系统提示保存成功，修改内容已生效并在页面中更新显示 |
| 3 |  | 2）临时运行成功，数据查询成功，系统给出成功反馈，相关页面/数据状态更新为最新 |

