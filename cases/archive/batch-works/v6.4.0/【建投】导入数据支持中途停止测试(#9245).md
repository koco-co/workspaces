---
suite_name: "【建投】导入数据支持中途停止测试(#9245)（XMind）"
description: "【建投】导入数据支持中途停止测试(#9245)（XMind）"
prd_id: 9245
prd_version: v6.4.0
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - 建投
  - 导入数据支持中途停止测试
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 66
origin: xmind
---
## 验证本地数据导入时取消导入功能正常(MySQL)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择MySQL, 点击新建表, 输入MySQL建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建mysql任务, 执行sql: select * from dtstack_employees_mysql; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建mysql任务, 执行sql: select * from dtstack_employees_mysql; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、MySQL表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | employee_id有唯一约束, 导入失败并给出弹窗报错 |  |

##### 【P2】创建msyql任务, 删除唯一约束: ALTER TABLE dtstack_employees_mysql DROP PRIMARY KEY;

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建msyql任务, 删除唯一约束: ALTER TABLE dtstack_employees_mysql DROP PRIMARY KEY; | 约束删除成功 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、MySQL表类型和表名(选择上述的脏数据表), 导入模式选择「覆盖」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 依次选择相同的csv文件、MySQL表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(SR)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择SR, 点击新建表, 输入SR唯一约束建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建sr任务, 执行sql: select * from dtstack_employees_sr2; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建sr任务, 执行sql: select * from dtstack_employees_sr2; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、SR表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的upsert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在一份csv文件记录 |  |

##### 【P2】重新点击导入, 选择相同的csv文件, 表类型选择SR, 点击新建表, 输入SR无约束表语句后确定, 导入模式选择「覆盖」后, 点击「导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 选择相同的csv文件, SR表类型和表名(无约束表),  点击新建表, 输入SR无约束表语句后确定, 导入模式选择「追加」后, 点击「导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(Oushu)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择Oushu, 点击新建表, 输入Oushu建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建oushu任务, 执行sql: select * from dtstack_employees_oushu; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oushu任务, 执行sql: select * from dtstack_employees_oushu; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、Oushu表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据在之前的脏数据表上加上完整的csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(Oracle)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择Oracle, 点击新建表, 输入Oracle建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建oracle任务, 执行sql: select * from dtstack_employees_oracle; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oracle任务, 执行sql: select * from dtstack_employees_oracle; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、Oracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | employee_id有唯一约束, 导入失败并给出弹窗报错 |  |

##### 【P2】创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_oracle DROP CONSTRAINT uq_employee_id;

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_oracle DROP CONSTRAINT uq_employee_id; | 约束删除成功 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、oracle表类型和表名(选择上述的脏数据表), 导入模式选择「覆盖」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 依次选择相同的csv文件、oracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(dmoracle)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择dmoracle, 点击新建表, 输入dmoracle建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建dmoracle任务, 执行sql: select * from dtstack_employees_dmoracle; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建dmoracle任务, 执行sql: select * from dtstack_employees_dmoracle; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、dmOracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | employee_id有唯一约束, 导入失败并给出弹窗报错 |  |

##### 【P2】创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_dmoracle DROP CONSTRAINT uq_employee_id;

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_dmoracle DROP CONSTRAINT uq_employee_id; | 约束删除成功 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、dmoracle表类型和表名(选择上述的脏数据表), 导入模式选择「覆盖」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 依次选择相同的csv文件、dmoracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(oboracle)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择oboracle, 点击新建表, 输入oboracle建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建oboracle任务, 执行sql: select * from dtstack_employees_oboracle; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oboracle任务, 执行sql: select * from dtstack_employees_oboracle; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、obOracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | employee_id有唯一约束, 导入失败并给出弹窗报错 |  |

##### 【P2】创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_oboracle DROP CONSTRAINT uq_employee_id;

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建oracle任务, 删除唯一约束: ALTER TABLE dtstack_employees_oboracle DROP CONSTRAINT uq_employee_id; | 约束删除成功 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、oboracle表类型和表名(选择上述的脏数据表), 导入模式选择「覆盖」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 依次选择相同的csv文件、oboracle表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |

## 验证本地数据导入时取消导入功能正常(GP)

##### 【P2】进入数据开发-周期任务页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【数据开发-周期任务】页面, 选中任一任务后, 点击导入, 选择csv文件后, 点击下一步 | 1) 导入csv文件后, 弹出本地数据导入页面, 并将csv文件内容自动解析<br>2) 点击下一步后, 可以选择表类型和表 |

##### 【P2】表类型选择gp, 点击新建表, 输入gp建表语句后确定

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 建表成功 |  |

##### 【P2】导入模式选择「覆盖」后, 点击导入, 检查页面窗口右上角样式

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮 |  |

##### 【P2】点击「取消导入」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「取消导入」 | 展示二次确认弹窗: <br>1) 确认取消导入吗?<br>2) 按钮: 取消、确认 |

##### 【P2】点击「确认」

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 点击「确认」 | toast提示“已取消导入” |

##### 【P2】创建gp任务, 执行sql: select * from dtstack_employees_gp; 检查表数据

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建gp任务, 执行sql: select * from dtstack_employees_gp; 检查表数据 | 1) 数据库操作为非事务，已导入的数据无需回滚，「停止」后的未导入的数据停止insert操作<br>2) 执行select * from 得到的表存在未回滚的脏数据 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、gp表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | employee_id有唯一约束, 导入失败并给出弹窗报错 |  |

##### 【P2】创建gp任务, 删除唯一约束:ALTER TABLE dtstack_employees_gp DROP CONSTRAINT uq_employee_id;

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 创建gp任务, 删除唯一约束:ALTER TABLE dtstack_employees_gp DROP CONSTRAINT uq_employee_id; | 约束删除成功 |

##### 【P2】重新点击导入, 依次选择相同的csv文件、gp表类型和表名(选择上述的脏数据表), 导入模式选择「覆盖」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行 select * from 得到的表数据与csv文件内容一致 |  |

##### 【P2】重新点击导入, 依次选择相同的csv文件、gp表类型和表名(选择上述的脏数据表), 导入模式选择「追加」后, 点击导入

> 前置条件
```
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 导入成功, 触发数据库的insert操作, 页面窗口右上角显示:<br>1) 正在导入的loading<br>2) 导入的表名+进度条<br>3) 「取消导入」按钮<br>执行select * from 得到的表数据存在两份csv文件记录 |  |