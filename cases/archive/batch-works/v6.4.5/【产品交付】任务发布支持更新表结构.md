# 【产品交付】任务发布支持更新表结构 v6.4.5
> 来源：zentao-cases/dtstack-platform/离线开发/archive-cases/v6.4.5/【产品交付】任务发布支持更新表结构.csv
> 用例数：86

---

## 离线开发-项目管理-发布管理

##### 验证【发布包校验】变更SQL失败后, 日志记录正常（Doris + 导入导出发布) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_21，且任务中包含 CREATE TABLE 语句
5) 建表语句：

项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    amount VARCHAR(100) COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    amount VARCHAR(200) COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
-- 插入测试数据
INSERT INTO source_table_fail VALUES (1, '张三', '100'), (2, '李四', 'abc');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_21, 添加关联表source_table_fail并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 修改字段 amount (VARCHAR(200) -> VARCHAR(100)) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败 |
| 5 | 点击查看, 进入【查看发布包】页面 | 显示所有任务发布对象 |
| 6 | 点击日志, 检查日志内容 | 存在发布失败信息: Cannot shorten string length |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + PRIMARY KEY / UNIQUE KEY 定义、变更） 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_20，且任务中包含 CREATE TABLE 语句
5) 建表语句：

场景1：PRIMARY KEY 定义（测试项目无主键，生产项目有主键）
项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_def (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_def (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景2：PRIMARY KEY 变更（主键字段不同）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_change (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
PRIMARY KEY(id, name)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_change (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景3：UNIQUE KEY 定义（测试项目无唯一键，生产项目有唯一键）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_def (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_def (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景4：UNIQUE KEY 变更（唯一键字段不同）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_change (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    email VARCHAR(100) COMMENT '邮箱'
)
UNIQUE KEY(id, email)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_change (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    email VARCHAR(100) COMMENT '邮箱'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景5：PRIMARY KEY 和 UNIQUE KEY 之间切换
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_key_switch (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_key_switch (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_20, 添加关联表source_table_pk_def等5张表并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + 字段DEFAULT值/COMMENT变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_default (
id INT COMMENT '主键ID',
name VARCHAR(100) DEFAULT '匿名' COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

项目 test_project_02 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（DEFAULT值和COMMENT不同）
CREATE TABLE IF NOT EXISTS source_table_default (
id INT COMMENT '主键ID',
name VARCHAR(100) DEFAULT '未知' COMMENT '用户姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table_default并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + 分区字段与普通字段同名) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- Doris表
-- 测试项目 test_project_01 中的表（普通字段dt）
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
dt VARCHAR(20) COMMENT '日期（普通字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02 中的表：
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
PARTITION BY RANGE(dt) (
PARTITION p202501 VALUES LESS THAN ('2025-02-01'),
PARTITION p202502 VALUES LESS THAN ('2025-03-01')
)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (“replication_num“ = “1“);
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_field_to_partition并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + 分区字段新增/删除/类型变更) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 建表语句：

场景1：分区字段类型变更
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02 中的表：
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt VARCHAR(20) COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景2：分区字段新增
项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期',
region VARCHAR(50) COMMENT '分区'
)
DUPLICATE KEY(id, dt, region)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景3：分区字段删除
测试项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期',
region VARCHAR(50) COMMENT '分区'
)
DUPLICATE KEY(id, dt, region)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_partition_type等3张表并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + 无结构变更/新建表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_17，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_same (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 新建表
CREATE TABLE IF NOT EXISTS source_table_create (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_same (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_17, 添加关联表source_table_same、source_table_create并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 导入导出发布 + 不勾选变更项) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_16，且任务中包含 CREATE TABLE 语句
5) 建表语句：

项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_16, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: source_table: 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) Toast提示: 发布成功2) 不会出现二次确认发布弹窗 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 勾选部分表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_15，且任务中包含 CREATE TABLE 语句
5) 建表语句：

项目 test_project_01 中的表：
-- 表1：存在新增字段
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在新增字段
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status VARCHAR(20) COMMENT '状态'
)
DUPLICATE KEY(order_id)
DISTRIBUTED BY HASH(order_id) BUCKETS 10;

-- 项目test_project_02
-- 数据源: Doris（生产集群地址）
-- 表1（缺少age字段）
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2（缺少status字段）
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
DUPLICATE KEY(order_id)
DISTRIBUTED BY HASH(order_id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_15, 添加关联表source_tableA、source_tableB并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - source_tableA: 新增字段 age- source_tableB: 新增字段 status |
| 4 | 勾选source_tableB的变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 1) source_tableA表结构不变2) source_tableB表结构变更成功 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 新建表 + 结构变更表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_14，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 表1：新建表（生产项目中不存在）
CREATE TABLE IF NOT EXISTS new_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
create_time DATETIME COMMENT '创建时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：结构变更表（生产项目中已存在）
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改）',
email VARCHAR(100) COMMENT '邮箱（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（将被修改）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_14, 添加关联表new_table、change_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: change_table: 删除字段 name, 新增字段 username, email |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 1) 新增表 new_table2)change_table表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 多表 + 多变更类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改）',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name VARCHAR(200) COMMENT '产品名称'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表3：存在新增字段
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status VARCHAR(20) COMMENT '状态（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（将被修改）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name VARCHAR(200) COMMENT '产品名称',
price DECIMAL(10,2) COMMENT '价格（将被删除）',
stock INT COMMENT '库存（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表3：初始状态
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_13, 添加关联表source_table_m1~m3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 1) source_table_m1: 删除字段 name, 新增字段 username, phone2) source_table_m2: 删除字段 price, stock3) source_table_m3: 新增字段 status |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 删除字段) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_12，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_delcol (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_delcol (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    age INT COMMENT '年龄（将被删除）',
    address VARCHAR(200) COMMENT '地址（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_12, 添加关联表source_table_delcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 删除字段 address |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 修改字段名称) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_11，且任务中包含 CREATE TABLE 语句
5) 建表语句：

--项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改后）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（修改前）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_11, 添加关联表source_table_rename并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 新增字段 username |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 修改字段类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
amount BIGINT COMMENT '金额（类型修改）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
amount INT COMMENT '金额（原始类型）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_10, 添加关联表source_table_type并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 修改字段 amount (INT -> BIGINT) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 导入导出发布 + 新增字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_09，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增字段）',
email VARCHAR(100) COMMENT '邮箱（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_09, 添加关联表source_table_addcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 新增字段 age (int) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL选项非持久化 （Doris + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
new_column VARCHAR(50) COMMENT '新增字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_nopersist并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更 |
| 6 | 勾选变更SQL选项 | 选项处于勾选状态 |
| 7 | 重新进入【发布包校验】页面 | 变更SQL选项恢复为未勾选状态 |
| 8 | 再次勾选“变更SQL“选项 | 选项可以正常勾选 |

##### 验证【发布包校验】变更SQL发布, 二次确认弹窗显示正常（Doris + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
address VARCHAR(200) COMMENT '地址（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_confirm并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更 |
| 6 | 点击【发布】按钮 | 显示二次确认弹窗1) 标题: 执行表结构变更SQL2) 文案: 当前操作将在发布过程中自动执行表结构变更 SQL，并直接作用于目标项目表结构，可能对任务和数据产生影响3) 按钮: 取消、确定 |

##### 验证【发布包校验】差异对比正常（Doris + 多表 + 多变更类型） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改字段名）',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（将被改名）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
order_date DATE COMMENT '订单日期（将被删除）',
amount DECIMAL(10,2) COMMENT '金额（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table1, source_table2并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更:1) 表名: source_table1变更说明: - 删除字段 old_field (int)- 新增字段 new_field (string)2) 表名: source_table2变更说明: - 删除字段 create_time (TIMESTAMP)- 修改字段 amount (DECIMAL(18,2) -> DECIMAL(10,2))- 修改字段 status (int -> string) |

##### 验证【发布包校验】差异对比正常（Doris + 单表 + 单字段变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_single并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更:1) 表名: test_tableA2) 变更说明: 修改字段 age (string -> int) |

##### 验证【发布包校验】 变更说明按风险顺序(删除→修改→新增)正确展示(Doris + 导入导出发布） 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
new_field VARCHAR(50) COMMENT '新增字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
old_field INT COMMENT '待删除字段',
age BIGINT COMMENT '待修改类型字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_multi并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【导出】按钮 | 导出zip包 |
| 4 | 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 5 | 点击【导入发布包】, 并上传zip包 | 显示【发布包检查】弹窗 |
| 6 | 勾选知晓风险并继续导入 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 7 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 8 | 查看【变更说明】区域的变更项排序 | 变更项按照风险顺序正确排列：删除字段 old_field(INT）修改字段 age (STRING -> INT）新增字段 new_field(STRING) |

##### 验证【发布包校验】 变更信息显示正常（Doris + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
create_time DATETIME COMMENT '创建时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
update_time DATETIME COMMENT '更新时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【导出】按钮 | 导出zip包 |
| 4 | 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 5 | 点击【导入发布包】, 并上传zip包 | 显示【发布包检查】弹窗 |
| 6 | 勾选知晓风险并继续导入 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 7 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 8 | 检查【表】模块信息 | “表”模块icon变更为黄色警告, 表格列信息:1) 标题: 本项目表结构存在变更2) 问号icon悬浮提示: 当前仅支持对 Doris、Hive 表的字段新增、字段删除及字段类型修改进行提示。在表格左侧勾选对应变更项后，系统将在发布过程中自动执行相应的表结构变更 SQL，变更将直接作用于目标项目的数据表结构，可能对下游任务及数据产生影响，请谨慎操作。3) 表格列包括:- 数据源名称：${数据源名称}（${数据源类型}）- Schema名称：展示表结构存在差异的表所在的schema名称- 表名：展示结构存在差异的表名- 变更说明：展示当前表内字段变更的情况，包括删除字段、修改字段类型、新增字段。按照变更风险从高到低的顺序展示，依次为：删除字段 → 修改字段 → 新增字段- 删除：删除字段 ${字段名称}（${字段类型}）- 修改：修改字段 ${字段名称}（${原字段类型} -> ${新字段类型}}）- 新增：新增字段 ${字段名称}（${字段类型}） |

##### 验证【发布包校验】变更SQL失败后, 日志记录正常（Hive + 导入导出发布) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 测试项目中的源表尝试将STRING类型改为INT，但目标表中该字段包含无法转换为INT的数据
6) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount INT COMMENT '金额（类型修改为INT）'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount STRING COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
STORED AS ORC;

-- 生产表中已插入测试数据
INSERT INTO TABLE source_table_fail VALUES (1, '张三', '100'), (2, '李四', 'abc');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_21, 添加关联表source_table_fail并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 修改字段 amount (string -> int) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败 |
| 5 | 点击查看, 进入【查看发布包】页面 | 显示所有任务发布对象 |
| 6 | 点击日志, 检查日志内容 | 正确显示发布失败信息 |

##### 验证未执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 导入导出发布） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」为2026-01-01 10:00:00
6) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_20, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) 发布成功, 记录发布包列表中的【发布时间】2) 不存在二次确认弹窗 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 进入【数据地图】→【数据表管理】→【source_table详情页】 | 1) 【存储信息】→【表结构最后变更时间】不变2) 【存储信息】→【数据最后变更时间】不变 |

##### 验证执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 导入导出发布 + 部分失败) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」
6) 建表语句：

-- 场景：多表变更，部分成功、部分失败
-- Hive表
-- 表1：成功变更（新增字段）
-- 测试项目 test_project_01 中的表
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_success1 (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（缺少age字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_success1 (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 表2：成功变更（修改字段类型，类型兼容）
-- 测试项目 test_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_success2 (
    id INT COMMENT '主键ID',
    amount BIGINT COMMENT '金额（类型为BIGINT）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（amount字段为INT）
CREATE TABLE IF NOT EXISTS source_table_success2 (
    id INT COMMENT '主键ID',
    amount INT COMMENT '金额（类型为INT）'
)
STORED AS ORC;

-- 表3：失败变更（字段类型不兼容，包含无法转换的数据）
-- 测试项目 test_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    amount INT COMMENT '金额（类型修改为INT）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    amount STRING COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table_success1~3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更- source_table_success1: 新增 age字段- source_table_success2: 修改amount字段 int -> bigint- source_table_success3: 修改amount字段 string -> int |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败, 记录发布包列表中的【发布人】、【发布时间】 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) source_table_success1~2 表结构已成功变更2) source_table_success3 表结构不变 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table_success1~3表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 分别进入【数据地图】→【数据表管理】→【source_table_success1~3详情页】 | 1) source_table_success1~2:- 【存储信息】→【表结构最后变更时间】更新为发布时间- 【存储信息】→【数据最后变更时间】更新为发布时间2) source_table_success3: - 【存储信息】→【表结构最后变更时间】不变- 【存储信息】→【数据最后变更时间】不变 |

##### 验证执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 导入导出发布) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」
6) 建表语句：

-- 项目 test_project_01 中的表：
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 新增字段 age |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布成功, 记录发布包列表中的【发布人】、【发布时间】 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 进入【数据地图】→【数据表管理】→【source_table详情页】 | 1) 【存储信息】→【表结构最后变更时间】更新为发布时间2) 【存储信息】→【数据最后变更时间】更新为发布时间 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 导入导出发布 + 分区字段与普通字段同名) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- Hive表
-- 测试项目 test_project_01 中的表（普通字段dt）
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
dt STRING COMMENT '日期（普通字段）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（dt为分区字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
PARTITIONED BY (dt STRING COMMENT '日期（分区字段）')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_field_to_partition并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 导入导出发布 + 分区字段新增/删除/类型变更) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 场景1：分区字段类型变更
-- 项目 test_project_01
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
PARTITIONED BY (dt DATE COMMENT '日期')
STORED AS ORC;

-- 项目 test_project_02
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 场景2：分区字段新增
-- 项目 test_project_01
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期', region STRING COMMENT '分区')
STORED AS ORC;

-- 项目 test_project_02
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 场景3：分区字段删除
-- 项目 test_project_01
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 项目 test_project_02
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期', region STRING COMMENT '分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 导入导出发布 + 无结构变更/新建表) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目test_project_01
CREATE TABLE IF NOT EXISTS source_table_same (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table_same (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_17, 添加关联表source_table_same并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 导入导出发布 + 不勾选变更项) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目test_project_01
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_16, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: source_table: 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) Toast提示: 发布成功2) 不会出现二次确认发布弹窗 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 勾选部分表) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

-- 项目test_project_01
CREATE TABLE IF NOT EXISTS source_tableA (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_tableA (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 项目test_project_01
CREATE TABLE IF NOT EXISTS source_tableB (
    order_id INT COMMENT '订单ID',
    amount DECIMAL(10,2) COMMENT '金额',
    status STRING COMMENT '状态'
)
STORED AS ORC;

-- 项目test_project_02
CREATE TABLE IF NOT EXISTS source_tableB (
    order_id INT COMMENT '订单ID',
    amount DECIMAL(10,2) COMMENT '金额'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_15, 添加关联表source_tableA、source_tableB并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - source_tableA: 新增字段 age- source_tableB: 新增字段 status |
| 4 | 勾选source_tableB的变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 1) source_tableA表结构不变2) source_tableB表结构变更成功 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 新建表 + 结构变更表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：新建表（生产项目中不存在）
CREATE TABLE IF NOT EXISTS new_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
create_time TIMESTAMP COMMENT '创建时间'
)
STORED AS ORC;

-- 表2：结构变更表（生产项目中已存在）
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改）',
email STRING COMMENT '邮箱（新增）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 表2：初始状态（表1不存在）
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（将被修改）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_14, 添加关联表new_table、change_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: change_table: 删除字段 name, 新增字段 username, email |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 1) 新增表 new_table2)change_table表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 多表 + 多变更类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改）',
age INT COMMENT '年龄',
phone STRING COMMENT '电话（新增）'
)
STORED AS ORC;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name STRING COMMENT '产品名称'
)
STORED AS ORC;

-- 表3：存在新增字段
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status STRING COMMENT '状态（新增）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（将被修改）',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name STRING COMMENT '产品名称',
price DECIMAL(10,2) COMMENT '价格（将被删除）',
stock INT COMMENT '库存（将被删除）'
)
STORED AS ORC;

-- 表3：初始状态
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_13, 添加关联表source_table_m1~m3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 1) source_table_m1: 删除字段 name, 新增字段 username, phone2) source_table_m2: 删除字段 price, stock3) source_table_m3: 新增字段 status |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 删除字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄（将被删除）',
address STRING COMMENT '地址（将被删除）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_12, 添加关联表source_table_delcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 修改字段名称) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_rename (
    id INT COMMENT '主键ID',
    username STRING COMMENT '用户名（修改后）',
    age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_rename (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名（修改前）',
    age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_11, 添加关联表source_table_rename并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 新增字段 username |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 修改字段类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount BIGINT COMMENT '金额（类型修改）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount INT COMMENT '金额（原始类型）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_10, 添加关联表source_table_type并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 修改字段 amount (INT -> BIGINT) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】,查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 导入导出发布 + 新增字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
phone STRING COMMENT '电话（新增字段）',
email STRING COMMENT '邮箱（新增字段）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_09, 添加关联表source_table_addcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 导入导出发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 新增字段 age (int) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 进入到【项目test_project_02】→【数据开发】→【周期任务】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL选项非持久化 （Hive + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
new_column STRING COMMENT '新增字段'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_nopersist并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更 |
| 6 | 勾选变更SQL选项 | 选项处于勾选状态 |
| 7 | 重新进入【发布包校验】页面 | 变更SQL选项恢复为未勾选状态 |
| 8 | 再次勾选“变更SQL“选项 | 选项可以正常勾选 |

##### 验证【发布包校验】变更SQL发布, 二次确认弹窗显示正常（Hive + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
address STRING COMMENT '地址（新增字段）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_confirm并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更 |
| 6 | 点击【发布】按钮 | 显示二次确认弹窗1) 标题: 执行表结构变更SQL2) 文案: 当前操作将在发布过程中自动执行表结构变更 SQL，并直接作用于目标项目表结构，可能对任务和数据产生影响3) 按钮: 取消、确定 |

##### 验证【发布包校验】差异对比正常（Hive + 导入导出发布 + 多表 + 多变更类型） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
6) 建表语句：

项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改字段名）',
age INT COMMENT '年龄',
phone STRING COMMENT '电话（新增字段）'
)
STORED AS ORC;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID'
)
STORED AS ORC;

项目 test_project_02 中的表：
-- 数据源: Hive（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（将被改名）',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID',
order_date STRING COMMENT '订单日期（将被删除）',
amount DECIMAL(10,2) COMMENT '金额（将被删除）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table1, source_table2并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更:1) 表名: source_table1变更说明: - 删除字段 old_field (int)- 新增字段 new_field (string)2) 表名: source_table2变更说明: - 删除字段 create_time (TIMESTAMP)- 修改字段 amount (DECIMAL(18,2) -> DECIMAL(10,2))- 修改字段 status (int -> string) |

##### 验证【发布包校验】差异对比正常（Hive + 导入导出发布 + 单表 + 单字段变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

项目 test_project_02 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（age字段类型不同）
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age STRING COMMENT '年龄'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_single并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 导出zip包, 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面, 上传zip包并发布 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 4 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 5 | 查看【表】模块信息 | 存在表结构变更:1) 表名: test_tableA2) 变更说明: 修改字段 age (string -> int) |

##### 验证【发布包校验】 变更说明按风险顺序(删除→修改→新增)正确展示(Hive + 导入导出发布） 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 test_project_02）
3) 项目 test_project_01 和test_project_02 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_02，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在多种类型的差异（删除、修改、新增）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
new_field STRING COMMENT '新增字段'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（包含需要修改和删除的字段）
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
old_field INT COMMENT '待删除字段',
age STRING COMMENT '待修改类型字段'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_multi并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【导出】按钮 | 导出zip包 |
| 4 | 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 5 | 点击【导入发布包】, 并上传zip包 | 显示【发布包检查】弹窗 |
| 6 | 勾选知晓风险并继续导入 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 7 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 8 | 查看【变更说明】区域的变更项排序 | 变更项按照风险顺序正确排列：删除字段 old_field(INT）修改字段 age (STRING -> INT）新增字段 new_field(STRING) |

##### 验证【发布包校验】 变更信息显示正常（Hive + 导入导出发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01和test_project_02）
3) test_project_01和test_project_02均配置了Hive数据源
4) test_project_01中存在1个周期任务A，任务中包含CREATE TABLE语句
5) 建表语句如下：
-- 数据源: Hive
-- test_project_01中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
create_time TIMESTAMP COMMENT '创建时间'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

-- 生产项目中的目标表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
update_time TIMESTAMP COMMENT '更新时间'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【导出】按钮 | 导出zip包 |
| 4 | 切换到【项目test_project_02】→【任务发布】→【发布至本项目】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 5 | 点击【导入发布包】, 并上传zip包 | 显示【发布包检查】弹窗 |
| 6 | 勾选知晓风险并继续导入 | Toast提示: 导入成功，系统给出导入成功提示，数据已同步至目标列表 |
| 7 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 8 | 检查【表】模块信息 | “表”模块icon变更为黄色警告, 表格列信息:1) 标题: 本项目表结构存在变更2) 问号icon悬浮提示: 当前仅支持对 Doris、Hive 表的字段新增、字段删除及字段类型修改进行提示。在表格左侧勾选对应变更项后，系统将在发布过程中自动执行相应的表结构变更 SQL，变更将直接作用于目标项目的数据表结构，可能对下游任务及数据产生影响，请谨慎操作。3) 表格列包括:- 数据源名称：${数据源名称}（${数据源类型}）- Schema名称：展示表结构存在差异的表所在的schema名称- 表名：展示结构存在差异的表名- 变更说明：展示当前表内字段变更的情况，包括删除字段、修改字段类型、新增字段。按照变更风险从高到低的顺序展示，依次为：删除字段 → 修改字段 → 新增字段- 删除：删除字段 ${字段名称}（${字段类型}）- 修改：修改字段 ${字段名称}（${原字段类型} -> ${新字段类型}}）- 新增：新增字段 ${字段名称}（${字段类型}） |

##### 验证【发布包校验】变更SQL失败后, 日志记录正常（Doris + 一键发布) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_21，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，表中的数据会导致字段类型转换失败
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
amount VARCHAR(100) COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
amount VARCHAR(200) COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
-- 插入测试数据
INSERT INTO source_table_fail VALUES (1, '张三', '100'), (2, '李四', 'abc');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_21, 添加关联表source_table_fail并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 修改字段 amount (VARCHAR(200) -> VARCHAR(100)) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败 |
| 5 | 点击查看, 进入【查看发布包】页面 | 显示所有任务发布对象 |
| 6 | 点击日志, 检查日志内容 | 存在发布失败信息: Cannot shorten string length |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + PRIMARY KEY / UNIQUE KEY 定义、变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_20，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅PRIMARY KEY / UNIQUE KEY存在差异（定义、变更）
6) 建表语句：

场景1：PRIMARY KEY 定义（测试项目无主键，生产项目有主键）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_def (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_def (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景2：PRIMARY KEY 变更（主键字段不同）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_change (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
PRIMARY KEY(id, name)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_pk_change (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景3：UNIQUE KEY 定义（测试项目无唯一键，生产项目有唯一键）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_def (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_def (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景4：UNIQUE KEY 变更（唯一键字段不同）
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_change (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
email VARCHAR(100) COMMENT '邮箱'
)
UNIQUE KEY(id, email)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_uk_change (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
email VARCHAR(100) COMMENT '邮箱'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景5：PRIMARY KEY 和 UNIQUE KEY 之间切换
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_key_switch (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
UNIQUE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_key_switch (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
PRIMARY KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_20, 添加关联表source_table_pk_def等5张表并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + 字段DEFAULT值/COMMENT变更） 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅DEFAULT值或COMMENT存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_default (
    id INT COMMENT '主键ID',
    name VARCHAR(100) DEFAULT '匿名' COMMENT '姓名',
    age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（DEFAULT值和COMMENT不同）
CREATE TABLE IF NOT EXISTS source_table_default (
    id INT COMMENT '主键ID',
    name VARCHAR(100) DEFAULT '未知' COMMENT '用户姓名',
    age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table_default并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + 分区字段与普通字段同名) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅分区字段存在差异
6) 建表语句：

-- Doris表
-- 测试项目 test_project_01 中的表（普通字段dt）
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
dt VARCHAR(20) COMMENT '日期（普通字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 生产项目 prod_project_01 中的表（dt为分区字段）
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
PARTITION BY RANGE(dt) (
PARTITION p202501 VALUES LESS THAN ('2025-02-01'),
PARTITION p202502 VALUES LESS THAN ('2025-03-01')
)
DISTRIBUTED BY HASH(id) BUCKETS 10
PROPERTIES (“replication_num“ = “1“);
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_field_to_partition并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + 分区字段新增/删除/类型变更) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅分区字段存在差异（新增、删除、类型变更）
6) 建表语句：

场景1：分区字段类型变更
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt VARCHAR(20) COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景2：分区字段新增
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期',
region VARCHAR(50) COMMENT '分区'
)
DUPLICATE KEY(id, dt, region)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

场景3：分区字段删除
测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
DUPLICATE KEY(id, dt)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期',
region VARCHAR(50) COMMENT '分区'
)
DUPLICATE KEY(id, dt, region)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_partition_type等3张表并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + 无结构变更/新建表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_17，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构完全相同
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_same (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 新建表
CREATE TABLE IF NOT EXISTS source_table_create (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（结构与测试项目完全一致）
CREATE TABLE IF NOT EXISTS source_table_same (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_17, 添加关联表source_table_same、source_table_create并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Doris + 一键发布 + 不勾选变更项) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_16，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异，但不勾选变更SQL选项
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（缺少age字段）
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_16, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: source_table: 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) Toast提示: 发布成功2) 不会出现二次确认发布弹窗 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 勾选部分表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_15，且任务中包含 CREATE TABLE 语句
5) 测试项目中的多个表与生产项目中的表存在差异，仅勾选部分表执行变更
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 表1：存在新增字段
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在新增字段
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status VARCHAR(20) COMMENT '状态'
)
DUPLICATE KEY(order_id)
DISTRIBUTED BY HASH(order_id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 表1（缺少age字段）
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2（缺少status字段）
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
DUPLICATE KEY(order_id)
DISTRIBUTED BY HASH(order_id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_15, 添加关联表source_tableA、source_tableB并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - source_tableA: 新增字段 age- source_tableB: 新增字段 status |
| 4 | 勾选source_tableB的变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) source_tableA表结构不变2) source_tableB表结构变更成功 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 新建表 + 结构变更表) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_14，且任务中包含 CREATE TABLE 语句
5) 测试项目中存在新建表（生产项目中不存在）和结构变更表（生产项目中已存在但有差异）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 表1：新建表（生产项目中不存在）
CREATE TABLE IF NOT EXISTS new_table (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    create_time DATETIME COMMENT '创建时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：结构变更表（生产项目中已存在）
CREATE TABLE IF NOT EXISTS change_table (
    id INT COMMENT '主键ID',
    username VARCHAR(100) COMMENT '用户名（修改）',
    email VARCHAR(100) COMMENT '邮箱（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 表2：初始状态（表1不存在）
CREATE TABLE IF NOT EXISTS change_table (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名（将被修改）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_14, 添加关联表new_table、change_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: change_table: 删除字段 name, 新增字段 username, email |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) 新增表 new_table2)change_table表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 多表 + 多变更类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_13，且任务中包含 CREATE TABLE 语句
5) 测试项目中的多个表与生产项目中的表存在多种类型的变更（新增、修改、删除）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改）',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name VARCHAR(200) COMMENT '产品名称'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表3：存在新增字段
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status VARCHAR(20) COMMENT '状态（新增）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table_m1 (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（将被修改）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table_m2 (
id INT COMMENT '主键ID',
product_name VARCHAR(200) COMMENT '产品名称',
price DECIMAL(10,2) COMMENT '价格（将被删除）',
stock INT COMMENT '库存（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表3：初始状态
CREATE TABLE IF NOT EXISTS source_table_m3 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_13, 添加关联表source_table_m1~m3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 1) source_table_m1: 删除字段 name, 新增字段 username, phone2) source_table_m2: 删除字段 price, stock3) source_table_m3: 新增字段 status |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 删除字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_12，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在删除字段差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄（将被删除）',
address VARCHAR(200) COMMENT '地址（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_12, 添加关联表source_table_delcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 删除字段 address |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 修改字段名称) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_11，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段名称差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改后）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（修改前）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_11, 添加关联表source_table_rename并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 新增字段 username |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 修改字段类型) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_10，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段类型差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_type (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    amount BIGINT COMMENT '金额（类型修改）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_type (
    id INT COMMENT '主键ID',
    name VARCHAR(100) COMMENT '姓名',
    amount INT COMMENT '金额（原始类型）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_10, 添加关联表source_table_type并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 修改字段 amount (INT -> BIGINT) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Doris + 一键发布 + 新增字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_09，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在新增字段差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增字段）',
email VARCHAR(100) COMMENT '邮箱（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_addcol (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_09, 添加关联表source_table_addcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 新增字段 age (int) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL选项非持久化 （Doris + 一键发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_06，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
new_column VARCHAR(50) COMMENT '新增字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_nopersist并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更 |
| 5 | 勾选变更SQL选项 | 选项处于勾选状态 |
| 6 | 重新进入【发布包校验】页面 | 变更SQL选项恢复为未勾选状态 |
| 7 | 再次勾选“变更SQL“选项 | 选项可以正常勾选 |

##### 验证【发布包校验】变更SQL发布, 二次确认弹窗显示正常（Doris + 一键发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
address VARCHAR(200) COMMENT '地址（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_confirm并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更 |
| 5 | 点击【发布】按钮 | 显示二次确认弹窗1) 标题: 执行表结构变更SQL2) 文案: 当前操作将在发布过程中自动执行表结构变更 SQL，并直接作用于目标项目表结构，可能对任务和数据产生影响3) 按钮: 取消、确定 |

##### 验证【发布包校验】差异对比正常（Doris + 多表 + 多变更类型） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_04，且任务中包含 CREATE TABLE 语句
5) 测试项目中的多个表与生产项目中的表存在多种类型的变更（新增、修改、删除）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
username VARCHAR(100) COMMENT '用户名（修改字段名）',
age INT COMMENT '年龄',
phone VARCHAR(20) COMMENT '电话（新增字段）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名（将被改名）',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id VARCHAR(50) COMMENT '订单ID',
order_date DATE COMMENT '订单日期（将被删除）',
amount DECIMAL(10,2) COMMENT '金额（将被删除）'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table1, source_table2并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更:1) 表名: source_table1变更说明: - 删除字段 old_field (int)- 新增字段 new_field (string)2) 表名: source_table2变更说明: - 删除字段 create_time (TIMESTAMP)- 修改字段 amount (DECIMAL(18,2) -> DECIMAL(10,2))- 修改字段 status (int -> string) |

##### 验证【发布包校验】差异对比正常（Doris + 单表 + 单字段变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_03，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在单个字段的变更
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（缺少age字段）
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_single并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更:1) 表名: test_tableA2) 变更说明: 修改字段 age (string -> int) |

##### 验证【发布包校验】 变更说明按风险顺序(删除→修改→新增)正确展示(Doris + 一键发布） 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 项目 test_project_01 和 test_project_02 均配置了 Doris 数据源
4) 项目 test_project_01 中存在周期任务 task_cycle_02，且任务中包含 CREATE TABLE 语句
5) 项目 test_project_01 和 test_project_02中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
new_field VARCHAR(50) COMMENT '新增字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（包含需要修改和删除的字段）
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
old_field INT COMMENT '待删除字段',
age BIGINT COMMENT '待修改类型字段'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table_multi并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【变更说明】区域的变更项排序 | 变更项按照风险顺序正确排列：删除字段 old_field(INT）修改字段 age (STRING -> INT）新增字段 new_field(STRING) |

##### 验证【发布包校验】 变更信息显示正常（Doris + 一键发布) 「P3」

> 前置条件
```
> 前置条件
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Doris 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_01，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Doris（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
create_time DATETIME COMMENT '创建时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;

生产项目 prod_project_01 中的表：
-- 数据源: Doris（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name VARCHAR(100) COMMENT '姓名',
age INT COMMENT '年龄',
update_time DATETIME COMMENT '更新时间'
)
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 10;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Doris表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 检查【表】模块信息 | “表”模块icon变更为黄色警告, 表格列信息:1) 标题: 本项目表结构存在变更2) 问号icon悬浮提示: 当前仅支持对 Doris、Hive 表的字段新增、字段删除及字段类型修改进行提示。在表格左侧勾选对应变更项后，系统将在发布过程中自动执行相应的表结构变更 SQL，变更将直接作用于目标项目的数据表结构，可能对下游任务及数据产生影响，请谨慎操作。3) 表格列包括:- 数据源名称：${数据源名称}（${数据源类型}）- Schema名称：展示表结构存在差异的表所在的schema名称- 表名：展示结构存在差异的表名- 变更说明：展示当前表内字段变更的情况，包括删除字段、修改字段类型、新增字段。按照变更风险从高到低的顺序展示，依次为：删除字段 → 修改字段 → 新增字段- 删除：删除字段 ${字段名称}（${字段类型}）- 修改：修改字段 ${字段名称}（${原字段类型} -> ${新字段类型}}）- 新增：新增字段 ${字段名称}（${字段类型}） |

##### 验证【发布包校验】变更SQL失败后, 日志记录正常（Hive + 一键发布) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_26，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，表中的数据会导致字段类型转换失败
6) 测试项目中的源表尝试将STRING类型改为INT，但目标表中该字段包含无法转换为INT的数据
7) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    amount INT COMMENT '金额（类型修改为INT）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    amount STRING COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
STORED AS ORC;

-- 生产表中已插入测试数据
INSERT INTO TABLE source_table_fail VALUES (1, '张三', '100'), (2, '李四', 'abc');
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_12, 添加关联表source_table_fail并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 修改字段 amount (string -> int) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败 |
| 5 | 点击查看, 进入【查看发布包】页面 | 显示所有任务发布对象 |
| 6 | 点击日志, 检查日志内容 | 正确显示发布失败信息 |

##### 验证未执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 一键发布） 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_20，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」为2026-01-01 10:00:00
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_20, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) 发布成功, 记录发布包列表中的【发布时间】2) 不存在二次确认弹窗 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 进入【数据地图】→【数据表管理】→【source_table详情页】 | 1) 【存储信息】→【表结构最后变更时间】不变2) 【存储信息】→【数据最后变更时间】不变 |

##### 验证执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 一键发布 + 部分失败) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」
6) 建表语句：

-- 场景：多表变更，部分成功、部分失败
-- Hive表
-- 表1：成功变更（新增字段）
-- 测试项目 test_project_01 中的表
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_success1 (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（缺少age字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_success1 (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 表2：成功变更（修改字段类型，类型兼容）
-- 测试项目 test_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_success2 (
id INT COMMENT '主键ID',
amount BIGINT COMMENT '金额（类型为BIGINT）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（amount字段为INT）
CREATE TABLE IF NOT EXISTS source_table_success2 (
id INT COMMENT '主键ID',
amount INT COMMENT '金额（类型为INT）'
)
STORED AS ORC;

-- 表3：失败变更（字段类型不兼容，包含无法转换的数据）
-- 测试项目 test_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
amount INT COMMENT '金额（类型修改为INT）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表
CREATE TABLE IF NOT EXISTS source_table_fail (
id INT COMMENT '主键ID',
amount STRING COMMENT '金额（原始为STRING，包含“abc“等非数字数据）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table_success1~3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更- source_table_success1: 新增 age字段- source_table_success2: 修改amount字段 int -> bigint- source_table_success3: 修改amount字段 string -> int |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布失败, 记录发布包列表中的【发布人】、【发布时间】 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) source_table_success1~2 表结构已成功变更2) source_table_success3 表结构不变 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table_success1~3表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 分别进入【数据地图】→【数据表管理】→【source_table详情页】 | 1) source_table_success1~2:- 【存储信息】→【表结构最后变更时间】更新为发布时间- 【存储信息】→【数据最后变更时间】更新为发布时间2) source_table_success3: - 【存储信息】→【表结构最后变更时间】不变- 【存储信息】→【数据最后变更时间】不变 |

##### 验证执行表结构变更SQL后, 数据地图中的表信息更新正确（Hive + 一键发布) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_19，且任务中包含 CREATE TABLE 语句
5) 生产项目 prod_project_01 中已存在目标表，且有历史「表结构最后变更时间」、「数据最后变更时间」
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_19, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 新增字段 age |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 发布成功, 记录发布包列表中的【发布人】、【发布时间】 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |
| 6 | 进入【数据地图】→【数据表管理】, 检查source_table表的责任人和创建时间 | 1) 【责任人】不变2) 【创建时间】不变 |
| 7 | 进入【数据地图】→【数据表管理】→【source_table详情页】 | 1) 【存储信息】→【表结构最后变更时间】更新为发布时间2) 【存储信息】→【数据最后变更时间】更新为发布时间 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 一键发布 + 分区字段与普通字段同名) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅分区字段存在差异
6) 建表语句：

-- Hive表
-- 测试项目 test_project_01 中的表（普通字段dt）
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
dt STRING COMMENT '日期（普通字段）'
)
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（dt为分区字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_field_to_partition (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
PARTITIONED BY (dt STRING COMMENT '日期（分区字段）')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_17, 添加关联表source_table_field_to_partition并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 一键发布 + 分区字段新增/删除/类型变更) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_18，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构仅分区字段存在差异
6) 建表语句：

-- 场景1：分区字段类型变更
-- Hive表
-- 测试项目 test_project_01 中的表（dt分区字段为DATE类型）
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt DATE COMMENT '日期'
)
PARTITIONED BY (dt DATE COMMENT '日期')
STORED AS ORC;
-- 生产项目 prod_project_01 中的表（dt分区字段为STRING类型）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 场景2：分区字段新增
-- Hive表
-- 测试项目 test_project_01 中的表（包含region分区字段）
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期', region STRING COMMENT '分区')
STORED AS ORC;
-- 生产项目 prod_project_01 中的表（缺少region分区字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_add (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 场景3：分区字段删除
-- Hive表
-- 测试项目 test_project_01 中的表（缺少region分区字段）
-- 数据源: Hive（测试集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期')
STORED AS ORC;

-- 生产项目 prod_project_01 中的表（包含region分区字段）
-- 数据源: Hive（生产集群地址）
CREATE TABLE IF NOT EXISTS source_table_partition_del (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
dt STRING COMMENT '日期'
)
PARTITIONED BY (dt STRING COMMENT '日期', region STRING COMMENT '分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_18, 添加关联表source_table_partition_type等3张表并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 一键发布 + 无结构变更/新建表) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_17，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构完全相同
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_same (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（结构与测试项目完全一致）
CREATE TABLE IF NOT EXISTS source_table_same (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名',
    age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_17, 添加关联表source_table_same并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 一键发布 + 不勾选变更项) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_16，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异，但不勾选变更SQL选项
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（缺少age字段）
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_16, 添加关联表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: source_table: 新增字段 age |
| 4 | 不勾选变更SQL选项, 点击发布 | 1) Toast提示: 发布成功2) 不会出现二次确认发布弹窗 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 勾选部分表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_15，且任务中包含 CREATE TABLE 语句
5) 测试项目中的多个表与生产项目中的表存在差异，仅勾选部分表执行变更
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 周期任务task_cycle_15中的源表
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 周期任务task_cycle_15对应的生产项目目标表（缺少age字段）
CREATE TABLE IF NOT EXISTS source_tableA (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;

-- 周期任务task_cycle_15中的源表
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额',
status STRING COMMENT '状态'
)
STORED AS ORC;

-- 周期任务task_cycle_15对应的生产项目目标表（缺少status字段）
CREATE TABLE IF NOT EXISTS source_tableB (
order_id INT COMMENT '订单ID',
amount DECIMAL(10,2) COMMENT '金额'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_15, 添加关联表source_tableA、source_tableB并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - source_tableA: 新增字段 age- source_tableB: 新增字段 status |
| 4 | 勾选source_tableB的变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) source_tableA表结构不变2) source_tableB表结构变更成功 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 新建表 + 结构变更表) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_10，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段类型差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：新建表（生产项目中不存在）
CREATE TABLE IF NOT EXISTS new_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
create_time TIMESTAMP COMMENT '创建时间'
)
STORED AS ORC;

-- 表2：结构变更表（生产项目中已存在）
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改）',
email STRING COMMENT '邮箱（新增）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 表2：初始状态（表1不存在）
CREATE TABLE IF NOT EXISTS change_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（将被修改）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_14, 添加关联表new_table、change_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: change_table: 删除字段 name, 新增字段 username, email |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 1) 新增表 new_table2)change_table表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 多表 + 多变更类型) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_10，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段类型差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table_m1 (
    id INT COMMENT '主键ID',
    username STRING COMMENT '用户名（修改）',
    age INT COMMENT '年龄',
    phone STRING COMMENT '电话（新增）'
)
STORED AS ORC;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table_m2 (
    id INT COMMENT '主键ID',
    product_name STRING COMMENT '产品名称'
)
STORED AS ORC;

-- 表3：存在新增字段
CREATE TABLE IF NOT EXISTS source_table_m3 (
    id INT COMMENT '主键ID',
    order_id STRING COMMENT '订单ID',
    amount DECIMAL(10,2) COMMENT '金额',
    status STRING COMMENT '状态（新增）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table_m1 (
    id INT COMMENT '主键ID',
    name STRING COMMENT '姓名（将被修改）',
    age INT COMMENT '年龄'
)
STORED AS ORC;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table_m2 (
    id INT COMMENT '主键ID',
    product_name STRING COMMENT '产品名称',
    price DECIMAL(10,2) COMMENT '价格（将被删除）',
    stock INT COMMENT '库存（将被删除）'
)
STORED AS ORC;

-- 表3：初始状态
CREATE TABLE IF NOT EXISTS source_table_m3 (
    id INT COMMENT '主键ID',
    order_id STRING COMMENT '订单ID',
    amount DECIMAL(10,2) COMMENT '金额'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_13, 添加关联表source_table_m1~m3并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 1) source_table_m1: 删除字段 name, 新增字段 username, phone2) source_table_m2: 删除字段 price, stock3) source_table_m3: 新增字段 status |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构不变（Hive + 一键发布 + 删除字段) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_12，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在删除字段差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_delcol (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄（将被删除）',
address STRING COMMENT '地址（将被删除）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_12, 添加关联表source_table_delcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 显示: 校验通过 |
| 4 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构不变 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 修改字段名称) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_11，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段名称差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改后）',
age INT COMMENT '年龄'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_rename (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（修改前）',
age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_11, 添加关联表source_table_rename并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: - 删除字段 name- 新增字段 username |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 修改字段类型) 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_10，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在字段类型差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount BIGINT COMMENT '金额（类型修改）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_type (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
amount INT COMMENT '金额（原始类型）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_10, 添加关联表source_table_type并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更: 修改字段 amount (INT -> BIGINT) |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL发布后, 目标表结构变更成功（Hive + 一键发布 + 新增字段) 「P1」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_09，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在新增字段差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_addcol (
 id INT COMMENT '主键ID',
 name STRING COMMENT '姓名',
 age INT COMMENT '年龄',
 phone STRING COMMENT '电话（新增字段）',
 email STRING COMMENT '邮箱（新增字段）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table_addcol (
 id INT COMMENT '主键ID',
 name STRING COMMENT '姓名',
 age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 勾选周期任务task_cycle_09, 添加关联表source_table_addcol并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 一键发布后, 检查【发布包校验】表结构变更信息 | 【发布包校验】弹窗存在表结构变更 |
| 4 | 勾选变更SQL选项, 并二次确认发布 | 系统提示发布成功，发布状态更新，相关页面可访问 |
| 5 | 切换到【生产项目prod_project_01】, 查询目标表结构 | 表结构已成功变更 |

##### 验证【发布包校验】变更SQL选项非持久化 （Hive + 一键发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_08，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
new_column STRING COMMENT '新增字段'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_nopersist (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_nopersist并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更 |
| 5 | 勾选变更SQL选项 | 选项处于勾选状态 |
| 6 | 重新进入【发布包校验】页面 | 变更SQL选项恢复为未勾选状态 |
| 7 | 再次勾选“变更SQL“选项 | 选项可以正常勾选 |

##### 验证【发布包校验】变更SQL发布, 二次确认弹窗显示正常（Hive + 一键发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_07，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
address STRING COMMENT '地址（新增字段）'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表
CREATE TABLE IF NOT EXISTS source_table_confirm (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_confirm并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更 |
| 5 | 点击【发布】按钮 | 显示二次确认弹窗1) 标题: 执行表结构变更SQL2) 文案: 当前操作将在发布过程中自动执行表结构变更 SQL，并直接作用于目标项目表结构，可能对任务和数据产生影响3) 按钮: 取消、确定 |

##### 验证【发布包校验】差异对比正常（Hive + 多表 + 多变更类型） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_06，且任务中包含 CREATE TABLE 语句
5) 测试项目中的多个表与生产项目中的表存在多种类型的变更（新增、修改、删除）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 表1：存在新增和修改字段
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
username STRING COMMENT '用户名（修改字段名）',
age INT COMMENT '年龄',
phone STRING COMMENT '电话（新增字段）'
)
STORED AS ORC;

-- 表2：存在删除字段
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID'
)
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 表1：初始状态
CREATE TABLE IF NOT EXISTS source_table1 (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名（将被改名）',
age INT COMMENT '年龄'
)
STORED AS ORC;

-- 表2：初始状态
CREATE TABLE IF NOT EXISTS source_table2 (
id INT COMMENT '主键ID',
order_id STRING COMMENT '订单ID',
order_date STRING COMMENT '订单日期（将被删除）',
amount DECIMAL(10,2) COMMENT '金额（将被删除）'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table1, source_table2并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更:1) 表名: source_table1变更说明: - 删除字段 old_field (int)- 新增字段 new_field (string)2) 表名: source_table2变更说明: - 删除字段 create_time (TIMESTAMP)- 修改字段 amount (DECIMAL(18,2) -> DECIMAL(10,2))- 修改字段 status (int -> string) |

##### 验证【发布包校验】差异对比正常（Hive + 单表 + 单字段变更） 「P2」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_05，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在单个字段的变更
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（age字段类型不同）
CREATE TABLE IF NOT EXISTS source_table_single (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age STRING COMMENT '年龄'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_single并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【表】模块信息 | 存在表结构变更:1) 表名: test_tableA2) 变更说明: 修改字段 age (string -> int) |

##### 验证【发布包校验】 变更说明按风险顺序(删除→修改→新增)正确展示(Hive + 一键发布） 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_02，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在多种类型的差异（删除、修改、新增）
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
new_field STRING COMMENT '新增字段'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（包含需要修改和删除的字段）
CREATE TABLE IF NOT EXISTS source_table_multi (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
old_field INT COMMENT '待删除字段',
age STRING COMMENT '待修改类型字段'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table_multi并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 查看【变更说明】区域的变更项排序 | 变更项按照风险顺序正确排列：删除字段 old_field(INT）修改字段 age (STRING -> INT）新增字段 new_field(STRING) |

##### 验证【发布包校验】 变更信息显示正常（Hive + 一键发布) 「P3」

> 前置条件
```
1) 已登录系统，账号具有任务发布权限
2) 系统中存在至少2个项目（test_project_01 和 prod_project_01）
3) 测试项目 test_project_01 和生产项目 prod_project_01 均配置了 Hive 数据源
4) 测试项目 test_project_01 中存在周期任务 task_cycle_01，且任务中包含 CREATE TABLE 语句
5) 测试项目中的表结构与生产项目中的表结构存在差异
6) 建表语句：

测试项目 test_project_01 中的表：
-- 数据源: Hive（测试集群地址）
-- 测试项目中的表
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
create_time TIMESTAMP COMMENT '创建时间'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;

生产项目 prod_project_01 中的表：
-- 数据源: Hive（生产集群地址）
-- 生产项目中的表（初始状态）
CREATE TABLE IF NOT EXISTS source_table (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄',
update_time TIMESTAMP COMMENT '更新时间'
)
PARTITIONED BY (dt STRING COMMENT '日期分区')
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 成功进入目标页面，页面内容正常加载显示，无报错 |
| 2 | 【对象类型】切换为【表】, 选择Hive表source_table并打包 | 打包成功, 跳转到【发布至目标项目】页面 |
| 3 | 点击【发布】按钮 | 进入【发布包校验】弹窗 |
| 4 | 检查【表】模块信息 | “表”模块icon变更为黄色警告, 表格列信息:1) 标题: 本项目表结构存在变更2) 问号icon悬浮提示: 当前仅支持对 Doris、Hive 表的字段新增、字段删除及字段类型修改进行提示。在表格左侧勾选对应变更项后，系统将在发布过程中自动执行相应的表结构变更 SQL，变更将直接作用于目标项目的数据表结构，可能对下游任务及数据产生影响，请谨慎操作。3) 表格列包括:- 数据源名称：${数据源名称}（${数据源类型}）- Schema名称：展示表结构存在差异的表所在的schema名称- 表名：展示结构存在差异的表名- 变更说明：展示当前表内字段变更的情况，包括删除字段、修改字段类型、新增字段。按照变更风险从高到低的顺序展示，依次为：删除字段 → 修改字段 → 新增字段- 删除：删除字段 ${字段名称}（${字段类型}）- 修改：修改字段 ${字段名称}（${原字段类型} -> ${新字段类型}}）- 新增：新增字段 ${字段名称}（${字段类型}） |

##### 验证【对象类型】选择【表】时，可正确展示当前项目中周期任务/手动任务/临时查询创建的(Doris) 「P2」

> 前置条件
```
1、项目中已存在以下Hive表（均由离线开发创建）：
周期任务创建：test_db.tb_cycle
手动任务创建：test_db.tb_manual
临时查询创建：test_db.tb_adhoc

2、三张表结构不同但均为Hive表，且都已实际创建
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 跳转到发布包创建页面 |
| 2 | 【对象类型】切换为【表】 | 列表数据刷新 |
| 3 | 检查表列表中的表信息 | 列表正确显示test_project_01中周期任务/手动任务/临时查询创建的所有表：1) task_table01（所属任务：周期任务1）2) task_table02（所属任务：手动任务1）3) task_table03（所属任务：临时查询1） |
| 4 | 勾选task_table01~03 | 表成功添加到右侧【发布列表】 |

##### 验证周期任务/手动任务打包时，通过【添加关联】可正确关联任务中创建的表（Doris） 「P2」

> 前置条件
```
1、当前项目已创建Doris数据源
2、项目中已存在一个Doris SQL周期任务和手动任务，任务中包含建表语句：

CREATE TABLE test_db.tb_create_in_task (
id INT,
name STRING,
create_time TIMESTAMP
) PARTITIONED BY (dt STRING);

CREATE TABLE test_db.tb_create_in_task2 (
id INT,
name STRING,
create_time TIMESTAMP
) PARTITIONED BY (dt STRING);

3、 该任务已提交并至少运行成功一次（表已实际创建）
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 跳转到创建发布包页面，【对象类型】默认为【周期任务】 |
| 2 | 选择周期任务A, 点击【添加关联】按钮 | 显示【添加关联】弹窗 |
| 3 | 切换到【关联表】tab页 | 列表正确显示周期任务A中所有创建的表, 包含Hive表test_source_tableA |
| 4 | 选择test_source_tableA，点击【确认】 | test_source_tableA成功添加到右侧【发布列表】 |
| 5 | 选择【对象类型】为【手动任务】, 选择手动任务B, 点击【添加关联】按钮 | 显示【添加关联】弹窗 |
| 6 | 切换到【关联表】, 选择test_source_tableB，点击【确认】 | test_source_tableB成功添加到右侧【发布列表】 |

##### 验证【对象类型】选择【表】时，可正确展示当前项目中周期任务/手动任务/临时查询创建的表(Hive) 「P2」

> 前置条件
```
1)已登录系统，账号具有任务发布权限
2)系统中存在至少1个测试项目test_project_01
3)测试项目test_project_01中配置了Hive数据源
4)测试项目test_project_01中存在以下任务：
- 周期任务1：包含CREATE TABLE语句创建表 task_table01
- 手动任务1：包含CREATE TABLE语句创建表 task_table02
- 临时查询1：包含CREATE TABLE语句创建表 task_table03
5) 建表语句如下:

周期任务1
-- 数据源: Hive
-- 测试项目中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS task_table01 (
task_id INT COMMENT '任务ID',
task_name STRING COMMENT '任务名称',
task_status STRING COMMENT '任务状态'
)
STORED AS ORC;

手动任务1
-- 数据源: Hive
-- 测试项目中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS task_table02 (
manual_id INT COMMENT '手动任务ID',
manual_name STRING COMMENT '手动任务名称',
manual_desc STRING COMMENT '手动任务描述'
)
STORED AS ORC;

临时查询1
-- 数据源: Hive
-- 测试项目中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS task_table03 (
query_id INT COMMENT '查询ID',
query_sql STRING COMMENT '查询SQL',
query_result STRING COMMENT '查询结果'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 跳转到发布包创建页面 |
| 2 | 【对象类型】切换为【表】 | 列表数据刷新 |
| 3 | 检查表列表中的表信息 | 列表正确显示test_project_01中周期任务/手动任务/临时查询创建的所有表：1) task_table01（所属任务：周期任务1）2) task_table02（所属任务：手动任务1）3) task_table03（所属任务：临时查询1） |
| 4 | 勾选task_table01~03 | 表成功添加到右侧【发布列表】 |

##### 验证周期任务/手动任务打包时，通过【添加关联】可正确关联任务中创建的表（Hive） 「P2」

> 前置条件
```
1)已登录系统，账号具有任务发布权限
2)系统中存在至少2个项目（测试项目test_project_01和生产项目prod_project_01）
3)测试项目test_project_01和生产项目prod_project_01均配置了Hive数据源
4)测试项目test_project_01中存在至少1个周期任务A或手动任务B，且任务中包含CREATE TABLE语句
5) 建表语句如下:
周期任务A
-- 数据源: Hive
-- 测试项目中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS test_source_tableA (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;

手动任务B
-- 数据源: Hive
-- 测试项目中的源表（任务中创建的表）
CREATE TABLE IF NOT EXISTS test_source_tableB (
id INT COMMENT '主键ID',
name STRING COMMENT '姓名',
age INT COMMENT '年龄'
)
STORED AS ORC;
```

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【测试项目test_project_01】→【任务发布】→【创建发布包】页面 | 跳转到创建发布包页面，【对象类型】默认为【周期任务】 |
| 2 | 选择周期任务A, 点击【添加关联】按钮 | 显示【添加关联】弹窗 |
| 3 | 切换到【关联表】tab页 | 列表正确显示周期任务A中所有创建的表, 包含Hive表test_source_tableA |
| 4 | 选择test_source_tableA，点击【确认】 | test_source_tableA成功添加到右侧【发布列表】 |
| 5 | 选择【对象类型】为【手动任务】, 选择手动任务B, 点击【添加关联】按钮 | 显示【添加关联】弹窗 |
| 6 | 切换到【关联表】, 选择test_source_tableB，点击【确认】 | test_source_tableB成功添加到右侧【发布列表】 |

