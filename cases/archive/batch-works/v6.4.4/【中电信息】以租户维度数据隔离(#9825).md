---
suite_name: "【中电信息】以租户维度数据隔离(#9825)（XMind）"
description: "【中电信息】以租户维度数据隔离(#9825)（XMind）"
prd_id: 9825
prd_version: v6.4.4
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - 创建项目
  - 项目管理
  - 数据源
  - 表查询
  - 函数管理
  - 任务开发
  - 离线配置项
  - 中电信息
  - 以租户维度数据隔离
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 29
origin: xmind
---
## 创建项目 ❯

##### 【P1】验证「Schema列表」租户隔离功能生效(初始化方式: 创建)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A、租户 B 均无项目
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 创建项目, 配置如下:<br>1) 项目名称&标识: Project01<br>2) 计算引擎配置勾选Hive, Doris, Dmmysql (创建) | 1) 项目创建成功, Project01项目-数据源页面存在hive, doris, dmmysql的meta数据源<br>2) 连接信息中的Schema均为project01 |
| 2 | 切换到租户 B 创建项目, 展开Hive计算引擎「对接已有 Schema」, 检查Schema列表 | 不展示租户 A 中创建的 Schema(project01) |
| 3 | 「Schema」输入框搜索project01 | 暂无数据, 无法查询到对应的Schema |
| 4 | 展开Doris计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中创建的 Schema(project01) |
| 5 | 展开DMMySQL计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中创建的 Schema(project01) |

##### 【P0】验证「Schema列表」租户隔离功能生效(初始化方式: 对接已有Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A 项目 Project01 已对接 schema(hive、doris、dmmysql)

5、租户 B 尚未创建任何项目
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 B 创建项目 → 选择Hive计算引擎「对接已有 Schema」 | Hive的Schema列表中不展示Schema(project01) |
| 2 | Schema输入框中输入project01, 检查schema列表 | 暂无数据, 依旧不展示Schema(project01) |
| 3 | 选择Doris引擎-对接已有Doris Schema, 重复步骤1~2 | Doris计算引擎Schema列表不展示Schema(project01) |
| 4 | 选择DMMySQL引擎-对接已有DMMySQL Schema, 重复步骤1~2 | DMMySQL计算引擎Schema列表不展示Schema(project01) |
| 5 | 创建项目配置:<br>1) 项目名称&标识: Project02<br>2) 列表中选择可用 Schema 创建项目 | 1) 项目创建成功, Project01项目-数据源页面存在hive, doris, dmmysql的meta数据源<br>2) 连接信息中的Schema为各自对接的schema名称 |

##### 【P1】验证「Schema列表」租户隔离功能生效(初始化方式: 不创建且不对接Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A 项目 Project01 已创建或对接 schema(hive、doris、dmmysql)
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 B 创建项目, 配置如下:<br>1) 项目名称&标识: Project03<br>2) 计算引擎配置勾选Hive, Doris, Dmmysql (不创建且不对接Schema) | 1) 项目创建成功, Project03项目-数据源页面存在hive, doris, dmmysql的meta数据源<br>2) 连接信息中无Schema |
| 2 | 租户 B 后续创建项目 → 对接已有 Schema | hive、doris、dmmysql计算引擎均不展示 Schema(Project01) |

##### 【P0】验证「项目删除」后租户隔离功能仍生效(初始化方式: 创建)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、控制台配置：Schema 租户隔离开关已开启
4、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

5、租户 A 项目 Project01 已对接 schema(hive、doris、dmmysql)
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 删除项目 Project01 | 项目删除成功 |
| 2 | 租户 B 创建项目 → 对接已有 Schema | hive, doris, dmmysql计算引擎均不展示Schema(project01) |
| 3 | 租户 B 创建项目 → 创建新Schema(项目标识: project01) | 报错提示: Schema(project01)已存在 |

##### 【P1】验证「项目删除」后租户隔离功能仍生效(初始化方式: 对接已有Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、控制台配置：Schema 租户隔离开关已开启
4、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

5、租户 A 项目 Project01 已对接 schema(hive、doris、dmmysql)
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 删除项目 Project01 | 项目删除成功 |
| 2 | 租户 A 创建新项目 Project02 → 对接已有 Schema | schema(project01) 可见 |
| 3 | 选择 schema(project01)  创建项目 | 项目创建成功 |

## 项目管理 ❯

##### 【P2】验证「添加计算引擎-Schema列表」租户隔离功能生效(初始化方式: 创建)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 创建项目Project12(仅对接mysql引擎) | 创建成功 |
| 2 | 进入项目管理, 分别添加Hive、Doris、DMMySQL计算引擎, 初始化方式: 创建 | 引擎添加成功, 三个数据源的连接信息均存在Schema: project12 |
| 3 | 切换到租户 B 创建项目, 展开Hive计算引擎「对接已有 Schema」, 检查Schema列表 | 不展示租户 A 中创建的 Schema(project12) |
| 4 | 「Schema」输入框搜索project12 | 暂无数据, 无法查询到对应的Schema |
| 5 | 展开Doris计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中创建的 Schema(project12) |
| 6 | 展开DMMySQL计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中创建的 Schema(project12) |

##### 【P1】验证「添加计算引擎-Schema列表」租户隔离功能生效(初始化方式: 对接已有Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 创建项目Project13(仅对接mysql引擎) | 创建成功 |
| 2 | 进入项目管理, 分别添加Hive、Doris、DMMySQL计算引擎, 初始化方式: 对接已有Schema | Hive、Doris、DMMySLQ的Schema列表中均不展示project06~10中创建或对接的schema |
| 3 | Schema输入框中分别输入project06~10中的Schema, 检查schema列表 | 暂无数据, 依旧不展示Schema(project06~10) |
| 4 | 切换到租户 B 创建项目, 展开Hive计算引擎「对接已有 Schema」, 检查Schema列表 | 不展示租户 A 中创建的 Schema(project13) |
| 5 | 「Schema」输入框搜索project13对接的Schema | 暂无数据, 无法查询到对应的Schema |
| 6 | 展开Doris计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中对接的 Schema(project13) |
| 7 | 展开DMMySQL计算引擎「对接已有 Schema」, 检查Schema列表并搜索查询 | 均不展示租户 A 中对接的 Schema(project13) |

##### 【P2】验证「添加计算引擎-Schema列表」租户隔离功能生效(初始化方式: 不创建且不对接Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 创建项目Project14(仅对接mysql引擎) | 创建成功 |
| 2 | 进入项目管理, 分别添加Hive、Doris、DMMySQL计算引擎, 初始化方式: 不创建且不对接schema | 1) 项目创建成功, Project14项目-数据源页面存在hive, doris, dmmysql的meta数据源<br>2) 连接信息中无Schema |

## 数据源 ❯

##### 【P1】验证「映射配置-Schema列表」租户隔离功能生效(初始化方式: 创建)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、将Project01和Project06绑定为发布关系:
Project01 > Project06
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 进入「Project01-数据源」页面, 选择Hive数据源, 点击「映射配置」 | 进入「映射配置(${Hive数据源名称})」页面 |
| 2 | 「发布目标」选择Project06中Hive数据源名称后, 新增一行Schema映射, 检查当前Schema和目标Schema下拉框 | 1) 当前Schema下拉框仅展示project01<br>2) 目标Schema 下拉框仅展示project06 |
| 3 | 当前Schema、目标Schema下拉框中, 分别输入project01~10中创建或对接的Schema | 1) 当前Schema下拉框仅查询到测试项目中创建的Schema(project01), 无法查询当前租户其它项目或其它租户项目中的Schema<br>2) 目标Schema下拉框仅查询到生产项目中创建的Schema(project06), 无法查询当前租户其它项目或其它租户项目中的Schema |
| 4 | 选择Doris数据源, 点击「映射配置」, 重复步骤2~3 | Doris映射配置中的Schema租户隔离功能正常 |
| 5 | 选择DMMySQL数据源, 点击「映射配置」, 重复步骤2~3 | DMMySQL映射配置中的Schema租户隔离功能正常 |

##### 【P1】验证「映射配置-Schema列表」租户隔离功能生效(初始化方式: 对接已有Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、将Project02和Project07绑定为发布关系:
Project02 > Project07
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 进入「Project02-数据源」页面, 选择Hive数据源, 点击「映射配置」 | 进入「映射配置(${Hive数据源名称})」页面 |
| 2 | 「发布目标」选择Project07中Hive数据源名称后, 新增一行Schema映射, 检查当前Schema和目标Schema下拉框 | 1) 当前Schema下拉框仅展示: ${project02对接的Schema}<br>2) 目标Schema 下拉框仅展示: ${project07对接的Schema} |
| 3 | 当前Schema、目标Schema下拉框中, 分别输入project01~10中创建或对接的Schema | 1) 当前Schema下拉框仅查询到测试项目中对接的Schema(project02), 无法查询当前租户其它项目或其它租户项目中的Schema<br>2) 目标Schema下拉框仅查询到生产项目中对接的Schema(project07), 无法查询当前租户其它项目或其它租户项目中的Schema |
| 4 | 选择Doris数据源, 点击「映射配置」, 重复步骤2~3 | Doris映射配置中的Schema租户隔离功能正常 |
| 5 | 选择DMMySQL数据源, 点击「映射配置」, 重复步骤2~3 | DMMySQL映射配置中的Schema租户隔离功能正常 |

##### 【P0】验证「映射配置-Schema列表」租户隔离功能生效(初始化方式: 不创建且不对接Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、将Project03和Project08绑定为发布关系:
Project03 > Project08
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 使用租户 A 进入「Project03-数据源」页面, 选择Hive数据源, 点击「映射配置」 | 进入「映射配置(${Hive数据源名称})」页面 |
| 2 | 「发布目标」选择Project08中Hive数据源名称后, 新增一行Schema映射, 检查当前Schema和目标Schema下拉框 | 1) 当前Schema下拉框展示: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema)<br>2) 目标Schema 下拉框展示: 租户B下项目已创建或已对接的 Schema(project06~10创建或对接的schema) |
| 3 | 当前Schema、目标Schema下拉框中, 分别输入project01~10中创建或对接的Schema | 1) 当前Schema下拉框仅查询到租户A中创建或对接的Schema(project01~05), 无法查询其它租户项目中的Schema(project06~10)<br>2) 目标Schema下拉框仅查询到租户B中创建或对接的Schema(project06~10), 无法查询其它租户项目中的Schema(project01~05) |
| 4 | 选择Doris数据源, 点击「映射配置」, 重复步骤2~3 | Doris映射配置中的Schema租户隔离功能正常 |
| 5 | 选择DMMySQL数据源, 点击「映射配置」, 重复步骤2~3 | DMMySQL映射配置中的Schema租户隔离功能正常 |

##### 【P1】验证「整库同步-Sink端Schema列表」 租户隔离功能生效(初始化方式: 创建)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
开启离线配置项:
default.data.source.show.all.schema = true

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 在Project01-数据源中选择一个数据源, 点击整库同步 | 进入整库同步页面 |
| 2 | 数据同步目标选择Hive数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的hive schema<br>2) 不展示projec06~10中创建或对接的hive schema<br>3) 不展示schema - project11 |
| 3 | 数据同步目标选择Doris数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的doris schema<br>2) 不展示projec06~10中创建或对接的doris schema<br>3) 不展示schema - project11 |
| 4 | 数据同步目标选择DMmysql数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的DMMySQL schema<br>2) 不展示projec06~10中创建或对接的DMMySQL schema<br>3) 不展示schema - project11 |
| 5 | 创建整库同步任务, 检查整库同步功能 | 功能正常 |
| 6 | 数据同步目标依次选择Doris、DMMySQL(非meta源), 检查Schema列表 | 展示Doris、DMMySQL数据源所有的Schema |
| 7 | 创建整库同步任务, 检查整库同步功能 | 功能正常 |

##### 【P1】验证「整库同步-Sink端Schema列表」 租户隔离功能不生效(初始化方式: 对接已有Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
开启离线配置项:
default.data.source.show.all.schema = true

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 在Project02-数据源中选择一个数据源, 点击整库同步 | 进入整库同步页面 |
| 2 | 数据同步目标选择Hive数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的hive schema<br>2) 不展示projec06~10中创建或对接的hive schema<br>3) 不展示schema - project11 |
| 3 | 数据同步目标选择Doris数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的doris schema<br>2) 不展示projec06~10中创建或对接的doris schema<br>3) 不展示schema - project11 |
| 4 | 数据同步目标选择DMmysql数据源后, 展开Schema下拉框 | 1) 仅展示projec01~05中创建或对接的DMMySQL schema<br>2) 不展示projec06~10中创建或对接的DMMySQL schema<br>3) 不展示schema - project11 |
| 5 | 创建整库同步任务, 检查整库同步功能 | 功能正常 |
| 6 | 数据同步目标依次选择Doris、DMMySQL(非meta源), 检查Schema列表 | 展示Doris、DMMySQL数据源所有的Schema |
| 7 | 创建整库同步任务, 检查整库同步功能 | 功能正常 |

## 表查询 ❯

##### 【P0】验证「表查询-Schema列表」租户隔离功能生效（初始化方式: 创建）

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 进入Project01-表查询-Hive Schema页面 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project01~11创建或对接的Schema名称 | 1) 仅查询到project01~05中创建或对接的Schema<br>2) 查询不到project06~11中的Schema |
| 3 | 依次进入project01~05中的Schema | 正常展示hive表的字段信息、分区信息、数据预览 |
| 4 | 进入表查询-Doris Schema页面, 重复步骤1~3 | Doris Schema租户隔离功能正常 |
| 5 | 进入表查询-DMMySQL Schema页面, 重复步骤1~3 | DMMySQL Schema租户隔离功能正常 |

##### 【P1】验证「表查询-Schema列表」租户隔离功能生效（初始化方式: 对接已有Schema）

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 进入Project02-表查询-Hive Schema页面 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project01~11创建或对接的Schema名称 | 1) 仅查询到project01~05中创建或对接的Schema<br>2) 查询不到project06~11中的Schema |
| 3 | 依次进入project01~05中的Schema | 正常展示hive表的字段信息、分区信息、数据预览 |
| 4 | 进入表查询-Doris Schema页面, 重复步骤1~3 | Doris Schema租户隔离功能正常 |
| 5 | 进入表查询-DMMySQL Schema页面, 重复步骤1~3 | DMMySQL Schema租户隔离功能正常 |

##### 【P1】验证「表查询-Schema列表」租户隔离功能生效（初始化方式: 不创建且不对接Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 进入Project03-表查询-Hive Schema页面 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project01~11创建或对接的Schema名称 | 1) 仅查询到project01~05中创建或对接的Schema<br>2) 查询不到project06~11中的Schema |
| 3 | 依次进入project01~05中的Schema | 正常展示hive表的字段信息、分区信息、数据预览 |
| 4 | 进入表查询-Doris Schema页面, 重复步骤1~3 | Doris Schema租户隔离功能正常 |
| 5 | 进入表查询-DMMySQL Schema页面, 重复步骤1~3 | DMMySQL Schema租户隔离功能正常 |

## 函数管理 ❯

##### 【P1】验证「自定义函数-Schema列表」租户隔离功能生效(Hive SQL)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用租户 A, 进入离线开发-Project03-「函数管理-HiveSQL-自定义函数」页面, 检查Schema列表 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project06~11创建或对接的Schema名称 | 暂无数据, 依旧不展示project06~11中的Schema |
| 3 | 选择Schema后, 检查自定义函数保存功能 | 保存功能正常 |

##### 【P0】验证「自定义函数-Schema列表」租户隔离功能生效(Doris SQL)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用租户 A, 进入离线开发-Project03-「函数管理-DorisSQL-自定义函数」页面, 检查Schema列表 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project06~11创建或对接的Schema名称 | 暂无数据, 依旧不展示project06~11中的Schema |
| 3 | 选择Schema后, 检查自定义函数保存功能 | 保存功能正常 |

##### 【P1】验证「自定义函数-Schema列表」租户隔离功能生效(DMMySQL)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用租户 A, 进入离线开发-Project03-「函数管理-DMMySQL-自定义函数」页面, 检查Schema列表 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 2 | 查询框搜索project06~11创建或对接的Schema名称 | 暂无数据, 依旧不展示project06~11中的Schema |
| 3 | 选择Schema后, 检查自定义函数保存功能 | 保存功能正常 |

## 任务开发 ❯

##### 【P0】验证「SQL任务-Schema列表」Scheam租户隔离功能生效(初始化方式: 不创建且不对接 Schema)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-周期任务】页面, 租户 A 在Project03 中创建HiveSQL任务 | 创建成功 |
| 2 | 展开HiveSQL任务的Schema下拉框 | 1) 仅展示projec01~05中创建或对接的hive schema<br>2) 不展示projec06~10中创建或对接的hive schema<br>3) 不展示schema - project11 |
| 3 | 创建DorisSQL任务, 检查Schema下拉框 | 1) 仅展示projec01~05中创建或对接的doris schema<br>2) 不展示projec06~10中创建或对接的doris schema<br>3) 不展示schema - project11 |
| 4 | 创建DMMySQL任务, 检查Schema下拉框 | 1) 仅展示projec01~05中创建或对接的DMMySQL schema<br>2) 不展示projec06~10中创建或对接的DMMySQL schema<br>3) 不展示schema - project11 |

##### 【P0】验证「数据同步-Schema列表」  租户隔离生效（Meta 源）

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
开启离线配置项:
default.data.source.show.all.schema = true

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 在Project01 中创建数据同步任务 | 创建成功 |
| 2 | source端选择hive数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的hive schema<br>2) 不展示projec06~10中创建或对接的hive schema<br>3) 不展示schema - project11 |
| 3 | source端选择doris数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的doris schema<br>2) 不展示projec06~10中创建或对接的doris schema<br>3) 不展示schema - project11 |
| 4 | source端选择dmmysql数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的DMMySQL schema<br>2) 不展示projec06~10中创建或对接的DMMySQL schema<br>3) 不展示schema - project11 |
| 5 | sink端选择hive数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的hive schema<br>2) 不展示projec06~10中创建或对接的hive schema<br>3) 不展示schema - project11 |
| 6 | sink端选择doris数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的doris schema<br>2) 不展示projec06~10中创建或对接的doris schema<br>3) 不展示schema - project11 |
| 7 | sink端选择dmmysql数据源, 检查schema下拉框 | 1) 仅展示projec01~05中创建或对接的DMMySQL schema<br>2) 不展示projec06~10中创建或对接的DMMySQL schema<br>3) 不展示schema - project11 |
| 8 | 配置hive2doris数据同步任务, 检查数据同步功能 | 数据同步功能正常 |
| 9 | 进入Project02, 重复步骤1~8 | 租户隔离功能生效 |
| 10 | 进入Project03, 重复步骤1~8 | 租户隔离功能生效 |

##### 【P1】验证「数据同步-Schema列表」 租户隔离不生效（非Meta 源）

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
4、开启配置项:
default.data.source.show.all.schema = true

5、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

6、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发】页面, 租户 A 在Project01 中创建数据同步任务 | 创建成功 |
| 2 | source端选择doris(非Meta)数据源, 检查schema下拉框 | 展示该数据源下所有的schema |
| 3 | source端选择dmmysql(非Meta)数据源, 检查schema下拉框 | 展示该数据源下所有的schema |
| 4 | sink端选择doris(非Meta)数据源, 检查schema下拉框 | 展示该数据源下所有的schema |
| 5 | sink端选择dmmysql(非Meta)数据源, 检查schema下拉框 | 展示该数据源下所有的schema |
| 6 | 配置hive2dmmysql数据同步任务, 检查数据同步功能 | 数据同步功能正常 |
| 7 | 进入Project02, 重复步骤2~6 | 租户隔离不生效 |
| 8 | 进入Project03, 重复步骤2~6 | 租户隔离不生效 |

##### 【P1】验证「数据同步-自定义SQL-Schema」租户隔离不生效(Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
开启离线配置项:
default.data.source.show.all.schema = true

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接mysql计算引擎

5、mysql在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-周期任务】页面, 租户 A 进入Project01 项目 | 进入成功 |
| 2 | 创建数据同步任务, 源端选择MySQL数据源, 自定义SQL依次输入并数据预览:<br>1) SELECT * FROM ${project01中的schema}.table;<br>2) SELECT * FROM ${project02中的schema}.table;<br>3) SELECT * FROM ${project04中的schema}.table;<br>4) SELECT * FROM ${project05中的schema}.table;<br>5) SELECT * FROM ${project06中的schema}.table;<br>6) SELECT * FROM ${project07中的schema}.table;<br>7) SELECT * FROM ${project09中的schema}.table;<br>8) SELECT * FROM ${project10中的schema}.table;<br>9) SELECT * FROM project11.table; | 数据预览成功, 正常返回结果 |
| 3 | 目标端选择任意数据源后, 依次执行数据同步任务 | 执行成功 |
| 4 | 查看目标端中同步的数据 | 数据同步成功 |

##### 【P2】验证「数据同步-自定义SQL-Schema」租户隔离不生效(非Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)
开启离线配置项:
default.data.source.show.all.schema = true

4、租户 A
Project01（引入schema）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接mysql计算引擎
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-周期任务】页面, 租户 A 进入Project01 项目 | 进入成功 |
| 2 | 创建数据同步任务, 源端选择MySQL数据源, 自定义SQL依次输入并数据预览:<br>1) SELECT * FROM ${mysql中的schemaA}.table;<br>2) SELECT * FROM ${mysql中的schemaB}.table; | 数据预览成功, 正常返回结果 |
| 3 | 目标端选择任意数据源后, 依次执行数据同步任务 | 执行成功 |
| 4 | 查看目标端中同步的数据 | 数据同步成功 |

##### 【P0】验证SQL代码不受Schema的租户隔离影响(周期任务, Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-周期任务】页面, 租户 A 进入Project01 项目 | 进入成功 |
| 2 | 创建Hive任务, 访问租户A、租户B、外部平台中的Hive Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 3 | 创建Doris任务, 访问租户A和租户B中的Doris Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 4 | 创建Dmmysql任务, 访问租户A和租户B中的DMmysql Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |

##### 【P2】验证SQL代码不受Schema的租户隔离影响(手动任务, Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-手动任务】页面, 租户 A 进入Project01 项目 | 进入成功 |
| 2 | 创建Hive任务, 访问租户A和租户B中的Hive Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 3 | 创建Doris任务, 访问租户A和租户B中的Doris Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 4 | 创建Dmmysql任务, 访问租户A和租户B中的DMmysql Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |

##### 【P2】验证SQL代码不受Schema的租户隔离影响(临时运行, Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-临时运行】页面, 租户 A 进入Project01 项目 | 进入成功 |
| 2 | 创建Hive任务, 访问租户A和租户B中的Hive Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 3 | 创建Doris任务, 访问租户A和租户B中的Doris Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |
| 4 | 创建Dmmysql任务, 访问租户A和租户B中的DMmysql Schema中的表:<br>SELECT * FROM ${project01中的schema}.table;<br>SELECT * FROM ${project02中的schema}.table;<br>SELECT * FROM ${project04中的schema}.table;<br>SELECT * FROM ${project05中的schema}.table;<br>SELECT * FROM ${project06中的schema}.table;<br>SELECT * FROM ${project07中的schema}.table;<br>SELECT * FROM ${project09中的schema}.table;<br>SELECT * FROM ${project10中的schema}.table;<br>SELECT * FROM project11.table; | 查询成功 |

##### 【P0】验证「SQL任务-本地数据导入」Schema租户隔离生效(Meta源)

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入【离线开发-周期任务】页面, 租户 A 进入Project03 项目 | 进入成功 |
| 2 | 创建HiveSQL任务, 点击「导入」并上传CSV文件 | 进入「本地数据导入」表单页面 |
| 3 | 点击下一步, 「表类型」选择Hive后, 检查「Schema」下拉框 | 1) 仅展示租户A中project01~05中创建或对接的schema<br>2) 不展示租户B中project06~10中创建或对接的schema<br>3) 不展示外部平台中创建的schema-project11 |
| 4 | 「Schema」输入框搜索project06~11创建或对接的schema | 暂无数据, 无法查询到对应的Schema |
| 5 | 依次选择Schema、表后, 点击「导入」 | 导入成功 |
| 6 | 执行Hive SQL: SELECT * FROM ${所选Schema}.${所选表}; | 查询成功, 本地数据导入正常 |
| 7 | 再次进行「本地数据导入」操作, 「表类型」选择Doris后, 重复步骤3~6 | 1) Doris Schema租户隔离功能正常<br>2) 本地数据导入功能正常 |
| 8 | 再次进行「本地数据导入」操作, 「表类型」选择DMMySQL后, 重复步骤3~6 | 1) DMMySQL Schema租户隔离功能正常<br>2) 本地数据导入功能正常 |

## 离线配置项  ❯

##### 【P1】验证配置项「default.data.source.show.all.schema = true」不再生效

> 前置条件
```
1、控制台已准备Hive、Doris、Dmmysql计算组件
2、数据源中心已准备Doris、Dmmysql数据源
3、已存在至少两个租户TenantA, TenantB (对接同一套集群和数据源)

4、租户 A
Project01（创建新schema）
Project02(对接已有 schema)
Project03（不创建且不对接 Schema）
Project04（创建过的Schema被删除的项目）
Project05（对接过的Schema被删除的项目）

租户 B
Project06（创建新schema）
Project07(对接已有 schema)
Project08（不创建且不对接 Schema）
Project09（创建过的Schema被删除的项目）
Project10（对接过的Schema被删除的项目）

以上每个项目的计算引擎均创建、对接、不创建且不对接的方式, 对接hive、doris、dmmysql三种计算引擎

5、Hive、Doris、Dmmysql分别在外部平台中创建Schema:
Create database Project11;

6、将Project03和Project08绑定为发布关系:
Project03 > Project08

7、 开启离线配置项(默认不开启, 默认值: false):
default.data.source.show.all.schema = true
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用租户 A , 进入离线开发模块 | 进入成功 |
| 2 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「创建项目-对接已有Schema」 | Schema列表: 仅过滤掉已被其他租户项目创建或对接的 Schema (project06~10) |
| 3 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-添加计算引擎」 | Schema列表: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema) |
| 4 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-映射配置-当前Schema&目标Schema」 | 1) 当前Schema列表: 租户A下已创建或对接的 Schema (project01~05创建或对接的schema)<br>2) 目标Schema列表: 租户B下已创建或对接的 Schema (project01~05创建或对接的schema) |
| 5 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-整库同步-数据同步目标」 | Schema列表: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema) |
| 6 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-数据同步-Source端Schema&Sink端Schema」 | Schema列表: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema) |
| 7 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-SQL任务」 | Schema列表: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema) |
| 8 | 依次检查如下页面中的Hive、Doris、DMMySQL的Schema下拉框: 「Project03-函数管理-自定义函数」 | Schema列表: 租户A下项目已创建或已对接的 Schema (project01~05创建或对接的schema) |