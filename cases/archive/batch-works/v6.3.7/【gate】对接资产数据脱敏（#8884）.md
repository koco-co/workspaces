---
suite_name: "【gate】对接资产数据脱敏（#8884）"
description: 【gate】对接资产数据脱敏
prd_id: 8884
prd_version: v6.3.7
prd_path: ""
product: batch-works
tags:
  - 离线开发
  - 周期任务
  - 手动任务
  - 临时查询
  - gate
  - 对接资产数据脱敏
create_at: 2026-03-29
status: ""
health_warnings: []
case_count: 23
origin: xmind
---

##### 【P2】验证「脱敏配置项」配置后的显示

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 开启离线脱敏配置项, 进入「离线开发-数据地图-脱敏管理」页面, 检查页面内容 | 提示: 已开启应用资产数据脱敏, 当前数据脱敏只取自资产内所配的规则 |
| 2 | 关闭配置项, 检查「脱敏管理」页面内容 | 关闭配置项, 检查「脱敏管理」页面内容 |

##### 【P0】验证「Doris数据源」跨集群脱敏功能正常(不脱敏)

> 前置条件

```
1) 数据资产-脱敏规则A: 将从第「1」位至第「7」位替换为：「*」

2) 离线开发-脱敏规则B: 算法脱敏

3) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data;

4) 开启离线脱敏配置项
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建Doris任务A, 执行SQL语句 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变, 重新进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位<br>2) 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位<br>3) 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位 |
| 3 | 进入「周期任务-基础属性」, 切换Doris数据源, 重新进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 报错, 当前数据源下没有对应的doris_asset_data表 |

## 周期任务 ❯

##### 【P0】验证「Doris数据源」数据拖敏功能正常(全部覆盖)

> 前置条件

```
1) 开启离线脱敏配置项 

2) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); 

3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 

4) 复杂查询「任务B」  SELECT * FROM doris_asset_data where 1=1; 

5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 

「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Doris项目. | 创建成功 |
| 2 | 进入「数据开发-周期任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 3 | 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加doris_asset_data脱敏表及字段A, 默认选项不变 | 添加成功 |
| 4 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 5 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 6 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 1) 自定义函数正常解析并执行<br>2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 7 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段A全部进行脱敏处理 |
| 8 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段A全部进行脱敏处理 |
| 9 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理<br>2) 「表查询-数据预览」中的字段A全部进行脱敏处理<br>3） 「数据同步-数据预览」中的字段A未进行脱敏处理 |

### 验证「Doris数据源」数据拖敏功能正常(部分覆盖)

- 使用非admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有Doris Schema」的Doris项目.
#### 进入「数据开发-周期任务」, 新建Doris任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入Doris任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入Doris任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P0】验证「Doris数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 

2) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 

3) 开启离线脱敏配置项 

4) 简单查询「任务A」 SELECT * FROM doris_asset_data; 

5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 

6) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加doris_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P0】验证「Doris数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 2) 开启离线脱敏配置项 3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加doris_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入Doris任务A, 执行SELECT * FROM doris_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

##### 【P0】验证「Doris数据源」脱敏规则冲突时功能正常

> 前置条件

```
1) 数据资产-脱敏规则A: 将从第「1」位至第「7」位替换为：「*」 

2) 离线开发-脱敏规则B: 算法脱敏 

3) 建表语句


DROP TABLE IF EXISTS doris_asset_data;

CREATE TABLE doris_asset_data (
id INT,
asset_id VARCHAR(20),
user_name VARCHAR(50),
id_card VARCHAR(20),
phone VARCHAR(15),
email VARCHAR(50),
amount DECIMAL(15,2),
create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=OLAP
DUPLICATE KEY(id)
DISTRIBUTED BY HASH(id) BUCKETS 4
PROPERTIES (
"replication_num" = "1"
);

INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES
(1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00),
(2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32),
(3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50),
(4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89);

SELECT * FROM doris_asset_data;
4) 开启离线脱敏配置项
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建Doris任务A , 执行SQL语句 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「离线开发-数据地图-脱敏管理」中添加「算法脱敏」规则B, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变, 然后进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |
| 3 | 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变, 重新进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位<br>2) 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位<br>3) 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位 |
| 4 | 关闭配置项, 重新进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 「数据资产-数据安全」中的脱敏规则失效, 「离线开发」中的脱敏规则生效<br>1) 「select结果预览」和下载后的文件中的字段phone经过AES对称加密<br>2) 「表查询-数据预览」中的字段phone经过AES对称加密<br>3） 「数据同步-数据预览」中的字段phone经过AES对称加密 |
| 5 | 删除脱敏规则B, 重新进入Doris任务A, 执行SELECT * FROM doris_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |

### 验证「Hive数据源」数据拖敏功能正常(全部覆盖)

- 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Hadoop项目.
#### 进入「数据开发-周期任务」, 新建Hive任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加hive_asset_data脱敏表及字段A, 默认选项不变

- 添加成功
#### 重新进入Hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 重新进入Hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Hive任务A, 执行查询「任务C」

- 1) 自定义函数正常解析并执行2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 进入「数据开发-表查询-Hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据地图-查找数据」, 点击hive_asset_data表, 查看「数据预览」内容

- 「数据地图-数据预览」中的字段A全部进行脱敏处理
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」数据地图-数据预览内容

- 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
2) 「表查询-数据预览」中的字段A全部进行脱敏处理
3） 「数据同步-数据预览」中的字段A未进行脱敏处理
4） 「数据地图-数据预览」中的字段A未进行脱敏处理
### 验证「Hive数据源」数据拖敏功能正常(部分覆盖)

- 切换租户,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有hive Schema」的Hive项目.
#### 进入「数据开发-周期任务」, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P1】验证「Hive数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 2) 开启离线脱敏配置项 3) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 4) 简单查询「任务A」 SELECT * FROM hive_asset_data; 5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 6)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加hive_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P1】验证「Hive数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 开启离线脱敏配置项 2) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 3) 简单查询「任务A」 SELECT * FROM hive_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 5)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加hive_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入hive任务A, 执行SELECT * FROM hive_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

##### 【P1】验证「Hive数据源」脱敏规则冲突时功能正常

> 前置条件

```
1) 数据资产-脱敏规则A: 将从第「1」位至第「7」位替换为：「*」 
2) 离线开发-脱敏规则B: 算法脱敏 

3) 离线开发-脱敏规则C: 针对Tidb的脱敏规则 4) 建表语句 DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 

5) 开启离线脱敏配置项
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 新建hive任务A , 执行SQL语句 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「离线开发-数据地图-脱敏管理」中添加「算法脱敏」规则B, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 然后进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |
| 3 | 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位<br>2) 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位<br>3) 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位 |
| 4 | 关闭配置项, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 「数据资产-数据安全」中的脱敏规则失效, 「离线开发」中的脱敏规则生效<br>1) 「select结果预览」和下载后的文件中的字段phone经过AES对称加密<br>2) 「表查询-数据预览」中的字段phone经过AES对称加密<br>3） 「数据同步-数据预览」中的字段phone经过AES对称加密 |
| 5 | 删除脱敏规则B, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |

##### 【P1】验证「Doris数据源」删表重新创建后的脱敏效果

> 前置条件

```
1) 开启离线脱敏配置项 

2) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); 

3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 

4) 复杂查询「任务B」  SELECT * FROM doris_asset_data where 1=1; 

5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 

「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 执行建表语句, 执行「任务A」 | 脱敏效果仍然存在 |
| 2 | 执行复杂查询「任务B」 | 脱敏效果仍然存在 |
| 3 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 脱敏效果仍然存在 |
| 4 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 脱敏效果仍然存在 |
| 5 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 脱敏效果仍然存在 |

##### 【P2】验证「Hive数据源」删表重新创建后的脱敏效果

> 前置条件

```
1) 开启离线脱敏配置项 

2) 建表语句

DROP TABLE IF EXISTS hive_asset_data_test;

CREATE TABLE hive_asset_data_test(
id INT,
asset_id STRING COMMENT'资产ID',
user_name STRING COMMENT'用户姓名',
id_card STRING COMMENT'身份证号',
phone STRING COMMENT'手机号',
email STRING COMMENT'邮箱',
amount DECIMAL(15,2) COMMENT'资产金额',
create_time TIMESTAMP
)COMMENT'资产信息表'ROW FORMAT DELIMITED FIELDS TERMINATED BY'\t'STORED AS TEXTFILE;

INSERT INTO hive_asset_data_test VALUES(1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'),
(2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'),
(3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'),
(4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00');

3) 简单查询「任务A」 SELECT * FROM hive_asset_data_test;

4) 复杂查询「任务B」  SELECT *, strSub("12345", 0 ,2) FROM hive_asset_data_test WHERE 1=1;

5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 

「任务C」 SELECT *, addStr("test") FROM hive_asset_data_test;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-周期任务」, 执行建表语句, 执行「任务A」 | 脱敏效果仍然存在 |
| 2 | 执行复杂查询「任务B」 | 脱敏效果仍然存在 |
| 3 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Hive任务A, 执行查询「任务C」 | 脱敏效果仍然存在 |
| 4 | 进入「数据开发-表查询-Doris」, 点击hive_asset_data_test表, 查看「数据预览」内容 | 脱敏效果仍然存在 |
| 5 | 进入「数据开发-周期任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data_test表, 点击「数据预览」, 查看「数据预览」内容 | 脱敏效果仍然存在 |

## 手动任务 ❯

##### 【P1】验证「Doris数据源」数据拖敏功能正常(全部覆盖)

> 前置条件

```
1) 开启离线脱敏配置项 

2) 建表语句 

DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); 

3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 


4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 


5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Doris项目. | 创建成功 |
| 2 | 进入「数据开发-手动任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 3 | 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加doris_asset_data脱敏表及字段A, 默认选项不变 | 添加成功 |
| 4 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 5 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 6 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 1) 自定义函数正常解析并执行<br>2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 7 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段A全部进行脱敏处理 |
| 8 | 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段A全部进行脱敏处理 |
| 9 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理<br>2) 「表查询-数据预览」中的字段A全部进行脱敏处理<br>3） 「数据同步-数据预览」中的字段A未进行脱敏处理 |

### 验证「Doris数据源」数据拖敏功能正常(部分覆盖)

- 使用非admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有Doris Schema」的Doris项目.
#### 进入「数据开发-手动任务」, 新建Doris任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入Doris任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入Doris任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P1】验证「Doris数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 

2) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 

3) 开启离线脱敏配置项 

4) 简单查询「任务A」 SELECT * FROM doris_asset_data; 

5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 

6) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-手动任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加doris_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P1】验证「Doris数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 2) 开启离线脱敏配置项 3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-手动任务」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加doris_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入Doris任务A, 执行SELECT * FROM doris_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

### 验证「Hive数据源」数据拖敏功能正常(全部覆盖)

- 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Hadoop项目.
#### 进入「数据开发-手动任务」, 新建Hive任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加hive_asset_data脱敏表及字段A, 默认选项不变

- 添加成功
#### 重新进入Hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 重新进入Hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Hive任务A, 执行查询「任务C」

- 1) 自定义函数正常解析并执行2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 进入「数据开发-表查询-Hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据地图-查找数据」, 点击hive_asset_data表, 查看「数据预览」内容

- 「数据地图-数据预览」中的字段A全部进行脱敏处理
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」数据地图-数据预览内容

- 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
2) 「表查询-数据预览」中的字段A全部进行脱敏处理
3） 「数据同步-数据预览」中的字段A未进行脱敏处理
4） 「数据地图-数据预览」中的字段A未进行脱敏处理
### 验证「Hive数据源」数据拖敏功能正常(部分覆盖)

- 切换租户,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有hive Schema」的Hive项目.
#### 进入「数据开发-手动任务」, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P0】验证「Hive数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 2) 开启离线脱敏配置项 3) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 4) 简单查询「任务A」 SELECT * FROM hive_asset_data; 5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 6)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-手动任务」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加hive_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P0】验证「Hive数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 开启离线脱敏配置项 2) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 3) 简单查询「任务A」 SELECT * FROM hive_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 5)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-手动任务」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加hive_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-手动任务」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入hive任务A, 执行SELECT * FROM hive_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

##### 【P0】验证「Hive数据源」脱敏规则冲突时功能正常

> 前置条件

```
1) 数据资产-脱敏规则A: 将从第「1」位至第「7」位替换为：「*」

2) 离线开发-脱敏规则B: 算法脱敏

3) 建表语句
DROP TABLE IF EXISTS hive_asset_data;

-- 创建资产数据表（Hive格式）
CREATE TABLE hive_asset_data (
id INT,
asset_id STRING COMMENT '资产ID',
user_name STRING COMMENT '用户姓名',
id_card STRING COMMENT '身份证号',
phone STRING COMMENT '手机号',
email STRING COMMENT '邮箱',
amount DECIMAL(15,2) COMMENT '资产金额',
create_time TIMESTAMP
) COMMENT '资产信息表'
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
STORED AS TEXTFILE;

INSERT INTO hive_asset_data VALUES
(1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'),
(2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'),
(3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'),
(4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00');

SELECT * FROM hive_asset_data;


4) 开启离线脱敏配置项
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-手动任务」, 新建hive任务A , 执行SQL语句 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「离线开发-数据地图-脱敏管理」中添加「算法脱敏」规则B, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 然后进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |
| 3 | 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位<br>2) 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位<br>3) 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位 |
| 4 | 关闭配置项, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 「数据资产-数据安全」中的脱敏规则失效, 「离线开发」中的脱敏规则生效<br>1) 「select结果预览」和下载后的文件中的字段phone经过AES对称加密<br>2) 「表查询-数据预览」中的字段phone经过AES对称加密<br>3） 「数据同步-数据预览」中的字段phone经过AES对称加密 |
| 5 | 删除脱敏规则B, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |

## 临时查询 ❯

##### 【P0】验证「Doris数据源」数据拖敏功能正常(全部覆盖)

> 前置条件

```
1) 开启离线脱敏配置项 

2) 建表语句 

DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); 

3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 


4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 


5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Doris项目. | 创建成功 |
| 2 | 进入「数据开发-临时查询」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 3 | 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加doris_asset_data脱敏表及字段A, 默认选项不变 | 添加成功 |
| 4 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 5 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 6 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 1) 自定义函数正常解析并执行<br>2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理 |
| 7 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段A全部进行脱敏处理 |
| 8 | 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段A全部进行脱敏处理 |
| 9 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理<br>2) 「表查询-数据预览」中的字段A全部进行脱敏处理<br>3） 「数据同步-数据预览」中的字段A未进行脱敏处理 |

### 验证「Doris数据源」数据拖敏功能正常(部分覆盖)

- 使用非admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有Doris Schema」的Doris项目.
#### 进入「数据开发-临时查询」, 新建Doris任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加doris_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入Doris任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入Doris任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的doris_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入Doris任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P1】验证「Doris数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 

2) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 

3) 开启离线脱敏配置项 

4) 简单查询「任务A」 SELECT * FROM doris_asset_data; 

5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 

6) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-临时查询」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加doris_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入Doris任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P1】验证「Doris数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 建表语句 DROP TABLE IF EXISTS doris_asset_data; CREATE TABLE doris_asset_data ( id INT, asset_id VARCHAR(20), user_name VARCHAR(50), id_card VARCHAR(20), phone VARCHAR(15), email VARCHAR(50), amount DECIMAL(15,2), create_time DATETIME DEFAULT CURRENT_TIMESTAMP ) ENGINE=OLAP DUPLICATE KEY(id) DISTRIBUTED BY HASH(id) BUCKETS 4 PROPERTIES ( "replication_num" = "1" ); INSERT INTO doris_asset_data (id, asset_id, user_name, id_card, phone, email, amount) VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89); SELECT * FROM doris_asset_data; 2) 开启离线脱敏配置项 3) 简单查询「任务A」 SELECT * FROM doris_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTRING(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTRING(phone, 1, 3)) AS total_amount_in_prefix FROM doris_asset_data WHERE amount > 100000 ) t ORDER BY phone_prefix, rank_in_prefix; 5) 自定义jar资源包及使用方法 https://dtstack.yuque.com/rd-center/wict2z/end0mqgnq3wevx3p 「任务C」 SELECT *, addStr("test") FROM doris_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-临时查询」, 新建Doris任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加doris_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入Doris任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入Doris任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Doris任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-Doris」, 点击doris_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择doris_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的doris_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入Doris任务A, 执行SELECT * FROM doris_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

### 验证「Hive数据源」数据拖敏功能正常(全部覆盖)

- 使用admin账号,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「创建」的Hadoop项目.
#### 进入「数据开发-临时查询」, 新建Hive任务A, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「全部覆盖」规则, 并添加hive_asset_data脱敏表及字段A, 默认选项不变

- 添加成功
#### 重新进入Hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 重新进入Hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入Hive任务A, 执行查询「任务C」

- 1) 自定义函数正常解析并执行2) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
#### 进入「数据开发-表查询-Hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段A全部进行脱敏处理
#### 进入「数据地图-查找数据」, 点击hive_asset_data表, 查看「数据预览」内容

- 「数据地图-数据预览」中的字段A全部进行脱敏处理
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」数据地图-数据预览内容

- 1) 「select结果预览」和下载后的文件中的字段A全部进行脱敏处理
2) 「表查询-数据预览」中的字段A全部进行脱敏处理
3） 「数据同步-数据预览」中的字段A未进行脱敏处理
4） 「数据地图-数据预览」中的字段A未进行脱敏处理
### 验证「Hive数据源」数据拖敏功能正常(部分覆盖)

- 切换租户,  进入「项目列表-创建项目」, 新建一个「初始化方式」为「对接已有hive Schema」的Hive项目.
#### 进入「数据开发-临时查询」, 执行简单查询「任务A」

- 正常查询出SQL结果, 数据未进行脱敏处理
#### 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变

- 添加成功
#### 重新进入hive任务A, 执行简单查询「任务A」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 重新进入hive任务A, 执行复杂查询「任务B」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」

- 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容

- 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容

- 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位
#### 编辑「数据资产」中的hive_asset_data脱敏表, 将「影响用户」改为「用户组」, 「用户组」选择非当前用户所在「用户组」, 进入hive任务A, 执行简单查询「任务A」 , 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容

- 1) 「select结果预览」和下载后的文件中的字段phone未进行脱敏处理
2) 「表查询-数据预览」中的字段phone未进行脱敏处理
3） 「数据同步-数据预览」中的字段phone未进行脱敏处理
##### 【P0】验证「Hive数据源」数据拖敏功能正常(转义脱敏)

> 前置条件

```
1) 脱敏规则A: A -> B @ -> @@@ 2) 开启离线脱敏配置项 3) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 4) 简单查询「任务A」 SELECT * FROM hive_asset_data; 5) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 6)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-临时查询」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「转义脱敏」规则A, 并添加hive_asset_data脱敏表及字段asset_id和email, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 7 | 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段asset_id 中的A替换为B, 字段email中的@替换为@@@ |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择当前用户所在「用户角色」, 进入hive任务A, 执行简单查询「任务A」, 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段未进行脱敏处理<br>2) 「表查询-数据预览」中的字段未进行脱敏处理<br>3） 「数据同步-数据预览」中的字段未进行脱敏处理 |

##### 【P1】验证「Hive数据源」数据拖敏功能正常(算法脱敏)

> 前置条件

```
1) 开启离线脱敏配置项 2) 建表语句 建表语句: DROP TABLE IF EXISTS hive_asset_data; -- 创建资产数据表（Hive格式） CREATE TABLE hive_asset_data ( id INT, asset_id STRING COMMENT '资产ID', user_name STRING COMMENT '用户姓名', id_card STRING COMMENT '身份证号', phone STRING COMMENT '手机号', email STRING COMMENT '邮箱', amount DECIMAL(15,2) COMMENT '资产金额', create_time TIMESTAMP ) COMMENT '资产信息表' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\t' STORED AS TEXTFILE; INSERT INTO hive_asset_data VALUES (1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'), (2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'), (3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'), (4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00'); SELECT * FROM hive_asset_data; 3) 简单查询「任务A」 SELECT * FROM hive_asset_data; 4) 复杂查询「任务B」 SELECT id, asset_id, user_name, id_card, phone, amount, email, phone_prefix, total_users_in_prefix, total_amount_in_prefix, RANK() OVER (PARTITION BY phone_prefix ORDER BY amount DESC) AS rank_in_prefix FROM ( SELECT id, asset_id, user_name, id_card, phone, amount, email, SUBSTR(phone, 1, 3) AS phone_prefix, COUNT(id) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_users_in_prefix, SUM(amount) OVER (PARTITION BY SUBSTR(phone, 1, 3)) AS total_amount_in_prefix FROM hive_asset_data WHERE amount > 100000 ) AS t ORDER BY phone_prefix, rank_in_prefix; 5)「任务C」 SELECT *, addStr("test") FROM hive_asset_data;
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-临时查询」, 新建hive任务A, 执行简单查询「任务A」 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「数据资产-数据安全」中添加「算法脱敏」规则A, 并添加hive_asset_data脱敏表及字段id_card, 默认选项不变 | 添加成功 |
| 3 | 重新进入hive任务A, 执行简单查询「任务A」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 4 | 重新进入hive任务A, 执行复杂查询「任务B」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 5 | 在资源管理中添加jar包, 然后在函数管理中添加「自定义函数」 , 重新进入hive任务A, 执行查询「任务C」 | 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密 |
| 6 | 进入「数据开发-表查询-hive」, 点击hive_asset_data表, 查看「数据预览」内容 | 「表查询-数据预览」中的字段id_card经过AES对称加密 |
| 7 | 进入「数据开发-临时查询」, 新建「数据同步」任务, 数据来源选择hive_asset_data表, 点击「数据预览」, 查看「数据预览」内容 | 「数据同步-数据预览」中的字段id_card经过AES对称加密 |
| 8 | 编辑「数据资产」中的hive_asset_data脱敏表, 将「人群圈选方式」改为「指定人群不脱敏」, 「影响用户」改为「用户角色」, 「用户角色」选择非当前用户所在「用户角色」, 进入hive任务A, 执行SELECT * FROM hive_asset_data; 查看「Select结果预览」、「表查询-数据预览」、「数据同步-数据预览」内容 | 1) 「select结果预览」和下载后的文件中的字段id_card经过AES对称加密<br>2) 「表查询-数据预览」中的字段id_card经过AES对称加密<br>3） 「数据同步-数据预览」中的字段id_card经过AES对称加密 |

##### 【P1】验证「Hive数据源」脱敏规则冲突时功能正常

> 前置条件

```
1) 数据资产-脱敏规则A: 将从第「1」位至第「7」位替换为：「*」

2) 离线开发-脱敏规则B: 算法脱敏

3) 建表语句
DROP TABLE IF EXISTS hive_asset_data;

-- 创建资产数据表（Hive格式）
CREATE TABLE hive_asset_data (
id INT,
asset_id STRING COMMENT '资产ID',
user_name STRING COMMENT '用户姓名',
id_card STRING COMMENT '身份证号',
phone STRING COMMENT '手机号',
email STRING COMMENT '邮箱',
amount DECIMAL(15,2) COMMENT '资产金额',
create_time TIMESTAMP
) COMMENT '资产信息表'
ROW FORMAT DELIMITED
FIELDS TERMINATED BY '\t'
STORED AS TEXTFILE;

INSERT INTO hive_asset_data VALUES
(1, 'A1001', '张三', '110105199001011234', '13800138000', 'zhangsan@example.com', 150000.00, '2023-01-01 10:00:00'),
(2, 'A1002', '李四', '310115198502029876', '13912345678', 'lisi@company.org', 987654.32, '2023-01-01 10:05:00'),
(3, 'A1003', '王五', '440305200010103333', '13755556666', 'wangwu@test.net', 50000.50, '2023-01-01 10:10:00'),
(4, 'A1004', '赵六', '510124197712128888', '18600001111', 'zhaoliu@demo.com', 1234567.89, '2023-01-01 10:15:00');

SELECT * FROM hive_asset_data;


4) 开启离线脱敏配置项
```

> 用例步骤

| 编号 | 步骤 | 预期 |
| --- | --- | --- |
| 1 | 进入「数据开发-临时查询」, 新建hive任务A , 执行SQL语句 | 正常查询出SQL结果, 数据未进行脱敏处理 |
| 2 | 在「离线开发-数据地图-脱敏管理」中添加「算法脱敏」规则B, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 然后进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |
| 3 | 在「数据资产-数据安全」中添加「部分覆盖」规则A, 并添加hive_asset_data脱敏表及字段phone, 默认选项不变, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone前7位替换为「*」, 仅显示后4位<br>2) 「表查询-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位<br>3) 「数据同步-数据预览」中的字段phone前7位替换为「*」, 仅显示后4位 |
| 4 | 关闭配置项, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 「数据资产-数据安全」中的脱敏规则失效, 「离线开发」中的脱敏规则生效<br>1) 「select结果预览」和下载后的文件中的字段phone经过AES对称加密<br>2) 「表查询-数据预览」中的字段phone经过AES对称加密<br>3） 「数据同步-数据预览」中的字段phone经过AES对称加密 |
| 5 | 删除脱敏规则B, 重新进入hive任务A, 执行SELECT * FROM hive_asset_data; | 1) 「select结果预览」和下载后的文件中的字段phone不脱敏<br>2) 「表查询-数据预览」中的字段phone 不脱敏<br>3) 「数据同步-数据预览」中的字段phone不脱敏 |

