# P3.5 Completion Notes

**Tag**: `refactor-v3-P3.5` · **Date**: 2026-04-28

## Sweep Universe (修正)

计划里 158 features 是虚高数字。实际：
- dataAssets：7 个 feature 带 per-feature `tests/`
- xyzh：0 个（所有 tests 都在 workspace/xyzh/tests 顶层）

7 个 dataAssets feature 全部处理：4 个 reorg 通过，3 个标 `MANUAL-TRIAGE.md`。

## 引擎测试通过数

基线 130 → P3.5 后 149（+19，全为新增 TDD 覆盖）

## 已交付工具

- `engine/src/lint/tests-layout.ts` —— L1-L7（L8 由 Phase X gitignore 修正补齐）
- `engine/src/migration/import-fix.ts` —— Phase 0 codemod（修复 250 个 broken import）
- `engine/src/migration/reorg-tests.ts` —— 含 emergent 的 `fixCaseImports`（见 M3）
- 3 个 CLI 子命令：`features:lint-tests`、`features:reorg-tests`、`features:import-fix`

## 延期项（留给后续 plan）

| 项 | 延期理由 | 严重度 |
|---|---|---|
| 拆分 pilot B 的 3 个超大 helper（rule-editor 2589 / rule-task 2167 / offline 1536） | 手工代码架构决策，超出 codemod 阶段范围 | L4 违规（3） |
| 拆分 swept feature 的 3 个超大 helper（key-range 923 / json-format-utils 848 / json-format-suite 835） | 同上 | L4 违规（3） |
| 全量从 META.title 自动生成 cases/README.md | 计划 Task 12 工具未建 | L3 违规/feature |
| 跨 feature 引用路径修复 | sweep 中的边界 case，需逐个 review | 散点 |
| 占位 slug（case-NN）升级为语义 kebab slug | 需读每个 test 的 title，手工 | 质量 |

## 重构候选（M3）

`fixCaseImports`（在 `engine/src/migration/reorg-tests.ts:150`）和 `rewriteSharedImports`（在 `import-fix.ts`）概念上重叠。后续可统一为单一 `engine/src/migration/import-rewriter.ts` 模块，同时覆盖 reorg 前后的路径修正。
